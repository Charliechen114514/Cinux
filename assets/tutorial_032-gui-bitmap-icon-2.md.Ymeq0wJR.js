import{c as e,Q as o,j as t,m as i}from"./chunks/framework.D9IbVcwA.js";const s=JSON.parse('{"title":"032-gui-bitmap-icon-2 · 位图与图标","description":"","frontmatter":{"title":"032-gui-bitmap-icon-2 · 位图与图标"},"headers":[],"relativePath":"tutorial/032-gui-bitmap-icon-2.md","filePath":"tutorial/032-gui-bitmap-icon-2.md","lastUpdated":1779686691000}'),l={name:"tutorial/032-gui-bitmap-icon-2.md"};function n(r,a,c,u,d,p){return o(),t("div",null,[...a[0]||(a[0]=[i('<h1 id="_032-2-编译期图标——用-consteval-打造嵌入式像素数据" tabindex="-1">032-2 编译期图标——用 consteval 打造嵌入式像素数据 <a class="header-anchor" href="#_032-2-编译期图标——用-consteval-打造嵌入式像素数据" aria-label="Permalink to &quot;032-2 编译期图标——用 consteval 打造嵌入式像素数据&quot;">​</a></h1><blockquote><p>标签：Cinux, GUI, consteval, 编译期计算, 图标, 调色板 前置：<a href="./032-gui-bitmap-icon-1">032-gui-bitmap-icon-1</a> — Canvas 位图渲染与透明机制</p></blockquote><h2 id="前言" tabindex="-1">前言 <a class="header-anchor" href="#前言" aria-label="Permalink to &quot;前言&quot;">​</a></h2><p>上一篇我们把 draw_bitmap 写好了，Canvas 能把像素数组画到屏幕上了。但像素数据从哪来？在一个成熟的桌面系统里，答案是&quot;从文件系统加载 PNG/SVG 文件&quot;。但在我们的内核里，文件系统还在 ramdisk 阶段，PNG 解码器更是八字没一撇。就算都有，为了一个 32x32 的小图标走一遍&quot;打开文件 → 分配缓冲区 → 解码 → 渲染 → 释放缓冲区&quot;的完整链路，怎么看都有点小题大做。</p><p>其实有一个更原始、更直接的方案：把像素数据直接编译进内核二进制。一个 32x32 的图标只有 1024 个 uint32_t，也就是 4KB——对内核来说微不足道。Linux 内核就是这么干启动 logo 的，在 <code>include/linux/linux_logo.h</code> 里用一大堆预处理宏把企鹅图片的数据塞进内核。不过 Linux 用的是 C 预处理宏，写起来又丑又容易出错。我们有 C++20，可以做得更优雅。</p><p>这一章要讲的就是 Cinux 的编译期图标数据系统：用 consteval 函数和模板，在编译时把人类可读的 hex 字符串转换成像素数组。最终效果是——你在代码里写 32 行看起来像 ASCII art 的字符串，编译器自动帮你转成 1024 个颜色值，零运行时开销。</p><h2 id="环境说明" tabindex="-1">环境说明 <a class="header-anchor" href="#环境说明" aria-label="Permalink to &quot;环境说明&quot;">​</a></h2><p>编译期图标系统是纯头文件实现，不涉及运行时环境的变化。编译器需要支持 C++20 的 consteval 函数和 inline constexpr 变量（GCC 10+ / Clang 10+ 完全满足）。生成的像素数据存储在二进制的 <code>.rodata</code> 段，和其他编译时常量一样被加载到内核的只读内存区域。</p><h2 id="第一步——调色板-用-16-色描述世界" tabindex="-1">第一步——调色板：用 16 色描述世界 <a class="header-anchor" href="#第一步——调色板-用-16-色描述世界" aria-label="Permalink to &quot;第一步——调色板：用 16 色描述世界&quot;">​</a></h2><p>如果让你直接写 1024 个 <code>0x00RRGGBB</code> 值来定义一个 32x32 的图标，你大概率会写疯——那些数字看起来全是噪声，根本看不出图标的形状。调色板系统就是解决这个可读性问题的。</p><p>思路是这样的：我们定义一个最多 16 种颜色的查找表（palette），用一个 hex 字符（&#39;0&#39;-&#39;9&#39;, &#39;a&#39;-&#39;f&#39;）来代表一种颜色。这样每个像素只需要一个字符来描述。一整行 32 个像素就是 32 个字符的字符串——看起来就像 ASCII art。</p><p>举个例子，Shell 图标的调色板把 &#39;0&#39; 映射为透明黑色、&#39;1&#39; 映射为深灰色（终端背景）、&#39;2&#39; 映射为标题栏灰色、&#39;3&#39; 映射为白色（文字）、&#39;4&#39; 映射为绿色（光标）。当你看到一行 <code>&quot;02111111111111111111111111111110&quot;</code> 时，直觉就能告诉你：左右各一个透明像素和标题栏像素，中间全是终端背景色——这就是终端窗口的一行。</p><p>调色板还有一个好处：不同的图标可以使用不同的调色板。在 Shell 图标里 &#39;2&#39; 是标题栏灰色，在 Calculator 图标里 &#39;2&#39; 是边框深灰色。同一个 hex 字符在不同上下文中表示不同颜色，让每个图标都能有自己独特的色彩方案。</p><h2 id="第二步——build-icon-编译时的-图片解码器" tabindex="-1">第二步——build_icon：编译时的&quot;图片解码器&quot; <a class="header-anchor" href="#第二步——build-icon-编译时的-图片解码器" aria-label="Permalink to &quot;第二步——build_icon：编译时的&quot;图片解码器&quot;&quot;">​</a></h2><p>有了调色板和图案字符串，还需要一个工具把它们转换成像素数组。这就是 build_icon 模板函数的工作。</p><p>build_icon 在编译时运行——注意它标记为 consteval（C++20），意味着它<strong>必须</strong>在编译期求值，不允许在运行时调用。它接受两个参数：一个 16 元素的颜色查找表（<code>const uint32_t (&amp;palette)[16]</code>）和一个包含 32 个字符串的数组（<code>const char* const (&amp;rows)[Rows]</code>）。函数内部遍历每个字符串的每个字符，调用 hex_nibble 把字符转成 0-15 的数字，然后从调色板查找颜色值，写入 <code>std::array&lt;uint32_t, 1024&gt;</code> 的对应位置。</p><p>有一个容易忽略的细节：hex_nibble 函数必须同时处理大写和小写的 hex 字符。如果你只处理了小写 &#39;a&#39;-&#39;f&#39; 而忘记了 &#39;A&#39;-&#39;F&#39;，那么在图案中不小心用了大写字母的像素全部会被映射为 0（透明色）——图标会出现莫名其妙的空洞。</p><p>还有一个 static_assert 检查行数必须恰好为 32。这是防止粗心大意的保险——如果你只写了 31 行或者多写了一行，编译会直接报错。</p><h2 id="和-linux-启动-logo-的对比" tabindex="-1">和 Linux 启动 Logo 的对比 <a class="header-anchor" href="#和-linux-启动-logo-的对比" aria-label="Permalink to &quot;和 Linux 启动 Logo 的对比&quot;">​</a></h2><p>Linux 内核在启动时显示企鹅 logo 的方式和我们的做法很相似，都是把像素数据编译进内核。但实现方式截然不同。</p><p>Linux 的做法是在 <code>include/linux/linux_logo.h</code> 中用一大堆预处理宏（<code>__INITDATA</code>、<code>PIXEL</code> 等）把像素值一个个列出来。每个像素是一个直接的 <code>0xRRGGBB</code> 值，可读性几乎为零。修改一个 Linux 启动 logo 需要用外部工具先把图片转成 C 数组，然后再手动粘贴到头文件里。</p><p>Cinux 的做法是用 consteval 模板 + 调色板索引，让图标定义保持在人类可读的范围内。你不需要任何外部工具——直接在代码里画 ASCII art 就行。修改图标形状只需要改几行字符串，改颜色只需要调整调色板的几个值。</p><p>从编译效果来看，两者没有区别——最终都是 <code>.rodata</code> 段的常量数据。但开发体验天差地别。Linux 的方式是&quot;工具生成 → 人工粘贴&quot;，Cinux 的方式是&quot;人工编写 → 编译器转换&quot;。后者在教育和迭代场景下明显更友好。</p><h2 id="第三步——两个实际图标的诞生" tabindex="-1">第三步——两个实际图标的诞生 <a class="header-anchor" href="#第三步——两个实际图标的诞生" aria-label="Permalink to &quot;第三步——两个实际图标的诞生&quot;">​</a></h2><p>有了 build_icon 工具，定义图标就变成了一件愉快的事情。</p><p>Shell 图标的设计目标是：一个看起来像终端的 32x32 小图。视觉上是这样的——深色的圆角矩形作为终端主体，顶部有一条浅灰色的标题栏，标题栏左端有红、黄、绿三个小圆点（模仿 macOS 的窗口按钮），中间区域有一个白色的 &quot;&gt;_&quot; 命令提示符和一个绿色的光标。整个图标有圆角——四个角的像素是透明的。</p><p>Calculator 图标的设计目标是：一个看起来像计算器的 32x32 小图。灰色圆角机身，顶部有一条绿色的 LCD 显示屏（上面显示 &quot;123&quot;），下面是灰色的按钮网格，最右边一列的等号按钮是橙色的。和 Shell 图标一样，四个角是透明的。</p><p>每个图标的定义分为两部分：调色板（8 种左右的有效颜色）和 32 行 hex 字符串。整个定义加起来不超过 40 行代码，但包含了 1024 个像素的完整信息。编译器在编译时调用 build_icon 把这些字符串展开成像素数组，最终结果存储在二进制的只读数据段中——内核运行时看到的就是一个普通的 <code>uint32_t</code> 数组，没有任何编译时计算的痕迹。</p><h2 id="为什么不从文件加载——再深入一点" tabindex="-1">为什么不从文件加载——再深入一点 <a class="header-anchor" href="#为什么不从文件加载——再深入一点" aria-label="Permalink to &quot;为什么不从文件加载——再深入一点&quot;">​</a></h2><p>你可能会问：等我们的 ramdisk 文件系统完善了，是不是应该改成从文件加载图标？</p><p>答案是&quot;看情况&quot;。从文件加载确实有好处——不需要重新编译就能更换图标，第三方应用可以自带图标文件。但也有代价：需要文件系统驱动正常工作，需要堆内存分配器来缓冲图像数据，需要图像格式解码器（哪怕是 BMP 这种最简单的格式），还需要一套图标资源管理器来处理查找和缓存。</p><p>对于 Cinux 当前的教学阶段来说，编译期嵌入是最佳选择。它零依赖（不需要文件系统、不需要堆分配、不需要解码器），零运行时开销（数据在 .rodata 段，不需要加载和解码时间），而且代码清晰可读（调色板索引 + hex 字符串，不像二进制图像文件那样不透明）。等到将来系统更成熟了，可以在此基础上添加文件加载能力，编译期嵌入的图标可以作为 fallback——文件加载失败时使用内置图标。</p><h2 id="收尾" tabindex="-1">收尾 <a class="header-anchor" href="#收尾" aria-label="Permalink to &quot;收尾&quot;">​</a></h2><p>到这里我们有了两个编译时生成的图标——Shell 和 Calculator。它们的数据存在于内核二进制的 <code>.rodata</code> 段，不需要任何运行时初始化，只要 include 了 icon.hpp 就能直接使用。下一章我们会把这些图标包装成 DesktopIcon 结构体，集成到窗口管理器中，让桌面不再空空荡荡。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>Linux Kernel: <a href="https://github.com/torvalds/linux/blob/master/include/linux/linux_logo.h" target="_blank" rel="noreferrer">include/linux/linux_logo.h</a> — 内核启动 logo 的编译期像素数据</li><li>SerenityOS: <a href="https://github.com/SerenityOS/serenity/blob/master/Userland/Libraries/LibGfx/Bitmap.h" target="_blank" rel="noreferrer">Bitmap.h</a> — 运行时加载 PNG 的位图类</li><li>OSDev Wiki: <a href="https://wiki.osdev.org/VBE" target="_blank" rel="noreferrer">Drawing In a Linear Framebuffer</a> — 像素渲染基础知识</li></ul>',36)])])}const b=e(l,[["render",n]]);export{s as __pageData,b as default};
