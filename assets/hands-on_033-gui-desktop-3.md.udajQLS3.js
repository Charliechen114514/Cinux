import{c as e,Q as a,j as i,m as o}from"./chunks/framework.iD28w_5a.js";const u=JSON.parse('{"title":"033-gui-desktop-3 · 桌面环境","description":"","frontmatter":{"title":"033-gui-desktop-3 · 桌面环境"},"headers":[],"relativePath":"hands-on/033-gui-desktop-3.md","filePath":"hands-on/033-gui-desktop-3.md","lastUpdated":1779686691000}'),p={name:"hands-on/033-gui-desktop-3.md"};function n(s,t,r,d,l,c){return a(),i("div",null,[...t[0]||(t[0]=[o('<h1 id="_033-gui-desktop-3-init-cpp-集成与桌面测试" tabindex="-1">033-gui-desktop-3: init.cpp 集成与桌面测试 <a class="header-anchor" href="#_033-gui-desktop-3-init-cpp-集成与桌面测试" aria-label="Permalink to &quot;033-gui-desktop-3: init.cpp 集成与桌面测试&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>前两篇我们分别实现了桌面图标管理和延迟终端创建机制。现在要把这些部件和内核的启动序列串起来，并且用充分的测试来验证整个桌面环境工作正确。本篇聚焦三个部分：init.cpp 中管道创建和 GUI 启动逻辑、gui_init.hpp/cpp 的接口变更、以及覆盖桌面环境的双轨测试策略。</p><p>完成本篇后，整个 033 tag 的桌面环境将完整可用——启动后看到桌面和图标，点击 Shell 图标弹出终端并显示 Shell 输出。前置知识是 tag 023 的 syscall init 流程和 tag 031 的 Terminal 窗口。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="init-cpp-启动序列的变化——管道先行-gui-在后" tabindex="-1">init.cpp 启动序列的变化——管道先行，GUI 在后 <a class="header-anchor" href="#init-cpp-启动序列的变化——管道先行-gui-在后" aria-label="Permalink to &quot;init.cpp 启动序列的变化——管道先行，GUI 在后&quot;">​</a></h3><p>在之前的 tag 中，init.cpp 的 GUI 初始化流程是：先调 gui_start() 拿到 Terminal 指针，然后创建管道，最后把管道连到 Terminal 上。但在新的架构中，顺序颠倒了——先创建管道，再调 set_shell_pipes() 存储管道指针，最后调 gui_start()。</p><p>新的序列是：挂载 ext2 文件系统，初始化 VFS，创建 stdin/stdout 管道对并绑定到 fd 0/fd 1，调用 set_shell_pipes() 存储管道指针，最后调用 gui_start() 注册桌面图标和 PIT tick callback。gui_start() 不再返回任何东西（返回类型从 Terminal* 变成了 void）。终端窗口延迟到用户点击 Shell 图标时才创建。</p><p>这个变化的好处是多方面的。首先，init.cpp 不再需要知道 Terminal 的具体类型——不需要 include terminal.hpp。头文件依赖只剩 <code>gui_init.hpp</code> 和 <code>ipc/pipe.hpp</code>。其次，GUI 子系统获得了终端创建的控制权——何时创建终端、怎么连接管道——这些变成了 GUI 内部的决策，不需要修改 init.cpp。</p><h3 id="gui-start-接口变更" tabindex="-1">gui_start() 接口变更 <a class="header-anchor" href="#gui-start-接口变更" aria-label="Permalink to &quot;gui_start() 接口变更&quot;">​</a></h3><p>gui_start() 的返回类型从 <code>Terminal*</code> 变成了 <code>void</code>。之前它创建 Terminal 窗口并返回指针，现在它只注册桌面图标和 PIT tick callback。终端创建被封装在 <code>create_shell_terminal()</code> 内部 helper 中，由 gui_tick_callback 在消费到 OpenShell action 时调用。</p><p>gui_init.hpp 新增了 <code>set_shell_pipes()</code> 函数，以及 <code>cinux::ipc::Pipe</code> 的前向声明。Terminal 的前向声明被移除了——gui_init.hpp 不再暴露 Terminal 类型。</p><h3 id="双轨测试策略——为什么需要两套测试" tabindex="-1">双轨测试策略——为什么需要两套测试 <a class="header-anchor" href="#双轨测试策略——为什么需要两套测试" aria-label="Permalink to &quot;双轨测试策略——为什么需要两套测试&quot;">​</a></h3><p>宿主端单元测试和内核端测试各有优势。宿主端测试在开发机上直接运行，编译快、调试方便、可以用 GDB 单步调试，但需要重新实现所有依赖（MockCanvas、MockPSFFont、甚至 MockWindowManager）。内核端测试在 QEMU 中运行，使用真实的 Framebuffer、Canvas 和 WindowManager，能发现宿主端 mock 无法覆盖的集成问题。</p><p>对于桌面环境这种多组件交互的场景，两套测试缺一不可。宿主端测试验证算法正确性（hit_test 边界、consume 重置、容量限制），内核端测试验证集成正确性（composite 不崩溃、图标和窗口的渲染顺序、真实的鼠标事件流）。</p><p>这两套测试加起来有 1658 行——比被测试的实现代码（约 200 行）多得多。这在 OS 开发中是正常的——GUI 逻辑的正确性对用户体验影响巨大，充分测试是必要的投入。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-修改-init-cpp-gui-启动逻辑" tabindex="-1">Step 1: 修改 init.cpp GUI 启动逻辑 <a class="header-anchor" href="#step-1-修改-init-cpp-gui-启动逻辑" aria-label="Permalink to &quot;Step 1: 修改 init.cpp GUI 启动逻辑&quot;">​</a></h3><p><strong>目标</strong>: init.cpp 先创建管道、调用 set_shell_pipes，再调用 gui_start()。</p><p><strong>设计思路</strong>: init.cpp 中只需要 include gui_init.hpp 和 ipc/pipe.hpp。管道创建的代码和之前类似（new Pipe + PipeReadOps/PipeWriteOps + Inode + File + FDTable 设置），但连接管道的对象从 Terminal 变成了 gui_init 模块——通过调用 set_shell_pipes() 存储。gui_start() 不再返回 Terminal 指针，不需要捕获返回值。</p><p>具体来说，init.cpp 删除了 <code>#include &quot;kernel/gui/terminal.hpp&quot;</code>，删除了 <code>auto* term = cinux::gui::gui_start()</code> 和后续的 <code>term-&gt;set_stdin_pipe/set_stdout_pipe</code> 调用。新增 <code>cinux::gui::set_shell_pipes(stdin_pipe, stdout_pipe)</code> 调用，以及将 <code>gui_start()</code> 调用移到 set_shell_pipes 之后。</p><p><strong>踩坑预警</strong>: set_shell_pipes 必须在 gui_start 之前调用。如果顺序反了，gui_start 注册的 tick callback 可能在管道指针存储之前就触发了——虽然第一次触发不太可能产生图标点击，但时序依赖总是危险的。</p><p><strong>验证</strong>: 构建内核并在 QEMU 中启动。串口日志应该依次显示管道连接成功、Shell pipes stored、Desktop icons registered。</p><h3 id="step-2-编写内核端桌面测试" tabindex="-1">Step 2: 编写内核端桌面测试 <a class="header-anchor" href="#step-2-编写内核端桌面测试" aria-label="Permalink to &quot;Step 2: 编写内核端桌面测试&quot;">​</a></h3><p><strong>目标</strong>: 在 QEMU 中验证 WindowManager 的桌面图标功能。</p><p><strong>设计思路</strong>: 内核端测试使用真实的 Framebuffer 和 Canvas，初始化 WindowManager 并添加图标，然后验证 hit_test、consume、composite 的行为。测试入口函数在 main_test.cpp 中声明和调用——通过 RUN_TEST 宏注册，在 kernel_main 的测试序列中执行。</p><p>测试分为几个组。WM 初始化和图标注册组验证 init 后添加图标成功、容量满后返回 false、多个图标分别可命中。hit_test 边界组验证边界像素（左上角命中、右下角+1 像素未命中）、重叠时后注册图标优先。consume 和点击分派组验证无 pending 时返回 None、点击图标设置 action、桌面空白点击不设 action。composite 渲染组验证图标在窗口之下。</p><p>测试图标使用纯色填充——白色 Shell 图标、橙色 Calculator 图标。不需要复杂的像素图案，因为测试关注的是位置和命中测试，不是图像内容。</p><p>内核端测试中需要特别注意 Framebuffer 的初始化——它需要有效的 BootInfo 结构。测试代码通常在固定的物理地址（0x7000）读取 BootInfo，确保 bootloader 已经正确设置了该地址的数据。如果 BootInfo 不正确，Framebuffer::init 会失败，后续所有依赖 Canvas 的测试都会崩溃。</p><p><strong>踩坑预警</strong>: CMakeLists.txt 中要把 test_desktop.cpp 添加到 big_kernel_test 的源文件列表。如果你忘了加，run_desktop_tests 函数的符号就不会被链接进来，内核启动时不会执行桌面测试。这个错误不会导致编译失败——只是测试不运行。</p><p><strong>验证</strong>: 在 QEMU 中运行 big_kernel_test，串口输出应该显示 &quot;Desktop Tests (033_gui_desktop)&quot; 测试区块，所有 16 个测试通过。特别注意 composite 相关测试——它们检查真实 framebuffer 上的像素值。</p><h3 id="step-3-编写宿主端桌面测试" tabindex="-1">Step 3: 编写宿主端桌面测试 <a class="header-anchor" href="#step-3-编写宿主端桌面测试" aria-label="Permalink to &quot;Step 3: 编写宿主端桌面测试&quot;">​</a></h3><p><strong>目标</strong>: 在开发机上验证桌面图标算法的正确性。</p><p><strong>设计思路</strong>: 宿主端测试完全重新实现了 WindowManager 中与桌面图标相关的所有类型和逻辑。MockCanvas 实现了完整的像素写入、draw_bitmap、clear 等方法。MockWindowManager 完整复现了 add_desktop_icon、hit_test_icon、consume_pending_icon_action、handle_mouse 的桌面图标逻辑。测试覆盖了 add/hit/consume 的所有边界条件。</p><p>MockCanvas 的关键方法是 <code>pixel()</code> 访问器——测试用它在 composite 后检查特定像素的值。MockWindowManager 中的 hit_test_icon 和 hit_test（窗口）都要从后向前遍历，保持和真实代码一致——如果 mock 的遍历方向反了，Z 优先级测试就会失败。</p><p>CMakeLists.txt 中新增 test_desktop 可执行文件，并将其加入 test_host、test_all、test_verbose 的依赖列表。这样每次运行 <code>make test_host</code> 都会自动编译和运行桌面测试。</p><p><strong>踩坑预警</strong>: MockWindowManager 中的 handle_mouse 实现要特别注意——它需要复现完整的点击分派逻辑（窗口优先 -&gt; 图标 -&gt; 桌面）。如果 mock 的分派逻辑和真实代码不一致，&quot;窗口覆盖图标时不触发图标 action&quot; 这个测试就会通过但实际行为是错的。</p><p><strong>验证</strong>: 在构建目录中运行 <code>ctest -R desktop --output-on-failure</code>，所有测试应该通过。大约有 20 个测试用例覆盖了 add、hit、consume、click、composite 等所有维度。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>最终构建分三步。</p><p>第一步构建宿主端测试并运行 ctest，确认 1123 行的宿主端测试全部通过。这些测试覆盖了桌面图标管理的所有算法正确性。</p><p>第二步构建 big_kernel_test 并在 QEMU 中运行，确认 535 行的内核端测试全部通过。这些测试验证了真实硬件环境下的集成正确性。</p><p>第三步构建 big_kernel 并在 QEMU 中启动，进行最终的视觉验证。你应该能看到深青色桌面背景、左侧两个图标（Shell 和 Calculator，白色标签居中）、鼠标光标可用。点击 Shell 图标，终端窗口弹出并显示 Shell 输出。整个流程从启动到桌面到交互一气呵成。</p><h2 id="测试策略的深层考量" tabindex="-1">测试策略的深层考量 <a class="header-anchor" href="#测试策略的深层考量" aria-label="Permalink to &quot;测试策略的深层考量&quot;">​</a></h2><p>在动手完成所有步骤之后，我们来看看这个 tag 的测试策略中有哪些值得讨论的设计决策。</p><h3 id="为什么宿主端测试要完全重实现-windowmanager" tabindex="-1">为什么宿主端测试要完全重实现 WindowManager <a class="header-anchor" href="#为什么宿主端测试要完全重实现-windowmanager" aria-label="Permalink to &quot;为什么宿主端测试要完全重实现 WindowManager&quot;">​</a></h3><p>一个常见的疑问是：为什么要花 1123 行代码重新实现一个 MockWindowManager，而不是想办法把内核的 WindowManager 拿到宿主端测试中？答案是依赖关系太深了。WindowManager 依赖 Canvas（依赖 Framebuffer）、PSFFont（依赖 BIOS 字体加载）、Window（依赖事件系统）、DesktopIcon（依赖 icon_data）——层层展开后，你几乎要把整个 GUI 子系统都搬到宿主端。</p><p>重实现虽然代码量大，但有几个好处。首先是隔离性——MockWindowManager 的行为完全由测试控制，不会因为内核代码的修改而意外改变。其次是编译速度——mock 类很轻量，不需要链接任何内核代码。最后是灵活性——你可以在 mock 中暴露内部状态（如 pending_icon_action() 访问器）供测试直接检查，而不用通过公开 API 间接验证。</p><h3 id="测试覆盖的边界案例" tabindex="-1">测试覆盖的边界案例 <a class="header-anchor" href="#测试覆盖的边界案例" aria-label="Permalink to &quot;测试覆盖的边界案例&quot;">​</a></h3><p>宿主端测试中有几个特别值得注意的边界案例。第一个是&quot;零尺寸图标&quot;——一个 width 或 height 为 0 的 DesktopIcon。contains() 方法在 width/height 为 0 时会立即返回 false（因为 mx &lt; x + 0 永远不成立），所以零尺寸图标永远不会被命中。这个行为是正确的——零尺寸的东西不应该有可点击区域。</p><p>第二个是&quot;图标在屏幕原点&quot;——DesktopIcon 的 x 和 y 都为 0。contains() 使用 <code>&gt;=</code> 和 <code>&lt;</code> 比较，所以 (0, 0) 是包含在内的。这个边界值在实现中经常出错——如果你不小心用了 <code>&gt;</code> 代替 <code>&gt;=</code>，原点就会变成不命中。</p><p>第三个是&quot;init 重置&quot;——在添加图标后重新调用 init，确认计数器和 pending action 都被重置。这个测试确保了 WindowManager 可以在测试之间安全地重复初始化，不会出现状态泄漏。</p><h3 id="与-xv6-gui-测试的对比" tabindex="-1">与 xv6-gui 测试的对比 <a class="header-anchor" href="#与-xv6-gui-测试的对比" aria-label="Permalink to &quot;与 xv6-gui 测试的对比&quot;">​</a></h3><p>xv6-gui 几乎没有 GUI 测试——它依赖&quot;在 QEMU 中跑起来看看&quot;来验证 GUI 功能。这在简单的单窗口 GUI 中还行，但在有多窗口、多图标交互的场景下会遗漏很多 bug。比如&quot;窗口覆盖图标时点击窗口不应该触发图标动作&quot;这种边界行为，不做自动化测试几乎不可能每次手动验证。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>宿主端测试编译错误</strong>: 检查 CMakeLists.txt 中的 include 目录是否包含了 mock 需要的头文件路径。test_framework.h 的路径要正确。如果链接时报 undefined reference，检查 CMakeLists.txt 中是否把 test_desktop.cpp 添加到了正确的可执行文件。</p><p><strong>内核测试中 get_pixel 返回错误颜色</strong>: 确认 Framebuffer 和 Canvas 已经正确初始化。composite 前调了 clear(0) 吗？如果 clear 调了但 get_pixel 仍然返回 0，说明 Canvas 的 init 有问题——检查 BootInfo 中的 framebuffer 地址和尺寸是否正确。</p><p><strong>终端弹出但 Shell 无响应</strong>: 检查 set_shell_pipes 是否在 gui_start 之前被调用。在 create_shell_terminal 中打印管道地址，确认 g_stdin_pipe 和 g_stdout_pipe 不为空。如果管道指针为空，说明 init.cpp 中的 set_shell_pipes 调用时序不对。</p><p><strong>管道连接但 Shell 无输出</strong>: 检查 Terminal::poll_output 是否被调用。在 poll_output 开头加 kprintf 确认。如果 poll 被调了但没有数据，检查 Shell 进程是否在运行——Shell 在 boot 时通过 launch_first_user 启动，它应该在等待 stdin 输入。如果 Shell 没有启动，检查 init.cpp 中 fd 0/fd 1 的绑定是否正确。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>变更</th><th>文件</th><th>关键点</th></tr></thead><tbody><tr><td>启动序列调整</td><td>init.cpp</td><td>管道创建 -&gt; set_shell_pipes -&gt; gui_start</td></tr><tr><td>gui_start 接口</td><td>gui_init.hpp</td><td>返回 void，新增 set_shell_pipes</td></tr><tr><td>内核端测试</td><td>kernel/test/test_desktop.cpp</td><td>535 行，16 个测试，真实 Framebuffer</td></tr><tr><td>宿主端测试</td><td>test/unit/test_desktop.cpp</td><td>1123 行，约 20 个测试，Mock 重实现</td></tr><tr><td>CMake 集成</td><td>两个 CMakeLists.txt</td><td>添加 test_desktop 到构建目标</td></tr><tr><td>main_test 注册</td><td>kernel/test/main_test.cpp</td><td>声明并调用 run_desktop_tests</td></tr></tbody></table>',61)])])}const _=e(p,[["render",n]]);export{u as __pageData,_ as default};
