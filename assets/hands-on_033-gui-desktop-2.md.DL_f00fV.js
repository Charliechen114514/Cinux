import{c as t,Q as i,j as l,m as a}from"./chunks/framework.D9IbVcwA.js";const h=JSON.parse('{"title":"033-gui-desktop-2 · 桌面环境","description":"","frontmatter":{"title":"033-gui-desktop-2 · 桌面环境"},"headers":[],"relativePath":"hands-on/033-gui-desktop-2.md","filePath":"hands-on/033-gui-desktop-2.md","lastUpdated":1779686691000}'),o={name:"hands-on/033-gui-desktop-2.md"};function n(p,e,r,s,d,c){return i(),l("div",null,[...e[0]||(e[0]=[a('<h1 id="_033-gui-desktop-2-延迟终端创建与管道解耦" tabindex="-1">033-gui-desktop-2: 延迟终端创建与管道解耦 <a class="header-anchor" href="#_033-gui-desktop-2-延迟终端创建与管道解耦" aria-label="Permalink to &quot;033-gui-desktop-2: 延迟终端创建与管道解耦&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一篇我们在 WindowManager 中搭建了桌面图标的管理和点击检测。现在问题来了：用户点击 Shell 图标后，pending_icon_action_ 被设为 OpenShell，但谁来消费这个 action？谁来创建终端窗口？这就是本篇要解决的核心问题——从&quot;图标被点击&quot;到&quot;终端窗口弹出来&quot;之间的延迟创建机制。</p><p>完成本篇后，点击 Shell 图标将弹出一个终端窗口，连接预先创建的 Shell 管道对。前置知识是 tag 031 的 Terminal 窗口和 tag 024 的管道 IPC。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="延迟终端创建——为什么不在启动时就建好" tabindex="-1">延迟终端创建——为什么不在启动时就建好 <a class="header-anchor" href="#延迟终端创建——为什么不在启动时就建好" aria-label="Permalink to &quot;延迟终端创建——为什么不在启动时就建好&quot;">​</a></h3><p>在之前的架构中，<code>gui_start()</code> 在内核初始化时就创建了一个 Terminal 窗口，init.cpp 直接拿到 Terminal 指针连接管道。这个方案简单直接，但有一个问题：Terminal 窗口在 GUI 启动时就立即出现，用户还没看到桌面就弹出了终端。</p><p>延迟创建的思路是：<code>gui_start()</code> 不再创建终端窗口，而是只注册桌面图标。终端窗口在用户点击 Shell 图标时才通过 <code>create_shell_terminal()</code> 创建。但 Shell 进程仍然在 boot 时就启动（通过 <code>launch_first_user</code>），管道在 init.cpp 中预先创建好并通过 <code>set_shell_pipes()</code> 存储到 gui_init 模块变量中。</p><p>这个设计的核心是&quot;延迟的是窗口创建，不是 Shell 启动&quot;。Shell 从 boot 开始就在运行，它的 stdin/stdout 绑定了 pipe fd。在用户点击 Shell 图标之前，Shell 的输出暂存在 pipe buffer 里。终端窗口创建后，Terminal 通过 <code>poll_output()</code> 取回暂存的输出并显示。</p><h3 id="set-shell-pipes——管道指针的延迟绑定" tabindex="-1">set_shell_pipes——管道指针的延迟绑定 <a class="header-anchor" href="#set-shell-pipes——管道指针的延迟绑定" aria-label="Permalink to &quot;set_shell_pipes——管道指针的延迟绑定&quot;">​</a></h3><p>init.cpp 在启动 GUI 之前创建好管道对，然后调用 <code>set_shell_pipes(stdin_pipe, stdout_pipe)</code> 将管道指针存入 gui_init 模块的匿名命名空间变量 <code>g_stdin_pipe</code> 和 <code>g_stdout_pipe</code>。之后 <code>gui_start()</code> 注册桌面图标和 PIT tick callback。当用户点击 Shell 图标时，<code>create_shell_terminal()</code> 从模块变量中取出管道指针连接到新创建的 Terminal。</p><p>这种&quot;预先存储、延迟使用&quot;的模式确保了管道的创建时序和 GUI 的启动时序解耦。init.cpp 负责管道创建和 Shell fd 绑定，gui_init.cpp 负责终端窗口的延迟创建。</p><h3 id="is-terminal-类型安全的-downcast" tabindex="-1">is_terminal() 类型安全的 downcast <a class="header-anchor" href="#is-terminal-类型安全的-downcast" aria-label="Permalink to &quot;is_terminal() 类型安全的 downcast&quot;">​</a></h3><p><code>gui_tick_callback</code> 需要对聚焦的 Terminal 窗口做 poll_output 和 render_to_canvas 操作。在之前只有一个 Terminal 的场景下，直接对 focused 窗口做 <code>static_cast&lt;Terminal*&gt;</code> 是安全的。但现在桌面上可以有非 Terminal 窗口（比如未来的 Calculator），盲转就会出现未定义行为。</p><p>解决方案是经典的 &quot;type tag via virtual function&quot; 模式：Window 基类定义一个虚函数返回 false，Terminal 子类 override 返回 true。在 gui_tick_callback 中先检查 <code>is_terminal()</code>，只有确认是 Terminal 时才做 downcast 并调用 poll_output 和 render_to_canvas。这比 C++ 的 <code>dynamic_cast</code>（需要 RTTI）更轻量，因为 RTTI 在 freestanding kernel 中通常被禁用。</p><p>这种模式在操作系统内核中很常见。Linux 内核的 <code>struct file_operations</code> 就是一个类似的概念——通过函数指针表（vtable）来区分不同文件类型的操作。Cinux 用 C++ 的虚函数实现了同样的效果，代码更简洁。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-实现-set-shell-pipes-与模块变量" tabindex="-1">Step 1: 实现 set_shell_pipes 与模块变量 <a class="header-anchor" href="#step-1-实现-set-shell-pipes-与模块变量" aria-label="Permalink to &quot;Step 1: 实现 set_shell_pipes 与模块变量&quot;">​</a></h3><p><strong>目标</strong>: 在 gui_init 模块中存储管道指针，供延迟终端创建时使用。</p><p><strong>设计思路</strong>: gui_init.cpp 的匿名命名空间中新增两个模块变量 <code>g_stdin_pipe</code> 和 <code>g_stdout_pipe</code>，类型都是 <code>cinux::ipc::Pipe*</code>，初始值为 nullptr。公开函数 <code>set_shell_pipes(stdin_pipe, stdout_pipe)</code> 将传入的指针存入这两个变量，并打印日志确认存储成功。</p><p>匿名命名空间保证了这两个变量只在 gui_init.cpp 内部可见——其他文件无法直接访问，只能通过 <code>set_shell_pipes()</code> 设置。这个封装确保了管道指针不会被意外修改。</p><p><strong>踩坑预警</strong>: <code>set_shell_pipes</code> 必须在 <code>gui_start()</code> 之前调用。如果顺序反了，<code>create_shell_terminal()</code> 在创建终端时 <code>g_stdin_pipe</code> 和 <code>g_stdout_pipe</code> 都是 nullptr，终端虽然会创建但不会连接管道——点击终端无法输入，Shell 的输出也不会显示。</p><p><strong>验证</strong>: 在 set_shell_pipes 中已有 kprintf 打印管道地址。串口日志应显示 &quot;[GUI] Shell pipes stored: stdin=0xXXXX stdout=0xXXXX&quot;。</p><h3 id="step-2-实现-create-shell-terminal-内部-helper" tabindex="-1">Step 2: 实现 create_shell_terminal 内部 helper <a class="header-anchor" href="#step-2-实现-create-shell-terminal-内部-helper" aria-label="Permalink to &quot;Step 2: 实现 create_shell_terminal 内部 helper&quot;">​</a></h3><p><strong>目标</strong>: 封装&quot;创建终端窗口 + 连接管道&quot;的完整流程。</p><p><strong>设计思路</strong>: 这个 helper 做三件事：计算终端尺寸和屏幕居中位置、创建 Terminal 对象并设置字体、连接预先存储的管道指针。</p><p>终端尺寸固定为 80 列乘 8 像素列宽、25 行乘 16 像素行高（即 640x400 像素）。位置在屏幕上居中——计算方法是 <code>(screen_width - terminal_width) / 2</code>。如果屏幕太小（终端加边距超过屏幕），就使用默认偏移（80, 60）。居中计算中要注意整数除法的截断——不用浮点数，因为内核态没有浮点运算支持。</p><p>创建 Terminal 时使用 <code>new</code> 在堆上分配，设置字体，然后检查模块变量 <code>g_stdin_pipe</code> 和 <code>g_stdout_pipe</code> 是否非空——非空就通过 <code>set_stdin_pipe</code> 和 <code>set_stdout_pipe</code> 连接。最后调用 <code>wm.add_window(term)</code> 将终端添加到窗口管理器。</p><p><strong>踩坑预警</strong>: 居中计算中如果 screen 为 null（gui_init 没被调用），会使用默认偏移 (80, 60)。这个默认值在 1024x768 的屏幕上看起来偏左上角，但至少不会崩溃。如果你发现终端位置不对，先检查 g_screen 是否被正确初始化。</p><p><strong>验证</strong>: 点击 Shell 图标后，串口输出应该显示 &quot;[GUI] Shell terminal created and connected.&quot;。屏幕上应该弹出一个居中的终端窗口，窗口内显示 Shell 暂存在 pipe buffer 中的输出。</p><h3 id="step-3-修改-gui-tick-callback-消费-action-并创建终端" tabindex="-1">Step 3: 修改 gui_tick_callback 消费 action 并创建终端 <a class="header-anchor" href="#step-3-修改-gui-tick-callback-消费-action-并创建终端" aria-label="Permalink to &quot;Step 3: 修改 gui_tick_callback 消费 action 并创建终端&quot;">​</a></h3><p><strong>目标</strong>: 在 tick callback 中消费 pending action，直接调用 create_shell_terminal。</p><p><strong>设计思路</strong>: gui_tick_callback 的执行顺序是：先处理输入事件（handle_mouse 设置 pending action），然后 consume_pending_icon_action() 检查是否有待处理的 action——如果是 OpenShell 就直接调用 create_shell_terminal()。然后对聚焦的窗口检查 is_terminal()，是 Terminal 就做 poll_output 和 render_to_canvas。最后 composite。</p><p>关键点：consume 和 create_shell_terminal 在同一个 tick callback 中同步执行。这与我们当前的单线程 GUI 模型一致——PIT tick callback 是 GUI 的主循环，所有 GUI 操作都在这个上下文中完成。</p><p><strong>踩坑预警</strong>: 注意 poll_output 只对聚焦的窗口执行，不是所有窗口。之前遍历所有窗口的做法在没有非 Terminal 窗口时是安全的，但现在需要通过 is_terminal() 守卫 static_cast。如果 focused 是非 Terminal 窗口（比如未来的 Calculator），不做 is_terminal() 检查就 static_cast 会导致未定义行为。</p><p><strong>验证</strong>: 点击 Shell 图标后终端窗口弹出，再次点击 Shell 图标会再弹出一个终端窗口——两个终端共享同一对管道。注意 tag 033 的设计是共享管道，后续 tag 会改为 per-terminal 管道。</p><h3 id="step-4-修改-init-cpp-启动序列" tabindex="-1">Step 4: 修改 init.cpp 启动序列 <a class="header-anchor" href="#step-4-修改-init-cpp-启动序列" aria-label="Permalink to &quot;Step 4: 修改 init.cpp 启动序列&quot;">​</a></h3><p><strong>目标</strong>: init.cpp 创建管道，调用 set_shell_pipes，再调用 gui_start。</p><p><strong>设计思路</strong>: init.cpp 的 GUI 部分分三步：先创建 stdin/stdout 管道对并绑定到 Shell 进程的 fd 0/fd 1，然后调用 <code>set_shell_pipes(stdin_pipe, stdout_pipe)</code> 存储管道指针，最后调用 <code>gui_start()</code> 注册桌面图标和 PIT tick callback。</p><p>注意 <code>gui_start()</code> 的返回类型从 <code>Terminal*</code> 变成了 <code>void</code>——它不再创建终端，也不再返回任何东西。init.cpp 不需要捕获返回值。init.cpp 也不再 include terminal.hpp——所有 Terminal 相关的操作都封装在 gui_init.cpp 内部。</p><p><strong>踩坑预警</strong>: init.cpp 中删除了之前对 <code>term-&gt;set_stdin_pipe()</code> 和 <code>term-&gt;set_stdout_pipe()</code> 的调用。这些操作现在在 <code>create_shell_terminal()</code> 内部完成。如果还保留旧代码，会在 nullptr 上调用方法导致崩溃。</p><p><strong>验证</strong>: 在 QEMU 中启动内核，串口日志应该依次显示：&quot;[INIT] Terminal-shell pipes connected: stdin_pipe=0xXXXX stdout_pipe=0xXXXX&quot;、&quot;[GUI] Shell pipes stored: stdin=0xXXXX stdout=0xXXXX&quot;、&quot;[GUI] Desktop icons registered: Shell, Calculator.&quot;。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>构建完整内核并在 QEMU 中启动。启动后应该看到深青色桌面背景和两个图标。用鼠标点击 Shell 图标，应该弹出一个居中的终端窗口。终端中应该显示 Shell 的启动输出——因为 Shell 在 boot 时就已启动，输出暂存在 pipe buffer 中，Terminal 连接后通过 poll_output 取回。</p><p>点击桌面空白区域，终端窗口应该失去焦点（标题栏变灰）。再点击终端窗口标题栏，终端重新获得焦点。</p><h2 id="设计考量——延迟窗口创建与管道解耦" tabindex="-1">设计考量——延迟窗口创建与管道解耦 <a class="header-anchor" href="#设计考量——延迟窗口创建与管道解耦" aria-label="Permalink to &quot;设计考量——延迟窗口创建与管道解耦&quot;">​</a></h2><p>在动手实现完成之后，我们退后一步，看看这个架构选择背后的设计权衡。</p><h3 id="延迟的是窗口-不是-shell" tabindex="-1">延迟的是窗口，不是 Shell <a class="header-anchor" href="#延迟的是窗口-不是-shell" aria-label="Permalink to &quot;延迟的是窗口，不是 Shell&quot;">​</a></h3><p>一个关键的设计选择是：Shell 进程仍然在 boot 时启动，不延迟。延迟的只是终端窗口的创建。这样做的好处是 Shell 的启动不受 GUI 的影响——即使 GUI 子系统还没初始化，Shell 就已经在运行了。坏处是管道需要在 init.cpp 中预先创建并存储。</p><p>另一种方案是点击图标时才 fork + execve Shell。这在多终端场景下更灵活（每次点击创建独立 Shell），但当前 tag 我们还没有多终端的需求，所以选择了更简单的&quot;预启动 Shell + 延迟窗口&quot;方案。</p><h3 id="terminal-析构函数的简化" tabindex="-1">Terminal 析构函数的简化 <a class="header-anchor" href="#terminal-析构函数的简化" aria-label="Permalink to &quot;Terminal 析构函数的简化&quot;">​</a></h3><p>Terminal 的析构函数在 tag 033 中被简化了——它不再关闭管道端点，只是将 stdin_pipe_ 和 stdout_pipe_ 指针设为 nullptr。原因是管道由外部（init.cpp）创建和拥有，Terminal 只持有引用。关闭管道端点应该是管道所有者的责任，不是 Terminal 的责任。这也为多终端共享管道的场景留了余地。</p><h3 id="为什么-is-terminal-而不是-enum-type" tabindex="-1">为什么 is_terminal() 而不是 enum type() <a class="header-anchor" href="#为什么-is-terminal-而不是-enum-type" aria-label="Permalink to &quot;为什么 is_terminal() 而不是 enum type()&quot;">​</a></h3><p>另一种实现类型安全 downcast 的方式是在 Window 基类中添加一个 <code>enum class WindowType { Generic, Terminal, Calculator }</code> 和一个 <code>virtual WindowType type() const</code> 方法。这种方式在有 5 种以上窗口类型时更有优势——你可以用 switch 语句统一处理，而不是一堆 if-else。</p><p>但 Cinux 目前只有两种窗口类型（Terminal 和非 Terminal），一个 bool 返回值就足够了。如果将来窗口类型增多，可以无缝迁移到 enum 方案——is_terminal() 可以变成 <code>type() == WindowType::Terminal</code> 的语法糖。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p>这一节列出本章最常见的四个 bug 和排查方法。</p><p><strong>点击 Shell 图标没反应</strong>: 在 gui_tick_callback 中加 kprintf 打印 consume_pending_icon_action() 的返回值。如果返回 None，说明 handle_mouse 没有检测到图标点击——检查 hit_test_icon 的坐标范围。如果返回 OpenShell 但终端没弹出来，检查 create_shell_terminal 是否被调用——在函数开头加 kprintf 确认。</p><p><strong>终端弹出但没有 Shell 输出</strong>: 检查 set_shell_pipes 是否在 gui_start 之前被调用。串口日志中应该有 &quot;[GUI] Shell pipes stored&quot; 输出，且地址非零。如果地址是 0x0，说明管道还没创建就调了 set_shell_pipes。另外检查 Terminal::poll_output 是否被调用——在 poll_output 开头加 kprintf。</p><p><strong>终端弹出后立即崩溃</strong>: 检查 Terminal::set_font 是否被调用了。没有 font 的 Terminal 在 render_to_canvas 时会访问空指针。在 create_shell_terminal 中加 kprintf 确认 set_font 被调用了，并且 g_font 不是 null。g_font 是在 gui_init() 中设置的——如果 gui_init 没被调用或者传入的 font 是 null，Terminal 就会出问题。</p><p><strong>管道连接但 Shell 无响应</strong>: 检查 init.cpp 中 fd 0 和 fd 1 是否正确绑定到管道的读写端。stdin 管道的读端应该绑定到 fd 0（Shell 通过 read(0) 读取），stdout 管道的写端应该绑定到 fd 1（Shell 通过 write(1) 写入）。如果读写端绑反了，Shell 读不到输入也写不出输出。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>职责</th><th>关键设计</th></tr></thead><tbody><tr><td>g_stdin_pipe / g_stdout_pipe</td><td>存储预创建的管道指针</td><td>模块变量，由 set_shell_pipes 设置</td></tr><tr><td>set_shell_pipes</td><td>延迟绑定管道</td><td>init.cpp 调用，gui_start 之前</td></tr><tr><td>create_shell_terminal</td><td>创建终端 + 连接管道</td><td>从模块变量取指针，add_window</td></tr><tr><td>gui_tick_callback</td><td>消费 action + poll 聚焦终端</td><td>直接调 create_shell_terminal</td></tr><tr><td>is_terminal()</td><td>类型安全 downcast</td><td>virtual 函数替代 RTTI</td></tr></tbody></table>',63)])])}const u=t(o,[["render",n]]);export{h as __pageData,u as default};
