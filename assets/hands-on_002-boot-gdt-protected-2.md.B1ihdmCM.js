import{c as o,Q as d,j as s,m as c}from"./chunks/framework.D9IbVcwA.js";const h=JSON.parse('{"title":"002-boot-gdt-protected-2 · GDT 与保护模式","description":"","frontmatter":{"title":"002-boot-gdt-protected-2 · GDT 与保护模式"},"headers":[],"relativePath":"hands-on/002-boot-gdt-protected-2.md","filePath":"hands-on/002-boot-gdt-protected-2.md","lastUpdated":1779686691000}'),t={name:"hands-on/002-boot-gdt-protected-2.md"};function a(i,e,p,n,l,r){return d(),s("div",null,[...e[0]||(e[0]=[c(`<h1 id="_002-2-·-linker-script-修正与-gdt-定义" tabindex="-1">002-2 · Linker Script 修正与 GDT 定义 <a class="header-anchor" href="#_002-2-·-linker-script-修正与-gdt-定义" aria-label="Permalink to &quot;002-2 · Linker Script 修正与 GDT 定义&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一篇我们理清了保护模式的核心概念，修掉了 <code>serial.S</code> 的 <code>.code16gcc</code> 汇编模式隐患。这一篇我们继续打地基：修正 linker script 的基地址、新增 QEMU debugcon 调试支持，然后在 <code>stage2.S</code> 中定义一张完整的 GDT。</p><p>这几个改动看起来都不起眼，但说实话，每一个不动都会在后续的保护模式切换中让你收获一个漂亮的三重故障（Triple Fault）。</p><p>我们先回顾一下内存布局的大图。MBR 通过 BIOS INT 0x13 扩展读把 stage2.bin 加载到物理地址 <code>0x8000</code>。linker script 必须以 <code>0x8000</code> 为基址，这样所有绝对地址引用（尤其是 GDT pointer 里的 base 地址）才能在运行时指向正确的物理内存。</p><p>如果 base 地址和加载地址不一致，<code>lgdt</code> 读到的 GDT 地址就是错的，CPU 加载一堆垃圾描述符，后续保护模式切换直接炸——而且这个问题的排查难度极高，因为症状和&quot;代码写错了&quot;完全一样（三重故障），但你得先意识到&quot;不是我代码写错了，是我给链接器的指示写错了&quot;才能找到根因。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-2-修正-linker-script-基地址" tabindex="-1">Step 2: 修正 Linker Script 基地址 <a class="header-anchor" href="#step-2-修正-linker-script-基地址" aria-label="Permalink to &quot;Step 2: 修正 Linker Script 基地址&quot;">​</a></h3><p><strong>目标</strong>: 把 Stage2 的 linker script 中 <code>.text</code> section 的起始地址从 <code>0x0</code> 改为 <code>0x8000</code>，同时新增一个 <code>.gdt</code> section 放在 <code>.text</code> 之后、<code>.rodata</code> 之前，并设置 8 字节对齐。在末尾添加断言，确保 stage2 总大小不超过 32KB。</p><p><strong>设计思路</strong>: 为什么 linker base 地址必须匹配加载地址？</p><p>因为 MBR 把 Stage2 从磁盘读出来后，是直接加载到物理内存 <code>0x8000</code> 的（在 <code>mbr.S</code> 中定义的 <code>STAGE2_LOAD_ADDR</code>）。如果 linker script 里写的 base 地址是 <code>0x0</code>，那链接器在解析所有绝对地址引用时（比如 GDT 指针里的 base 地址），都会基于 <code>0x0</code> 来计算。</p><p>但运行时这段代码实际在 <code>0x8000</code>，于是 <code>lgdt</code> 读到的 GDT 地址会指向错误的位置——比如 <code>gdt</code> 符号被链接成 <code>0x0160</code>，但实际物理地址应该是 <code>0x8160</code>，中间差了 <code>0x8000</code>。轻则加载了垃圾数据，重则直接 triple fault。</p><p>把 base 改成 <code>0x8000</code> 之后，链接器算出来的所有绝对地址都会加上 <code>0x8000</code> 的偏移，和运行时的物理地址完全匹配。</p><p>这个问题的排查难度极高，因为症状和&quot;代码写错了&quot;完全一样——三重故障——但你得先意识到&quot;不是我代码写错了，是我给链接器的指示写错了&quot;才能找到根因。</p><p>新增的 <code>.gdt</code> section 是为了让 GDT 数据结构有自己独立的、8 字节对齐的存储区域，而不是混在 <code>.text</code> 或 <code>.rodata</code> 里。Intel SDM Vol.3A Section 3.5.1 建议描述符表 8 字节对齐以提高性能，虽然不是强制要求，但对齐访问确实更高效，而且描述符本身就是 8 字节的，对齐后每个描述符的起始地址都是整齐的。</p><p>如果 GDT 定义放在 <code>.text</code> 中间，会有两个问题：一是 GDT 可能不被 8 字节对齐，二是 GDT 数据会被夹杂在可执行代码中间，影响 <code>.text</code> 的 cache 局部性。</p><p>linker script 还需要加一个断言：Stage2 的总大小不能超过 <code>0x10000</code>（即 32KB，从 0x8000 到 0x10000），这是 MBR 加载时的空间限制。修改位置在 <code>boot/CMakeLists.txt</code> 中通过 <code>file(WRITE ...)</code> 生成的 linker script。</p><p>修改后的 linker script 结构如下：<code>.text</code> 在最前面（从 <code>0x8000</code> 开始），<code>.gdt</code> 紧随其后并 8 字节对齐，然后是 <code>.rodata</code>、<code>.data</code> 和 <code>.bss</code>。最后的 <code>ASSERT(. &lt;= 0x10000, ...)</code> 确保整个 stage2 不超过 32KB。</p><p><strong>踩坑预警</strong>: 你可能会问：为什么 stage2 被加载到 <code>0x8000</code> 而不是别的地址？</p><p>这是在 MBR 的 <code>mbr.S</code> 里决定的——MBR 通过 BIOS INT 0x13 扩展读把 stage2 加载到 <code>0x0000:0x8000</code>。这个地址选择没有特殊含义，只要不跟 MBR 自身（<code>0x7C00</code>）、BIOS 数据区（<code>0x400</code>-<code>0x4FF</code>）、VESA 缓冲区（<code>0x6000</code>-<code>0x64FF</code>）冲突就行。</p><p>但关键是：linker script 的 base 必须与这个加载地址严格一致，否则所有绝对地址引用全错。xv6 把它的 boot sector 链接在 <code>0x7C00</code>（bootasm.S 和 MBR 放在同一个位置），而 Cinux 的两阶段设计让 stage2 有了自己独立的加载地址和链接地址。</p><p><strong>验证</strong>: 构建完成后，用 <code>objdump</code> 检查 Stage2 ELF 文件中的符号地址，确认所有地址都在 <code>0x8000</code> 以上。</p><p>具体来说，<code>_start</code> 应该在 <code>0x8000</code>，GDT 相关的符号（比如 <code>gdt</code>、<code>gdt_ptr</code>）应该在 <code>0x8xxx</code> 范围内：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/boot/stage2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -E</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;(gdt|_start|pm_entry)&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>如果 <code>gdt</code> 的地址显示为 <code>0x0160</code> 这样的值（小于 <code>0x8000</code>），说明 linker script 的 base 没改对。</p><p>你也可以用 <code>readelf -s build/boot/stage2 | grep gdt</code> 来查看 gdt 相关符号的链接地址，确认它们都在 <code>0x8000</code> 以上。</p><hr><h3 id="step-2-5-配置-qemu-debug-console" tabindex="-1">Step 2.5: 配置 QEMU Debug Console <a class="header-anchor" href="#step-2-5-配置-qemu-debug-console" aria-label="Permalink to &quot;Step 2.5: 配置 QEMU Debug Console&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>cmake/qemu.cmake</code> 中添加 debugcon 支持，让保护模式下的 <code>outb</code> 输出能被 QEMU 捕获。</p><p><strong>设计思路</strong>: 进入保护模式之后，BIOS INT 10h 就不能用了——保护模式下没有 BIOS 中断服务。我们需要一个新的调试输出手段，而且这个手段最好不需要初始化任何硬件——因为我们正处于一个脆弱的过渡期，任何复杂的初始化都可能引入新的 bug。</p><p>QEMU 提供了一个非常方便的 debug console（调试控制台），对应 I/O 端口 <code>0xE9</code>：向这个端口写一个字节，QEMU 就会把这个字节输出到宿主机的终端或文件。不需要初始化串口，不需要配置波特率，一个简单的输出指令就能搞定。</p><p>这个功能最早是 Bochs 仿真器引入的（所以有时候你会看到它被称为 &quot;Bochs debug port&quot;），QEMU 兼容了这个约定，OSDev 社区也把它当成了事实标准。</p><p>我们需要在 <code>cmake/qemu.cmake</code> 的 <code>QEMU_COMMON_FLAGS</code> 中添加两行参数。</p><p>第一行是 <code>-debugcon file:debug.log</code>，告诉 QEMU 把 debug console 的输出写到 <code>debug.log</code> 文件而不是 <code>stdio</code>——我们选择写文件是因为 <code>stdio</code> 已经被串口（<code>-serial stdio</code>）占用了，混在一起不好分辨。</p><p>第二行是 <code>-global isa-debugcon.iobase=0xe9</code>，把 debug console 的 I/O 端口设为 <code>0xE9</code>。</p><p>当然，debugcon 的局限在于它是 QEMU 专有的，真实硬件上不存在。但在 Bootloader 开发阶段，我们几乎 100% 时间都在 QEMU 里调试，这个局限完全可接受。后续进入 long mode 之后我们会切换到 COM1 串口作为正式的输出通道。</p><p>使用方式极其简单：在保护模式下的代码里，只需要向端口 <code>0xE9</code> 写入一个字节，QEMU 就会把这个字节输出到 <code>debug.log</code> 文件。比如我们后面会向它写入 <code>0x50</code>（字母 <code>P</code> 的 ASCII 码），如果一切顺利，<code>debug.log</code> 里就会看到这个 <code>P</code>。</p><p><strong>验证</strong>: 修改后重新构建并运行，确认 QEMU 正常启动（debugcon 参数不会导致报错）：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Ctrl+C 退出后</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> debug.log</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>此时 <code>debug.log</code> 应该是空的（因为我们还没写保护模式切换代码），但 QEMU 不应该报错。</p><p>如果你看到 QEMU 报了类似 &quot;invalid option&quot; 的错误，大概率是 <code>-debugcon</code> 或 <code>-global isa-debugcon.iobase=0xe9</code> 的参数格式写错了。检查 <code>cmake/qemu.cmake</code> 中的 <code>QEMU_COMMON_FLAGS</code>，确保两行参数的格式与本文描述一致。</p><p>另一个可能的问题是 <code>debug.log</code> 文件没有出现在 build 目录下——这可能是因为你的工作目录不在 build 里，或者 QEMU 因为其他原因提前退出了。用绝对路径检查一下：<code>ls -la $(pwd)/debug.log</code>。</p><p>如果你在 <code>debug.log</code> 里看到了意料之外的字符（比如乱码），那可能是因为之前的运行残留了旧的日志。删掉 <code>debug.log</code> 后重新运行即可。</p><hr><h3 id="step-3-定义-gdt" tabindex="-1">Step 3: 定义 GDT <a class="header-anchor" href="#step-3-定义-gdt" aria-label="Permalink to &quot;Step 3: 定义 GDT&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>stage2.S</code> 中新增一个 <code>.gdt</code> section，在里面定义三个段描述符（null、code32、data32）和 GDT 指针结构 <code>gdt_ptr</code>。</p><p><strong>设计思路</strong>: 我们先理清楚需要定义什么，再逐个解释每个字段的含义。GDT 的定义放在 <code>stage2.S</code> 的末尾，位于独立的 <code>.section .gdt,&quot;a&quot;</code> 中，用 <code>.align 8</code> 确保 8 字节对齐。<code>&quot;a&quot;</code> 标记表示 allocatable（可分配），这是段属性的标准写法。</p><p>首先是一个 Null 描述符，占 8 字节全零，放在 GDT 的第 0 个位置（索引 0）。Intel 硬件规定这个条目必须存在且必须全零，段选择子值为 <code>0x00</code> 时指向它，CPU 试图用 null selector 访问内存会触发 General Protection Fault。</p><p>这不是可选的装饰，是强制的。这个设计的目的是让&quot;忘记初始化段寄存器&quot;这种编程错误能被立即捕获，而不是静默地读到随机数据。这是一个非常典型的硬件辅助调试设计——x86 架构里有很多类似的&quot;帮你踩刹车&quot;的机制。</p><p>第二个是 32 位代码段描述符，放在索引 1，选择子值为 <code>0x08</code>。这个描述符定义了保护模式下的代码段：基地址（Base）全部为 0，大小限制（Limit）的低 16 位填 <code>0xFFFF</code>、高 4 位填 <code>0xF</code>，合起来 20 位就是 <code>0xFFFFF</code>。</p><p>粒度位 G 设为 1（表示以 4KB 为单位），所以 <code>0xFFFFF * 4096 = 4GB</code>，覆盖整个地址空间。Access Byte 的值是 <code>0x9A</code>，我们拆开来看：最高位 P=1 表示段在内存中存在，接下来两位 DPL=00 表示 Ring 0 内核级权限，S=1 表示这是代码/数据段（不是系统段），Type 字段的 4 位是 <code>1010</code>，在代码段类型中表示可执行且可读。</p><p>Flags 字节是 <code>0xCF</code>，其中高 4 位是 <code>1100</code>：G=1（4KB 粒度）、D=1（32 位默认操作数大小），剩下两位是 Limit 的高 4 位 <code>0xF</code>。</p><p>第三个是 32 位数据段描述符，放在索引 2，选择子值为 <code>0x10</code>。布局和代码段几乎一模一样，唯一的区别在 Access Byte：值是 <code>0x92</code>，P=1、DPL=00、S=1，Type 是 <code>0010</code> 表示可读写的数据段。</p><p>数据段不需要可执行权限，但需要可写权限——栈和数据都在这个段里。Limit 和 Base 的值与代码段完全相同，实现平坦内存模型。你会发现两个描述符的 Base 都是 <code>0x00000000</code>，Limit 都是 4GB——整个地址空间被完全平铺，没有任何分段隔离。这就是所谓的 Flat Memory Model。</p><p>然后是 GDT 指针结构 <code>gdt_ptr</code>，这是 <code>lgdt</code> 指令需要读取的 6 字节数据：前 2 字节是 GDT 的大小减 1（limit 字段），后 4 字节是 GDT 的线性基地址。</p><p>大小可以用汇编表达式 <code>gdt_end - gdt - 1</code> 自动计算——三个描述符各 8 字节共 24 字节，减 1 得到 <code>0x17</code>（23）。Limit 之所以要减 1，是因为 limit 字段只有 16 位，能表示的最大值是 65535，所以约定是&quot;GDT 的最后一个有效字节地址&quot;，也就是总大小减 1。</p><p>基地址直接写 <code>gdt</code> 标号即可，因为 linker script 已经把 base 设成了 <code>0x8000</code>，链接器会自动算出正确的绝对地址。</p><p>这里有一个非常关键的点需要注意：<code>lgdt</code> 指令在实模式下执行时，CPU 计算物理地址的方式是 <code>DS * 16 + offset</code>。所以我们在 <code>stage2.S</code> 中调用 <code>lgdt</code> 之前，必须先确保 <code>%ds</code> 为 0，否则 <code>lgdt</code> 实际读到的 <code>gdt_ptr</code> 地址就会偏移 <code>DS * 16</code>，读到的 GDT base 地址就是错的。这个问题我们下一篇实操时再详细展开。</p><p><strong>实现约束</strong>: <code>.gdt</code> section 必须用 <code>.align 8</code> 确保 8 字节对齐；null 描述符必须全零；<code>gdt_ptr</code> 的格式是严格的 2 字节 limit + 4 字节 base，顺序不能反。另外要注意 <code>.bss</code> section 在 linker script 里的通配符必须写成 <code>*(.bss*)</code>（带星号），因为某些汇编器可能给 <code>.bss</code> section 加上额外标记。</p><p><strong>验证</strong>: 编译通过后，检查 <code>.gdt</code> section 在 ELF 文件中的大小和内容：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .gdt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/boot/stage2</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>你应该能看到三个 8 字节的描述符条目和最后的 6 字节 gdt_ptr。null 描述符全零，code 描述符第 5 字节是 <code>0x9A</code>、第 6 字节是 <code>0xCF</code>，data 描述符第 5 字节是 <code>0x92</code>。</p><p>也可以用 <code>readelf</code> 来查看 GDT 符号的链接地址：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readelf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/boot/stage2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gdt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>确认 <code>gdt</code> 符号的地址在 <code>0x8xxx</code> 范围内（和 linker base <code>0x8000</code> 对齐）。如果地址低于 <code>0x8000</code>，说明 linker script 的 base 没有正确设置，需要回到 <code>boot/CMakeLists.txt</code> 检查。</p><p><strong>进一步验证</strong>: 为了确保 GDT 的每个字节都正确无误，你可以用 <code>xxd</code> 以十六进制方式查看 <code>.gdt</code> section 的完整内容：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objcopy</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> binary</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .gdt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/boot/stage2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/gdt.bin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">xxd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/gdt.bin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>正确的输出应该以 8 个零字节（null 描述符）开头，紧接着是代码段描述符（FF FF 00 00 00 9A CF 00），然后是数据段描述符（FF FF 00 00 00 92 CF 00），最后是 6 字节的 gdt_ptr。</p><p>如果你看到的不是这些值——比如 Access Byte 的位置出现了 00 或者 FF——说明描述符定义有误。逐字节对照本篇的设计思路部分，检查 Limit、Base、Access Byte 和 Flags 的每一个字段。</p><p>这种逐字节验证虽然繁琐，但在调试保护模式切换时是必不可少的——因为 <code>lgdt</code> 不会校验 GDT 内容的正确性，如果描述符有误，CPU 只会在后续使用时触发 #GP，到那时候你很难分清是 GDT 定义错了还是别的什么原因。</p><p><strong>关于 <code>.gdt</code> section 的位置</strong>: 你可能注意到 GDT section 被放在了 <code>.text</code> 之后。这意味着 GDT 数据在内存中的位置紧跟着代码段。这种布局是合理的，因为 stage2 的代码区域从 <code>0x8000</code> 开始，代码段结束后紧跟着就是 GDT 数据，中间没有浪费空间。</p><p>但是要注意一个细节：如果你在 <code>.text</code> section 里增加了大量代码，GDT 的位置会后移，<code>gdt_ptr</code> 里的 base 地址也会相应改变。这不是问题——因为 <code>gdt_ptr</code> 里的地址是由链接器自动计算的，不需要你手动维护。但你需要确保 <code>.text</code> 里的代码不会超过 <code>.gdt</code> section 的空间，否则链接器会报错。这也是为什么我们在 linker script 末尾加了 <code>ASSERT(. &lt;= 0x10000, ...)</code> 断言的原因。</p><hr><h2 id="本篇小结" tabindex="-1">本篇小结 <a class="header-anchor" href="#本篇小结" aria-label="Permalink to &quot;本篇小结&quot;">​</a></h2><p>到这里，GDT 定义好了、linker script 修正了、debugcon 配置到位了——整个舞台已经搭好，只差最后也是最关键的一幕：让 CPU 真正从实模式切换到保护模式。</p><p>回顾一下本篇的三个核心改动。第一，linker script 的 location counter 从 <code>0x0</code> 改成了 <code>0x8000</code>，确保所有绝对地址引用与运行时的物理地址匹配——这是整个 GDT 能正确工作的基础，因为 <code>gdt_ptr</code> 里的 base 地址就是由链接器根据这个 location counter 计算出来的。</p><p>第二，<code>cmake/qemu.cmake</code> 新增了 <code>-debugcon file:debug.log</code> 和 <code>-global isa-debugcon.iobase=0xe9</code> 两个参数，为保护模式下的调试输出提供基础设施。这比初始化串口简单得多，一个输出指令就能把字符写到日志文件里。</p><p>第三，<code>stage2.S</code> 末尾新增了独立的 <code>.gdt</code> section，定义了三个段描述符（null/code32/data32）和 <code>gdt_ptr</code> 伪描述符。每个描述符的 Access Byte 和 Flags 字段都经过了仔细设计，确保代码段可执行可读、数据段可读写、都覆盖完整的 4GB 地址空间。</p><p>下一篇我们就来做保护模式切换。整个过程涉及六个严格有序的步骤：<code>cli</code> 关中断、DS 清零、<code>lgdt</code> 加载 GDT 指针、CR0.PE 置 1、远跳转刷新 CS、以及 32 位环境下的段寄存器初始化和栈设置。每一步都有不可省略的理由，顺序不能调换，任何一步出错都会让你收获一个漂亮的三重故障。</p>`,80)])])}const b=o(t,[["render",a]]);export{h as __pageData,b as default};
