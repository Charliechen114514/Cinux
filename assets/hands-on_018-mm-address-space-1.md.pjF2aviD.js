import{c as a,Q as s,j as i,m as t}from"./chunks/framework.D9IbVcwA.js";const k=JSON.parse('{"title":"018-mm-address-space-1 · 地址空间","description":"","frontmatter":{"title":"018-mm-address-space-1 · 地址空间"},"headers":[],"relativePath":"hands-on/018-mm-address-space-1.md","filePath":"hands-on/018-mm-address-space-1.md","lastUpdated":1779686691000}'),n={name:"hands-on/018-mm-address-space-1.md"};function p(r,e,d,o,l,c){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="_018-1-进程地址空间的设计与类接口" tabindex="-1">018-1 进程地址空间的设计与类接口 <a class="header-anchor" href="#_018-1-进程地址空间的设计与类接口" aria-label="Permalink to &quot;018-1 进程地址空间的设计与类接口&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>到 tag 017 为止，我们的内核已经拥有了一套完整的内存管理基础设施：PMM 管理物理页的分配回收，VMM 负责虚拟地址到物理地址的映射翻译，Heap 在这之上提供了内核堆分配器。 但这一切都运行在同一个、全局唯一的地址空间里——bootloader 在启动时搭建的那套页表，内核从开机到关机都在用这一张 PML4。如果我们想支持多进程（哪怕只是最原始的两个进程轮流执行），第一件要做的事情就是给每个进程一张独立的页表。 不同的进程看到同一套虚拟地址，但背后对应着不同的物理页——这就是&quot;地址空间隔离&quot;，也是现代操作系统最基本的安全边界之一。这一章我们开始搭建 AddressSpace 这个抽象层，先把设计想清楚、把类接口定义好。 和之前的 tag 不太一样的是，这一篇我们把重点放在&quot;设计&quot;上——别急着动手写代码，磨刀不误砍柴工。</p><p>知识前置：你需要理解 tag 016 中实现的 x86-64 四级分页结构（PML4 -&gt; PDPT -&gt; PD -&gt; PT），以及 VMM 的 map/unmap/translate 接口。 本章的核心就是在 VMM 之上再包一层——每个 AddressSpace 持有自己的 PML4 根，操作时把自己的 PML4 地址告诉 VMM。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="为什么需要独立的地址空间" tabindex="-1">为什么需要独立的地址空间 <a class="header-anchor" href="#为什么需要独立的地址空间" aria-label="Permalink to &quot;为什么需要独立的地址空间&quot;">​</a></h3><p>想象一下我们现在的处境：内核运行在一张全局唯一的页表上，所有代码、数据、MMIO 映射都挤在这张表里。到目前为止这没有任何问题，因为我们的内核是唯一的&quot;进程&quot;——键盘轮询循环和中断处理程序共享同一个地址空间，谁也不会干扰谁。 但如果我们想让两个用户程序同时存在于内存中呢？进程 A 在虚拟地址 0x400000 映射了自己的代码段，进程 B 也要在 0x400000 映射自己的代码段——两个进程争同一个虚拟地址，但必须指向不同的物理页。 这在单张页表里根本做不到，因为一个虚拟地址在一张页表里只能映射到一个物理页。</p><p>解决方案显而易见：给每个进程一张独立的页表。进程 A 切换运行时，CPU 使用 A 的页表；进程 B 切换运行时，CPU 使用 B 的页表。这样两个进程的 0x400000 就可以各自指向不同的物理页，互不干扰。 x86-64 用 CR3 寄存器来指定当前使用哪张 PML4 表。CR3 里存的是 PML4 表的物理基地址，CPU 每次翻译虚拟地址时都从 CR3 指向的 PML4 开始遍历。 所以&quot;切换地址空间&quot;的本质就是把新进程的 PML4 物理地址写入 CR3。这个操作会隐式刷新 TLB 中所有非全局页的缓存（Intel SDM Vol.3A Section 4.10.2），让 CPU 从新的页表重新翻译所有虚拟地址。</p><h3 id="经典设计对比-xv6-与-linux" tabindex="-1">经典设计对比：xv6 与 Linux <a class="header-anchor" href="#经典设计对比-xv6-与-linux" aria-label="Permalink to &quot;经典设计对比：xv6 与 Linux&quot;">​</a></h3><p>在设计自己的地址空间抽象之前，我们先看看两个经典 OS 是怎么处理&quot;每进程页表&quot;这个问题的。</p><p>xv6（MIT 的教学操作系统，RISC-V 版本）在每次进入内核态时会从用户页表切换到内核的全局页表（<code>kernel_pagetable</code>），离开内核态时再切回来。 这种方式更干净（内核页表和用户页表完全分离），但每次系统调用都要做两次页表切换。 另外，xv6 的双页表设计还带来了一个限制——内核无法直接通过用户虚拟地址访问用户内存（因为内核运行在自己的页表上），必须通过 <code>copyin</code>/<code>copyout</code> 函数手动翻译。</p><p>Cinux 选择了另一种方式——和 Linux 一样，把内核映射放到每个进程的页表里，这样进程切换时只需要一次 CR3 写入，而且系统调用和中断处理不需要额外切换页表。两种方式各有取舍，但对于教学操作系统来说，Cinux 的方式更简单，而且在 x86-64 上更自然——因为 x86-64 的 syscall/sysret 指令对页表切换的支持不如 RISC-V 的 S 模式灵活。</p><p>Linux 的做法和 Cinux 更像。Linux 用 <code>mm_struct</code> 结构管理进程的地址空间，<code>pgd_alloc()</code> 分配新的 PGD（Page Global Directory，在 x86-64 上 PGD 就是 PML4），然后通过 <code>pgd_ctor()</code> 把内核条目复制到 PGD 的对应位置——这和 Cinux 的构造函数逐条复制 PML4[256..511] 在逻辑上完全一致。进程切换时，<code>switch_mm()</code> 把新进程的 PGD 写入 CR3，和 Cinux 的 <code>activate()</code> 干的事情一模一样。不过 Linux 的 <code>mm_struct</code> 比 Cinux 的 <code>AddressSpace</code> 复杂得多——它还包含 VMA 链表、RSS 计数器、mmap 信号量等大量成员。Cinux 的 <code>AddressSpace</code> 目前就是 Linux <code>mm_struct</code> 的最小子集。</p><p>参考：</p><ul><li>Intel SDM Vol.3A Section 4.5.2, pp.4-20 to 4-21（CR3 在四级分页中的角色）</li><li>OSDev Wiki — <a href="https://wiki.osdev.org/Higher_Half_Kernel" target="_blank" rel="noreferrer">Higher Half Kernel</a></li><li>xv6 RISC-V <code>kernel/proc.c</code> — <code>proc_pagetable()</code> 和 <code>userinit()</code></li></ul><h3 id="pml4-的分割-用户空间-vs-内核空间" tabindex="-1">PML4 的分割：用户空间 vs 内核空间 <a class="header-anchor" href="#pml4-的分割-用户空间-vs-内核空间" aria-label="Permalink to &quot;PML4 的分割：用户空间 vs 内核空间&quot;">​</a></h3><p>x86-64 的 48 位 canonical 地址空间在 PML4 层面被天然地一分为二。PML4 有 512 个表项（索引 0-511），每个表项控制 512 GB 的虚拟地址空间。 索引 0-255 覆盖低位地址（0x0000000000000000 - 0x00007FFFFFFFFFFF），这就是用户空间； 索引 256-511 覆盖高位地址（0xFFFF800000000000 - 0xFFFFFFFFFFFFFFFF），这就是内核空间。</p><p>这个分割并不是硬件强制的——从 CPU 的角度来看，512 个 PML4 条目地位完全平等。 之所以有&quot;用户空间&quot;和&quot;内核空间&quot;的划分，是因为 canonical address 约定：bit 47 是&quot;符号位&quot;，低半部分的 bit 48-63 全是 0，高半部分的 bit 48-63 全是 1。 PML4 索引由虚拟地址的 bit 39-47 决定，所以 bit 47 = 0 对应索引 0-255，bit 47 = 1 对应索引 256-511。 这个分割恰好和 x86 的特权级机制配合——PML4 条目的 U/S 位（bit 2）控制着这 512 GB 区域是否允许用户态（ring 3）访问。</p><p>这个分割给我们的设计带来了一个极其便利的特性：每个进程的 PML4 表中，用户空间的那一半（条目 0-255）是进程私有的，而内核空间的那一半（条目 256-511）在所有进程之间是完全相同的。 所有进程共享同一套内核映射——这和 Linux 的做法如出一辙。 Linux 的 <code>pgd_alloc()</code> 在创建新的页全局目录时，就是从内核的 PGD 中复制高半部分条目（源码见 <code>mm/pgtable.c</code>），Cinux 做的事情在原理上完全一样。 所以当一个新进程被创建时，我们只需要分配一张全新的 PML4 表，清零所有 512 个条目，然后把内核 PML4 的条目 256-511 逐个复制过来。 这张新 PML4 表的用户空间部分是空的——就像一张白纸，进程可以自由地在上面建立自己的映射。这种&quot;上半部分私有、下半部分共享&quot;的模式，就是所谓的 Higher Half Kernel 设计。</p><p>这里有一个微妙但重要的细节值得强调：复制 PML4[256..511] 是&quot;浅拷贝&quot;——只复制了 PML4 条目本身的 8 字节 raw 值，没有递归复制 PDPT/PD/PT 这些下层页表页。 所以所有地址空间通过相同的 PML4 条目值（相同的物理地址）指向同一套内核页表结构。这意味着修改内核 PML4 模板中的某个条目会立刻影响所有地址空间的内核映射——这种共享语义在大多数情况下是我们想要的（比如新增 MMIO 映射），但如果某个 AddressSpace 的内核条目被意外篡改，也会影响到所有其他地址空间。 这也是为什么析构函数只释放用户空间的子树（条目 0-255），绝不碰内核空间的条目（条目 256-511）——那些条目指向的页表页不属于当前 AddressSpace，释放它们就是 use-after-free。</p><p>参考：</p><ul><li>Intel SDM Vol.3A Section 4.5.4, pp.4-22 to 4-25（四级分页地址翻译流程）</li><li>Intel SDM Vol.3A Table 4-15, pp.4-26 to 4-27（PML4 条目格式）</li><li>OSDev Wiki — <a href="https://wiki.osdev.org/Paging" target="_blank" rel="noreferrer">Paging</a></li></ul><h3 id="init-kernel-保存-模板" tabindex="-1">init_kernel()：保存&quot;模板&quot; <a class="header-anchor" href="#init-kernel-保存-模板" aria-label="Permalink to &quot;init_kernel()：保存&quot;模板&quot;&quot;">​</a></h3><p>在内核启动的早期阶段，bootloader 已经帮我们搭建好了一套完整的页表——identity map、higher-half map、MMIO 映射等等。这套页表的 PML4 物理地址存在 CR3 里。 我们的 <code>init_kernel()</code> 静态方法只需要做一件事：把 CR3 的值读出来，保存到一个静态成员变量中。 这个被保存的值就是&quot;内核 PML4 模板&quot;——以后每创建一个新的 AddressSpace，都从这个模板复制内核部分的条目（PML4[256-511]）。</p><p>调用时机很重要：<code>init_kernel()</code> 必须在 VMM 初始化之后、任何 AddressSpace 对象创建之前调用。 因为 VMM 的 <code>init()</code> 完成后，页表才处于一个稳定的、我们可以信赖的状态——VMM 可能会在初始化过程中修改 CR3 指向的页表（比如映射 MMIO 区域），所以我们必须等 VMM 完全就绪后再读取 CR3。 而如果在 <code>init_kernel()</code> 之前就尝试构造 AddressSpace，内核 PML4 的值还是 0，复制过来的内核条目就全是空的——一旦 activate 到那张页表，内核立刻就失去所有映射，CPU 连下一条指令都取不到， 接下来就是三重故障重启了。在 Linux 中，等价的工作是由 <code>swapper_pg_dir</code> 完成的——内核在编译时就把初始页表静态分配好了，启动后所有进程的 PGD 都从这个模板复制。 Cinux 的做法更动态一些：我们在运行时读取 CR3，因为我们的初始页表是 bootloader 动态构建的，地址不是固定的。</p><h3 id="类设计概览" tabindex="-1">类设计概览 <a class="header-anchor" href="#类设计概览" aria-label="Permalink to &quot;类设计概览&quot;">​</a></h3><p>在动手写接口之前，我们先整理一下 AddressSpace 类需要提供的全部能力。从职责上分，它要做三组事情。</p><p>整个类的数据成员只有两个——实例成员 pml4_phys_（当前空间的 PML4 物理地址）和静态成员 kernel_pml4_（启动时保存的内核 PML4 物理地址）。加起来 16 字节。你可以把它类比为一张&quot;身份证&quot;——上面写着一个物理地址，凭借这个地址就能找到一整套四级页表结构。pml4_phys_ 是否为 0 是整个对象状态的&quot;哨兵&quot;：构造成功后非 0，移动后变为 0，析构时也变为 0，所有生命周期操作都先检查它。 第一组是生命周期管理：构造函数创建一张新的 PML4 表（从 PMM 分配、清零、复制内核条目），析构函数释放所有用户空间页表页和 PML4 页本身，拷贝被禁止，移动被允许——这是 RAII 管理物理页所有权的标准做法。 第二组是页表操作：map、unmap、translate 三个方法委托给 VMM，但传入自己的 PML4 根而不是用默认的内核 PML4，这样每个 AddressSpace 的映射操作只影响自己的页表，不会污染其他地址空间。 第三组是地址空间切换：activate() 把当前 AddressSpace 的 PML4 物理地址写入 CR3，让 CPU 切换到这个地址空间。 这个操作是进程切换的核心——未来调度器在切换进程时，就会调用新进程的 AddressSpace 的 activate()。</p><h3 id="为什么拷贝被禁止" tabindex="-1">为什么拷贝被禁止 <a class="header-anchor" href="#为什么拷贝被禁止" aria-label="Permalink to &quot;为什么拷贝被禁止&quot;">​</a></h3><p>这个设计决策值得单独拿出来说。拷贝构造和拷贝赋值被 <code>= delete</code> 禁止，这不是偷懒——而是安全性的硬要求。</p><p>试想如果允许拷贝构造，两个 AddressSpace 对象的 pml4_phys_ 就指向同一个物理 PML4 页。 当第一个对象析构时，它把 PML4 归还给 PMM，第二个对象手里就剩下一个悬空指针。 后续任何 map/unmap/translate 操作都会往已经不属于自己的物理内存上写东西——这是经典的 use-after-free 场景。 更糟糕的是，PMM 的 free_page 可能只是标记物理页为空闲，不清零内容，所以问题不会立刻暴露。 直到 PMM 把这个物理页重新分配出去给别的用途，两个不同的调用者以为各自独占了一页，结果互相踩数据——这种 bug 的排查过程堪称噩梦。</p><p>移动语义则是允许的。移动构造和移动赋值把 pml4_phys_ 的所有权转移给目标对象，源对象的 pml4_phys_ 被置 0。 这样源对象析构时检测到 pml4_phys_ == 0 就跳过释放，只有目标对象会执行真正的释放逻辑。 移动操作被标记为 noexcept——这很重要，因为标准库容器（如 std::vector）在 realloc 时会优先使用 noexcept 的移动操作。既然拷贝已经被删了，如果移动不是 noexcept，容器根本无法编译。</p><p>在 C++ 中，这种&quot;禁止拷贝、允许移动&quot;的模式是管理独占资源（exclusive ownership）的标准做法。和它类似的例子有 std::unique_ptr、std::thread、std::fstream 等——它们都持有某种不能被多个对象共享的资源，因此都删除了拷贝语义。AddressSpace 持有的&quot;资源&quot;是一个物理 PML4 页，它和 unique_ptr 持有的堆内存本质上是一样的——谁分配谁释放，只能转移不能共享。</p><h3 id="委托模式-addressspace-作为-vmm-的包装器" tabindex="-1">委托模式：AddressSpace 作为 VMM 的包装器 <a class="header-anchor" href="#委托模式-addressspace-作为-vmm-的包装器" aria-label="Permalink to &quot;委托模式：AddressSpace 作为 VMM 的包装器&quot;">​</a></h3><p>map、unmap 和 translate 三个操作不需要 AddressSpace 自己实现页表遍历——tag 016 的 VMM 已经把四级页表遍历写得清清楚楚了。 AddressSpace 只需要把自己的 pml4_phys_ 传给 VMM，告诉它&quot;请用我的 PML4 根来操作&quot;就行了。</p><p>VMM 的 map/unmap/translate 方法都有一个可选的 pml4_root 参数——传空指针时使用默认的内核 PML4（全局地址空间），传非空指针时使用指针指向的值作为 PML4 根。 所以 AddressSpace 的 map 调用就是 <code>g_vmm.map(virt, phys, flags, &amp;pml4_phys_)</code>——传入 pml4_phys_ 的地址，VMM 解引用这个指针拿到 PML4 物理地址，然后从这张 PML4 开始遍历。</p><p>这种设计避免了代码重复——页表遍历逻辑只在 VMM 中存在一份。AddressSpace 的三个委托方法加起来不到十行代码，但它们把&quot;在哪个地址空间中操作&quot;这个语义清晰地表达了出来。</p><p>这种&quot;所有权包装器 + 委托&quot;的模式在内核代码中非常常见。另一个例子是 Linux 的 <code>mm_struct</code>——它持有 PGD 的物理地址（所有权），但页表操作通过底层的 <code>pgd_xxx</code> / <code>pud_xxx</code> / <code>pmd_xxx</code> / <code>pte_xxx</code> 内联函数完成（委托）。Cinux 的设计遵循了同样的思路，只是规模更小。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-创建-addressspace-类声明文件" tabindex="-1">Step 1: 创建 AddressSpace 类声明文件 <a class="header-anchor" href="#step-1-创建-addressspace-类声明文件" aria-label="Permalink to &quot;Step 1: 创建 AddressSpace 类声明文件&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>kernel/mm/address_space.hpp</code> 中定义 AddressSpace 类的完整接口，包括构造/析构、静态初始化、页表操作和成员变量。</p><p><strong>设计思路</strong>: AddressSpace 是一个 RAII 风格的资源管理类——构造时分配 PML4 物理页并初始化，析构时递归释放所有用户空间页表并归还 PML4 页。类的核心数据成员只有一个：<code>pml4_phys_</code>，一个 <code>uint64_t</code>，保存着这张 PML4 表的物理基地址。此外还有一个静态成员 <code>kernel_pml4_</code>，保存启动时从 CR3 读出的内核 PML4 物理地址——所有 AddressSpace 实例共享这一个值。构造函数执行三步操作：从 PMM 分配一页作为新的 PML4 表，把整张表清零（512 个条目全部置 0），从内核 PML4 复制条目 256-511。析构函数负责清理：遍历用户空间部分（PML4 条目 0-255），对每个 present 的条目递归释放整棵子树（PDPT -&gt; PD -&gt; PT），最后释放 PML4 页本身。递归释放子树的逻辑放在私有方法 free_subtree 中，它接收一个页表的物理地址和当前层级（3 表示 PDPT，2 表示 PD，1 表示 PT），递归遍历并释放所有中间级页表页。</p><p>拷贝语义被明确删除（<code>= delete</code>），因为两个 AddressSpace 不能共享同一张物理 PML4 表。 移动语义是支持的——移动构造和移动赋值把 pml4_phys_ 的所有权转移给目标对象，源对象的 pml4_phys_ 被置 0，这样源对象析构时检测到 0 就跳过释放。 公开操作包括 map(virt, phys, flags) 返回 bool 表示映射是否成功、unmap(virt) 无返回值、translate(virt) 返回物理地址（未映射返回 0）， 以及 activate() 把 pml4_phys_ 写入 CR3。前三个直接委托给 VMM 的对应方法，传入 <code>&amp;pml4_phys_</code> 作为自定义 PML4 根。</p><p><strong>实现约束</strong>: 命名空间 <code>cinux::mm</code>，头文件用 <code>#pragma once</code> 作为 include guard。需要 include <code>&lt;stdint.h&gt;</code>。类声明中把构造/析构、拷贝删除、移动支持、静态初始化、页表操作、访问器分成不同的注释区块，保持可读性。</p><p><strong>踩坑预警</strong>: <code>kernel_pml4_</code> 是一个静态成员变量，它必须在 .cpp 文件中被定义并初始化为 0——如果你忘了在实现文件中写这行定义，链接器会报未定义符号错误（<code>undefined reference to cinux::mm::AddressSpace::kernel_pml4_</code>）。另外，删除拷贝构造和拷贝赋值的同时一定要提供移动构造和移动赋值，否则 AddressSpace 对象就无法被放入容器——未来的进程表会需要这个能力。如果你尝试在测试代码中拷贝一个 AddressSpace 对象（比如 <code>auto as2 = as1;</code>），编译器会报 &quot;use of deleted function&quot; 错误——这正是我们想要的保护效果。</p><p><strong>验证</strong>: 这个步骤只需要编译通过即可。类声明本身不涉及运行时行为，在后续步骤中实现各个方法后才能做运行时验证。</p><h3 id="step-2-实现-init-kernel-静态方法" tabindex="-1">Step 2: 实现 init_kernel() 静态方法 <a class="header-anchor" href="#step-2-实现-init-kernel-静态方法" aria-label="Permalink to &quot;Step 2: 实现 init_kernel() 静态方法&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>kernel/mm/address_space.cpp</code> 中实现 <code>init_kernel()</code>，读取当前 CR3 并保存为内核 PML4 模板。</p><p><strong>设计思路</strong>: 实现非常直截了当——调用之前在 tag 016 中写好的 <code>read_cr3()</code> 内联函数，把返回值赋给静态成员 <code>kernel_pml4_</code>。然后用 kprintf 打印一条日志，显示保存的 PML4 物理地址，方便后续调试时确认 init 是否正确执行。日志格式建议使用 <code>[AS]</code> 前缀，这样可以通过 <code>grep &#39;\\[AS\\]&#39;</code> 快速过滤出 AddressSpace 相关的输出。在 kernel/main.cpp 的启动序列中，我们需要把 <code>init_kernel()</code> 的调用插入到 VMM 初始化之后、Heap 初始化之前。具体修改是把原来的 Step 9（Heap 初始化）往后挪一位变成 Step 10，新插入一个 Step 9 来调用 <code>AddressSpace::init_kernel()</code>。同时需要在 main.cpp 顶部新增 <code>#include &quot;kernel/mm/address_space.hpp&quot;</code>。</p><p><strong>实现约束</strong>: address_space.cpp 需要包含 kernel/arch/x86_64/paging.hpp（获取 read_cr3）、kernel/lib/kprintf.hpp（日志输出）、kernel/mm/address_space.hpp。静态成员 kernel_pml4_ 的定义（<code>uint64_t AddressSpace::kernel_pml4_ = 0;</code>）要放在匿名命名空间之外、命名空间 <code>cinux::mm</code> 之内。这是 C++ 的要求——静态成员变量只能在类声明中&quot;声明&quot;，必须在某个 .cpp 文件中&quot;定义&quot;一次。</p><p><strong>踩坑预警</strong>: 调用 init_kernel() 的时机是关键。如果你在 VMM 初始化之前就调用了它，CR3 指向的可能还是 bootloader 临时搭建的页表，而不是内核最终的页表——虽然在这个项目中两者的 PML4 恰好相同（bootloader 和内核使用同一张 PML4 表），但这是一个脆弱的隐式假设。正确的调用位置是 VMM init() 之后。另外，init_kernel() 只能调用一次——重复调用会覆盖掉之前保存的值。如果未来需要更严格的保障，可以加一个 <code>static bool initialized = false;</code> 的标志位，重复调用时直接 panic。</p><p><strong>验证</strong>: 修改 main.cpp 后重新编译运行内核，在串口输出中应该能看到类似 <code>[AS] Kernel PML4 saved at phys 0x...</code> 的日志。这个物理地址应该和之前 VMM 初始化时输出的 PML4 地址一致——通常是 0x10000 或 0x20000 附近的一个 4KB 对齐地址。如果日志显示 <code>Kernel PML4 saved at phys (nil)</code> 或 0x0，说明 read_cr3() 返回了 0，这几乎不可能发生——检查 main.cpp 中的调用顺序，确认 init_kernel() 是在 VMM 初始化之后调用的。</p><p><strong>init_kernel() 只能调用一次</strong>: 虽然当前的实现没有防重复调用的保护，但重复调用会覆盖之前保存的值。如果未来需要更严格的保障，可以在函数内部加一个 <code>static bool initialized = false;</code> 标志位，重复调用时直接 panic。不过在当前阶段，只要把调用安排在 kernel_main() 的确定位置，就不会有重复调用的问题。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 编译并运行内核，检查 init_kernel 日志</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  qemu-system-x86_64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/big_kernel.bin</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -serial</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -display</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> none</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;\\[AS\\]&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>到目前为止我们只实现了类声明和 init_kernel()。 编译后运行内核，串口输出中应该出现 <code>[AS] Kernel PML4 saved at phys ...</code>，后续的 Heap、Framebuffer、Console 初始化应该不受影响——因为我们只是读取并保存了 CR3， 没有修改任何页表。 如果串口输出在 [AS] 日志之后突然中断，大概率是 init_kernel() 的插入位置不对——检查 main.cpp 中的调用顺序是否正确，确认 VMM 在 Step 8 初始化，AddressSpace 在 Step 9 初始化。 另外记得更新 kernel/CMakeLists.txt，把 mm/address_space.cpp 加入 big_kernel_common 的源文件列表中。</p><p>在这个阶段，你还可以做一些额外的验证来确保一切正常：</p><ol><li>确认 <code>[AS]</code> 日志输出的物理地址和 VMM 初始化日志中的 CR3 地址一致（VMM 的 init 函数通常会打印它使用的 PML4 地址）。</li><li>确认 Heap 初始化（Step 10）和 Framebuffer 初始化（Step 11）仍然正常完成——这说明 init_kernel() 的插入没有破坏任何后续操作。</li><li>如果你有 GDB 调试环境，可以在 init_kernel() 处设断点，检查 read_cr3() 的返回值是否和预期一致。这个地址通常是 0x10000 或 0x20000 附近的一个 4KB 对齐值。</li></ol><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 编译并运行内核，检查 init_kernel 日志</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  qemu-system-x86_64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/big_kernel.bin</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -serial</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -display</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> none</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;\\[AS\\]&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>init_kernel 返回 0</strong>: 如果日志显示 <code>Kernel PML4 saved at phys (nil)</code> 或 0x0，说明 read_cr3() 返回了 0。这几乎不可能发生——CR3 在 bootloader 阶段就被设置了。如果真的出现了，大概率是你在错误的时机调用了 init_kernel——比如在分页还没启用之前。检查 main.cpp 中的调用顺序。</p><p><strong>链接错误 &quot;undefined reference to kernel_pml4_&quot;</strong>: 这是忘记在 .cpp 文件中定义静态成员变量的典型症状。确保 address_space.cpp 中有 <code>uint64_t AddressSpace::kernel_pml4_ = 0;</code> 这行，而且它在 <code>cinux::mm</code> 命名空间内，不在匿名命名空间里。</p><p><strong>编译错误 &quot;use of deleted function&quot;</strong>: 如果你尝试在测试代码中拷贝一个 AddressSpace 对象（比如 <code>auto as2 = as1;</code>），编译器会报这个错——这正是我们想要的。如果你确实需要转移所有权，使用 <code>std::move()</code>。</p><p><strong>CMakeLists 忘记添加源文件</strong>: 如果编译时出现 <code>undefined reference to cinux::mm::AddressSpace::init_kernel()</code> 之类的链接错误，检查 kernel/CMakeLists.txt 中 big_kernel_common 的源文件列表是否包含了 mm/address_space.cpp。</p><p><strong>启动序列步骤编号变化</strong>: 插入 init_kernel() 之后，原来的 Step 9（Heap 初始化）变成了 Step 10，后续步骤也需要顺延编号。这个编号变化不影响运行时行为，但如果后续代码注释中引用了步骤编号，需要同步更新。建议在 main.cpp 的注释中标注清楚每一步的依赖关系，方便日后维护。</p><p><strong>Higher-half 映射的前提</strong>: init_kernel() 读取 CR3 后保存的是物理地址，但后续构造函数需要通过 phys_to_virt() 把物理地址转换成虚拟地址来访问页表内容。这个转换依赖内核启动时建立的 higher-half 直接映射——物理地址 phys 对应的虚拟地址是 phys + KERNEL_VMA。如果 higher-half 映射不完整（比如没有覆盖到 PML4 所在的物理页范围），后续构造函数访问新分配的 PML4 时就会触发 page fault。在 Cinux 的当前实现中，VMM 初始化时已经建立了完整的 higher-half 映射，所以这不是问题——但理解这个前提条件对于排查未来的 bug 很重要。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>到这里我们已经完成了 AddressSpace 的骨架：类接口定义清晰，init_kernel() 在启动时保存了内核 PML4 模板，CMakeLists 也更新好了。 整个模块的&quot;蓝图&quot;已经画出来了——RAII 风格的 PML4 所有权管理、拷贝删除 + 移动支持、委托给 VMM 的页表操作、CR3 切换的 activate。</p><p>回顾一下我们做的设计决策：</p><ul><li><strong>Higher Half 策略</strong>：PML4 的用户空间（条目 0-255）每个进程独立，内核空间（条目 256-511）所有进程共享。这是 x86-64 canonical 地址空间的天然分割，也是 Linux 采用的经典设计。</li><li><strong>浅拷贝而非深拷贝</strong>：构造函数只复制 PML4 条目的 raw 值，不递归复制下层页表页。所有地址空间共享同一套内核页表结构，修改一处全局生效。</li><li><strong>禁止拷贝</strong>：两个 AddressSpace 不能共享同一个物理 PML4，从编译期杜绝 use-after-free。</li><li><strong>显式保存内核 PML4</strong>：用 init_kernel() 静态方法在启动时保存 CR3，而不是在构造函数中读取，确保模板来源确定且一致。</li><li><strong>委托 VMM</strong>：AddressSpace 不自己实现页表遍历，而是把 pml4_phys_ 传给 VMM，复用已有的 map/unmap/translate 逻辑。</li></ul><p>下一步是实现构造函数和析构函数——那是资源管理的核心，也是最容易踩坑的地方（递归释放子树要精确控制终止层级、move 语义的所有权转移要保证自赋值安全）。这些留到下一篇文章。</p>`,71)])])}const u=a(n,[["render",p]]);export{k as __pageData,u as default};
