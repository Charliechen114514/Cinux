import{c as o,Q as t,j as r,m as d}from"./chunks/framework.D9IbVcwA.js";const m=JSON.parse('{"title":"015-mm-pmm-2 · 物理内存管理","description":"","frontmatter":{"title":"015-mm-pmm-2 · 物理内存管理"},"headers":[],"relativePath":"hands-on/015-mm-pmm-2.md","filePath":"hands-on/015-mm-pmm-2.md","lastUpdated":1779686691000}'),a={name:"hands-on/015-mm-pmm-2.md"};function c(n,e,i,p,_,s){return t(),r("div",null,[...e[0]||(e[0]=[d('<h1 id="_015-2-pmm-初始化与位图放置" tabindex="-1">015-2 PMM 初始化与位图放置 <a class="header-anchor" href="#_015-2-pmm-初始化与位图放置" aria-label="Permalink to &quot;015-2 PMM 初始化与位图放置&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一章我们把 E820 解析器和位图辅助函数准备好了，现在要把这些零件组装成一个完整的 PMM 初始化流程。这一章要回答的核心问题是：位图应该放在物理内存的哪个位置？初始化时如何保证不把内核、栈、位图自身所在的内存页分配出去？</p><p>PMM 的初始化采用的是一种&quot;先全锁、再解锁&quot;的保守策略——先把所有页面标记为已用（位图全填 0xFF），然后逐个解锁 E820 报告的可用区域，最后再把内核映像、栈、位图自身重新标记回已用。这种方式虽然笨拙，但有一个巨大的好处：如果你遗漏了某个需要保护的区域，最坏的结果是那个区域被当成空闲页分配出去了——但如果你反过来用&quot;先全空、再标记已用&quot;的策略，遗漏一个区域就意味着某个本该空闲的页被错误地保护了，而且你很难发现这种&quot;少了可用内存&quot;的 bug。</p><p>说实话，这是整个 tag 015 中最容易翻车的地方。不是因为算法复杂——算法非常简单——而是因为涉及物理地址和虚拟地址的来回转换、链接器符号的正确访问、以及&quot;到底哪些内存不能碰&quot;的完整枚举。笔者在这一步上踩过的坑可以绕地球一圈，所以我们一个一个来。</p><p>知识前置：上一章的 <code>parse_memory_map</code> 和位图辅助函数必须已经实现。你需要理解 linker symbol 的访问模式（在 tag 006 中我们遇到过——<code>extern char __kernel_stack_top; addr = &amp;__kernel_stack_top</code>，取地址而不是直接读值），以及 higher-half 内核的虚拟-物理地址转换。如果你对 linker symbol 的概念还有点模糊，别担心——后面会有一个专门的段落来详细解释它。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="保守标记策略-先全锁、再解锁" tabindex="-1">保守标记策略：&quot;先全锁、再解锁&quot; <a class="header-anchor" href="#保守标记策略-先全锁、再解锁" aria-label="Permalink to &quot;保守标记策略：&quot;先全锁、再解锁&quot;&quot;">​</a></h3><p>设想一下，如果你用&quot;乐观&quot;策略——先假设所有内存都可用，然后逐个标记已知被占用的区域——你需要知道哪些区域被占用了。内核映像占用的区域你可以通过 linker symbol 算出来，BootInfo 结构体的位置你知道在 0x7000，但还有多少隐含的被占用区域？bootloader 的实模式栈、BIOS 的临时数据结构、MMIO 映射区域……你永远无法穷举。更危险的是，某些被占用区域你可能根本不知道它们的存在——比如某些 BIOS 实现会在低内存中存放 APM（高级电源管理）的数据结构，但 E820 不一定会报告这些区域。</p><p>所以保守策略的做法是：第一步，把位图全部填为 0xFF（所有页面标记为已用）。第二步，遍历 <code>parse_memory_map</code> 的输出，把每个可用区域的对应 bit 清零（标记为空闲）。注意，位图初始是全 1 的，所以 <code>mark_region_free</code> 只会把 E820 确认可用的区域释放出来——任何 E820 没有标记为 type=1 的区域，会自动保持&quot;已用&quot;状态。第三步，在已释放的可用区域内，把内核映像、内核栈和位图本身重新标记为已用——因为这些结构虽然位于&quot;可用 RAM&quot;中，但它们显然不能被分配出去。</p><p>这种策略的妙处在于：即使 E820 报告有遗漏（比如某个新硬件的 MMIO 区域没被正确标记为 reserved），那些区域也不会被分配出去，因为它们本来就在&quot;全锁&quot;状态。安全代价是可能浪费一些确实可用但没被 E820 报告的内存——但这对教学 OS 来说完全可以接受。Linux 的 bootmem 分配器也采用了完全相同的策略——先全锁再按 E820 解锁，证明这是一个经过实战检验的可靠方案。</p><h3 id="位图的物理放置" tabindex="-1">位图的物理放置 <a class="header-anchor" href="#位图的物理放置" aria-label="Permalink to &quot;位图的物理放置&quot;">​</a></h3><p>位图本身也需要占用物理内存，而且这块内存在初始化完成之前就必须可访问。那么位图放在哪里？</p><p>Cinux 的方案是：把位图放在内核栈的紧后面。链接脚本定义了 <code>__kernel_stack_top</code> 符号——它标记了内核栈的最高地址（虚拟地址）。位图的起始地址取 <code>__kernel_stack_top</code> 向上对齐到页边界后的值。这样做有几个好处：位图和内核映像在物理内存中是连续的（或者紧挨着的），不会浪费碎片空间；位图在 higher-half 虚拟地址空间中可以直接通过虚拟地址访问，不需要额外的映射；对齐到页边界保证了位图不会和栈顶重叠。另外一个好处是，对齐到页边界（4096 字节）自然也就满足了 8 字节对齐的要求——<code>bm_find_first_free</code> 中用 <code>reinterpret_cast&lt;const uint64_t*&gt;(bm)</code> 做 64 位扫描时需要位图起始地址是 8 字节对齐的，而 4096 是 8 的倍数。</p><p>位图占用的物理内存大小是 <code>(highest_page + 7) / 8</code> 字节（向上取整到字节边界）。对于一个有 128MB 内存的 QEMU 虚拟机，这是 4096 字节——恰好一个页。对于 1GB 内存，大约 32KB，8 个页。位图本身占用的页面也需要在初始化完成后被标记为已用——位图总不能把自己分配出去。</p><p>位图放在栈后面这个方案虽然简洁，但有一个隐含的限制：位图的大小受限于栈后面连续可用内存的大小。不过在实际的 QEMU 配置中（内核映像通常小于 1MB、栈 64KB、位图 4KB），这完全不是问题——第一个可用区域从 1MB 开始，到 128MB 结束，有超过 126MB 的连续空间。如果将来内核映像增长到几十 MB，就需要考虑更灵活的位图放置策略。</p><h3 id="higher-half-地址转换" tabindex="-1">Higher-Half 地址转换 <a class="header-anchor" href="#higher-half-地址转换" aria-label="Permalink to &quot;Higher-Half 地址转换&quot;">​</a></h3><p>Cinux 的内核运行在虚拟地址 <code>0xFFFFFFFF80000000</code> 以上的空间，但 PMM 操作的是物理地址。所以我们需要在虚拟地址和物理地址之间做转换。</p><p>转换公式很简单：物理地址 = 虚拟地址 - KERNEL_VMA。比如位图的虚拟地址是 <code>__kernel_stack_top</code> 对齐后的值，减去 <code>0xFFFFFFFF80000000</code> 就得到位图的物理地址。同理，内核映像的虚拟结束地址（也就是位图起始虚拟地址）减去 KERNEL_VMA 就得到内核映像的物理结束地址。</p><p>这个转换看起来简单到不值一提，但在 init 流程中它出现了三次——确定位图物理地址、计算内核占用区域的物理结束地址、计算位图自身的物理地址——任何一次计算出错都会导致位图被写入了错误的位置或者错误的页面被保护。而且这种错误通常不会立刻 crash，而是会在后续的页表构建中引发莫名其妙的 page fault，排查起来非常痛苦。</p><p>内核映像的物理起始地址是 <code>info.kernel_phys_base</code>——这是 BootInfo 中记录的值，告诉我们 bootloader 把内核 ELF 加载到了物理内存的哪个位置（通常是 0x1000000 即 16MB）。所以&quot;内核映像 + 栈 + 位图&quot;占用的物理内存范围是从 <code>kernel_phys_base</code> 到 <code>(位图虚拟地址 - KERNEL_VMA)</code>。</p><p>参考：</p><ul><li><a href="https://wiki.osdev.org/Higher_Half_Kernel" target="_blank" rel="noreferrer">OSDev Wiki - Higher Half Kernel</a></li><li>Intel SDM Vol.3A Section 2.5 (Control Registers) -- CR3 保存的是物理地址，PMM 提供的物理页面将用于构建页表</li></ul><h3 id="linker-symbol-访问模式" tabindex="-1">Linker Symbol 访问模式 <a class="header-anchor" href="#linker-symbol-访问模式" aria-label="Permalink to &quot;Linker Symbol 访问模式&quot;">​</a></h3><p>这是我们在 tag 006 中踩过的大坑，这里再强调一遍。链接脚本中定义的符号（比如 <code>__kernel_end</code>、<code>__kernel_stack_top</code>）本质上是地址常量——链接器把它们当作&quot;在这个地址有一个东西&quot;，但并不真的在那里分配了内存。在 C/C++ 代码中访问这些符号的正确方式是：</p><p>声明为 <code>extern char __kernel_stack_top</code>（注意是 char，不是 char 数组或者函数），然后通过取地址操作 <code>&amp;__kernel_stack_top</code> 获得它的地址值。如果你直接访问 <code>__kernel_stack_top</code>（不加 &amp;），编译器会试图读取那个地址处的内存内容——你拿到的是一个随机值而不是地址本身。这个陷阱极其隐蔽，因为编译不会报错，运行时只会产生错误的地址。</p><p>为了加深理解，我们来看一个具体的例子。假设链接脚本中 <code>__kernel_stack_top</code> 的值是 <code>0xFFFFFFFF80110000</code>。正确做法 <code>&amp;__kernel_stack_top</code> 返回 <code>0xFFFFFFFF80110000</code>——这就是栈顶的虚拟地址。错误做法 <code>__kernel_stack_top</code>（不加 &amp;）会读取地址 <code>0xFFFFFFFF80110000</code> 处的一个字节——这个字节可能是 0x00（如果栈顶恰好是干净的），也可能是 0x55 或者任何其他值（如果栈顶残留了数据）。然后你把这个字节值当成地址来用——后果可想而知。</p><p>之所以声明为 <code>char</code> 而不是 <code>char[]</code> 或者其他类型，是因为 <code>char</code> 的大小恰好是 1 字节——当我们对它取地址时，得到的地址就是符号本身的地址，不会因为类型大小而产生偏移。</p><blockquote><p>参考：<a href="https://wiki.osdev.org/Writing_A_Page_Frame_Allocator" target="_blank" rel="noreferrer">OSDev Wiki - Writing A Page Frame Allocator</a> -- 使用 <code>endkernel</code> 符号的经典模式</p><p>这种&quot;链接器符号取地址&quot;的模式在 OS 开发中无处不在。Linux 内核用 <code>_text</code> 和 <code>_end</code> 来标记内核映像的范围，xv6 用 <code>end</code> 来确定第一个空闲页的起始位置。掌握了这个技巧，阅读其他内核的内存管理代码时就不会被那些奇怪的 <code>extern char</code> 声明搞糊涂了。如果你还是不太确定，下一章的 Step 2 中会有更详细的代码和验证步骤。</p></blockquote><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-实现内部辅助方法-mark-region-used-和-mark-region-free" tabindex="-1">Step 1: 实现内部辅助方法 mark_region_used 和 mark_region_free <a class="header-anchor" href="#step-1-实现内部辅助方法-mark-region-used-和-mark-region-free" aria-label="Permalink to &quot;Step 1: 实现内部辅助方法 mark_region_used 和 mark_region_free&quot;">​</a></h3><p><strong>目标</strong>: 编写两个内部方法，用于在位图中标记一段物理内存区域的占用状态。</p><p><strong>设计思路</strong>: 这两个方法接受物理地址和长度，把它们转换为页索引范围，然后逐页调用位图辅助函数。<code>mark_region_used</code> 把范围内的每个空闲页标记为已用（只处理当前状态为&quot;空闲&quot;的页，避免重复计数），同时减少 <code>free_pages_</code> 计数。<code>mark_region_free</code> 正相反，把范围内的每个已用页标记为空闲，同时增加 <code>free_pages_</code> 计数。这两个方法在 init 流程中扮演着&quot;画笔&quot;的角色——init 的八步操作中，有四步（4、5、6、7）都在调用它们。</p><p>页索引的计算方式：起始页 = 物理地址 / PAGE_SIZE（向下取整），结束页 = (物理地址 + 长度 + PAGE_SIZE - 1) / PAGE_SIZE（向上取整）。然后遍历从起始页到结束页（不含）的每一页，检查当前状态后再翻转——这样做是为了保证 <code>free_pages_</code> 计数的精确性。如果不做&quot;检查当前状态&quot;这一步，对同一个页连续调用两次 <code>mark_region_free</code> 会导致 <code>free_pages_</code> 被多加一次。</p><p>遍历时还需要一个边界检查：<code>p &lt; highest_page_</code>。因为传入的物理地址可能超出 PMM 管理的范围（比如 MMIO 区域在高地址），如果不做检查就会越界访问位图。</p><p><strong>实现约束</strong>: 两个方法都是 PMM 类的 private 成员。使用上一章定义的 <code>bm_test</code>、<code>bm_set</code>、<code>bm_clear</code> 辅助函数。放在 <code>cinux::mm</code> 命名空间中。</p><p>这里有一个&quot;防御性编程&quot;的设计选择值得讨论。这两个方法在 init 流程中会被连续调用多次，区域之间可能存在重叠——比如 E820 报告的两个可用区域有一小段重叠。如果我们不做&quot;先检查再操作&quot;，对重叠部分调用两次 <code>mark_region_free</code> 就会导致 <code>free_pages_</code> 被多加一次。虽然在实际的 QEMU 和主流 BIOS 中这种情况很少出现，但加上这个检查的成本几乎为零（每次循环多一次 <code>bm_test</code> 调用），换来的是对各种边界情况的鲁棒性。这种&quot;低成本高回报&quot;的防御措施在内核开发中特别值得投入。</p><p><strong>踩坑预警</strong>: 结束页的计算是向上取整——<code>(phys + length + PAGE_SIZE - 1) / PAGE_SIZE</code>。如果你不小心写成了 <code>(phys + length) / PAGE_SIZE</code>，那么当一个区域恰好结束在某页边界上时（比如物理地址 0x100000 + 长度 0x1000），最后一页会被漏掉——那页的 bit 没被 set，可能被后续的分配器分配出去，然后两个系统组件就会&quot;共享&quot;同一个物理页，后果不堪设想。这种&quot;差一个页&quot;的 bug 在内核开发中特别常见，因为页边界对齐的错误在很多情况下不会立即 crash——它只是默默地把一个本该保护的页面标记为空闲。</p><p><strong>验证</strong>: 这个方法在 <code>init</code> 流程中被调用，暂时没有独立的验证手段。它的正确性会在最终的 PMM 统计输出中体现——如果 <code>free_pages_</code> 和可用内存的总量吻合，说明标记逻辑是对的。具体来说，<code>free_pages_</code> 应该等于 <code>E820 可用区域的总页数 - 内核映像占用的页数 - 栈占用的页数 - 位图占用的页数</code>。在 QEMU 的 128MB 配置中，这个值大约是 32300 页左右（约 126MB）。</p><h3 id="step-2-实现-pmm-init-主流程" tabindex="-1">Step 2: 实现 PMM::init 主流程 <a class="header-anchor" href="#step-2-实现-pmm-init-主流程" aria-label="Permalink to &quot;Step 2: 实现 PMM::init 主流程&quot;">​</a></h3><p><strong>目标</strong>: 完成从 BootInfo 到完整位图的八步初始化序列。</p><p><strong>设计思路</strong>: init 函数的整体流程可以概括为八步。这八步的顺序是严格固定的——你不能先解锁可用区域再计算位图大小（因为你还没创建位图），也不能先保护内核区域再初始化位图为全 1（因为全 1 初始化会把保护操作覆盖掉）。每一步都依赖于前面步骤的结果。</p><p>第一步，调用上一章实现的 <code>parse_memory_map</code>，从 BootInfo 中提取可用内存区域列表。输出到一个局部的 <code>MemoryRegion</code> 数组（最多 32 个条目），这个数组是在栈上分配的，init 完成后就丢弃了。32 个条目足以覆盖任何实际硬件配置——即使是最复杂的 BIOS 也不会返回超过 20 个 E820 条目。</p><p>第二步，遍历所有可用区域，找出最高的物理地址。这个值决定了位图需要覆盖多大的范围——位图必须能索引从 0 到最高地址的所有页。<code>highest_page_ = max_addr / PAGE_SIZE</code>，这里 <code>highest_page_</code> 实际上代表&quot;总页数&quot;（即最大页索引 + 1），后续代码中它被用作 <code>p &lt; highest_page_</code> 的循环上界。<code>total_pages_ = highest_page_</code>，<code>bitmap_size_ = (highest_page_ + 7) / 8</code>（位图字节数，向上取整到字节边界）。</p><p>第三步，确定位图的放置位置。取 <code>__kernel_stack_top</code> 的地址值，向上对齐到页边界，这就是位图的虚拟起始地址。<code>bitmap_</code> 指针就指向这个虚拟地址——因为 Cinux 的 higher-half 映射已经把这个虚拟地址映射到了对应的物理地址，所以后续通过 <code>bitmap_</code> 指针读写位图就是直接操作物理内存。这里的关键是：位图不需要额外的页表映射步骤——它已经被 1:1 的 higher-half 映射覆盖了。</p><p>第四步，保守初始化——把位图的每个字节都设为 0xFF。此时 <code>free_pages_ = 0</code>，所有页面都被标记为已用。这是整个&quot;先全锁、再解锁&quot;策略的第一步，也是安全性的基石——从这一刻起，位图中的每一个 bit 都是 1（已用），除非后续步骤主动把它清零，否则它永远不会变成 0（空闲）。</p><p>第五步，解锁可用区域——遍历 parse_memory_map 的输出，对每个区域调用 <code>mark_region_free</code>。这一步结束后，所有 E820 报告为 type=1 的物理页都被标记为空闲了。此时 <code>free_pages_</code> 应该等于所有可用区域的页数之和。</p><p>第六步，重新保护内核映像和栈。物理范围是从 <code>info.kernel_phys_base</code>（内核映像物理起始）到 <code>(位图虚拟地址 - KERNEL_VMA)</code>（内核映像 + 栈的物理结束）。调用 <code>mark_region_used</code> 把这个范围内的所有页重新标记为已用。注意这一步的结束地址用的是 <code>bm_virt - KERNEL_VMA</code>（位图虚拟地址减去 VMA 偏移），而不是 <code>__kernel_end</code>——因为栈在 <code>__kernel_end</code> 之后、位图之前。</p><p>第七步，标记位图自身。位图的物理地址 = 位图虚拟地址 - KERNEL_VMA。这是我们必须处理的最后一个地址转换步骤。位图占用的字节数向上取整到页边界——即使位图只需要 4097 字节，也要占用 2 个页（8192 字节），因为内存分配的最小粒度是页。这个取整确保了 <code>mark_region_used</code> 保护的范围覆盖位图的每一个字节。调用 <code>mark_region_used</code> 标记这些页。这一步确保位图不会把自己分配出去——否则分配器返回的某个&quot;空闲页&quot;可能恰好是位图本身占用的页，写入那个页就会破坏位图数据，导致后续的分配和释放完全混乱。</p><p>第八步，打印统计信息——总内存大小和空闲内存大小。</p><p>这八步操作的顺序是严格固定的——你不能先解锁可用区域再创建位图，也不能在全 1 初始化之前就做保护操作。每一步都依赖前面步骤的结果。这是唯一一个&quot;输出&quot;步骤，不影响 PMM 的功能，但对调试至关重要。</p><p><strong>实现约束</strong>: 整个函数在 <code>pmm.cpp</code> 中实现，是 PMM 类的 public 方法。需要引用两个 linker symbol——<code>__kernel_end</code> 和 <code>__kernel_stack_top</code>——通过 <code>extern &quot;C&quot;</code> 声明（因为链接脚本是 C 级别的符号），然后用 <code>reinterpret_cast&lt;uintptr_t&gt;(&amp;symbol)</code> 取地址值。<code>KERNEL_VMA</code> 常量在 <code>pmm.cpp</code> 中定义为 <code>0xFFFFFFFF80000000ULL</code>。统计输出使用 <code>kprintf</code>。</p><p>一个关于 init 函数整体风格的注意事项：这个函数有八个步骤，每一步都有对应的注释标记（<code>// Step 1:</code> 到 <code>// Step 8:</code>）。这种注释风格在整个 Cinux 代码库中是一致的——它让读者可以快速定位到感兴趣的步骤，也方便在调试时用 kprintf 在特定步骤前后插入打印。保持注释风格和现有代码一致是一种好习惯。另外，init 函数内部不应该有任何 <code>return</code> 语句——它必须从头执行到尾，跳过任何步骤都会导致 PMM 处于不一致的状态。</p><p><strong>踩坑预警</strong>: 这里最关键的坑是 linker symbol 的取地址操作。<code>__kernel_stack_top</code> 声明为 <code>extern &quot;C&quot; char __kernel_stack_top</code>，你必须用 <code>&amp;__kernel_stack_top</code> 获取地址。如果你不小心写了 <code>reinterpret_cast&lt;uintptr_t&gt;(__kernel_stack_top)</code>——没有取地址符——你会读到栈顶地址处的一个字节值（比如 0x00），然后把它当成一个地址来用。这个 bug 在 QEMU 中可能碰巧不 crash（因为 0x00 附近可能碰巧有映射），但在真实硬件上几乎一定会 triple fault。笔者在这里被坑了整整一个下午，最后是在 GDB 里打印 <code>__kernel_stack_top</code> 和 <code>&amp;__kernel_stack_top</code> 的值才发现差异的——前者是一个很小的数（地址处的一个字节），后者才是正确的栈顶地址。</p><p>另一个容易忽略的点是第六步中&quot;内核映像 + 栈&quot;的物理结束地址的计算——应该是 <code>bm_virt - KERNEL_VMA</code>，而不是 <code>&amp;__kernel_end - KERNEL_VMA</code>。<code>__kernel_end</code> 标记的是内核映像（.text + .rodata + .data + .bss）的结束位置，但不包括栈。而位图是放在栈之后的，所以位图起始虚拟地址减去 KERNEL_VMA 才是整个&quot;内核占用的物理内存&quot;的结束位置。如果你用了 <code>__kernel_end</code>，栈占用的页面就不会被保护，后续分配器可能会把栈所在的物理页分配出去——后果不堪设想。</p><p><strong>验证</strong>: init 完成后会打印统计信息。在 QEMU 中（默认 128MB 内存），你应该看到类似这样的输出：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[PMM] Total: 128MB, Free: 126MB</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>具体的 Free 值取决于内核映像大小和栈大小，通常应该是 Total 减去几 MB。如果 Free 值异常小（比如只有几 MB），说明 <code>mark_region_used</code> 覆盖了过大的范围；如果 Free 等于 Total，说明内核区域没有被正确保护。</p><p>统计信息中的 <code>Total</code> 是按 <code>total_pages_ * PAGE_SIZE / (1024 * 1024)</code> 计算的——总页数乘以页大小再除以 1MB。这个计算在整数运算中是精确的，因为 128MB 正好是 32768 个页。但如果物理内存不是 MB 的整数倍（比如 127.5MB），统计中会显示 127MB 而不是 127.5MB——因为整数除法截断了小数部分。</p><h3 id="step-3-在-kernel-main-中集成-pmm-初始化" tabindex="-1">Step 3: 在 kernel_main 中集成 PMM 初始化 <a class="header-anchor" href="#step-3-在-kernel-main-中集成-pmm-初始化" aria-label="Permalink to &quot;Step 3: 在 kernel_main 中集成 PMM 初始化&quot;">​</a></h3><p><strong>目标</strong>: 将 PMM init 插入到内核启动流程的正确位置。</p><p><strong>设计思路</strong>: 回顾当前的 kernel_main 初始化序列：Serial -&gt; GDT -&gt; IDT -&gt; PIC -&gt; IRQ handlers -&gt; PIT -&gt; BootInfo breakpoint -&gt; Framebuffer -&gt; Font -&gt; Console -&gt; Keyboard -&gt; unmask + sti。PMM 应该插在哪里？这个位置的选择需要考虑 PMM 的依赖和被依赖关系。</p><p>答案是：在 BootInfo breakpoint 之后、Framebuffer 之前。具体来说，顺序变成：BootInfo breakpoint -&gt; <strong>PMM init</strong> -&gt; Framebuffer -&gt; Font -&gt; Console -&gt; Keyboard -&gt; unmask + sti。之所以放在 Framebuffer 之前，有两个原因。第一，PMM 的初始化不依赖任何输出设备——它只需要 BootInfo 和 linker symbol。第二，将来的 VMM（虚拟内存管理器）初始化需要 PMM 来分配页表页面，而 VMM 的初始化可能会在很早的位置（比如 Framebuffer 的 MMIO 映射就需要页表），所以 PMM 越早越好。</p><p>注意在这个位置，中断还是关闭的（<code>sti</code> 还没执行），所以 PMM init 不需要担心并发问题。虽然当前的 PMM 完全不涉及中断操作，但保持&quot;所有硬件初始化在关中断状态下完成&quot;是一个好习惯——这避免了中断 handler 在初始化过程中访问半初始化的数据结构。</p><p><code>boot_info</code> 指针在之前的 breakpoint 代码中已经通过 <code>reinterpret_cast&lt;const BootInfo*&gt;(BOOT_INFO_PHYS)</code> 获取了。我们只需要在这个指针有效之后、进入图形初始化之前，调用 <code>cinux::mm::g_pmm.init(*boot_info)</code> 就行了。注意这里用的是 <code>*boot_info</code> 解引用——<code>init</code> 方法接收的是 <code>const BootInfo&amp;</code> 引用，而不是指针。</p><p><strong>实现约束</strong>: 在 <code>kernel/main.cpp</code> 中，include <code>kernel/mm/pmm.hpp</code>，然后在使用 <code>boot_info</code> 的第一个位置之后插入 <code>cinux::mm::g_pmm.init(*boot_info)</code> 调用。同时更新文件顶部的注释中的初始化步骤编号——PMM 变成第 7 步，后面的步骤依次后移。注意不要在 init 前后加多余的空行——保持和现有代码风格一致。</p><p>main.cpp 的修改量其实很小：加一行 include，加一行 init 调用，再更新一下注释中的步骤编号。但这一行的意义非同寻常——它标志着你的内核从一个&quot;只读不写内存&quot;的被动观察者，变成了一个能够主动管理物理内存资源的操作系统内核。</p><p><strong>踩坑预警</strong>: 千万不要在 <code>sti</code> 之后再调用 PMM init。虽然当前的 PMM init 不涉及中断操作，但 init 里面会大量写入位图内存——如果此时有定时器中断或者键盘中断在异步触发，虽然它们不会直接干扰位图（因为中断 handler 不碰 PMM），但保持&quot;所有硬件初始化在关中断状态下完成&quot;是一个好习惯。另外，<code>boot_info</code> 指针必须在 init 之前就已经被正确设置——如果你不小心把 <code>auto* boot_info = ...</code> 那行移到了 init 调用之后，init 就会读到一个未初始化的指针，大概率 triple fault。这是一个&quot;顺序依赖&quot;的典型问题——init 需要在 <code>boot_info</code> 有效之后调用，而 <code>boot_info</code> 的赋值和 init 的调用之间只有几行代码，但就是这几行的顺序至关重要。</p><p><strong>验证</strong>: 构建并在 QEMU 中启动内核，串口输出应该包含 PMM 的统计行：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[PMM] Total: 128MB, Free: 126MB</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>而且这行应该出现在 Framebuffer 初始化日志之前。如果内核在 PMM init 处 crash，用 GDB 在 <code>PMM::init</code> 入口设断点，单步跟踪看是哪一步出了问题——最可能的原因是 linker symbol 取地址错误或者位图写入了不可访问的地址。GDB 的使用方式是：<code>gdb build/big_kernel.elf</code>，然后 <code>target remote :1234</code>（QEMU 需要用 <code>-s -S</code> 参数启动），在 <code>cinux::mm::PMM::init</code> 设断点，<code>continue</code> 执行到断点处，然后 <code>step</code> 逐行跟踪。init 函数总共只有约 30 行代码，单步跟踪一遍通常几分钟就能完成。重点关注第三步（位图放置）和第六步（内核区域保护）的地址值是否合理。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>确保 <code>kernel/CMakeLists.txt</code> 中 <code>big_kernel_common</code> 库包含了 <code>mm/pmm.cpp</code>，<code>kernel/main.cpp</code> 中 include 了 <code>kernel/mm/pmm.hpp</code> 并调用了 <code>g_pmm.init</code>。构建并运行 QEMU：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cmake --build build &amp;&amp; ./build/big_kernel.elf</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>串口输出中应该看到 <code>[PMM]</code> 开头的统计行，并且内核继续正常初始化 Framebuffer、Console、Keyboard。如果一切正常，你的内核现在知道这台机器有多少物理内存，也知道了哪些页面可以被安全分配。这一刻值得庆祝——你的内核终于不再是一个&quot;内存文盲&quot;了。</p><p>如果你想确认位图确实被正确放置了，可以在 init 完成后加一条 kprintf 打印位图的虚拟地址和物理地址——虚拟地址应该在 <code>__kernel_stack_top</code> 之后（对齐到页边界），物理地址应该在内核映像之后。不过这个调试打印在验证完成后记得删掉——保持正式内核的串口输出简洁是一种好习惯。</p><p>这里有一个细节值得一提：PMM init 被放在 BootInfo breakpoint 之后、Framebuffer init 之前。这个位置的选择有两个考量。第一，PMM 不依赖任何输出设备——它只需要 BootInfo 和 linker symbol。第二，后续的 VMM（虚拟内存管理器）初始化需要 PMM 来分配页表页面，而 VMM 的初始化可能在很早的位置就需要用到——比如 Framebuffer 的 MMIO 映射可能需要新的页表条目。所以 PMM 越早初始化越好，但必须在 BootInfo 可用之后。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>PMM init 导致 triple fault</strong>: 绝大多数情况是位图写入了一个不可访问的地址。用 GDB 在 <code>PMM::init</code> 入口设断点，检查 <code>bitmap_</code> 指针的值——它应该是一个 higher-half 虚拟地址（<code>0xFFFFFFFF8XXXXXXX</code> 范围）。如果这个地址看起来不对，检查 <code>__kernel_stack_top</code> 的取地址操作是否正确。你也可以在 GDB 中手动计算 <code>&amp;__kernel_stack_top</code> 的值，看它是否落在链接脚本中定义的栈区域范围内。另外一个常见的 triple fault 原因是位图大小计算错误导致越界写入——比如 <code>bitmap_size_</code> 算出来一个很大的值，然后 <code>for (uint64_t i = 0; i &lt; bitmap_size_; i++) bitmap_[i] = 0xFF;</code> 写到了位图范围之外的内存，覆盖了关键的数据结构。</p><p><strong>Free 值明显偏小（比如只有几 MB）</strong>: 检查 <code>mark_region_used</code> 的参数。最可能的问题是第六步中&quot;内核映像 + 栈&quot;的结束地址算错了——如果你用了 <code>__kernel_end</code> 而不是 <code>bm_virt - KERNEL_VMA</code>，那么栈占用的页就没被算进内核区域，但这些页在第五步已经被标记为空闲了，又没在第六步被重新保护——所以它们&quot;看起来&quot;可用，但实际上栈正在用。这不会立刻 crash，但一旦分配器把这些页分配出去了，栈数据就会被覆盖，内核行为变得完全不可预测。这种&quot;延迟崩溃&quot;是内核开发中最令人崩溃的 bug 类型之一。</p><p><strong>Free 值等于 Total（内核区域没有被保护）</strong>: 说明第六步的 <code>mark_region_used</code> 没有生效。检查 <code>info.kernel_phys_base</code> 的值——如果 BootInfo 结构体没有被正确填写（比如 bootloader 没有设置这个字段），它的值可能是 0，那么 <code>mark_region_used(0, ...)</code> 会把物理地址 0 开始的一大段标记为已用，但这不是我们想要的。用 kprintf 打印 <code>info.kernel_phys_base</code> 和 <code>bm_virt - KERNEL_VMA</code> 的值来确认。</p><p><strong>构建时 undefined reference to __kernel_stack_top</strong>: 这说明链接脚本中没有定义 <code>__kernel_stack_top</code> 符号。检查你的 <code>kernel.ld</code>——它应该在大内核栈的定义之后导出这个符号。如果符号名有变化（比如改成了 <code>__big_kernel_stack_top</code>），<code>pmm.cpp</code> 中的 extern 声明也要同步修改。一个快速确认的方法是用 <code>nm build/big_kernel.elf | grep kernel_stack_top</code> 看看符号是否存在以及它的地址值。</p><p><strong>init 后 Free 比 Total 小太多（比如少了 10+ MB）</strong>: 这可能是因为 <code>mark_region_used</code> 保护了过大的范围。最常见的原因是 <code>bm_virt - KERNEL_VMA</code> 计算出了一个远大于预期的物理结束地址。用 kprintf 打印 <code>bm_virt</code>（位图虚拟地址）、<code>bm_virt - KERNEL_VMA</code>（位图物理地址）、以及 <code>bitmap_size_</code> 的值，确认它们是否合理。位图虚拟地址应该紧接在栈顶之后，物理地址通常在 16-17MB 的范围内。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>这一章我们把 PMM 的初始化流程从头到尾走了一遍。八步操作看似繁杂，但核心逻辑只有一条线：先全锁、再按 E820 解锁、最后把内核区域和位图锁回去。真正容易出错的地方在于地址转换——三次 higher-half 转换（位图物理地址、内核区域结束地址、位图自身地址）和两个链接器符号的取地址操作。这些操作本身不复杂，但它们之间的依赖关系和顺序要求让 init 成了整个 PMM 中最容易翻车的函数。</p><p>位图放置在栈后面的方案简洁高效，但也有其局限性——如果将来内核栈和位图的总大小增长到超过可用空间的连续区域，就需要考虑更灵活的位图放置策略（比如在 E820 可用区域的末尾分配位图）。但对于当前的教学 OS 来说，这个方案完全够用。</p><p>下一章要实现分配和释放的运行时接口，然后通过双轨测试来验证整个 PMM 的正确性。有了这一章的基础，分配和释放的实现会顺畅得多——因为所有棘手的地址计算和链接器符号处理都已经在 init 中搞定了。</p><table tabindex="0"><thead><tr><th>概念</th><th>关键点</th></tr></thead><tbody><tr><td>保守标记策略</td><td>先全填 0xFF（全用），再按 E820 解锁可用区域，最后保护内核区域</td></tr><tr><td>位图放置</td><td>紧跟内核栈顶（<code>__kernel_stack_top</code>），向上对齐到页边界</td></tr><tr><td>Higher-Half 转换</td><td>物理地址 = 虚拟地址 - KERNEL_VMA (0xFFFFFFFF80000000)</td></tr><tr><td>内核区域保护</td><td>kernel_phys_base 到 (位图虚拟地址 - KERNEL_VMA) 标记为已用</td></tr><tr><td>位图自身保护</td><td>位图占用的页也必须标记为已用</td></tr><tr><td>Linker Symbol</td><td><code>extern &quot;C&quot; char sym; addr = &amp;sym</code>，取地址而不是直接读值</td></tr><tr><td>init 在 kernel_main 中的位置</td><td>BootInfo breakpoint 之后、Framebuffer 之前</td></tr></tbody></table><p>有了这一章的基础，下一章要实现的分配和释放接口会顺畅得多——因为所有棘手的地址计算和链接器符号处理都已经在 init 中搞定了。 | mark_region_used/free | 先检查状态再翻转，保证 free_pages_ 计数精确 |</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li><a href="https://wiki.osdev.org/Higher_Half_Kernel" target="_blank" rel="noreferrer">OSDev Wiki - Higher Half Kernel</a> — 虚拟-物理地址偏移转换</li><li><a href="https://wiki.osdev.org/Writing_A_Page_Frame_Allocator" target="_blank" rel="noreferrer">OSDev Wiki - Writing A Page Frame Allocator</a> — linker symbol <code>endkernel</code> 访问模式</li><li>Intel SDM Vol.3A Section 2.5 (Control Registers) — CR3 保存的是物理地址，PMM 为 VMM 提供页表页面</li><li>Intel SDM Vol.3A Section 4.5 — 4KB 页对齐要求</li><li>Linux bootmem: <a href="https://lwn.net/Articles/761215/" target="_blank" rel="noreferrer">LWN</a> — 同样的保守位图初始化策略</li></ul>',91)])])}const u=o(a,[["render",c]]);export{m as __pageData,u as default};
