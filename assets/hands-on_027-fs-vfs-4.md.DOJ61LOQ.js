import{c as e,Q as t,j as a,m as l}from"./chunks/framework.D9IbVcwA.js";const k=JSON.parse('{"title":"027-fs-vfs-4 · VFS 虚拟文件系统","description":"","frontmatter":{"title":"027-fs-vfs-4 · VFS 虚拟文件系统"},"headers":[],"relativePath":"hands-on/027-fs-vfs-4.md","filePath":"hands-on/027-fs-vfs-4.md","lastUpdated":1779686691000}'),i={name:"hands-on/027-fs-vfs-4.md"};function n(p,s,r,o,h,d){return t(),a("div",null,[...s[0]||(s[0]=[l(`<h1 id="_027-4-hands-on-用户态集成与测试验证-—-shell-里-cat-出文件内容" tabindex="-1">027-4 Hands-on: 用户态集成与测试验证 — shell 里 cat 出文件内容 <a class="header-anchor" href="#_027-4-hands-on-用户态集成与测试验证-—-shell-里-cat-出文件内容" aria-label="Permalink to &quot;027-4 Hands-on: 用户态集成与测试验证 — shell 里 cat 出文件内容&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一章我们把内核侧的 open/read/write/close/getdents 全链路打通了，系统调用能正确地通过 VFS 找到 Ramdisk 里的文件并读取数据。但这条链路还需要最后两块拼图才能在 shell 里真正使用：用户态的 libc wrapper（让用户程序能调用 open/close/getdents）和 shell 命令实现（cat 和 ls）。同时，我们需要一套完整的测试矩阵来验证从 syscall handler 到 VFS 到 Ramdisk 的每一层都工作正常。</p><p>完成本章后，你应该能在 Cinux 的 shell 里输入 <code>cat /hello.txt</code> 看到文件内容被打印出来，输入 <code>ls /</code> 看到所有文件名列出来。至此，milestone 027 的完整目标——open/read/write/close syscall 框架可用——就达成了。</p><p>知识前置：027-3（Ramdisk 适配与系统调用）、024（Shell 基础框架）。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="用户态-libc-wrapper-—-桥接用户程序和内核" tabindex="-1">用户态 libc wrapper — 桥接用户程序和内核 <a class="header-anchor" href="#用户态-libc-wrapper-—-桥接用户程序和内核" aria-label="Permalink to &quot;用户态 libc wrapper — 桥接用户程序和内核&quot;">​</a></h3><p>用户态程序不能直接调用内核函数——它们通过 syscall 指令触发系统调用。libc wrapper 的作用就是封装这个指令调用过程：把函数参数装进正确的寄存器（rdi/rsi/rdx/r10/r8/r9），执行 syscall，返回结果。对于 open/close/getdents 三个新系统调用，我们需要在 <code>user/libc/syscall.cpp</code> 和 <code>syscall.h</code> 中添加对应的 wrapper 函数。</p><h3 id="shell-cat-命令-—-open→read→write→close" tabindex="-1">shell cat 命令 — open→read→write→close <a class="header-anchor" href="#shell-cat-命令-—-open→read→write→close" aria-label="Permalink to &quot;shell cat 命令 — open→read→write→close&quot;">​</a></h3><p>cat 命令的实现逻辑非常直接：用 sys_open 打开用户指定的路径，循环 sys_read 读取文件内容到缓冲区，每次读取后用 sys_write(fd=1) 把内容打印到终端，读完后 sys_close 关闭文件描述符。这里有一个需要注意的点：sys_read 返回 0 表示 EOF（文件读完），返回 -1 表示错误——两种情况都要处理，但只有错误需要打印提示。</p><h3 id="shell-ls-命令-—-open→getdents-loop→close" tabindex="-1">shell ls 命令 — open→getdents loop→close <a class="header-anchor" href="#shell-ls-命令-—-open→getdents-loop→close" aria-label="Permalink to &quot;shell ls 命令 — open→getdents loop→close&quot;">​</a></h3><p>ls 命令比 cat 稍微复杂一点：用 sys_open 打开目录路径（我们的 Ramdisk 根目录 &quot;/&quot; 是一个 type=Directory 的 inode），然后循环调用 sys_getdents 读取目录项名称。每次 getdents 成功后，把名称打印出来（sys_write fd=1 加换行），直到 getdents 返回 0 表示目录遍历完毕。最后 close fd。</p><h3 id="测试矩阵-—-分层验证" tabindex="-1">测试矩阵 — 分层验证 <a class="header-anchor" href="#测试矩阵-—-分层验证" aria-label="Permalink to &quot;测试矩阵 — 分层验证&quot;">​</a></h3><p>我们的测试策略是分三层：第一层是 host 单元测试（test_fd_table 和 test_vfs_mount），在开发机上直接运行，验证 FDTable 和挂载点表的逻辑正确性；第二层是 kernel 集成测试（test_ramdisk 中的 VFS 部分和 test_vfs_syscall），在 QEMU 内核里运行，验证从 syscall 到 VFS 到 Ramdisk 的完整路径；第三层是手动验证——在 shell 里实际执行 cat 和 ls 命令看输出。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-添加用户态-libc-wrapper" tabindex="-1">Step 1: 添加用户态 libc wrapper <a class="header-anchor" href="#step-1-添加用户态-libc-wrapper" aria-label="Permalink to &quot;Step 1: 添加用户态 libc wrapper&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>user/libc/syscall.cpp</code> 和 <code>syscall.h</code> 中添加 sys_open、sys_close、sys_getdents 的 wrapper。</p><p><strong>设计思路</strong>: 每个 wrapper 遵循相同的模式——把系统调用号放入 rax，参数放入 rdi/rsi/rdx/r10/r8/r9，执行 syscall 指令，返回 rax 的值。sys_open 的参数是路径指针和 flags；sys_close 只有一个 fd 参数；sys_getdents 参数是 fd、缓冲区指针和缓冲区大小。注意 sys_open 在内核侧会通过 resolve_user_path() 自动处理 cwd 解析。</p><p><strong>实现约束</strong>: 系统调用号必须和 kernel 侧的 SyscallNr 枚举一致（SYS_open=2, SYS_close=3, SYS_getdents=78）。返回值类型是 int64_t，-1 表示错误。</p><p><strong>验证</strong>: 用户态程序编译通过。</p><h3 id="step-2-实现-shell-cat-命令" tabindex="-1">Step 2: 实现 shell cat 命令 <a class="header-anchor" href="#step-2-实现-shell-cat-命令" aria-label="Permalink to &quot;Step 2: 实现 shell cat 命令&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>user/programs/shell/cmd_cat.cpp</code>。</p><p><strong>设计思路</strong>: 从命令行参数中提取文件路径，调用 sys_open 打开文件，然后循环 sys_read（每次读 256 字节）到缓冲区，每次读完后 sys_write(fd=1, buf, bytes_read) 打印内容。sys_read 返回 0 时退出循环，返回 -1 时打印错误信息并退出。最后 sys_close(fd)。</p><p><strong>实现约束</strong>: cat 命令需要在 shell 的命令分发表中注册——识别 &quot;cat&quot; 前缀后提取后面的路径参数。路径参数可能带前导空格，需要跳过。缓冲区大小建议 256 字节——对 Ramdisk 里的小文件足够了。</p><p><strong>踩坑预警</strong>: sys_open 的路径参数必须是绝对路径（以 &#39;/&#39; 开头），因为我们的 vfs_resolve 依赖前导 &#39;/&#39; 来匹配挂载点。如果用户输入 &quot;hello.txt&quot; 而不是 &quot;/hello.txt&quot;，vfs_resolve 找不到任何挂载点，sys_open 会返回 -1。</p><p><strong>验证</strong>: 在 QEMU shell 中输入 <code>cat /hello.txt</code>，预期看到 &quot;Hello from Cinux!&quot; 被打印出来。</p><h3 id="step-3-实现-shell-ls-命令" tabindex="-1">Step 3: 实现 shell ls 命令 <a class="header-anchor" href="#step-3-实现-shell-ls-命令" aria-label="Permalink to &quot;Step 3: 实现 shell ls 命令&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>user/programs/shell/cmd_ls.cpp</code>。</p><p><strong>设计思路</strong>: 从参数中提取目录路径（默认 &quot;/&quot;），sys_open 打开目录，循环 sys_getdents 读取目录项名称，每读到一个就 sys_write 打印出来（加换行符）。sys_getdents 返回 0 时退出循环。最后 sys_close。</p><p><strong>实现约束</strong>: sys_getdents 每次调用返回一个目录项的名称长度，名称被写入用户提供的缓冲区。目录项的顺序由 Ramdisk 的 readdir 实现决定——先是 &quot;.&quot; 和 &quot;..&quot;，然后是 Ramdisk 中的所有文件。</p><p><strong>验证</strong>: 在 QEMU shell 中输入 <code>ls /</code>，预期看到 &quot;.&quot;、&quot;..&quot;、&quot;hello.txt&quot;、&quot;readme.txt&quot;、&quot;etc/passwd&quot; 每行一个被打印出来。</p><h3 id="step-4-注册-shell-命令" tabindex="-1">Step 4: 注册 shell 命令 <a class="header-anchor" href="#step-4-注册-shell-命令" aria-label="Permalink to &quot;Step 4: 注册 shell 命令&quot;">​</a></h3><p><strong>目标</strong>: 修改 <code>user/programs/shell/shell.hpp</code> 和 <code>main.cpp</code>，注册 cat 和 ls 命令。</p><p><strong>设计思路</strong>: 在 shell 的命令分发表中添加 &quot;cat&quot; 和 &quot;ls&quot; 条目，分别指向对应的处理函数。同时更新 help 命令的输出列表。</p><p><strong>验证</strong>: shell 中输入 <code>help</code> 应该能看到 cat 和 ls 的说明。</p><h3 id="step-5-编写-host-测试-—-test-fd-table" tabindex="-1">Step 5: 编写 host 测试 — test_fd_table <a class="header-anchor" href="#step-5-编写-host-测试-—-test-fd-table" aria-label="Permalink to &quot;Step 5: 编写 host 测试 — test_fd_table&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>test/unit/test_fd_table.cpp</code>，验证 FDTable 的所有操作。</p><p><strong>设计思路</strong>: 测试覆盖 11 个分类——构造（所有槽位 nullptr）、顺序分配（3/4/5）、表满返回 FD_NONE、释放后重用最低 fd、正常关闭、无效 fd 关闭、get 正常/越界/关闭后、File 字段存储正确、压力测试（填满→关闭一半→重分配）。</p><p><strong>实现约束</strong>: 共 24 个测试用例。使用项目自带的 TEST_FRAMEWORK（和之前的 host 测试一致）。链接 kernel/fs/file.cpp，不需要任何硬件模拟。</p><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_fd_table</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./test/unit/test_fd_table</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>预期：24 个测试全部通过。</p><h3 id="step-6-编写-host-测试-—-test-vfs-mount" tabindex="-1">Step 6: 编写 host 测试 — test_vfs_mount <a class="header-anchor" href="#step-6-编写-host-测试-—-test-vfs-mount" aria-label="Permalink to &quot;Step 6: 编写 host 测试 — test_vfs_mount&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>test/unit/test_vfs_mount.cpp</code>，验证挂载点表的所有操作。</p><p><strong>设计思路</strong>: 需要一个 MockFileSystem（简单的 FileSystem 子类，mount 返回 true，lookup 返回 nullptr）。测试覆盖——init 后表为空、添加单个/多个挂载点、表满返回 false、nullptr 参数拒绝、正常删除、删除不存在的返回 false、精确匹配和最长前缀匹配、无匹配返回 nullptr、g_global_fd_table 返回同一引用。</p><p><strong>实现约束</strong>: 共 19 个测试用例。链接 kernel/fs/vfs_mount.cpp 和 kernel/fs/file.cpp。</p><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_vfs_mount</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./test/unit/test_vfs_mount</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>预期：19 个测试全部通过。</p><h3 id="step-7-编写-kernel-集成测试-—-test-vfs-syscall" tabindex="-1">Step 7: 编写 kernel 集成测试 — test_vfs_syscall <a class="header-anchor" href="#step-7-编写-kernel-集成测试-—-test-vfs-syscall" aria-label="Permalink to &quot;Step 7: 编写 kernel 集成测试 — test_vfs_syscall&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>kernel/test/test_vfs_syscall.cpp</code>，在 QEMU 内核中验证全链路。</p><p><strong>设计思路</strong>: 每个测试用 setup_vfs() 初始化环境（vfs_mount_init + new Ramdisk + mount + vfs_mount_add），测试完后 teardown_vfs() 清理。7 个测试组——挂载注册（4 个）、sys_open（6 个）、sys_read（5 个）、sys_write（3 个）、sys_close（2 个）、完整生命周期（2 个）、sys_getdents（6 个），共 28 个用例。</p><p><strong>实现约束</strong>: 所有 syscall 函数通过 reinterpret_cast 把内核地址当参数传入（因为是内核态测试，地址本身就是有效的）。注意 path 参数需要是 const char* 再转成 uint64_t。</p><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qemu-system-x86_64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -serial</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_test.bin</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;VFS Syscall&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>预期看到测试部分打印，全部 PASS。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>完整的验证流程：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_host</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>然后启动内核（非 test 版本）验证 shell 命令：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qemu-system-x86_64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -serial</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel.bin</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 在 shell 中输入：</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ls /</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cat /hello.txt</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cat /readme.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>cat 命令返回 -1 但文件确实存在</strong>: 用 kprintf 在 sys_open 里打印 resolve 的结果——如果 vfs_resolve 返回 nullptr，说明路径没有匹配到任何挂载点。最常见的原因是用户输入了相对路径（没有前导 &#39;/&#39;）。</p><p><strong>ls 输出只有 &quot;.&quot; 和 &quot;..&quot;</strong>: 检查 Ramdisk 的 mount() 是否成功——如果 initrd 为空或 mount 失败，root_ctx_.count 为 0，readdir 在 &quot;.&quot; 和 &quot;..&quot; 之后就结束了。</p><p><strong>host 测试链接失败</strong>: test_fd_table 和 test_vfs_mount 需要链接正确的 kernel 源文件。检查 test/CMakeLists.txt 中的 target_sources 和 include_directories 设置。</p><p><strong>kernel 测试中 new 返回 nullptr</strong>: 确认内核堆已经初始化。test_vfs_syscall 中 setup_vfs() 使用了 <code>new Ramdisk()</code>，需要堆就绪。在 main_test.cpp 中确认 run_vfs_syscall_tests() 在堆初始化之后被调用。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>libc wrapper</td><td>封装 syscall 指令，匹配 kernel 侧系统调用号</td></tr><tr><td>cat 命令</td><td>open → read loop → write(fd=1) → close</td></tr><tr><td>ls 命令</td><td>open → getdents loop → write(fd=1) → close</td></tr><tr><td>test_fd_table</td><td>24 个 host 测试，覆盖 FDTable 全操作</td></tr><tr><td>test_vfs_mount</td><td>19 个 host 测试，覆盖挂载点表和 resolve</td></tr><tr><td>test_vfs_syscall</td><td>28 个 kernel 测试，覆盖 syscall 全链路</td></tr><tr><td>分层测试策略</td><td>host 单元测试 → kernel 集成测试 → shell 手动验证</td></tr></tbody></table>`,68)])])}const u=e(i,[["render",n]]);export{k as __pageData,u as default};
