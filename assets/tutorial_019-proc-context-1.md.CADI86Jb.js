import{c as e,Q as a,j as i,m as t}from"./chunks/framework.D9IbVcwA.js";const k=JSON.parse('{"title":"019-proc-context-1 · 进程上下文","description":"","frontmatter":{"title":"019-proc-context-1 · 进程上下文"},"headers":[],"relativePath":"tutorial/019-proc-context-1.md","filePath":"tutorial/019-proc-context-1.md","lastUpdated":1779686691000}'),n={name:"tutorial/019-proc-context-1.md"};function c(d,s,p,o,l,r){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="从单任务到多任务-内核线程的诞生" tabindex="-1">从单任务到多任务：内核线程的诞生 <a class="header-anchor" href="#从单任务到多任务-内核线程的诞生" aria-label="Permalink to &quot;从单任务到多任务：内核线程的诞生&quot;">​</a></h1><blockquote><p>标签：上下文切换, TCB, CpuContext, TaskBuilder, callee-saved, x86-64 ABI 前置：<a href="./018-mm-address-space-3">018-3 地址空间的最后一公里：从创建到销毁的全生命周期</a></p></blockquote><h2 id="前言" tabindex="-1">前言 <a class="header-anchor" href="#前言" aria-label="Permalink to &quot;前言&quot;">​</a></h2><p>到 tag 018 为止，Cinux 已经搭好了完整的内存管理栈——PMM 管物理页、VMM 管虚拟映射、Heap 管动态分配、AddressSpace 管进程隔离。看起来很完备了对吧？但你会发现一个尴尬的事实：整个内核从头到尾只有一条执行流。<code>kernel_main</code> 从头跑到尾，中间没有任何&quot;暂停当前任务、去干点别的、再回来&quot;的机制。这对一个玩具内核来说没问题，可一旦你想让两个东西&quot;同时&quot;跑——比如一个处理键盘输入、一个刷新屏幕——单任务就完全不够用了。</p><p>这一章我们正式进入多任务的世界。tag 019 的目标很具体：让两个内核线程各自打印 5 轮消息，通过 <code>yield()</code> 主动让出 CPU，实现合作式交替运行。听起来简单？说实话，从&quot;知道上下文切换的概念&quot;到&quot;真正写出一个能跑的调度器&quot;之间的距离，远比想象中大。你需要回答一连串问题：切换时保存哪些寄存器？新线程的栈怎么初始化？线程退出了怎么办？这些问题的答案都藏在数据结构的设计里。</p><p>在进入代码之前，我们先看看其他操作系统是怎么做这件事的，这能帮我们理解 Cinux 的设计选择背后的&quot;为什么&quot;。</p><h2 id="经典设计对比-别人家的上下文切换" tabindex="-1">经典设计对比：别人家的上下文切换 <a class="header-anchor" href="#经典设计对比-别人家的上下文切换" aria-label="Permalink to &quot;经典设计对比：别人家的上下文切换&quot;">​</a></h2><p>上下文切换的核心思想其实全世界都一样——保存当前线程的寄存器快照，恢复下一个线程的寄存器快照，然后跳过去执行。区别在于细节。</p><p>xv6（MIT 的教学操作系统，32 位 x86）的 <code>swtch.S</code> 是最经典的参考。它保存 4 个 callee-saved 寄存器（ebp、ebx、esi、edi）加上隐式保存在栈上的 EIP，然后把 ESP 存到 <code>*old</code>、从 <code>new</code> 加载 ESP。整个切换过程非常简洁，但有一个特点：xv6 的线程切换必须经过中间的 scheduler 线程——从进程 A 切到进程 B 的路径是 A -&gt; scheduler -&gt; B，而不是 A 直接到 B。这是因为 xv6 给每个 CPU 维护了一个独立的 scheduler 上下文，进程释放 CPU 后先回到 scheduler，由 scheduler 决定下一个运行谁。简单归简单，但每次切换多了一趟&quot;过路&quot;。xv6 的 <code>struct context</code> 直接定义了 edi/esi/ebx/ebp/eip 五个字段，存在内核栈的底部——这和 Cinux 把 <code>CpuContext</code> 作为 <code>Task</code> 结构体的第一个字段是类似的思路，只不过 xv6 用指针间接访问（<code>proc-&gt;context</code>），而 Cinux 用内嵌（<code>task.ctx</code>）。</p><p>Linux 的 <code>__switch_to_asm</code>（arch/x86/entry/entry_64.S）做的是同一件事，只不过规模大得多。它保存 rbp、rbx、r12-r15 这 6 个 callee-saved 寄存器，把 RSP 存入 <code>task_struct-&gt;thread.sp</code>，加载下一个 task 的 RSP，弹出寄存器，然后跳到 <code>__switch_to</code>（一个 C 函数，负责 FPU、debug 寄存器等附加状态）。Linux 的调度策略体系（CFS、RT、Deadline、Idle）通过 <code>SchedulingClass</code> 层次结构组织，每个 CPU 有自己的运行队列 <code>struct rq</code>，里面有一个 <code>curr</code> 指针指向当前正在跑的 task。这个架构和 Cinux 后面要做的非常相似——Cinux 的 <code>SchedulingClass</code> 虚接口和 <code>Scheduler::current_</code> 的设计灵感就来源于此。</p><p>值得一提的是 Linux 的 <code>switch_to</code> 宏有一个著名的&quot;prev 指针谜题&quot;。<code>switch_to</code> 接受三个参数：<code>prev</code>、<code>next</code>、<code>last</code>。为什么需要第三个参数？因为 <code>switch_to</code> 是在 <code>context_switch()</code> 函数内部调用的，当 <code>next</code> 线程被切回来继续执行时，它&quot;醒来&quot;的位置还是 <code>context_switch()</code> 函数内部——但此时栈上所有局部变量的值都是 <code>next</code> 线程自己的，不是 <code>prev</code> 线程的。Linux 用 <code>last</code> 参数来保存&quot;谁把我唤醒了&quot;这个信息，以便 <code>context_switch()</code> 返回后调用者能知道上一个运行的是谁。Cinux 是单 CPU 内核，不存在这个问题——<code>current_</code> 始终指向当前线程，不存在&quot;我在另一个 CPU 上醒来&quot;的场景。</p><p>PintOS 的 <code>switch_threads()</code> 则更加直白：保存 ebx、ebp、esi、edi，把 ESP 存入当前线程的 <code>thread-&gt;stack</code> 字段（通过一个编译期计算出的固定偏移量 <code>thread_stack_ofs</code> 来定位），加载下一个线程的 ESP，恢复寄存器，返回。PintOS 的线程结构体用 C 实现（<code>struct thread</code>），但栈偏移量通过一个运行时计算的技巧获取——在 <code>thread.c</code> 中定义一个辅助函数，用 <code>offsetof</code> 在初始化阶段算出 <code>stack</code> 字段在结构体中的偏移，然后汇编代码通过这个偏移量直接访问。这种做法比 xv6 的指针传递更高效（少一次内存间接寻址），但需要在 C 和汇编之间维护一个&quot;魔法偏移量&quot;的约定。PintOS 有一个值得注意的设计——<code>kernel_thread()</code> 封装函数。每个新线程不是直接从用户提供的入口函数开始跑，而是从 <code>kernel_thread()</code> 开始，由它调用用户函数，函数返回后自动调用 <code>thread_exit()</code>。这个&quot;自动退出&quot;的模式后来也启发了 Cinux 的 <code>exit_current</code> 栈上地址技巧，我们在第三篇会详细讲。</p><p>Intel SDM Vol. 3A 第 8.7 节明确指出：&quot;In 64-bit mode, the task switching mechanism available in protected mode is not supported. Task management and switching must be performed by software.&quot; 意思是 64 位模式下硬件任务切换（通过 JMP/CALL 到 TSS 描述符）不再可用，处理器会对这种操作触发 #GP。所以我们没有选择，必须用软件实现上下文切换——而软件切换的核心就是保存和恢复 callee-saved 寄存器。</p><h2 id="为什么只保存这些寄存器" tabindex="-1">为什么只保存这些寄存器？ <a class="header-anchor" href="#为什么只保存这些寄存器" aria-label="Permalink to &quot;为什么只保存这些寄存器？&quot;">​</a></h2><p>这是理解整个上下文切换设计的关键问题。x86-64 的 System V ABI 把通用寄存器分成两类：caller-saved（rax、rcx、rdx、rsi、rdi、r8-r11）和 callee-saved（rbx、rbp、r12-r15）。调用约定规定 callee-saved 寄存器在被调用函数返回时必须保持原值不变——如果被调用函数想用这些寄存器，它必须自己在栈上保存并在返回前恢复。caller-saved 寄存器则没有这个约束，调用者如果需要保留它们的值，必须在调用之前自己保存。</p><p>这个约定和上下文切换有什么关系？关系很大。<code>context_switch()</code> 本身就是一个函数调用。当某个线程调用 <code>Scheduler::yield()</code> -&gt; <code>context_switch()</code> 时，编译器已经在调用 <code>context_switch()</code> 之前把所有 caller-saved 寄存器保存在当前线程的栈上了（如果它们还有用的话）。所以 <code>context_switch()</code> 只需要保存 callee-saved 的 6 个寄存器，再加上 RSP（栈指针，切换的核心）和 RIP（恢复执行的地址），就足以完整保存一个线程的执行状态。rax 之类的寄存器不是丢掉了——它们保存在当前线程的栈上，等这个线程被切回来的时候，栈恢复了，那些值自然也就恢复了。</p><p>这就是 Cinux 的 <code>CpuContext</code> 保存 8 个 64 位值的全部理由：r15、r14、r13、r12、rbp、rbx、rsp、rip，总共 64 字节。不是偷懒，而是 ABI 已经帮我们把工作分担好了。</p><h2 id="环境说明" tabindex="-1">环境说明 <a class="header-anchor" href="#环境说明" aria-label="Permalink to &quot;环境说明&quot;">​</a></h2><p>本章涉及的核心文件是 <code>kernel/proc/process.hpp</code>（约 200 行声明）和 <code>kernel/proc/process.cpp</code>（约 146 行实现）。运行环境和前面几个 tag 一致——GCC cross-compiler（<code>x86_64-elf</code>）、QEMU、higher-half 内核（<code>KERNEL_VMA = 0xFFFFFFFF80000000</code>）。前置条件是 PMM、VMM 和 Heap 已经初始化完毕，因为 <code>TaskBuilder::build()</code> 需要它们来分配和映射内核栈。</p><p>这个 tag 新增了 2264 行代码，涉及 21 个文件的变更，核心是四个新文件：<code>process.hpp</code>、<code>process.cpp</code>、<code>scheduler.hpp</code>、<code>scheduler.cpp</code>，以及一个汇编文件 <code>context_switch.S</code>。另外还有约 970 行的测试代码（QEMU 集成测试 + Host 单元测试）。这是 Cinux 到目前为止最大的一个 tag，也是第一个让内核从&quot;单任务&quot;跃升到&quot;多任务&quot;的里程碑。</p><p>这一章是 tag 019 的基础——所有后续的调度器和上下文切换都建立在这些数据结构之上。如果你理解了 <code>CpuContext</code> 为什么只有 8 个字段、<code>Task</code> 的 9 个字段各司什么职、<code>TaskBuilder::build()</code> 的 7 个步骤分别干什么，那么你已经掌握了多线程调度的核心数据结构基础。</p><h2 id="cpucontext-结构体" tabindex="-1">CpuContext 结构体 <a class="header-anchor" href="#cpucontext-结构体" aria-label="Permalink to &quot;CpuContext 结构体&quot;">​</a></h2><p>有了上面的理论基础，我们来看 Cinux 的具体实现。</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> alignas(16)</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CpuContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r15;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r14;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r13;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r12;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rbp;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rbx;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rsp;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rip;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>结构体的字段顺序不是随便排的。8 个字段对应 6 个 callee-saved GPR 加上 RSP 和 RIP，偏移量从 0 到 56，每 8 字节一个，整整齐齐。整个结构体 <code>alignas(16)</code> 保证对齐，<code>sizeof</code> 为 64 字节。</p><p>你可能注意到了——字段顺序必须和汇编代码中的偏移量严格一致。为了防止&quot;改了 C++ 头文件忘了改汇编&quot;这种经典翻车，Cinux 用了一排 <code>static_assert</code> 来编译期锁定：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static_assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">offsetof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CpuContext, r15) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;r15 at offset 0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static_assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">offsetof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CpuContext, rsp) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 48</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rsp at offset 48&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static_assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">offsetof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CpuContext, rip) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 56</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rip at offset 56&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static_assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CpuContext) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CpuContext must be 64 bytes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>这种&quot;声明即文档、编译即校验&quot;的做法和 Linux 的 <code>pt_regs</code> 设计理念一脉相承。如果有人往中间插了个字段或者改了顺序，编译直接报错，不可能漏到运行时才发现偏移量对不上。</p><h2 id="taskstate-线程的生命周期" tabindex="-1">TaskState：线程的生命周期 <a class="header-anchor" href="#taskstate-线程的生命周期" aria-label="Permalink to &quot;TaskState：线程的生命周期&quot;">​</a></h2><p>在进入 <code>Task</code> 结构体之前，先看看线程的状态模型。<code>TaskState</code> 用 <code>scoped enum</code>（<code>enum class : uint8_t</code>）定义了四个状态：<code>Running</code>、<code>Ready</code>、<code>Blocked</code>、<code>Dead</code>。一个枚举值只占 1 字节，对齐紧凑。</p><p>这四个状态覆盖了合作式调度的核心生命周期。新创建的线程处于 <code>Ready</code> 状态，等待调度器把它加入运行队列。被调度器选中后变成 <code>Running</code>，独占 CPU 执行。线程调用 <code>yield()</code> 后回到 <code>Ready</code>，重新排队等待。如果线程因等待某个资源而主动挂起，进入 <code>Blocked</code> 状态，直到资源就绪被唤醒回 <code>Ready</code>。线程退出后变成 <code>Dead</code>，调度器把它从运行队列中移除。</p><p>对比 Linux 的状态机（<code>TASK_RUNNING</code>/<code>TASK_INTERRUPTIBLE</code>/<code>TASK_UNINTERRUPTIBLE</code>/<code>EXIT_ZOMBIE</code>/<code>EXIT_DEAD</code> 等，用位图组合），Cinux 的状态集是一个极简子集。Linux 多出来的 <code>Zombie</code> 状态是为了让父进程通过 <code>waitpid()</code> 获取子进程的退出状态——Cinux 目前还没有实现 <code>waitpid</code>，所以暂时不需要这个中间态。等后续实现进程回收的时候，在 <code>TaskState</code> 中加入 <code>Zombie</code> 即可。</p><h2 id="task-线程控制块" tabindex="-1">Task：线程控制块 <a class="header-anchor" href="#task-线程控制块" aria-label="Permalink to &quot;Task：线程控制块&quot;">​</a></h2><p>有了寄存器快照的容器，我们还需要一个更大的结构来承载线程的全部状态。</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    CpuContext ctx;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    TaskState state;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tid;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> priority;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kernel_stack;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kernel_stack_top;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    cinux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::AddressSpace</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> addr_space;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> name;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SchedulingClass</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sched_class;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p><code>Task</code> 是一个精简但完整的 TCB——9 个字段覆盖了调度所需的核心信息。<code>ctx</code> 字段放在最前面，它的地址和 <code>Task</code> 的地址完全相同，在某些场景下可以省去一个偏移计算。<code>state</code> 是线程生命周期状态机的当前节点。<code>tid</code> 是单调递增的任务 ID。<code>priority</code> 留给未来的优先级调度。<code>kernel_stack</code> 和 <code>kernel_stack_top</code> 记录栈的虚拟地址范围——前者定位栈底溢出魔数，后者设置 TSS 的 RSP0。<code>addr_space</code> 指向进程私有页表，纯内核线程为 nullptr。<code>sched_class</code> 指向该任务所属的调度策略对象。</p><p>对比 Linux 的 <code>task_struct</code>（超过 9 KB，包含文件描述符、信号处理、命名空间等），Cinux 的 <code>Task</code> 选择了&quot;够用就好&quot;的策略：先实现多线程调度所需的最小字段集合，后续功能在对应的 tag 中逐步添加。<code>name</code> 是一个 <code>const char*</code>，指向静态字符串字面量，不持有所有权——如果传入栈上的局部 char 数组，函数返回后就会变成悬垂指针，后续的调度器日志打印会读到垃圾数据。</p><h2 id="taskbuilder-把构造的复杂性藏起来" tabindex="-1">TaskBuilder：把构造的复杂性藏起来 <a class="header-anchor" href="#taskbuilder-把构造的复杂性藏起来" aria-label="Permalink to &quot;TaskBuilder：把构造的复杂性藏起来&quot;">​</a></h2><p>直接裸分配 <code>Task</code> + 手动初始化每个字段非常容易出错——忘了写溢出魔数、栈映射失败但 TCB 已经分配了、返回地址忘了压到栈上……Cinux 用 Builder 模式把这些细节封装到 <code>TaskBuilder</code> 里。</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task_a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TaskBuilder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_entry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(thread_a)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;thread_a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>build()</code> 是真正干活的地方。我们来一步步看。</p><p>首先，入口检查和 TCB 分配。函数一开头检查 <code>entry_</code> 是否为空，如果调用者忘了调 <code>set_entry()</code>，直接返回 nullptr 而不是在后面某个步骤崩溃——防御性编程的基本操作。然后用 placement new 在内核堆上分配 <code>Task</code> 结构体，对齐到 <code>alignof(Task)</code>。分配完后手动逐字节清零，因为内核堆的 <code>new</code> 不保证零初始化，不清零的话 <code>state</code>、<code>tid</code> 等字段可能是垃圾值。</p><p>接下来是物理栈的分配和映射。PMM 分配 4 个连续物理页（16 KB），然后通过 <code>alloc_stack_vaddr</code> 从内核虚拟地址空间（<code>0xFFFF800000100000</code> 起）分配连续的虚拟页。物理页逐页映射到虚拟地址空间（<code>0x03</code> 标志是 <code>FLAG_PRESENT | FLAG_WRITABLE</code>），如果任何一页映射失败，函数会释放已分配的 TCB 并返回空指针。这种&quot;分配失败就回滚&quot;的模式在内核代码中非常重要，因为内核没有异常处理机制——你只能自己检查每一步的返回值。</p><p>映射完成后，在栈底写入溢出检测魔数 <code>0xDEADC0DE</code>。如果线程的栈增长到覆盖了栈底的魔数，调试时通过检查魔数是否被破坏就能判断是否发生了栈溢出。</p><p>最后也是最精巧的部分——CpuContext 的初始化：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">task-&gt;ctx.rsp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stack_virt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stack_size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*reinterpret_cast&lt;uint64_t*&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(task-&gt;ctx.rsp) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    reinterpret_cast&lt;uint64_t&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Scheduler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::exit_current);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">task-&gt;ctx.rip </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> reinterpret_cast&lt;uint64_t&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entry_);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>ctx.rip</code> 指向线程入口函数，但 <code>ctx.rsp</code> 不是简单的栈顶。它指向 <code>stack_virt + stack_size - 8</code>，并且在那个位置预先写入了 <code>Scheduler::exit_current</code> 的地址。这是因为 <code>context_switch</code> 用 <code>jmp</code>（不是 <code>call</code>）跳到入口函数，不会在栈上压入返回地址。当线程函数执行到最后的 <code>ret</code> 时，CPU 从 RSP 指向的位置弹出一个 8 字节值作为返回地址——我们预先放的 <code>exit_current</code> 地址正好被弹出，线程进入干净的退出流程。这个设计很像 PintOS 的 <code>kernel_thread()</code> 封装，只不过 Cinux 把退出地址直接压在了栈上而不是用一个 wrapper 函数。至于这个设计在开发过程中曾经翻过什么车……第三篇踩坑实录会详细讲。</p><p>这个栈上退出地址的设计是 Cinux 线程管理中最精巧的部分之一。理解它的关键是意识到 <code>context_switch</code> 用 <code>jmp</code> 而不是 <code>call</code> 跳到入口——这意味着 C 编译器生成的函数 prologue/epilogue 假设栈上有一个返回地址的约定被打破了。我们把 <code>exit_current</code> 的地址预先放在栈顶，相当于模拟了一个 <code>call exit_current</code> 的效果——线程函数 return 时 <code>ret</code> 弹出的正是这个地址。</p><p>把上面的文字梳理一下，一个新线程的栈布局长这样：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>高地址</span></span>
<span class="line"><span>┌──────────────┐  ← kernel_stack_top</span></span>
<span class="line"><span>│ exit_current │  ← ctx.rsp 指向这里（线程返回地址）</span></span>
<span class="line"><span>│  (free)      │</span></span>
<span class="line"><span>│  (free)      │  ← 16 KB 的空栈空间，线程运行时使用</span></span>
<span class="line"><span>│  (free)      │</span></span>
<span class="line"><span>│ 0xDEADC0DE   │  ← 栈底魔数，溢出检测</span></span>
<span class="line"><span>└──────────────┘  ← kernel_stack</span></span>
<span class="line"><span>低地址</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>ctx.rsp</code> 指向 <code>exit_current</code> 的位置，<code>ctx.rip</code> 指向线程入口函数。当 <code>context_switch</code> 通过 <code>jmp</code> 跳到入口函数时，线程从入口函数开始执行，栈顶只有一个 <code>exit_current</code> 地址——这是线程函数 return 后的唯一去处。栈中间的空白区域就是线程运行时使用的空间——局部变量、函数调用链、中断帧都会压在这里。</p><p>最后是 TCB 元数据的填充。<code>tid</code> 通过 <code>next_tid++</code> 分配，初始状态设为 <code>Ready</code>，等待调度器把它加入运行队列。<code>addr_space</code> 为 nullptr 表示这是一个纯内核线程，没有独立的用户地址空间。<code>sched_class</code> 为 nullptr 时由 <code>Scheduler::add_task()</code> 自动分配默认的 RoundRobin 策略。所有这些字段加在一起，就是一个完整的、随时可以运行的线程。</p><p>值得注意的是 <code>next_tid</code> 从 1 而不是 0 开始——tid=0 被预留给 boot_task（启动上下文），所以用户创建的第一个任务 tid=1。在 kernel_main 中手动构造的 boot_task 会设 <code>tid = 0</code>。这个约定不是硬性要求，只是一个让调试更清晰的选择——看到 tid=0 就知道是 boot context。</p><p>另外一个容易忽略的细节是栈的虚拟地址分配。<code>alloc_stack_vaddr</code> 从 <code>0xFFFF800000100000</code> 起始单调递增，每个线程栈占 4 页（16KB）。这个地址选在 canonical 地址空间的高半部分，跳过前 1MB 避免和 MMIO 区域冲突。连续分配多个线程的栈不会重叠，但这个简化实现在线程销毁后不会回收虚拟地址空间。</p><h2 id="设计决策回顾" tabindex="-1">设计决策回顾 <a class="header-anchor" href="#设计决策回顾" aria-label="Permalink to &quot;设计决策回顾&quot;">​</a></h2><p>到这里我们已经把多任务的基础数据结构全部过了一遍。回顾一下几个关键的设计选择，以及为什么这样选而不是那样选。</p><p><code>CpuContext</code> 为什么用命名字段 + <code>static_assert</code> 而不是 <code>uint64_t regs[8]</code> 加宏定义偏移量？因为命名字段本身就是文档——看到 <code>ctx.rsp</code> 你就知道这是栈指针，看到 <code>regs[6]</code> 你还得去查宏定义才知道这是第几个寄存器。<code>static_assert</code> 在编译期锁定偏移量，保证 C++ 声明和汇编实现永远一致。如果用数组加宏，新增字段时很容易忘了更新宏定义，而且编译器不会帮你检查。这种&quot;声明即文档、编译即校验&quot;的做法和 Linux 的 <code>pt_regs</code> 设计理念一脉相承。</p><p><code>Task</code> 为什么保持精简而不是一次性把所有字段都加上？因为每个 tag 的变更范围应该可控。Linux 的 <code>task_struct</code> 经过了二十多年的演化才变成今天的样子——超过 9 KB，包含几百个字段。Cinux 选择&quot;够用就好&quot;——先实现调度所需的最小字段集合，文件描述符、进程树、工作目录等功能在后续的 tag 中逐步添加。这样读者更容易跟踪 <code>Task</code> 结构体的演化过程，每个 tag 的 diff 也更聚焦。</p><p>栈为什么用魔数而不是 guard page？<code>STACK_MAGIC</code> 是一个轻量级的溢出检测手段——写在栈底，检查是否被覆盖就能判断栈溢出。guard page 更强（硬件辅助，访问就触发 #PF），但需要额外的虚拟地址空间和页表操作。当前的魔数方案已经能覆盖最常见的栈溢出场景，后续可以按需加入 guard page。值得注意的是，0xDEADC0DE 这个魔数的选择不是随意的——它是一个明显无效的地址，如果 RIP 跳到这里很容易在调试器中识别出来。这也是一种&quot;fail loudly&quot;的设计思想。</p><p>为什么不用 RAII 管理 Task 的生命周期？Task 的生命周期比一个作用域更复杂——它被创建后加入调度队列，可能被多个 CPU（未来 SMP）引用，最终在线程退出时被销毁。这种生命周期用 unique_ptr 不行（所有权是共享/转移的），用 shared_ptr 需要引用计数开销。Linux 的 <code>task_struct</code> 也是手动管理生命周期的——alloc_task_struct 分配，free_task 释放，中间由调度器持有。</p><p><code>name</code> 为什么是 <code>const char*</code> 而不是某种字符串类？因为内核中没有通用的字符串类型——我们没有 <code>std::string</code>（没有 C++ 标准库）。<code>const char*</code> 指向静态字符串字面量是最简单、最安全的方案：不需要动态分配，不需要释放，生命周期和内核一样长。唯一的约束是调用者必须传字符串字面量，不能传栈上的局部 char 数组——否则 Task 的 name 指针会在函数返回后变成悬垂指针。</p><p><code>SchedulingClass</code> 为什么用虚基类而不是直接写 RoundRobin？为扩展做准备。Linux 的调度策略体系（CFS / RT / Deadline / Idle）通过类似的 sched_class 层次结构组织。先抽象接口，以后加新策略不需要改 Scheduler 的代码。目前 SchedulingClass 只有四个虚方法（enqueue、dequeue、pick_next、name），但已经足够覆盖大多数调度策略的核心操作了。</p><h2 id="收尾" tabindex="-1">收尾 <a class="header-anchor" href="#收尾" aria-label="Permalink to &quot;收尾&quot;">​</a></h2><p>这一章我们搭好了多任务的基础积木：<code>CpuContext</code> 定义了&quot;一个线程的执行状态长什么样&quot;，<code>Task</code> 把线程的全部元数据打包在一起，<code>TaskBuilder</code> 封装了构造过程中所有容易出错的细节。</p><p>在结束之前，我们站远一点看看这个设计的全貌。Cinux 的线程管理分为三层：最底层是 <code>CpuContext</code>——64 字节的寄存器快照，是上下文切换的原子操作对象；中间层是 <code>Task</code>——包含 <code>CpuContext</code> 加上调度状态、内存信息的 TCB；最上层是 <code>TaskBuilder</code>——封装了从 TCB 分配到栈映射到上下文初始化的全部细节。三层各司其职，互不耦合：修改 <code>CpuContext</code> 只影响上下文切换的汇编代码；修改 <code>Task</code> 只影响调度器和进程管理；修改 <code>TaskBuilder</code> 只影响线程创建流程。</p><p>但这些东西本身不会让线程跑起来——真正让两个线程交替执行的，是下一章要讲的 <code>context_switch.S</code> 汇编原语和 <code>RoundRobin</code> 调度器。而真正让开发过程跌宕起伏的，是第三篇要讲的那些让内核崩掉的 bug。</p><h2 id="关键要点总结" tabindex="-1">关键要点总结 <a class="header-anchor" href="#关键要点总结" aria-label="Permalink to &quot;关键要点总结&quot;">​</a></h2><ol><li><strong>CpuContext 只有 64 字节</strong>：6 个 callee-saved GPR + rsp + rip，不多不少。ABI 已经帮我们分担了 caller-saved 寄存器的保存工作。</li><li><strong>static_assert 是不可省略的</strong>：C++ 和汇编之间的偏移量约定没有运行时检查，编译期断言是唯一可靠的安全网。</li><li><strong>栈顶的 exit_current 地址是精巧的约定</strong>：它弥补了 <code>jmp</code> vs <code>call</code> 的语义差异，让线程函数能干净退出。</li><li><strong>Task 保持精简</strong>：只包含调度所需的最小字段集，后续功能逐步添加。</li><li><strong>TaskBuilder 封装复杂性</strong>：分配、映射、初始化、回滚——所有容易出错的细节都在 build() 里集中处理。</li></ol><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>Intel SDM Vol. 3A, Chapter 8 &quot;Task Management&quot;, Section 8.7 &quot;Task Management in 64-Bit Mode&quot; (PDF pages 281-282)：64 位模式不支持硬件任务切换，必须由软件完成</li><li>System V AMD64 ABI: <a href="https://gitlab.com/x86-psABIs/x86-64-ABI" target="_blank" rel="noreferrer">https://gitlab.com/x86-psABIs/x86-64-ABI</a> — callee-saved 寄存器定义</li><li>xv6 <code>swtch.S</code> / <code>proc.h</code>: <a href="https://github.com/mit-pdos/xv6-public" target="_blank" rel="noreferrer">https://github.com/mit-pdos/xv6-public</a> — 32 位上下文切换和 TCB 设计对比</li><li>Linux <code>__switch_to_asm</code>: <a href="https://blog.codingconfessions.com/p/linux-context-switching-internals" target="_blank" rel="noreferrer">https://blog.codingconfessions.com/p/linux-context-switching-internals</a> — 64 位上下文切换的工业级实现</li><li>PintOS <code>switch.S</code>: <a href="https://uchicago-cs.github.io/mpcs52030/switch.html" target="_blank" rel="noreferrer">https://uchicago-cs.github.io/mpcs52030/switch.html</a> — 线程退出封装模式参考</li><li>OSDev Wiki &quot;Context Switching&quot;: <a href="https://wiki.osdev.org/Context_Switching" target="_blank" rel="noreferrer">https://wiki.osdev.org/Context_Switching</a> — 软件上下文切换的通用方法</li><li>xv6 <code>proc.h</code> / <code>swtch.S</code>: <a href="https://github.com/mit-pdos/xv6-public" target="_blank" rel="noreferrer">https://github.com/mit-pdos/xv6-public</a> — 32 位上下文切换和 TCB 设计对比</li><li>Linux <code>__switch_to_asm</code>: <a href="https://blog.codingconfessions.com/p/linux-context-switching-internals" target="_blank" rel="noreferrer">https://blog.codingconfessions.com/p/linux-context-switching-internals</a> — 64 位上下文切换的工业级实现</li><li>Linux kernel context switch 演化史: <a href="https://www.maizure.org/projects/evolution_x86_context_switch_linux/" target="_blank" rel="noreferrer">https://www.maizure.org/projects/evolution_x86_context_switch_linux/</a> — 从 2.6 到现在的实现变化</li></ul>`,70)])])}const u=e(n,[["render",c]]);export{k as __pageData,u as default};
