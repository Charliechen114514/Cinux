import{c as o,Q as d,j as i,m as c}from"./chunks/framework.D9IbVcwA.js";const h=JSON.parse('{"title":"004-boot-load-mini-kernel-c-3 · 内核加载 (C)","description":"","frontmatter":{"title":"004-boot-load-mini-kernel-c-3 · 内核加载 (C)"},"headers":[],"relativePath":"hands-on/004-boot-load-mini-kernel-c-3.md","filePath":"hands-on/004-boot-load-mini-kernel-c-3.md","lastUpdated":1779686691000}'),a={name:"hands-on/004-boot-load-mini-kernel-c-3.md"};function t(n,e,l,r,s,p){return d(),i("div",null,[...e[0]||(e[0]=[c('<h1 id="_004-c-动手篇-补完-构建系统、debugcon-配置与端到端验证" tabindex="-1">004-C 动手篇（补完）：构建系统、debugcon 配置与端到端验证 <a class="header-anchor" href="#_004-c-动手篇-补完-构建系统、debugcon-配置与端到端验证" aria-label="Permalink to &quot;004-C 动手篇（补完）：构建系统、debugcon 配置与端到端验证&quot;">​</a></h1><blockquote><p>本章完成后的可见效果：<code>debug.log</code> 输出完整序列 <code>OPLJ123G4===CPPGC1V123B===END</code></p><p>前置要求：已完成本系列上篇（BootInfo 填充与高半核跳转）和中篇（内核入口与 C++ 运行时）</p></blockquote><hr><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>前两篇我们把 BootInfo 填充、高半核跳转、内核入口序列、C++ 运行时支撑这些核心功能都讲完了。但还差几个基础设施层面的事情没做：debugcon 需要在正常模式下也能输出（不然 <code>make run</code> 什么都看不到）、Mini Kernel 的 CMakeLists.txt 需要配置正确的编译选项和链接脚本、objcopy 后处理步骤需要确保 flat binary 正确生成。这些事情单个来看都不复杂，但漏了任何一个都会导致构建失败或者运行时看不到输出。</p><p>这一篇就专门把这些收尾工作讲清楚，然后做一次完整的端到端验证——从 CMake 配置到 QEMU 启动，确认 <code>OPLJ123G4===CPPGC1V123B===END</code> 这个字符序列完整出现。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="objcopy-o-binary-从-elf-到-flat-binary-的转换" tabindex="-1">objcopy -O binary：从 ELF 到 Flat Binary 的转换 <a class="header-anchor" href="#objcopy-o-binary-从-elf-到-flat-binary-的转换" aria-label="Permalink to &quot;objcopy -O binary：从 ELF 到 Flat Binary 的转换&quot;">​</a></h3><p>我们的内核编译链的最终产物是 flat binary，但编译和链接阶段产出的是 ELF 格式。这两者的区别在于：ELF 包含丰富的元数据（section header table、program header table、符号表、重定位信息），链接器需要这些信息来解析符号和计算地址。但 Bootloader 不会解析 ELF——它只是用 INT 13h 把磁盘上的二进制数据原封不动地读到物理地址 0x20000，然后跳转过去。</p><p><code>objcopy -O binary</code> 做的事情就是把 ELF 转成 Bootloader 能用的格式：剥离所有 ELF 元数据，只保留各段的原始二进制内容，按地址顺序排列输出。输出文件的起始地址是 ELF 中最低的 LMA（Load Memory Address），在我们的链接脚本中就是 0x20000。但由于 flat binary 没有&quot;基地址&quot;的概念，objcopy 实际上从 LMA=0 开始输出——也就是说，文件开头对应的是 LMA 最低的那个段的第一个字节。</p><p>这个转换过程有一个容易忽视的细节：<code>.bss</code> 段不会出现在 flat binary 中。BSS 在 ELF 里不占空间（只记录大小和起始地址），<code>objcopy -O binary</code> 会跳过它。这是正确的行为——BSS 段的内容应该全是零，由运行时（<code>boot.S</code> 中的 <code>rep stosb</code>）负责清零，不需要在文件中占空间。如果 BSS 被包含进了 flat binary，内核的磁盘镜像会无故增大，而且 Bootloader 加载的那段零值数据在 BSS 清除时又会被重新清零一次，完全是浪费。</p><h3 id="mcmodel-large-为什么不能用默认的-small-模型" tabindex="-1">-mcmodel=large：为什么不能用默认的 small 模型 <a class="header-anchor" href="#mcmodel-large-为什么不能用默认的-small-模型" aria-label="Permalink to &quot;-mcmodel=large：为什么不能用默认的 small 模型&quot;">​</a></h3><p>x86-64 的代码模型决定了编译器如何生成访问全局变量和函数的指令。默认的 <code>small</code> 模型假设所有代码和数据都在低 2GB 地址空间内（0 到 0x7FFFFFFF），编译器可以用 32 位相对地址或绝对地址来引用符号——这在用户态程序中完全没问题，因为虚拟地址空间通常从 0x400000 附近开始。</p><p>但我们的内核 VMA 是 <code>0xFFFFFFFF80020000</code>，这个地址远超 2GB。如果用 small 模型，编译器生成类似 <code>movq symbol(%rip), %rax</code> 这样的 RIP-relative 寻址指令时，32 位的偏移量不够表示从当前指令位置到 <code>0xFFFFFFFF80020000</code> 的距离——溢出后变成一个完全错误的地址，运行时访问到垃圾数据或者直接 Page Fault。</p><p><code>-mcmodel=large</code> 告诉编译器使用 64 位绝对地址来引用符号——每条全局变量访问都需要 <code>movabsq $0xFFFFFFFF80022XXX, %rax</code> 这样的 10 字节指令（REX 前缀 + 操作码 + 8 字节立即数）。代码体积会增大（每条全局变量访问多几个字节），但对于内核代码来说这点开销完全可以接受。</p><h3 id="mno-red-zone-内核代码的安全红线" tabindex="-1">-mno-red-zone：内核代码的安全红线 <a class="header-anchor" href="#mno-red-zone-内核代码的安全红线" aria-label="Permalink to &quot;-mno-red-zone：内核代码的安全红线&quot;">​</a></h3><p>x86-64 ABI 定义了一个叫 &quot;Red Zone&quot; 的优化区域：栈指针（%rsp）以下 128 字节的空间被保留给当前函数使用，信号处理程序和中断处理程序不能触碰这个区域。在用户态程序中，这 128 字节可以用来存储局部变量而不需要调整 %rsp，省掉了 push/pop 的开销。</p><p>但在内核代码中，这个优化是致命的。内核代码随时可能被硬件中断打断——中断发生时 CPU 不会帮你保存 Red Zone 的内容（Red Zone 的设计假设中断处理会切换到内核栈，但在内核本身就是中断处理者的情况下这个假设不成立）。如果编译器把某个关键变量存在了 Red Zone 里，中断到来时这个变量就会被中断处理程序的栈帧覆盖，导致数据损坏和难以调试的随机崩溃。</p><p><code>-mno-red-zone</code> 禁用这个优化，强制编译器为所有局部变量显式分配栈空间。代价是每帧多几字节的栈使用和一两条额外的 <code>sub %rsp</code> 指令，但避免了中断上下文中的数据损坏。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-更新-cmake-qemu-cmake——debugcon-全局启用" tabindex="-1">Step 1: 更新 cmake/qemu.cmake——debugcon 全局启用 <a class="header-anchor" href="#step-1-更新-cmake-qemu-cmake——debugcon-全局启用" aria-label="Permalink to &quot;Step 1: 更新 cmake/qemu.cmake——debugcon 全局启用&quot;">​</a></h3><p><strong>目标</strong>：把 debugcon 相关的 QEMU 参数从 <code>QEMU_DEBUG_FLAGS</code> 移到 <code>QEMU_COMMON_FLAGS</code>，确保 <code>make run</code>（非调试模式）也能看到 debugcon 输出。</p><p><strong>设计思路</strong>：之前 debugcon 只在 <code>make run-debug</code> 模式下启用，正常模式不启用。但从 004C 开始，内核的 <code>boot.S</code> 和 <code>main.cpp</code> 都依赖 debugcon 输出来验证执行状态——如果正常模式下不启用，<code>outb %al, $0xE9</code> 就是一个空操作，你什么调试信息都看不到。把它移到 COMMON 后，不管以什么模式运行，debugcon 输出都会写入 <code>debug.log</code> 文件。</p><p><strong>实现约束</strong>：修改位置在 <code>cmake/qemu.cmake</code> 文件。<code>QEMU_COMMON_FLAGS</code> 需要包含 <code>-debugcon file:debug.log -global isa-debugcon.iobase=0xe9</code> 这两个参数。<code>QEMU_DEBUG_FLAGS</code> 只保留 <code>-s -S</code>（GDB 调试用的参数）。不要删掉其他已有的 COMMON 参数（<code>-m 512M -serial stdio -no-reboot -no-shutdown</code>）。</p><p><strong>验证</strong>：运行 <code>make run</code> 后检查 <code>debug.log</code> 文件是否存在且内容不为空。如果文件不存在，检查 CMake 配置是否正确应用了新的 QEMU 参数。可以用 <code>cmake --build build --target help</code> 确认 build 目标存在，或者直接 <code>./build/kernel/mini/mini_kernel --help</code> 不行的话就手动检查 <code>build/compile_commands.json</code> 里 QEMU 参数是否正确。</p><p><strong>踩坑预警</strong>：<code>-global isa-debugcon.iobase=0xe9</code> 这个参数看起来多余（QEMU 的 isa-debugcon 默认端口就是 0xE9），但显式指定是为了防止未来的默认值变更。如果你省略了这个参数，在某次 QEMU 升级后可能突然发现 debugcon 不工作了，排查起来非常隐蔽。另外一个容易搞错的地方是 <code>-debugcon file:debug.log</code>——注意是 <code>file:</code> 前缀，不是 <code>stdio:</code> 或 <code>vc:</code>。<code>file:</code> 模式把输出写到文件，不影响 QEMU 的标准输出（标准输出已经被 <code>-serial stdio</code> 占用了）。</p><h3 id="step-2-配置-mini-kernel-的-cmakelists-txt" tabindex="-1">Step 2: 配置 Mini Kernel 的 CMakeLists.txt <a class="header-anchor" href="#step-2-配置-mini-kernel-的-cmakelists-txt" aria-label="Permalink to &quot;Step 2: 配置 Mini Kernel 的 CMakeLists.txt&quot;">​</a></h3><p><strong>目标</strong>：确保 <code>kernel/mini/CMakeLists.txt</code> 配置了正确的源文件、编译选项、链接选项和后处理步骤。</p><p><strong>设计思路</strong>：源文件列表需要包含三个核心文件：<code>arch/x86_64/boot.S</code>（内核入口汇编）、<code>arch/x86_64/crt_stub.cpp</code>（C++ 运行时支撑）、<code>main.cpp</code>（内核主函数）。CMake 会自动识别 <code>.S</code> 文件需要用 GCC 的汇编模式编译（带 C 预处理），<code>.cpp</code> 文件用 C++ 编译。</p><p>编译选项的关键几项：<code>-ffreestanding</code>（不依赖标准库）、<code>-fno-exceptions</code>（禁用异常）、<code>-fno-rtti</code>（禁用 RTTI）、<code>-fno-pie</code>（不做位置无关可执行文件）、<code>-mcmodel=large</code>（大代码模型，允许访问任意 64 位地址——高半核地址远超默认 small 模型的 2GB 限制）、<code>-mno-red-zone</code>（禁用 red zone——内核代码不能依赖 red zone，因为中断可能在任何时刻打断执行，而中断处理不会帮你保存 red zone 的内容）。</p><p>链接选项需要 <code>-T linker.ld</code> 指定链接脚本、<code>-nostdlib</code> 不链接标准库、<code>-no-pie</code> 不做位置无关。Post-build 步骤用 <code>objcopy -O binary</code> 把 ELF 转成纯二进制（<code>mini_kernel.bin</code>），因为 Bootloader 不知道怎么解析 ELF 格式——它只是把磁盘上的二进制数据原样读入内存然后跳转。</p><p><strong>踩坑预警</strong>：<code>-mcmodel=large</code> 是不能省的。如果用了默认的 <code>-mcmodel=small</code>，编译器生成的代码会假设所有符号都在低 2GB 地址空间内，用 32 位相对地址来引用全局变量。但我们的内核 VMA 是 0xFFFFFFFF80000000，远超 2GB——运行时这些地址引用会被截断成错误的值。这个坑的特点是编译和链接都不会报错，只有运行时才炸，排查难度极高。</p><p>include 路径需要包含 <code>boot/</code> 目录（项目根目录下的），因为 <code>main.cpp</code> 要引用 <code>boot_info.h</code>。</p><p><strong>验证</strong>：编译后确认 <code>build/kernel/mini/mini_kernel.bin</code> 文件存在且大小合理（应该在几十 KB 左右）。用 <code>file build/kernel/mini/mini_kernel</code> 确认它是 ELF 64-bit x86-64 格式，用 <code>readelf -h build/kernel/mini/mini_kernel</code> 确认入口地址是 <code>0xffffffff80020000</code>。如果入口地址不对，说明链接脚本没有正确加载。</p><p><strong>额外检查</strong>：用 <code>readelf -S build/kernel/mini/mini_kernel | grep -E &quot;\\.text|\\.data|\\.init_array|\\.bss&quot;</code> 确认所有段都存在。特别是 <code>.init_array</code> 段——如果它不存在，说明链接器的 section garbage collection 把它砍掉了（<code>KEEP()</code> 没写对），全局构造函数永远不会被调用。用 <code>objdump -t build/kernel/mini/mini_kernel | grep boot_info</code> 确认 <code>__boot_info_ptr</code> 在 <code>.data</code> 段而不是 <code>.bss</code> 段。</p><h3 id="step-3-完整的端到端验证" tabindex="-1">Step 3: 完整的端到端验证 <a class="header-anchor" href="#step-3-完整的端到端验证" aria-label="Permalink to &quot;Step 3: 完整的端到端验证&quot;">​</a></h3><p><strong>目标</strong>：从头构建整个项目，运行 QEMU，确认 debugcon 输出完整序列。</p><p><strong>验证步骤</strong>：</p><p>先确认所有源文件都已正确保存，然后执行以下步骤：</p><ol><li>清空 build 目录重新构建（排除缓存问题）</li><li>运行 CMake 配置和编译</li><li>运行 QEMU</li><li>检查 <code>debug.log</code> 文件内容</li><li>如果有任何字符缺失，按步骤 4 的排查手册定位问题</li></ol><p>预期的完整输出序列是 <code>OPLJ123G4===CPPGC1V123B===END</code>。逐字符解读：</p><ul><li><code>O</code>：MBR 加载 Stage2 成功</li><li><code>P</code>：保护模式切换完成</li><li><code>L</code>：Long Mode 进入</li><li><code>J</code>：Bootloader 跳转到 Mini Kernel</li><li><code>1</code>：内核入口 <code>_start</code> 到达</li><li><code>2</code>：栈设置完成</li><li><code>3</code>：BSS 清除完成</li><li><code>G</code>：全局对象 <code>GlobalCounter</code> 构造（在 <code>_init_global_ctors</code> 中）</li><li><code>4</code>：全局构造函数调用完毕</li><li><code>===CPP</code>：C++ 特性测试开始</li><li><code>C1</code>：简单类构造验证</li><li><code>V</code>：虚函数类构造</li><li><code>2</code>：虚函数测试通过</li><li><code>3</code>：全局对象验证通过</li><li><code>B</code>：BootInfo 验证通过</li><li><code>===END</code>：全部测试通过</li></ul><p>如果序列在某处中断，根据最后一个字符就能定位问题。比如停在 <code>OPLJ12</code> 说明 BSS 清除出了问题，停在 <code>123G</code> 说明全局构造之后的恢复出了问题，停在 <code>===CPPC1V</code> 说明虚函数之后的某个验证失败了。</p><h3 id="step-4-异常排查手册" tabindex="-1">Step 4: 异常排查手册 <a class="header-anchor" href="#step-4-异常排查手册" aria-label="Permalink to &quot;Step 4: 异常排查手册&quot;">​</a></h3><p>如果端到端验证没有通过，按下面的流程排查：</p><p><strong>问题：只看到 OPL，没有 J</strong>——BootInfo 填充或跳转代码没有执行。检查 <code>stage2.S</code> 中 <code>long_mode_entry</code> 之后的代码。</p><p><strong>问题：看到 J 但立刻 Triple Fault</strong>——高半核页表映射有问题。用 GDB 检查 PML4[511] 和 PDPT[510] 的内容。</p><p><strong>问题：看到 J1 但没有 2</strong>——栈设置失败。检查 <code>__mini_stack_top</code> 符号是否存在（链接脚本中的 <code>.bss</code> 段末尾）。</p><p><strong>问题：看到 12 但没有 3</strong>——BSS 清除出了问题。检查 <code>__bss_start</code> 和 <code>__bss_end</code> 的值是否正确（链接脚本中的符号）。</p><p><strong>问题：看到 123 但没有 G4</strong>——全局构造函数调用失败。检查 <code>.init_array</code> 段是否存在（用 <code>readelf -S build/kernel/mini/mini_kernel | grep init_array</code>），检查 <code>__init_array_start</code> 和 <code>__init_array_end</code> 是否正确。</p><p><strong>问题：看到 123G4 但没有 ===CPP</strong>——<code>mini_kernel_main</code> 没有被调用，或者 <code>__boot_info_ptr</code> 恢复失败。检查 boot.S 最后 <code>call mini_kernel_main</code> 之前的 <code>movq __boot_info_ptr, %rdi</code> 是否正确。也可能是 <code>mini_kernel_main</code> 函数本身有编译或链接错误——用 <code>objdump -t build/kernel/mini/mini_kernel | grep mini_kernel_main</code> 确认符号存在。如果符号不存在，检查 <code>main.cpp</code> 是否有 <code>extern &quot;C&quot;</code> 标记——没有这个标记的话 C++ 编译器会对函数名做 name mangling，汇编里的 <code>call mini_kernel_main</code> 就找不到对应的符号。</p><p><strong>问题：看到 ===CPP 但后续字符不完整</strong>——某个 C++ 特性验证失败了。根据最后一个输出的字符定位是哪个测试出了问题。比如 <code>C1</code> 之后没有 <code>V</code>，说明虚函数类的构造函数出了问题；有 <code>V</code> 但没有 <code>2</code>，说明虚函数调用本身失败（可能是 vtable 损坏或 <code>__cxa_pure_virtual</code> 被调用）；有 <code>2</code> 但没有 <code>3</code>，说明全局对象的构造函数没有被调用（<code>.init_array</code> 段可能被链接器丢弃了）。</p><p><strong>问题：QEMU 启动后直接重启，连 O 都没有</strong>——MBR 加载就失败了。检查 <code>build_image.sh</code> 是否正确把 MBR 写入了磁盘镜像的 LBA 0。也可能是磁盘镜像本身损坏了——删除后重新生成。</p><p><strong>问题：看到 OPLJ123G4===CPPGC1V123B===END 后 QEMU 一直在循环重启</strong>——<code>mini_kernel_main</code> 的末尾 <code>while(1) { cli; hlt }</code> 循环没有正确执行。可能是编译器优化把死循环优化掉了（加 <code>-O0</code> 或者用 <code>__asm__ volatile</code> 确保不被优化），也可能是 <code>hlt</code> 指令之后的 <code>cli</code> 没有紧跟（检查生成的汇编）。</p><h3 id="step-5-用-objdump-和-readelf-深入检查构建产物" tabindex="-1">Step 5: 用 objdump 和 readelf 深入检查构建产物 <a class="header-anchor" href="#step-5-用-objdump-和-readelf-深入检查构建产物" aria-label="Permalink to &quot;Step 5: 用 objdump 和 readelf 深入检查构建产物&quot;">​</a></h3><p>当端到端验证失败时，以下命令可以帮助快速定位问题：</p><p>检查 ELF 入口地址：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readelf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -h</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/mini/mini_kernel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Entry point&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>应该输出 <code>Entry point address: 0xffffffff80020000</code>。</p><p>检查段的 VMA 和 LMA：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readelf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/mini/mini_kernel</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>注意看 <code>VirtAddr</code> 和 <code>PhysAddr</code> 列——<code>.text</code> 段的 VirtAddr 应该从 <code>0xffffffff80020000</code> 开始，PhysAddr 应该从 <code>0x20000</code> 开始。</p><p>检查符号表中的关键符号：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/mini/mini_kernel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -E</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;boot_info|init_array|bss_start|bss_end|_start|mini_kernel&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>确认每个符号都在预期的段中，且地址没有重叠。</p><p>检查 flat binary 大小：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -la</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/mini/mini_kernel.bin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>大小应该在几十 KB 左右。如果太小（比如只有几百字节），说明 objcopy 命令参数有误；如果太大（超过 416KB），说明 BSS 段被包含进了 flat binary（不应该发生，因为 objcopy -O binary 会跳过 BSS）。</p><h3 id="step-6-用-gdb-验证完整启动序列" tabindex="-1">Step 6: 用 GDB 验证完整启动序列 <a class="header-anchor" href="#step-6-用-gdb-验证完整启动序列" aria-label="Permalink to &quot;Step 6: 用 GDB 验证完整启动序列&quot;">​</a></h3><p>如果上面的排查步骤没有解决问题，可以用 GDB 进行更深度的调试。推荐的调试流程：</p><ol><li>用 <code>make run-debug</code> 启动 QEMU（会等待 GDB 连接）</li><li>在另一个终端启动 GDB：<code>gdb build/kernel/mini/mini_kernel</code></li><li>在 GDB 中连接 QEMU：<code>target remote :1234</code></li><li>设置断点：<code>break _start</code> 和 <code>break mini_kernel_main</code></li><li><code>continue</code> 运行到 <code>_start</code> 断点</li><li>检查 <code>%rdi</code>：<code>info registers rdi</code> 应该显示 <code>0x7000</code></li><li>单步执行到 <code>rep stosb</code> 之后：检查 <code>%rdi</code> 已经改变（不再是 0x7000）</li><li>检查 <code>__boot_info_ptr</code> 的值：<code>x/gx &amp;__boot_info_ptr</code> 应该显示 <code>0x7000</code></li><li><code>continue</code> 到 <code>mini_kernel_main</code> 断点</li><li>检查 BootInfo 内容：<code>x/gx (uint64_t*)__boot_info_ptr</code> 应该显示 <code>0xFFFFFFFF80020000</code></li></ol><p>这个调试流程可以帮助你精确定位问题出现在哪个阶段——是 BootLoader 传递参数出错，还是 BSS 清除破坏了保存的指针，还是全局构造函数覆盖了 <code>.data</code> 段的数据。每一步的 GDB 输出都应该和预期值一致，如果不一致就找到了问题所在。</p><p>还有一个非常有用的 GDB 技巧：在 QEMU 中可以用 <code>-d int</code> 选项记录所有中断和异常。如果你看到 Triple Fault，这个日志会告诉你是哪种异常触发的（Page Fault、General Protection Fault、Double Fault 等），以及异常发生时的 <code>%rip</code> 值——有了 <code>%rip</code> 就知道是哪条指令出了问题。</p><h2 id="tag-边界确认" tabindex="-1">Tag 边界确认 <a class="header-anchor" href="#tag-边界确认" aria-label="Permalink to &quot;Tag 边界确认&quot;">​</a></h2><p>最后确认一下 004C 三个文件各自的覆盖范围。c-1（上篇）覆盖 Bootloader 侧：高半核页表映射、BootInfo 填充、跳转到内核。c-2（中篇）覆盖内核侧：SSE 启用、栈设置、BSS 清除与指针保存、符号冲突修复、C++ 运行时（crt_stub）、链接器脚本。c-3（本篇）覆盖构建系统：CMakeLists.txt 编译/链接选项、debugcon 配置、objcopy 后处理、端到端验证、异常排查。三个文件合起来覆盖了 004C tag 的全部内容，和 004A（Real Mode E820+磁盘读取）和 004B（BootInfo 定义+416KB 加载+构建系统）的边界清晰，没有重叠。特别注意 004B 和 004C 的分界线：004B 负责把完整的 Mini Kernel flat binary 从磁盘读到物理地址 0x20000，004C 从 Long Mode 进入后开始工作（高半核映射、BootInfo 填充、跳转）。004B 的最后一件事是调用 <code>enter_long_mode</code> 进入 Long Mode 并输出 <code>L</code>，004C 的第一件事就是在 Long Mode 中填充 BootInfo。这个分界线非常清晰——<code>L</code> 字符的输出属于 003/004B 的阶段，<code>J</code> 字符的输出属于 004C。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>debugcon 全局启用</td><td><code>-debugcon file:debug.log</code> 移到 COMMON_FLAGS，确保所有运行模式可见</td></tr><tr><td>编译选项</td><td><code>-ffreestanding -fno-exceptions -fno-rtti -mcmodel=large -mno-red-zone</code></td></tr><tr><td><code>-mcmodel=large</code></td><td>高半核地址超出 small 模型 2GB 限制，必须用 large 模型访问任意 64 位地址</td></tr><tr><td><code>-mno-red-zone</code></td><td>内核代码不能依赖 red zone，中断随时打断会覆盖 red zone 数据</td></tr><tr><td>链接选项</td><td><code>-T linker.ld -nostdlib -no-pie</code></td></tr><tr><td>objcopy 后处理</td><td><code>-O binary</code> 将 ELF 转为 flat binary，Bootloader 直接加载，BSS 不包含在输出中</td></tr><tr><td>ELF 入口地址</td><td><code>readelf -h</code> 确认入口为 <code>0xffffffff80020000</code></td></tr><tr><td>段检查</td><td><code>.init_array</code> 必须存在（<code>KEEP()</code> 保护），<code>__boot_info_ptr</code> 必须在 <code>.data</code> 段</td></tr><tr><td>端到端验证序列</td><td><code>OPLJ123G4===CPPGC1V123B===END</code>，每个字符对应一个检查点</td></tr><tr><td>构建产物</td><td><code>build/kernel/mini/mini_kernel</code>（ELF）+ <code>mini_kernel.bin</code>（flat binary）</td></tr><tr><td>GDB 调试流程</td><td><code>break _start</code> -&gt; 检查 <code>%rdi</code>=0x7000 -&gt; 单步到 BSS 清除后 -&gt; 检查 <code>__boot_info_ptr</code></td></tr><tr><td>004C 三篇分工</td><td>c-1=Bootloader 侧(高半核+BootInfo+跳转), c-2=内核侧(SSE+栈+BSS+C++ 运行时), c-3=构建系统(编译选项+debugcon+验证)</td></tr></tbody></table>',79)])])}const m=o(a,[["render",t]]);export{h as __pageData,m as default};
