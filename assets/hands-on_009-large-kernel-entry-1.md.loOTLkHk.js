import{c as a,Q as e,j as n,m as i}from"./chunks/framework.iD28w_5a.js";const k=JSON.parse('{"title":"009-large-kernel-entry-1 · 大内核入口","description":"","frontmatter":{"title":"009-large-kernel-entry-1 · 大内核入口"},"headers":[],"relativePath":"hands-on/009-large-kernel-entry-1.md","filePath":"hands-on/009-large-kernel-entry-1.md","lastUpdated":1779686691000}'),t={name:"hands-on/009-large-kernel-entry-1.md"};function p(l,s,r,d,o,c){return e(),n("div",null,[...s[0]||(s[0]=[i(`<h1 id="_009-1-higher-half-大内核启动-—-链接脚本-启动汇编-c-运行时" tabindex="-1">009-1 Higher-Half 大内核启动 — 链接脚本 + 启动汇编 + C++ 运行时 <a class="header-anchor" href="#_009-1-higher-half-大内核启动-—-链接脚本-启动汇编-c-运行时" aria-label="Permalink to &quot;009-1 Higher-Half 大内核启动 — 链接脚本 + 启动汇编 + C++ 运行时&quot;">​</a></h1><h2 id="章节导语" tabindex="-1">章节导语 <a class="header-anchor" href="#章节导语" aria-label="Permalink to &quot;章节导语&quot;">​</a></h2><p>上一章（008）我们把 ATA 磁盘驱动和 ELF 加载器搞定了，mini kernel 已经有能力从磁盘读取一个大内核 ELF 文件、解析它的 PT_LOAD 段、把它们搬运到正确的物理地址。但问题在于——跳过去之后第一件事做什么？大内核的代码链接在什么样的地址上？这些问题在 008 里一笔带过了，因为那时候大内核本身还不存在。</p><p>这一章我们要做的事情就是让大内核真正&quot;站起来&quot;。具体来说，我们需要编写大内核自己的链接脚本（linker script），把代码链接到 higher-half 虚拟地址 <code>0xFFFFFFFF80000000</code>；编写一段启动汇编（boot.S），负责在 mini kernel 跳过来之后的第一时间设置栈、清零 BSS、运行全局构造器、最后跳进 C++ 世界；再写一组 C++ 运行时桩函数（crt_stub），把编译器在 freestanding 环境下期望的那些符号全部补齐。完成本章后，你会看到 QEMU 串口输出 <code>[BIG] Big kernel running @ 0x1000000</code>——这意味着大内核的第一行代码已经成功执行了。</p><p>本章的前置知识是上一章的磁盘驱动和 ELF 加载器，因为大内核是被 mini kernel 从磁盘加载并跳转过来的。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="为什么内核要住在-higher-half" tabindex="-1">为什么内核要住在 higher-half？ <a class="header-anchor" href="#为什么内核要住在-higher-half" aria-label="Permalink to &quot;为什么内核要住在 higher-half？&quot;">​</a></h3><p>我们的 mini kernel 不是一直在物理地址的 identity mapping 下跑得好好的吗？为什么大内核非得搬到 <code>0xFFFFFFFF80000000</code> 这么远的地址上去？</p><p>原因有好几个。最直接的一个是<strong>用户空间的地址空间规划</strong>。在现代操作系统中，内核和用户进程共享同一个虚拟地址空间，如果我们把内核放在低地址，用户进程的代码就没法用这些地址了。而把内核放到虚拟地址空间的最高端（x86-64 的 canonical high 区域），低地址就完全留给用户进程——每个进程都能拥有一整块连续的地址空间，内核映射在所有进程的页表里始终存在，系统调用的时候不需要切换页表，只需要修改 CS:RIP 跳到高地址就行了。</p><p>另一个原因是<strong>安全性</strong>。Higher-half 的布局天然地把内核地址和用户地址分开了，只要在页表项里把 U/S bit 设对，用户态的代码根本不可能碰触到内核空间。所以几乎所有现代 OS（Linux、Windows、macOS）都采用 higher-half 内核的设计。我们选择的 <code>0xFFFFFFFF80000000</code> 是一个常见的基址选择——Linux 内核也用这个地址作为 <code>__START_KERNEL_map</code>。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>x86-64 虚拟地址空间布局 (48-bit canonical):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>0x0000000000000000 ┌──────────────────┐</span></span>
<span class="line"><span>                  │   用户空间 (128 TB) │  低半部分，每个进程独立</span></span>
<span class="line"><span>0x00007FFFFFFFFFFF ├──────────────────┤ ← canonical boundary</span></span>
<span class="line"><span>                  │  不可访问 (hole)   │  非 canonical 地址，访问触发 #GP</span></span>
<span class="line"><span>0xFFFF800000000000 ├──────────────────┤</span></span>
<span class="line"><span>                  │   内核空间 (128 TB) │  高半部分，所有进程共享</span></span>
<span class="line"><span>0xFFFFFFFF80000000 │  ← KERNEL_VMA     │  大内核从这里开始</span></span>
<span class="line"><span>0xFFFFFFFFFFFFFFFF └──────────────────┘</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="vma-和-lma-到底是什么" tabindex="-1">VMA 和 LMA 到底是什么？ <a class="header-anchor" href="#vma-和-lma-到底是什么" aria-label="Permalink to &quot;VMA 和 LMA 到底是什么？&quot;">​</a></h3><p>在链接脚本里你会频繁看到两个概念：VMA（Virtual Memory Address）和 LMA（Load Memory Address），理解这两个概念是搞懂 higher-half 内核链接过程的关键。</p><p>VMA 是&quot;链接器认为这个代码/数据在运行时应该出现在哪个虚拟地址&quot;，编译器生成相对跳转、绝对地址引用、函数指针这些的时候，用的都是 VMA。LMA 是&quot;这个代码/数据在加载时实际应该被放到哪个物理地址&quot;。我们的 ELF 加载器在加载大内核的时候，页表还是 mini kernel 设置的 identity mapping，CPU 还不认识 <code>0xFFFFFFFF80000000</code> 这个虚拟地址——所以段数据必须被加载到一个物理地址（也就是 LMA），代码才能在 identity mapping 下正常执行。</p><p>在链接脚本里，关键公式是 <code>LMA = VMA - KERNEL_VMA</code>。每个 section 用 <code>AT()</code> 伪操作显式指定了 LMA。这样一来，ELF 文件的程序头里 <code>p_vaddr</code> 是高地址（供将来开启分页后使用），<code>p_paddr</code> 是低地址（供当前 identity mapping 使用），两边都不耽误。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>链接脚本地址关系:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>VMA (链接器看到的地址):        LMA (加载器放到内存的地址):</span></span>
<span class="line"><span>0xFFFFFFFF80000000 ─┐          0x01000000 ─┐</span></span>
<span class="line"><span>  .text.start       │ -0xFFFFFFFF80000000   │</span></span>
<span class="line"><span>  .text             │  ──────────────────→   │ 物理内存中的实际位置</span></span>
<span class="line"><span>  .rodata           │  AT() 做的减法         │</span></span>
<span class="line"><span>  .data             │                        │</span></span>
<span class="line"><span>  .init_array       │                        │</span></span>
<span class="line"><span>  .bss              │                        │</span></span>
<span class="line"><span>  stack             │                        │</span></span>
<span class="line"><span>0xFFFFFFFF80XXXXXX ─┘          0x01XXXXXX ─┘</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><h3 id="init-array-和全局构造器" tabindex="-1">.init_array 和全局构造器 <a class="header-anchor" href="#init-array-和全局构造器" aria-label="Permalink to &quot;.init_array 和全局构造器&quot;">​</a></h3><p>如果你写的是 C 内核，启动汇编只需要设置栈、清 BSS、调用 main 就行了。但 C++ 有一套额外的机制：全局对象的构造函数必须在 main 之前被调用。编译器处理全局对象的方式是生成一个构造器函数指针，把它放进 <code>.init_array</code> section 里。启动代码需要遍历这个数组，逐个调用这些构造器。如果遗漏了这一步，所有带非平凡构造函数的全局对象都会处于未初始化状态——虚函数表指针是垃圾数据，调用任何方法都是未定义行为。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-编写大内核链接脚本" tabindex="-1">Step 1: 编写大内核链接脚本 <a class="header-anchor" href="#step-1-编写大内核链接脚本" aria-label="Permalink to &quot;Step 1: 编写大内核链接脚本&quot;">​</a></h3><p><strong>目标</strong>：定义大内核的内存布局，把代码链接到 higher-half 虚拟地址空间，同时确保 ELF 的程序头包含正确的物理地址信息供 mini kernel 的 ELF 加载器使用。</p><p><strong>设计思路</strong>：</p><p>我们需要创建一个链接脚本 <code>kernel/linker.ld</code>。开头声明输出格式为 <code>elf64-x86-64</code>、目标架构为 <code>i386:x86-64</code>、入口点为 <code>_start</code>（定义在 boot.S 里）。然后定义两个地址常量：<code>KERNEL_VMA = 0xFFFFFFFF80000000</code>（higher-half 虚拟基地址）和 <code>KERNEL_LMA = 0x1000000</code>（16MB 物理加载地址）。这个 16MB 要和 mini kernel 的 <code>BIG_KERNEL_LOAD_ADDR</code> 常量一致。</p><p>SECTIONS 块里最关键的一行是把位置计数器设为 <code>KERNEL_VMA + KERNEL_LMA</code>。之后所有 section 的 VMA 会从 <code>0xFFFFFFFF80100000</code> 开始递增。每个 section 都用 <code>AT(ADDR(.section) - KERNEL_VMA)</code> 显式指定了 LMA——把 VMA 减去 <code>0xFFFFFFFF80000000</code> 得到纯物理偏移量。</p><p>Section 排列顺序是有讲究的：<code>.text.start</code> 必须放在 <code>.text</code> 最前面（确保 <code>_start</code> 出现在 ELF 文件最开头）；<code>.text</code> 和 <code>.rodata</code> 合并；<code>.data</code> 单独一个段；<code>.init_array</code> 用 <code>KEEP()</code> 防止被 <code>--gc-sections</code> 删掉；<code>.bss</code> 不需要 <code>AT()</code> 因为它不占文件空间；最后分配 16KB 的 <code>.stack (NOLOAD)</code> 段。</p><p><strong>踩坑预警</strong>：<code>KERNEL_LMA</code> 的值必须和 mini kernel 的 <code>BIG_KERNEL_LOAD_ADDR</code> 一致，否则加载器搬过去的段对不上链接器期望的物理地址，整个内核就是一团乱。</p><p><strong>验证</strong>：完成链接脚本后，配合后续的 boot.S 和 CMakeLists.txt 一起构建，用 <code>readelf -l</code> 检查生成的 ELF 的 program header，确认 <code>p_vaddr</code> 是 <code>0xFFFFFFFF80100000</code> 起始的高地址，<code>p_paddr</code> 是 <code>0x1000000</code> 起始的物理地址。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 构建完成后检查 ELF headers</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readelf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/big/big_kernel</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：看到 PT_LOAD 段，p_paddr 从 0x1000000 开始，p_vaddr 从 0xFFFFFFFF80100000 开始</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="step-2-编写大内核启动汇编-boot-s" tabindex="-1">Step 2: 编写大内核启动汇编 boot.S <a class="header-anchor" href="#step-2-编写大内核启动汇编-boot-s" aria-label="Permalink to &quot;Step 2: 编写大内核启动汇编 boot.S&quot;">​</a></h3><p><strong>目标</strong>：实现 <code>_start</code> 函数——大内核执行的第一段代码。它需要依次完成六步操作：禁用中断、设置栈、清零 BSS、运行全局构造器、调用 kernel_main、halt 循环。</p><p><strong>设计思路</strong>：</p><p>把这段代码放在 <code>.section .text.start, &quot;ax&quot;</code> 里，确保它被链接到 ELF 文件的最开头。标记为 <code>.code64</code> 因为 mini kernel 在 long mode 下跳转过来。</p><p>整个流程是六个步骤的严格序列，每一步都依赖前一步的正确完成。</p><p><strong>实现约束</strong>：</p><p>Step 1 只需要一条 <code>cli</code> 指令禁用中断——因为大内核还没有设置自己的 IDT，如果中断开启，任何硬件中断都会触发 CPU 去查 mini kernel 的 IDT，大概率崩溃。</p><p>Step 2 设置栈：把链接脚本里定义的 <code>__kernel_stack_top</code> 的地址加载到 RSP，然后把 RBP 清零标记调用链底部。x86-64 的栈是向下增长的，所以&quot;栈顶&quot;是这片内存的最高地址。</p><p>Step 3 清零 BSS 是非常关键的一步，把 <code>__bss_start</code> 的地址放进 <code>%rdi</code>、<code>__bss_end</code> 放进 <code>%rcx</code>，算出字节数后用 <code>rep stosb</code> 指令填充零。我们不在 ELF 加载器中依赖 BSS 清零，而是在启动汇编里无条件做一遍，保证万无一失。</p><p><strong>踩坑预警</strong>：<code>rep stosb</code> 会破坏 <code>%rdi</code>、<code>%rcx</code>、<code>%rax</code> 的值！这意味着如果 mini kernel 在跳转时通过 <code>%rdi</code> 传递了 BootInfo 指针（System V AMD64 ABI 的第一个参数寄存器），经过 BSS 清零之后这个值就丢了。目前 milestone 009 不需要 BootInfo，所以 Step 5 直接传 NULL。如果后续需要 BootInfo，必须在 BSS 清零之前把 <code>%rdi</code> 保存起来（比如压栈或者存到另一个寄存器），清零之后再恢复。</p><p>Step 4 调用 <code>_init_global_ctors()</code>，遍历 <code>.init_array</code> section 中的函数指针数组。当前内核还没有带非平凡构造函数的全局对象，所以 <code>.init_array</code> 是空的，这个调用什么也不做——但基础设施必须提前搭好。</p><p>Step 5 调用 <code>kernel_main()</code>，进入 C++ 世界。因为 BSS 清零破坏了 <code>%rdi</code>，这里显式清零后传 NULL 给 kernel_main。</p><p>Step 6 是一个三指令死循环：<code>cli; hlt; jmp .halt</code>。为什么三行而不是一行 <code>hlt</code>？因为 <code>hlt</code> 在收到 NMI 时会被唤醒，没有 <code>jmp</code> 的话 CPU 会继续往下执行垃圾指令，导致 triple fault。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 配合后续步骤构建完整内核后，查看反汇编确认 _start 位于文件开头</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/big/big_kernel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -30</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：第一条指令是 cli (0xFA)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="step-3-实现-c-运行时桩函数-crt-stub-cpp" tabindex="-1">Step 3: 实现 C++ 运行时桩函数 crt_stub.cpp <a class="header-anchor" href="#step-3-实现-c-运行时桩函数-crt-stub-cpp" aria-label="Permalink to &quot;Step 3: 实现 C++ 运行时桩函数 crt_stub.cpp&quot;">​</a></h3><p><strong>目标</strong>：提供 freestanding C++ 环境下编译器期望的运行时函数，包括纯虚函数调用处理、栈保护失败处理、atexit 注册、全局构造器初始化、operator new/delete 的 stub。</p><p><strong>设计思路</strong>：</p><p>这个文件里的函数分为两类：一类代表&quot;不应该发生但编译器需要链接&quot;的编程错误处理（<code>__cxa_pure_virtual</code>、<code>__stack_chk_fail</code>），另一类是内核环境的必要适配（<code>__cxa_atexit</code>、<code>_init_global_ctors</code>、operator new/delete）。</p><p><strong>实现约束</strong>：</p><p>编程错误处理函数应该在 QEMU 的 debug console（I/O 端口 0xE9）输出一个标识字符（比如 &#39;V&#39; 代表纯虚函数、&#39;S&#39; 代表栈保护），然后死循环。这样在 <code>debug.log</code> 里看到一个字符就知道出了什么问题。</p><p><code>__cxa_atexit</code> 直接返回 0（成功），内核永不退出，不需要析构注册。</p><p><code>_init_global_ctors</code> 获取链接脚本里定义的 <code>__init_array_start</code> 和 <code>__init_array_end</code> 两个边界符号，遍历其间的函数指针数组逐个调用。注意要检查 NULL 指针，因为对齐填充可能导致空隙。</p><p>operator new/delete 必须放在 <code>extern &quot;C&quot;</code> 块的外面（需要 C++ name mangling），实现为死循环而非返回 nullptr——如果有人不小心用了 new，CPU 会立刻卡住，在 GDB 里很容易定位。</p><p><strong>踩坑预警</strong>：<code>__init_array_start</code> 和 <code>__init_array_end</code> 在 C++ 里的声明方式是 <code>extern void (*__init_array_start[])()</code>——把它们声明为函数指针数组。链接器分配给这两个符号的&quot;值&quot;就是它们的地址本身，声明成数组后 start 和 end 就分别指向数组首元素和尾后元素。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 构建 + 链接，确认没有 undefined reference 报错</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：链接成功，没有 __cxa_pure_virtual、__stack_chk_fail 等符号的未定义错误</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="step-4-编写-kernel-main-入口" tabindex="-1">Step 4: 编写 kernel_main 入口 <a class="header-anchor" href="#step-4-编写-kernel-main-入口" aria-label="Permalink to &quot;Step 4: 编写 kernel_main 入口&quot;">​</a></h3><p><strong>目标</strong>：实现 <code>kernel_main()</code> 函数——大内核的 C++ 入口点，初始化串口并打印里程碑消息。</p><p><strong>设计思路</strong>：</p><p>这个函数做的事情很直白：先调用 <code>kprintf_init()</code> 初始化串口（COM1，115200 8N1），然后打印一行里程碑消息 <code>[BIG] Big kernel running @ 0x1000000</code>，最后进入死循环。大内核使用的是 <code>kernel/lib/kprintf.hpp</code>（命名空间 <code>cinux::lib</code>），和 mini kernel 的 kprintf 是两套独立的实现。之所以要重新初始化串口，是因为不能假设 mini kernel 留下的硬件状态。</p><p>函数签名是 <code>extern &quot;C&quot; void kernel_main()</code>，不返回。boot.S 的 <code>.halt</code> 标签也有死循环兜底。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 完整构建并运行（需要所有文件就绪）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期 QEMU 串口输出：</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [MINI] ... (mini kernel 的加载日志)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [BIG] Big kernel running @ 0x1000000</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><hr><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>完整的构建需要配合 CMakeLists.txt 中定义的 <code>big_kernel</code> target。构建系统会编译大内核的所有源文件（boot.S、crt_stub.cpp、serial.cpp、kprintf.cpp、main.cpp），用 <code>kernel/linker.ld</code> 链接，生成 ELF 文件。<code>build_image.sh</code> 脚本会把 MBR + Stage2 + mini kernel + big kernel ELF 写入磁盘镜像。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 完整构建</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：QEMU 启动，串口输出包含 [BIG] Big kernel running @ 0x1000000</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><hr><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><h3 id="常见-bug-1-大内核不输出任何信息" tabindex="-1">常见 Bug 1: 大内核不输出任何信息 <a class="header-anchor" href="#常见-bug-1-大内核不输出任何信息" aria-label="Permalink to &quot;常见 Bug 1: 大内核不输出任何信息&quot;">​</a></h3><p>如果 QEMU 串口没有任何 <code>[BIG]</code> 输出，说明大内核的 <code>_start</code> 可能没有被执行到。排查步骤：先用 GDB 在 <code>_start</code> 设断点，看 mini kernel 是否成功跳转过来。如果断点没命中，检查 mini kernel 的 ELF 加载器是否正确解析了入口点地址（<code>e_entry - KERNEL_VMA</code>）。</p><h3 id="常见-bug-2-串口输出乱码" tabindex="-1">常见 Bug 2: 串口输出乱码 <a class="header-anchor" href="#常见-bug-2-串口输出乱码" aria-label="Permalink to &quot;常见 Bug 2: 串口输出乱码&quot;">​</a></h3><p>通常是串口初始化问题。确认 <code>kprintf_init()</code> 被调用了，确认 QEMU 启动参数带了 <code>-serial stdio</code>。如果串口配置不正确（比如 LCR 设错了），输出的波特率/数据位不匹配会导致乱码。</p><h3 id="常见-bug-3-kernel-main-之后-triple-fault" tabindex="-1">常见 Bug 3: kernel_main 之后 triple fault <a class="header-anchor" href="#常见-bug-3-kernel-main-之后-triple-fault" aria-label="Permalink to &quot;常见 Bug 3: kernel_main 之后 triple fault&quot;">​</a></h3><p>检查 BSS 清零是否越界（<code>__bss_start</code> 和 <code>__bss_end</code> 的值是否合理），检查栈指针 <code>__kernel_stack_top</code> 是否指向有效内存。用 GDB 的 <code>info registers</code> 查看 RSP 的值是否在合理范围内。</p><h3 id="gdb-调试命令" tabindex="-1">GDB 调试命令 <a class="header-anchor" href="#gdb-调试命令" aria-label="Permalink to &quot;GDB 调试命令&quot;">​</a></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 在另一个终端启动 GDB</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/big/big_kernel</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remote</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> :1234</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">break</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> _start</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">break</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kernel_main</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">continue</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><hr><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>KERNEL_VMA</td><td><code>0xFFFFFFFF80000000</code>，higher-half 虚拟基地址</td></tr><tr><td>KERNEL_LMA</td><td><code>0x1000000</code>（16MB），物理加载地址</td></tr><tr><td>VMA/LMA 分离</td><td>链接脚本用 <code>AT(ADDR(.section) - KERNEL_VMA)</code></td></tr><tr><td>boot.S 流程</td><td>cli → 设栈 → 清 BSS → 全局构造器 → kernel_main → halt</td></tr><tr><td>rep stosb</td><td>清零 BSS，破坏 %rdi/%rcx/%rax</td></tr><tr><td>.init_array</td><td>全局构造器指针数组，KEEP() 防止被删</td></tr><tr><td>crt_stub</td><td>__cxa_pure_virtual、__stack_chk_fail、__cxa_atexit、new/delete</td></tr><tr><td>operator new/delete</td><td>死循环（无堆分配器），放在 extern &quot;C&quot; 外</td></tr></tbody></table>`,81)])])}const b=a(t,[["render",p]]);export{k as __pageData,b as default};
