import{c as o,Q as d,j as c,m as t}from"./chunks/framework.iD28w_5a.js";const f=JSON.parse('{"title":"005-mini-kernel-entry-2 · 内核入口","description":"","frontmatter":{"title":"005-mini-kernel-entry-2 · 内核入口"},"headers":[],"relativePath":"hands-on/005-mini-kernel-entry-2.md","filePath":"hands-on/005-mini-kernel-entry-2.md","lastUpdated":1779686691000}'),a={name:"hands-on/005-mini-kernel-entry-2.md"};function n(r,e,i,p,l,s){return d(),c("div",null,[...e[0]||(e[0]=[t(`<h1 id="_005-2-动手篇-kprintf-格式化输出库" tabindex="-1">005-2 动手篇：kprintf 格式化输出库 <a class="header-anchor" href="#_005-2-动手篇-kprintf-格式化输出库" aria-label="Permalink to &quot;005-2 动手篇：kprintf 格式化输出库&quot;">​</a></h1><blockquote><p>本章完成后的可见效果：内核通过串口输出包含数值的格式化字符串，如 <code>[MINI] Bootstrap kernel running @ 0x20000</code>，其中地址和数值通过 %p、%d 等格式符动态生成</p><p>前置要求：已完成本系列上一篇（串口驱动），Serial 类的 <code>putc</code>/<code>puts</code> 方法可用</p></blockquote><hr><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一篇我们给内核装上了串口驱动，现在内核能&quot;说话&quot;了——但只能说固定的字符串。如果你调用 <code>puts(&quot;Hello&quot;)</code>，终端就老老实实显示 Hello，一个字都改不了。这对于调试来说远远不够：你想知道某个变量的值是多少，想看看地址 0x7000 处的 BootInfo 指针是否正确，想输出一个十进制整数或者十六进制地址——这些都需要格式化输出，也就是内核版的 <code>printf</code>。</p><p>这一章我们要实现一个叫 <code>kprintf</code> 的格式化输出函数，它支持 <code>%d</code>（有符号十进制）、<code>%u</code>（无符号十进制）、<code>%x</code>/<code>%X</code>（十六进制）、<code>%s</code>（字符串）、<code>%c</code>（字符）、<code>%p</code>（指针地址）、<code>%b</code>（二进制）和 <code>%%</code>（百分号本身）这些格式符。同时还要支持宽度修饰符（比如 <code>%04d</code> 表示四位零填充），这对输出对齐的调试日志很有用。</p><p>这个 <code>kprintf</code> 的设计有一个比较精巧的地方：格式化逻辑和输出设备是解耦的。格式化函数本身不知道字符最终去了串口还是 debugcon——它只管把格式化结果送到一个&quot;字符输出函数&quot;里。<code>kprintf</code> 用串口的 <code>putc</code> 作为输出函数，<code>kdebugf</code> 用 debugcon 的 <code>putc</code> 作为输出函数，同一个格式化引擎服务两个输出通道。这种设计在 C++ 里用模板函数来实现，既避免了虚函数的开销，又保持了代码的复用性。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="可变参数机制-va-list-的黑魔法" tabindex="-1">可变参数机制：va_list 的黑魔法 <a class="header-anchor" href="#可变参数机制-va-list-的黑魔法" aria-label="Permalink to &quot;可变参数机制：va_list 的黑魔法&quot;">​</a></h3><p>C 语言的 <code>printf</code> 能够接受任意数量、任意类型的参数，靠的是可变参数机制。这个机制由四个宏组成：<code>va_list</code>、<code>va_start</code>、<code>va_arg</code>、<code>va_end</code>。</p><p><code>va_list</code> 是一个类型，代表&quot;可变参数列表&quot;的抽象——在 x86-64 的 System V ABI 下，它的底层实现比较复杂（前几个参数通过寄存器传递，多余的通过栈传递），但使用时不需要关心这些细节。</p><p><code>va_start</code> 初始化一个 <code>va_list</code>，你需要告诉它最后一个固定参数的名字（在我们的场景下就是 <code>format</code> 字符串），它据此计算出可变参数在栈/寄存器中的起始位置。</p><p><code>va_arg</code> 每次从列表中取出一个参数。你需要指定参数的类型——<code>va_arg(args, int)</code> 取出一个 int，<code>va_arg(args, uint64_t)</code> 取出一个 64 位无符号整数。每次调用 <code>va_arg</code> 都会让列表的&quot;游标&quot;前进一步。</p><p><code>va_end</code> 做清理工作。</p><p>在内核环境下使用可变参数有一点需要注意：我们用 <code>-ffreestanding</code> 编译，没有标准库，所以 <code>va_list</code> 等定义不是来自 <code>&lt;stdarg.h&gt;</code>，而是来自编译器内置的 <code>__builtin_va_*</code> 系列。不过 GCC 会自动处理这个问题——<code>#include &lt;stdarg.h&gt;</code> 在 freestanding 模式下也能工作，它内部会重定向到编译器的内置实现。</p><p>有一个非常重要的陷阱：<code>va_arg(args, char)</code> 是<strong>未定义行为</strong>。C 标准规定，比 int 窄的整型参数在传递时会被提升为 int，所以 <code>va_arg(args, char)</code> 实际上读的是不对的。正确的写法是 <code>va_arg(args, int)</code> 然后转型为 char。同理，<code>float</code> 会被提升为 <code>double</code>。</p><h3 id="数字到字符串的转换算法" tabindex="-1">数字到字符串的转换算法 <a class="header-anchor" href="#数字到字符串的转换算法" aria-label="Permalink to &quot;数字到字符串的转换算法&quot;">​</a></h3><p>把一个整数变成人能读的字符串，说起来简单，但细节很值得琢磨。以十进制为例，最直觉的想法是&quot;除以 10 取余数&quot;——但这样得到的数字序列是反的：12345 除以 10 的余数序列是 5、4、3、2、1，你得把它翻转过来才是 &quot;12345&quot;。我们的实现思路是先把反向序列存到一个临时数组里，然后反向复制到输出缓冲区。</p><p>十六进制也是一样的套路——除以 16（或者说右移 4 位然后取低 4 位）得到每个&quot;数字&quot;，数字到字符的映射通过一个查找表完成（<code>&quot;0123456789abcdef&quot;</code> 或 <code>&quot;0123456789ABCDEF&quot;</code>）。</p><p>二进制格式化比较特殊。如果按除以 2 取余的方式做，64 位整数最多产生 64 个字符，临时数组和翻转操作都是浪费。我们的做法是先从高位开始扫描，找到最高的那个置 1 的 bit，然后从那个 bit 开始往低位输出——这样自然就压缩了前导零，0b0001010 输出为 &quot;1010&quot; 而不是 &quot;0000000000001010&quot;。</p><p>有一个非常关键的边界情况：<code>INT64_MIN</code>（-9223372036854775808）。这个值的绝对值超出了 int64_t 的表示范围（int64_t 最大正值是 9223372036854775807），所以你不能简单地 <code>value = -value</code>。我们的处理方式是对这个特殊值硬编码字符串——直接把 &quot;-9223372036854775808&quot; 这个字面量复制到输出缓冲区。虽然丑，但这是保证正确性的唯一方法。</p><h3 id="模板化设计-outputfn-策略模式" tabindex="-1">模板化设计：OutputFn 策略模式 <a class="header-anchor" href="#模板化设计-outputfn-策略模式" aria-label="Permalink to &quot;模板化设计：OutputFn 策略模式&quot;">​</a></h3><p><code>kprintf</code> 和 <code>kdebugf</code> 的格式化逻辑完全相同，唯一的区别是输出字符去了哪里——前者去串口，后者去 debugcon。如果写两份格式化代码，不但冗余而且以后改格式符的时候很容易改了一边忘了另一边。</p><p>C++ 的模板函数完美地解决了这个问题。我们写一个模板函数 <code>vkprintf_impl</code>，它接受一个&quot;输出函数&quot;作为模板参数。格式化引擎每产生一个字符，就调用这个输出函数来发送它。<code>kprintf</code> 调用 <code>vkprintf_impl</code> 时传入一个 lambda，lambda 内部调用 <code>Serial::putc()</code>；<code>kdebugf</code> 传入另一个 lambda，调用 <code>debugcon_putc()</code>。编译器在编译时就能把模板实例化为两份具体的代码，零运行时开销。</p><p>在实际代码中，<code>vkprintf_impl</code> 被放在独立的头文件 <code>private/vkprintf_impl.h</code> 中，这样 Host 端测试也可以 include 它来测试格式解析逻辑，而不需要链接串口驱动或 I/O 端口代码。</p><p>这种模式在 C++ 里有个名字叫&quot;策略模式&quot;（Strategy Pattern），用模板实现在编译期绑定比用虚函数在运行时绑定高效得多。Linux 内核的 <code>printf</code> 实现也用了类似的方式——函数指针作为输出策略，只是它用的是 C 的函数指针而不是 C++ 的模板。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-实现数字格式化函数" tabindex="-1">Step 1: 实现数字格式化函数 <a class="header-anchor" href="#step-1-实现数字格式化函数" aria-label="Permalink to &quot;Step 1: 实现数字格式化函数&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel/mini/lib/private/format.h</code> 和 <code>format.cpp</code> 中实现三个格式化函数——<code>format_decimal</code>、<code>format_hex</code>、<code>format_binary</code>，它们把整数转换成字符串存入缓冲区。</p><p><strong>设计思路</strong>：这三个函数的签名统一：接受一个值、一个输出缓冲区指针、一个缓冲区大小参数，返回写入的字符数（不含末尾的 null 终止符）。<code>format_hex</code> 多一个布尔参数控制大小写。</p><p><code>format_decimal</code> 处理有符号 int64_t。首先检查 INT64_MIN 特殊值——直接把硬编码的字面字符串复制到缓冲区返回。对于其他负数，取反后处理。取绝对值后进入 <code>do-while</code> 循环：每次 <code>% 10</code> 取最低位数字，<code>/ 10</code> 去掉最低位，直到商为 0。这个循环用 <code>do-while</code> 而不是 <code>while</code> 的原因是处理 value=0 的情况——<code>do-while</code> 至少执行一次，0 的余数是 0，写入 &#39;0&#39;，正确。如果用 <code>while</code> 循环，0 的情况直接跳过，缓冲区里什么都没写，输出为空。</p><p><code>format_hex</code> 处理无符号 uint64_t。循环体是 <code>value &amp; 0xF</code> 取最低 4 位，<code>value &gt;&gt;= 4</code> 右移去掉已处理的位。数字到字符的转换通过一个 16 字符的查找表完成。循环结束后反向复制临时数组。</p><p><code>format_binary</code> 的做法是从 bit 63 开始往下扫描，找到第一个为 1 的位（或者扫到 bit 0 发现全是零），然后从那个高位开始往低位逐位输出 &#39;0&#39; 或 &#39;1&#39;。</p><p>所有三个函数都要检查 <code>buffer_size</code>，防止缓冲区溢出——如果缓冲区太小就只写入能放下的部分，然后在末尾加 null 终止符。</p><p><strong>实现约束</strong>：这三个函数放在 <code>private/</code> 子目录下，因为它们是 <code>kprintf</code> 的内部实现细节，不应该被外部代码直接调用。头文件的 include guard 要用 <code>#pragma once</code>，保持项目风格一致。</p><p><strong>踩坑预警</strong>：<code>format_decimal</code> 的 <code>do-while</code> vs <code>while</code> 选择是一个经典的 bug 来源。如果你不小心用了 <code>while (abs_val &gt; 0)</code> 循环，传 0 进去就会输出空字符串。这个 bug 在 Host 端测试里很容易发现（测试用例直接检查 0 的输出），但在内核里如果你的 kprintf 输出了一个格式化为 %d 的 0 值却什么都不显示，你会以为内核挂了，实际上只是格式化函数没处理零值。</p><p><strong>验证</strong>：这是最适合在 Host 端做单元测试的模块。下一篇的测试框架会覆盖这些函数，但现在你可以手动验证：在一个临时测试文件里调用 <code>format_decimal(42, buffer, 64)</code> 然后检查 buffer 内容是否为 &quot;42&quot;；调用 <code>format_hex(0xDEADBEEF, buffer, 64, true)</code> 检查是否为 &quot;deadbeef&quot;；调用 <code>format_binary(0b101010, buffer, 64)</code> 检查是否为 &quot;101010&quot;。</p><h3 id="step-2-实现-vkprintf-impl-格式解析引擎" tabindex="-1">Step 2: 实现 vkprintf_impl 格式解析引擎 <a class="header-anchor" href="#step-2-实现-vkprintf-impl-格式解析引擎" aria-label="Permalink to &quot;Step 2: 实现 vkprintf_impl 格式解析引擎&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel/mini/lib/kprintf.cpp</code> 中实现核心的格式字符串解析函数，它是整个 kprintf 的引擎。</p><p><strong>设计思路</strong>：<code>vkprintf_impl</code> 是一个模板函数，模板参数是一个&quot;输出函数&quot;类型（<code>OutputFn</code>）。函数签名接受三样东西：输出函数（右值引用，完美转发）、格式字符串指针、<code>va_list</code> 参数列表。</p><p>函数的主循环遍历格式字符串的每个字符。如果当前字符不是 <code>%</code>，直接调用输出函数发送这个字符。如果遇到 <code>%</code>，就进入格式符解析流程：</p><p>第一步，解析宽度修饰符。先看 <code>%</code> 后面是不是 <code>0</code>——如果是，设置零填充标志，跳过这个 <code>0</code>。然后继续读连续的数字字符（<code>&#39;0&#39;</code> 到 <code>&#39;9&#39;</code>），把它们拼成一个十进制整数作为宽度值。比如 <code>%04d</code> 的解析结果是零填充标志=true，宽度=4。</p><p>第二步，读取格式类型字符（<code>d</code>、<code>u</code>、<code>x</code>、<code>X</code>、<code>s</code>、<code>c</code>、<code>p</code>、<code>b</code>、<code>%</code> 之一），根据类型进行分支处理。<code>%</code> 输出一个百分号字符；<code>%c</code> 通过 <code>va_arg(args, int)</code> 取一个 int 然后转成 char 输出；<code>%s</code> 取一个 <code>const char*</code> 指针，逐字符输出（如果指针是 NULL 则输出 &quot;(null)&quot;）；<code>%d</code> 和 <code>%u</code> 调用 <code>format_decimal</code>（有符号取 <code>int64_t</code>，无符号取 <code>uint64_t</code>）；<code>%x</code> 和 <code>%X</code> 调用 <code>format_hex</code>；<code>%b</code> 调用 <code>format_binary</code>；<code>%p</code> 输出 &quot;0x&quot; 前缀后调用 <code>format_hex</code> 输出 64 位地址值。</p><p>第三步，处理宽度填充。数值型格式符（<code>d</code>、<code>u</code>、<code>x</code>、<code>X</code>、<code>b</code>）产生的字符串长度如果小于指定的宽度，需要用空格或零在前面填充。多个 case 共用一个 <code>do_padding</code> 标签来完成这个后处理——格式化函数先把结果写入临时缓冲区并返回长度，然后统一跳到 <code>do_padding</code> 做填充和输出。</p><p><strong>实现约束</strong>：<code>do_padding</code> 用的是 <code>goto</code>。是的，在 C++ 代码里用 <code>goto</code> 看起来不太&quot;现代&quot;，但这种场景下它是最干净的解决方案——多个 switch case 需要共享同一段后处理代码，提取成函数的话要传递一堆参数，而 <code>goto</code> 直接跳过去，零开销。Linux 内核的 <code>printf</code> 实现也用了类似的方式。</p><p><strong>踩坑预警</strong>：宽度填充的实现里有一个容易搞错的地方——填充字符应该在格式化结果的前面，不是后面。也就是说 <code>%04d</code> 对应 7 应该输出 &quot;0007&quot;（前面补零），而不是 &quot;7000&quot;。这意味着填充循环在输出格式化字符串之前执行。另外，<code>%p</code> 格式符要记得先输出 &quot;0x&quot; 前缀——如果忘了，地址看起来就是一串裸的十六进制数字，很难辨认。</p><p><strong>验证</strong>：这个函数需要配合 Step 3 的 <code>kprintf</code>/<code>kdebugf</code> 包装函数一起验证。但你可以先在 Host 端测试——下一篇的测试框架会详细测试各种格式符和边界条件。</p><h3 id="step-3-实现-kprintf-和-kdebugf-的包装函数" tabindex="-1">Step 3: 实现 kprintf 和 kdebugf 的包装函数 <a class="header-anchor" href="#step-3-实现-kprintf-和-kdebugf-的包装函数" aria-label="Permalink to &quot;Step 3: 实现 kprintf 和 kdebugf 的包装函数&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel/mini/lib/kprintf.h</code> 和 <code>kprintf.cpp</code> 中实现两个公开接口——<code>kprintf</code>（走串口）和 <code>kdebugf</code>（走 debugcon）。</p><p><strong>设计思路</strong>：这两个函数是 <code>vkprintf_impl</code> 的简单包装。它们各自用 <code>va_start</code>/<code>va_end</code> 初始化和清理可变参数列表，然后用不同的输出函数调用 <code>vkprintf_impl</code>。</p><p><code>kprintf</code> 的输出函数是一个 lambda，它捕获 Serial 单例的引用，内部调用 <code>Serial::putc()</code>。<code>kdebugf</code> 的输出函数是另一个 lambda，内部调用 <code>debugcon_putc()</code>（单条 <code>outb</code> 到端口 0xE9）。</p><p>函数声明放在头文件里，使用 <code>extern &quot;C&quot;</code> 风格的声明（虽然函数本身不是 extern &quot;C&quot;，但需要确保 C++ name mangling 正常工作，因为它们会被内核的 C++ 代码调用）。</p><p><strong>实现约束</strong>：头文件需要声明 <code>kprintf</code> 和 <code>kdebugf</code>，但不能暴露 <code>vkprintf_impl</code> 这个模板函数的细节——它应该只在内部可见（实际代码中放在 <code>private/vkprintf_impl.h</code>，由 <code>kprintf.cpp</code> include）。<code>debugcon_putc</code> 可以声明为文件作用域的 static 函数或匿名命名空间里的函数，不需要在头文件里暴露。</p><p><strong>踩坑预警</strong>：<code>va_start</code> 和 <code>va_end</code> 必须成对出现。如果你在某个错误路径上 <code>va_start</code> 了但没有 <code>va_end</code> 就 return 了，在某些平台上会导致栈损坏。虽然 GCC 在大多数情况下不会因此出问题，但这是未定义行为，值得注意。另外，<code>va_start</code> 的第二个参数必须是函数签名中 <code>...</code> 前面的那个参数的名字——在我们的场景下就是 <code>format</code>。如果写错了参数名，<code>va_start</code> 会从错误的位置开始读参数，后续所有的 <code>va_arg</code> 取值都是错的。</p><p><strong>验证</strong>：现在可以写一个综合性的测试了。在 <code>main.cpp</code> 的 <code>mini_kernel_main()</code> 里，用 <code>kprintf</code> 输出一段包含各种格式符的字符串：</p><ul><li>纯字符串：<code>kprintf(&quot;Hello, Cinux!\\n&quot;)</code></li><li>十进制整数：<code>kprintf(&quot;Decimal: %d\\n&quot;, -12345)</code></li><li>十六进制：<code>kprintf(&quot;Hex: %x\\n&quot;, 0xDEADBEEF)</code></li><li>指针：<code>kprintf(&quot;Address: %p\\n&quot;, some_pointer)</code></li><li>宽度填充：<code>kprintf(&quot;[%04d] [%4d]\\n&quot;, 7, 7)</code></li></ul><p>构建并运行：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cmake --build build</span></span>
<span class="line"><span>cd build &amp;&amp; make run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>串口终端应该出现格式化后的输出：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Hello, Cinux!</span></span>
<span class="line"><span>Decimal: -12345</span></span>
<span class="line"><span>Hex: deadbeef</span></span>
<span class="line"><span>Address: 0x20000</span></span>
<span class="line"><span>[0007] [   7]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>同时检查 <code>debug.log</code> 里是否出现了通过 <code>kdebugf</code> 输出的内容。</p><p>如果 <code>kprintf</code> 输出了乱码但 <code>kdebugf</code> 输出正确，问题不在格式化函数而在串口驱动——因为两者使用同一套格式化引擎，只是输出后端不同。这就是模板化设计的调试优势：你可以通过对比两个通道的输出来快速缩小问题范围。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>本篇涉及的文件清单：</p><ul><li><code>kernel/mini/lib/private/format.h</code> — 格式化函数声明</li><li><code>kernel/mini/lib/private/format.cpp</code> — 格式化函数实现</li><li><code>kernel/mini/lib/kprintf.h</code> — kprintf/kdebugf 声明</li><li><code>kernel/mini/lib/kprintf.cpp</code> — 核心引擎和包装函数</li><li><code>kernel/mini/main.cpp</code> — 内核入口，调用 kprintf 输出</li></ul><p>确保 <code>kernel/mini/CMakeLists.txt</code> 里把 <code>driver/serial.cpp</code>、<code>lib/kprintf.cpp</code>、<code>lib/private/format.cpp</code> 加到了源文件列表中。构建并运行后，<code>main.cpp</code> 中的 kprintf 调用应该能在串口终端产生格式化输出。</p><p>如果编译时报 &quot;undefined reference to format_decimal&quot; 之类的链接错误，说明 <code>format.cpp</code> 没加到 CMakeLists.txt 的源文件列表里。如果编译通过但运行时输出乱码或格式不对，先检查格式字符串本身有没有写错（比如漏了 <code>\\n</code>），然后下一篇的 Host 端测试可以精确定位是哪个格式化函数出了问题。</p><p>另外值得注意的是，实际代码中的 <code>vkprintf_impl</code> 被放在了 <code>kernel/mini/lib/private/vkprintf_impl.h</code> 这个独立的头文件中，而不是直接嵌入 <code>kprintf.cpp</code>。这种拆分是有意的：<code>vkprintf_impl</code> 是纯模板代码（不依赖任何硬件），把它单独放在头文件中可以让 Host 端测试直接 include 它来测试格式解析逻辑，而不需要链接串口驱动。<code>kprintf.cpp</code> 通过 <code>#include &quot;private/vkprintf_impl.h&quot;</code> 引入它。</p><p>实际的格式化引擎还支持左对齐（<code>%-Ns</code> 格式符），通过在宽度数字前加 <code>-</code> 标志来启用。这个特性在后续的内核输出中会用到，比如让表格对齐。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><h3 id="利用双通道对比排查格式化-bug" tabindex="-1">利用双通道对比排查格式化 bug <a class="header-anchor" href="#利用双通道对比排查格式化-bug" aria-label="Permalink to &quot;利用双通道对比排查格式化 bug&quot;">​</a></h3><p><code>kprintf</code> 和 <code>kdebugf</code> 使用同一套格式化引擎，所以如果 <code>kprintf</code> 输出的十进制数不对，但 <code>kdebugf</code> 输出的同样数是对的，那问题不在格式化函数而在串口驱动（字符在传输过程中丢了或错了）。反过来，如果两个通道的输出都不对，问题就锁定在格式化函数本身。</p><h3 id="宽度填充的调试" tabindex="-1">宽度填充的调试 <a class="header-anchor" href="#宽度填充的调试" aria-label="Permalink to &quot;宽度填充的调试&quot;">​</a></h3><p>宽度填充是最容易出错的格式化特性。如果 <code>%04d</code> 对应 7 的输出是 &quot;7&quot; 而不是 &quot;0007&quot;，可能是宽度解析部分没有正确读取 <code>0</code> 标志；如果输出是 &quot;0007&quot; 但紧接着下一行对不齐，可能是填充字符选错了（应该填 &#39;0&#39; 但填了空格）。逐一测试 <code>%d</code>、<code>%04d</code>、<code>%4d</code>、<code>%x</code>、<code>%08x</code> 这几种组合能快速定位问题。</p><h3 id="在-host-端快速迭代" tabindex="-1">在 Host 端快速迭代 <a class="header-anchor" href="#在-host-端快速迭代" aria-label="Permalink to &quot;在 Host 端快速迭代&quot;">​</a></h3><p>下一篇会详细讲测试框架，但这里先提一下思路：<code>format.cpp</code> 是纯算法代码，不依赖任何硬件。你可以直接在 Linux 用户态写一个 <code>main()</code> 函数来测试它——用 <code>printf</code> 来对比输出结果。这样可以避免每次改动都要重新构建内核镜像、启动 QEMU 的漫长循环。</p><h3 id="关于-d-和-u-的参数类型差异" tabindex="-1">关于 %d 和 %u 的参数类型差异 <a class="header-anchor" href="#关于-d-和-u-的参数类型差异" aria-label="Permalink to &quot;关于 %d 和 %u 的参数类型差异&quot;">​</a></h3><p><code>%d</code> 通过 <code>va_arg(args, int)</code> 取参数，<code>%u</code> 通过 <code>va_arg(args, unsigned int)</code> 取参数。两者都先转型为 <code>int64_t</code> 再传给 <code>format_decimal</code>。这种做法看起来有点奇怪——为什么 <code>%u</code> 用 <code>unsigned int</code> 而不是直接用 <code>uint64_t</code>？原因是保持和标准 <code>printf</code> 的兼容性：<code>%u</code> 对应 <code>unsigned int</code>，<code>%d</code> 对应 <code>int</code>。如果你传了一个 <code>uint64_t</code> 给 <code>%u</code>，在 x86-64 上 <code>va_arg(args, unsigned int)</code> 只会读出低 32 位——这是你的调用约定错误，不是格式化函数的 bug。</p><h3 id="debugcon-putc-的实现位置" tabindex="-1">debugcon_putc 的实现位置 <a class="header-anchor" href="#debugcon-putc-的实现位置" aria-label="Permalink to &quot;debugcon_putc 的实现位置&quot;">​</a></h3><p><code>debugcon_putc</code> 函数在 <code>kprintf.cpp</code> 中定义，位于匿名命名空间里（文件作用域 static 的 C++ 写法）。这意味着它只在 <code>kprintf.cpp</code> 内部可见，其他文件无法直接调用它。这是有意为之的——外部代码应该通过 <code>kdebugf</code> 来使用 debugcon 输出，而不是绕过格式化引擎直接调用底层的字符输出函数。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>va_list 可变参数</td><td><code>va_start</code>/<code>va_arg</code>/<code>va_end</code> 三件套，<code>va_arg(args, char)</code> 是 UB 必须用 int</td></tr><tr><td>format_decimal</td><td>反向取余 + 翻转，<code>do-while</code> 处理零值，INT64_MIN 硬编码</td></tr><tr><td>format_hex</td><td>逐 nibble 提取 + 查找表映射，大小写由表决定</td></tr><tr><td>format_binary</td><td>从高位扫描找到最高 set bit，自然压缩前导零</td></tr><tr><td>模板化 vkprintf_impl</td><td>OutputFn 策略模式，编译期绑定零开销</td></tr><tr><td>格式字符串解析</td><td><code>%</code> 触发解析 -&gt; 宽度修饰符 -&gt; 类型字符 -&gt; 格式化 -&gt; 填充输出</td></tr><tr><td>kprintf vs kdebugf</td><td>前者走串口 Serial::putc，后者走 debugcon outb(0xE9)</td></tr><tr><td>do_padding goto</td><td>多 case 共享后处理代码，C 风格但实用</td></tr><tr><td>缓冲区保护</td><td>所有 format 函数检查 buffer_size，防溢出</td></tr></tbody></table><hr><p><strong>下一篇预告</strong>：格式化函数写好了，但还没有自动化测试来验证它们的正确性。另外，我们在开发过程中发现了两个隐蔽的 bug（PDPT 索引和链接脚本 LMA），下一篇我们搭一套测试框架，顺便把这两个坑的故事讲清楚。</p>`,86)])])}const b=o(a,[["render",n]]);export{f as __pageData,b as default};
