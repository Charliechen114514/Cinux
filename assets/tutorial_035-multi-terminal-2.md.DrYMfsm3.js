import{c as t,Q as a,j as r,m as l}from"./chunks/framework.D9IbVcwA.js";const d=JSON.parse('{"title":"035-multi-terminal-2 · 多终端","description":"","frontmatter":{"title":"035-multi-terminal-2 · 多终端"},"headers":[],"relativePath":"tutorial/035-multi-terminal-2.md","filePath":"tutorial/035-multi-terminal-2.md","lastUpdated":1779686691000}'),s={name:"tutorial/035-multi-terminal-2.md"};function i(o,e,_,p,c,n){return a(),r("div",null,[...e[0]||(e[0]=[l('<h1 id="从-ext2-执行-shell——execve-完整链路" tabindex="-1">从 ext2 执行 Shell——execve 完整链路 <a class="header-anchor" href="#从-ext2-执行-shell——execve-完整链路" aria-label="Permalink to &quot;从 ext2 执行 Shell——execve 完整链路&quot;">​</a></h1><blockquote><p>标签：execve, ELF, ext2, usermode, syscall, swapgs 前置：tag 035-1 COW 页错误处理</p></blockquote><h2 id="前言" tabindex="-1">前言 <a class="header-anchor" href="#前言" aria-label="Permalink to &quot;前言&quot;">​</a></h2><p>到这一篇为止，Shell 一直是嵌入内核二进制的 blob（通过 objcopy 的 _binary_shell_bin_start/end 符号引用）。说实话这套方案在真正的操作系统面前实在站不住脚——你不能指望把每个用户程序都编译进内核。这一篇我们要实现一个质的飞跃：Shell 变成从 ext2 文件系统独立加载的 ELF 可执行文件。</p><p>这涉及 usermode.cpp 的大幅重构（launch_first_user 被移除，usermode_init 简化为只配置 MSR 和 GS 页）、per-CPU 数据页的引入、以及 create_shell_terminal 中完整的 fork -&gt; execve -&gt; jump_to_usermode 链路。过程中还踩了一个差点让我放弃的页内偏移 bug，以及一个&quot;cli 后不要 sti&quot;的时序陷阱。</p><h2 id="环境说明" tabindex="-1">环境说明 <a class="header-anchor" href="#环境说明" aria-label="Permalink to &quot;环境说明&quot;">​</a></h2><p>ext2 文件系统挂载在 AHCI port 1。构建脚本 create_ext2_disk.sh 自动把 user/shell ELF 复制到 ext2 镜像的 /bin/sh。usermode_init 在内核启动时调用一次，分配 per-CPU GS 数据页。内核栈大小 16KB（STACK_PAGES=4），用户栈大小由 USER_STACK_PAGES 定义，位于 USER_STACK_TOP 下方。execve 的 ELF 加载器支持 PT_LOAD 段的逐页映射。</p><h2 id="usermode-init-简化——职责分离" tabindex="-1">usermode_init 简化——职责分离 <a class="header-anchor" href="#usermode-init-简化——职责分离" aria-label="Permalink to &quot;usermode_init 简化——职责分离&quot;">​</a></h2><p>之前的 launch_first_user() 做了太多事：分配用户地址空间、映射 shell 代码页、映射用户栈、激活地址空间、设置 GS base、跳转 Ring 3。这个函数承担了太多职责，违反了单一职责原则。一旦要支持多终端（每个终端需要不同的 shell 进程），全局性的 launch_first_user 就完全没法用了——你不能 fork 一次然后让多个终端共享同一个 launch_first_user 创建的地址空间。</p><p>重构后 usermode_init 只做两件事。第一，调用 usermode_init_asm() 配置 STAR 和 EFER MSR（SYSCALL/SYSRET 的硬件基础）。STAR MSR 的值设为 kernel CS = 0x08，这样 SYSRET 自动计算出 user CS = 0x1B 和 user SS = 0x23。第二，分配 per-CPU GS 数据页——一个 4KB 的物理页（通过 g_pmm.alloc_page()），gs:0 存储内核栈指针（调度器每次 context switch 时更新），gs:8 存储 user RSP scratch。这个数据页的物理地址加上 KERNEL_VMA 得到虚拟地址，保存到 g_per_cpu.gs_page_vaddr，同时写入 MSR_KERNEL_GS_BASE。</p><p>具体来说，usermode_init 的流程是：调用 usermode_init_asm() -&gt; 分配物理页 -&gt; 通过 phys + KERNEL_VMA 得到虚拟地址 -&gt; gs_virt[0] = 0（内核栈指针初始为 0，由调度器填充）-&gt; gs_virt[1] = 0 -&gt; write_msr(MSR_KERNEL_GS_BASE, gs_phys + KERNEL_VMA) -&gt; 设置 g_per_cpu.gs_page_vaddr。删除了 _binary_shell_bin_start/end 符号引用、AddressSpace 头文件、以及所有 shell 代码页映射逻辑。</p><p>shell 的加载和用户态跳转完全由调用者负责——GUI 模式下是 create_shell_terminal()，非 GUI 模式下是 kernel_init_thread()。这种职责分离使得同一个 execve + jump_to_usermode 逻辑可以在不同场景复用。launch_first_user() 的声明和实现都从 usermode.hpp 和 usermode.cpp 中移除了。</p><h2 id="per-cpu-数据页——syscall-的栈指针来源" tabindex="-1">per-CPU 数据页——syscall 的栈指针来源 <a class="header-anchor" href="#per-cpu-数据页——syscall-的栈指针来源" aria-label="Permalink to &quot;per-CPU 数据页——syscall 的栈指针来源&quot;">​</a></h2><p>syscall_entry 的入口汇编使用 <code>swapgs; movq %gs:0, %rsp</code> 来获取内核栈指针。gs:0 指向的就是 per-CPU 数据页的第一个 8 字节。这个值必须和当前任务的内核栈顶一致——否则 syscall 会用错误的栈，大概率崩溃。</p><p>g_per_cpu.update_syscall_stack(stack_top) 方法做两件事：更新 kernel_stack 字段，以及把 stack_top 写入 gs:0（通过 gs_page_vaddr 计算出的虚拟地址，使用 volatile uint64_t 指针写入）。调度器每次 context switch 后调用这个方法，确保 gs:0 始终指向当前任务的栈顶。这个调用被添加到 schedule()、exit_current()、run_first() 三个调度器函数中。</p><p>PerCPU 结构体本身很简单——一个 Task* current 指针、一个 uint64_t kernel_stack、一个 uint64_t gs_page_vaddr。update_syscall_stack 在写入 gs:0 之前检查 gs_page_vaddr != 0，防止在 GS 页尚未分配时就写入（比如启动早期还没调用 usermode_init 时）。</p><p>Linux 的 per-CPU 机制更复杂——它使用 DEFINE_PER_CPU 宏在编译时分配 per-CPU 变量，运行时通过 __per_cpu_offset 数组索引到对应 CPU 的副本。SMP 系统下每个 CPU 有自己的 GS base 值。但 Cinux 是单核的，只有一个 per-CPU 数据页，所以直接用全局变量 + GS 段前缀就够了。</p><h2 id="create-shell-terminal——从图标到进程" tabindex="-1">create_shell_terminal——从图标到进程 <a class="header-anchor" href="#create-shell-terminal——从图标到进程" aria-label="Permalink to &quot;create_shell_terminal——从图标到进程&quot;">​</a></h2><p>点击 Shell 图标后的完整链路：PIT tick 中断 -&gt; gui_tick_callback 检测到 IconAction::OpenShell -&gt; 设置 deferred action（g_pending_action.store(action, memory_order_release)）-&gt; gui_worker 线程 -&gt; gui_process_pending -&gt; g_pending_action.exchange(IconAction::None) -&gt; create_shell_terminal。</p><p>为什么不在 PIT tick 中直接 fork？因为 PIT tick 在 Interrupt gate 下运行（IF=0），而 fork 需要分配物理页和映射页表——这些操作可能需要锁（VMM 和 PMM 的自旋锁），IF=0 下拿锁会死锁。延迟工作队列（std::atomic + worker 线程）是一个经典的 ISR + deferred work 模式，Linux 也有类似的 tasklet / workqueue 机制。</p><p>create_shell_terminal 每次调用都创建独立的资源。首先是生成唯一标题（g_terminal_counter++, &quot;Shell #1&quot;, &quot;Shell #2&quot;...），然后 new Terminal，创建 stdin/stdout pipe 对。pipe 需要包装成 Inode 才能放进 FDTable——因为 FDTable 存的是 File 对象，File 引用的是 Inode。stdin pipe 的 read 端用 PipeReadOps 包装成 stdin_read_inode，stdout pipe 的 write 端用 PipeWriteOps 包装成 stdout_write_inode。两个 inode 的 type 都设为 InodeType::Regular。</p><p>fork 后子进程创建新的 AddressSpace（父进程 gui_worker 是内核线程没有地址空间）、新的 FDTable（fd 0 绑 stdin_read_inode，fd 1 绑 stdout_write_inode），然后 execve(&quot;/bin/sh&quot;)。execve 失败就 exit_current，成功后分配用户栈页、activate 地址空间、update_syscall_stack、jump_to_usermode。</p><p>fork 失败时要清理所有已分配的资源——delete pipe ops、inode、pipe 本身、Terminal 对象，然后 return。这是 RAII 之前的 C 风格资源管理，每个资源都要手动清理。</p><h2 id="cli-后不要-sti——一个时序陷阱" tabindex="-1">cli 后不要 sti——一个时序陷阱 <a class="header-anchor" href="#cli-后不要-sti——一个时序陷阱" aria-label="Permalink to &quot;cli 后不要 sti——一个时序陷阱&quot;">​</a></h2><p>子进程路径中有一行 <code>__asm__ volatile(&quot;cli&quot;)</code>，cli 后一直到 jump_to_usermode 之间不能 sti。原因很精妙：SYSRETQ 指令会从 R11 恢复 RFLAGS（包括 IF 位）。jump_to_usermode 在进入前把 R11 设为包含 IF=1 的值，所以 SYSRETQ 执行后中断自动开启——这是原子操作，没有窗口。</p><p>如果在 jump_to_usermode 前显式 sti，PIT tick 可能在子进程的 CR3 下触发。此时用户地址空间刚设置好但内核的恒等映射可能不完整（特别是 framebuffer 区域），gui_tick_callback 的 composite 操作可能把 demand-page 零页映射到 framebuffer MMIO 区域上，直接黑屏。这个 bug 非常难定位——因为黑屏时内核实际上还在跑，只是 framebuffer 内容被覆盖了。</p><p>cli 到 SYSRETQ 之间的窗口必须是中断关闭的。这不是&quot;可选优化&quot;——这是正确性的硬性要求。子进程从 cli 到 jump_to_usermode 的整个路径（创建 AddressSpace、设置 FDTable、execve、分配用户栈、activate、update_syscall_stack）都在 IF=0 下运行，任何一步都不能 sti。</p><h2 id="页内偏移-bug——shell-只回显不执行命令" tabindex="-1">页内偏移 bug——shell 只回显不执行命令 <a class="header-anchor" href="#页内偏移-bug——shell-只回显不执行命令" aria-label="Permalink to &quot;页内偏移 bug——shell 只回显不执行命令&quot;">​</a></h2><p>这个 bug 的表现特别诡异：shell 能实时回显按键（按 p 显示 p），但不显示欢迎信息和提示符。原因在于 execve 加载 ELF 时，页内偏移计算错误导致 .rodata 段全是零。</p><p>旧代码把 <code>page_base_offset = vaddr - p_vaddr</code> 直接当作页内偏移。第一页 offset=0 没问题，第二页 offset=0xDA0，第三页 offset=0x1DA0...写入 dst+0x1DA0 越界到相邻内存，dst 本身保持全零。.text 在第一页所以代码能执行，.rodata 在后续页所以字符串常量变成 \\x00。这就是为什么 shell 的代码逻辑（读键盘、写 buf）能工作，但字符串常量（&quot;cinux&gt; &quot; 提示符、&quot;Cinux shell - type &#39;help&#39; for commands&quot; 欢迎信息）全部丢失。</p><p>调试线索非常关键：栈变量（char c; write_buf(&amp;c, 1)）正常，.rodata 字符串（write_str(&quot;cinux&gt; &quot;)）丢失。这个差异直接指向了 ELF 加载器 bug——栈由内核显式映射并初始化，.rodata 由 execve 从 ELF 文件填充，加载器 bug 只影响后者。</p><p>修复方案是正确区分 in_page_off（数据在当前页内的偏移，永远 &lt; PAGE_SIZE）和 seg_offset（段内偏移，用于文件读取位置）。data_vaddr = max(vaddr, p_vaddr) 处理第一页非页对齐的情况。这样 in_page_off 永远 &lt; PAGE_SIZE，dst + in_page_off 不会越界；seg_offset 是段内偏移，用于计算文件读取位置——和页内写入位置完全解耦。copy_len 取 min(p_filesz - seg_offset, PAGE_SIZE - in_page_off)，确保既不超出文件大小也不超出页边界。</p><h2 id="收尾" tabindex="-1">收尾 <a class="header-anchor" href="#收尾" aria-label="Permalink to &quot;收尾&quot;">​</a></h2><p>到这里 Shell 从 ext2 独立加载的完整链路已经打通。usermode_init 只负责 MSR 和 GS 页配置，shell 加载由调用者驱动。下一篇我们把这个能力用到多终端桌面上——每次点击 Shell 图标创建一个独立的终端窗口，每个终端绑定自己的 shell 进程和 pipe 对。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>Intel SDM Vol.3A Section 5.8.7 &quot;Performing Fast Calls to System Procedures&quot; — SYSCALL/SYSRET</li><li>OSDev Wiki SYSCALL: <a href="https://wiki.osdev.org/SYSCALL" target="_blank" rel="noreferrer">https://wiki.osdev.org/SYSCALL</a></li><li>Linux execve (do_execve): <a href="https://github.com/torvalds/linux/blob/master/fs/exec.c" target="_blank" rel="noreferrer">https://github.com/torvalds/linux/blob/master/fs/exec.c</a></li><li>ELF-64 Object File Format: <a href="https://uclibc.org/docs/elf-64-gen.pdf" target="_blank" rel="noreferrer">https://uclibc.org/docs/elf-64-gen.pdf</a></li></ul>',36)])])}const h=t(s,[["render",i]]);export{d as __pageData,h as default};
