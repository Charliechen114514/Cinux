import{c as s,Q as t,j as a,m as i}from"./chunks/framework.D9IbVcwA.js";const c=JSON.parse('{"title":"027-fs-vfs-1 · VFS 虚拟文件系统","description":"","frontmatter":{"title":"027-fs-vfs-1 · VFS 虚拟文件系统"},"headers":[],"relativePath":"hands-on/027-fs-vfs-1.md","filePath":"hands-on/027-fs-vfs-1.md","lastUpdated":1779686691000}'),n={name:"hands-on/027-fs-vfs-1.md"};function l(o,e,d,p,r,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_027-1-hands-on-vfs-数据结构设计-—-inode、file-与-fdtable" tabindex="-1">027-1 Hands-on: VFS 数据结构设计 — Inode、File 与 FDTable <a class="header-anchor" href="#_027-1-hands-on-vfs-数据结构设计-—-inode、file-与-fdtable" aria-label="Permalink to &quot;027-1 Hands-on: VFS 数据结构设计 — Inode、File 与 FDTable&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一个 tag（026）我们实现了 Ramdisk，把 ustar 格式的 initrd 归档解析了出来，能打印出文件名和大小。但说实话，那时候的 Ramdisk 只是一个&quot;解析器&quot;——你没法 open 一个文件、没法 read 它的内容、更没法在 shell 里 cat 出来。整个内核里根本不存在&quot;文件描述符&quot;这个概念，sys_read 只认 fd=0（键盘），sys_write 只认 fd=1（串口）。</p><p>现在问题来了：如果我们以后要加 ext2 支持，难道要给 ext2 再写一套独立的 open/read/write？如果同时挂载了 Ramdisk 和 ext2 分区，sys_read 怎么知道该找谁读数据？答案是加一层抽象——Virtual File System，虚拟文件系统。VFS 的核心思想非常简单：定义一套通用的数据结构和接口，所有文件系统后端都实现这套接口，系统调用只跟 VFS 打交道，不关心底层到底是 Ramdisk 还是 ext2 还是网络文件系统。</p><p>这一章我们从 VFS 大厦的地基开始——Inode、InodeOps、FileSystem 抽象基类、File 结构体、FDTable。完成本章后，我们会拥有一套可以承载 open/read/write/close 的数据框架，虽然还没有挂载点表和系统调用集成，但接口和表结构已经就位。</p><p>知识前置：你需要先完成 026_fs_ramdisk，理解 ustar 归档的解析流程以及 Ramdisk 类的基本结构。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="inode-—-文件系统对象的统一表示" tabindex="-1">Inode — 文件系统对象的统一表示 <a class="header-anchor" href="#inode-—-文件系统对象的统一表示" aria-label="Permalink to &quot;Inode — 文件系统对象的统一表示&quot;">​</a></h3><p>在 Unix 世界里，inode 是一切文件系统操作的起点。不管你底层是 ext4、FAT32 还是内存盘，上层看到的都是一个 inode——它代表一个文件或目录，包含编号、大小、类型等元数据。你可以把它理解为一个&quot;文件的身份信息卡&quot;：里面不存文件名（文件名归目录管），但存了文件的物理位置和属性。Linux 内核里的 inode 结构体有几十个字段（权限、时间戳、所属用户等等），我们不需要那么复杂——在教学 OS 里，编号、大小、类型就够用了。</p><p>Cinux 的 Inode 有一个特别的字段叫 fs_private——这是一个 void 指针，留给后端文件系统挂自己的私有数据。比如 Ramdisk 可以用它指向内部的 RamdiskEntry（包含文件名和数据指针），而 ext2 可以用它指向磁盘上的块地址列表。这种&quot;公共部分 + 私有指针&quot;的模式在 Linux 里也广泛使用，Linux 的 inode 有一个 i_private 字段做完全一样的事情。</p><h3 id="inodeops-—-函数指针表实现多态操作" tabindex="-1">InodeOps — 函数指针表实现多态操作 <a class="header-anchor" href="#inodeops-—-函数指针表实现多态操作" aria-label="Permalink to &quot;InodeOps — 函数指针表实现多态操作&quot;">​</a></h3><p>InodeOps 在 Cinux 里是一个纯 C 风格的函数指针结构体（struct），不是虚基类。每个字段都是一个函数指针，指向具体的实现函数。不支持的操作就把对应指针设为 nullptr。这种方式在 Linux 内核中也广泛使用——file_operations、inode_operations 都是函数指针表。</p><p>InodeOps 函数指针表里定义了多个操作：read（从文件读取数据）、write（向文件写入数据）、readdir（读取目录项）、create（创建文件）、mkdir（创建目录）、unlink（删除文件）、stat（获取文件信息）。每个函数指针都可以为 nullptr——比如普通文件的 ops 不需要设置 readdir（保持 nullptr 即可），而目录的 ops 不需要设置 read（保持 nullptr）。调用方在调用前需要检查函数指针是否为 nullptr。</p><h3 id="file-与-fdtable-—-进程视角的打开文件" tabindex="-1">File 与 FDTable — 进程视角的打开文件 <a class="header-anchor" href="#file-与-fdtable-—-进程视角的打开文件" aria-label="Permalink to &quot;File 与 FDTable — 进程视角的打开文件&quot;">​</a></h3><p>Inode 是文件系统视角的对象，而 File 是进程视角的对象。为什么要分两层？因为同一个文件可以被同一个进程打开多次——每次 open 都应该有独立的读写位置。如果直接在 Inode 上存 offset，两个 open 同一个文件的 fd 就会互相干扰。Linux 的做法是把这两个概念分开：inode 是全局共享的，file 结构体是每次 open 独立创建的，file 里面存 offset 和指向 inode 的指针。</p><p>FDTable 就是进程的文件描述符表，本质上是一个固定大小的指针数组。按照 Unix 传统，fd 0/1/2 分别是 stdin/stdout/stderr——在我们的内核里，fd=0 目前绑定键盘输入，fd=1 绑定 kprintf 输出，fd=2 暂时未用。所以新分配的 fd 从 3 开始。FDTable 的三个核心操作非常直观：alloc 找空位分配、close 释放置空、get 按 fd 编号查找。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-定义-inodetype-枚举和-inodeops-函数指针结构体" tabindex="-1">Step 1: 定义 InodeType 枚举和 InodeOps 函数指针结构体 <a class="header-anchor" href="#step-1-定义-inodetype-枚举和-inodeops-函数指针结构体" aria-label="Permalink to &quot;Step 1: 定义 InodeType 枚举和 InodeOps 函数指针结构体&quot;">​</a></h3><p><strong>目标</strong>: 创建头文件 <code>kernel/fs/inode.hpp</code>，定义 Inode 相关的所有类型。</p><p><strong>设计思路</strong>: InodeType 枚举只需要三个值——Unknown(0)、Regular(1) 和 Directory(2)。InodeOps 是一个函数指针结构体（struct），每个字段都是函数指针，如 <code>int64_t (*read)(const Inode*, uint64_t, void*, uint64_t)</code>。不支持的操作设为 nullptr。注意 read 的第一个参数是 const Inode*（读不修改 inode），而 write 的第一个参数是 Inode*（写可能修改 size 等元数据）。readdir 的语义比较特殊：返回 1 表示读到一项、0 表示目录已读完、-1 表示错误。</p><p><strong>实现约束</strong>: 在定义 InodeOps 之前，需要先做前置声明 <code>struct Inode;</code>，因为函数指针的参数里用到了 Inode*。InodeOps 的函数指针可以为 nullptr，调用方需要在调用前检查对应指针是否为空。</p><p><strong>踩坑预警</strong>: readdir 的 index 参数是 0-based 的索引，不是字节偏移量。调用方（通常是 sys_getdents）需要自己维护这个索引——我们的做法是复用 File 结构体的 offset 字段当索引用，每次 readdir 成功后 offset++。</p><p><strong>验证</strong>: 此步完成后，<code>kernel/fs/inode.hpp</code> 应该能独立编译通过（只依赖 stdint.h 和 stddef.h）。检查 sizeof(InodeType) == 1（它是 uint8_t 枚举）。</p><h3 id="step-2-定义-inode-结构体" tabindex="-1">Step 2: 定义 Inode 结构体 <a class="header-anchor" href="#step-2-定义-inode-结构体" aria-label="Permalink to &quot;Step 2: 定义 Inode 结构体&quot;">​</a></h3><p><strong>目标</strong>: 在同一个 <code>kernel/fs/inode.hpp</code> 中，紧接 InodeOps 之后定义 Inode 结构体。</p><p><strong>设计思路</strong>: Inode 有五个核心字段：ino（uint64_t，文件系统特定的编号）、size（uint64_t，文件字节大小）、type（InodeType 枚举）、ops（InodeOps 指针，可为 nullptr）、fs_private（void 指针，后端私有数据）。此外还有 mode/uid/gid/nlink/atime/ctime/mtime/blocks 等为 stat 系统调用预留的字段。所有字段都有默认初始化值。</p><p><strong>实现约束</strong>: Inode 的生命周期由后端文件系统管理——Ramdisk 的 Inode 是嵌入在 RamdiskEntry 里的预分配对象，不需要动态分配。调用方不应该 free/delete 一个 Inode，它归后端所有。</p><p><strong>验证</strong>: <code>static_assert(sizeof(Inode) &gt; 0)</code> 应该通过。Inode 的大小约为 96 字节左右（ino 8 + size 8 + type 1 + padding 7 + ops 8 + fs_private 8 + mode 4 + uid 4 + gid 4 + nlink 4 + atime 8 + ctime 8 + mtime 8 + blocks 8）。</p><h3 id="step-3-定义-filesystem-抽象基类" tabindex="-1">Step 3: 定义 FileSystem 抽象基类 <a class="header-anchor" href="#step-3-定义-filesystem-抽象基类" aria-label="Permalink to &quot;Step 3: 定义 FileSystem 抽象基类&quot;">​</a></h3><p><strong>目标</strong>: 创建头文件 <code>kernel/fs/vfs_filesystem.hpp</code>，定义所有文件系统后端的统一接口。</p><p><strong>设计思路</strong>: 这个基类有两个纯虚函数——mount() 负责初始化后端（解析磁盘数据结构、建立索引等），lookup(path) 负责根据相对路径找到对应的 Inode。加上一个虚析构函数 <code>virtual ~FileSystem() = default</code> 确保通过基类指针释放时能正确析构。</p><p><strong>实现约束</strong>: mount() 返回 bool（true 成功，false 失败），lookup() 返回 Inode*（找到返回指针，没找到返回 nullptr）。注意 lookup 的参数是相对路径——挂载点前缀由 VFS 上层剥离后传入，所以 Ramdisk 挂在 &quot;/&quot; 下时，lookup 收到的可能是 &quot;hello.txt&quot; 而不是 &quot;/hello.txt&quot;。</p><p><strong>验证</strong>: 编译通过。这个头文件只依赖 inode.hpp 和 stdint.h。</p><h3 id="step-4-定义-openflags、file-和-fdtable" tabindex="-1">Step 4: 定义 OpenFlags、File 和 FDTable <a class="header-anchor" href="#step-4-定义-openflags、file-和-fdtable" aria-label="Permalink to &quot;Step 4: 定义 OpenFlags、File 和 FDTable&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>kernel/fs/file.hpp</code>（声明）和 <code>kernel/fs/file.cpp</code>（实现）。</p><p><strong>设计思路</strong>: OpenFlags 枚举值——RDONLY=0, WRONLY=1, RDWR=2——和 Linux 的 O_RDONLY/O_WRONLY/O_RDWR 完全一致，方便用户态直接传值。File 结构体有显式构造函数和三个核心字段：inode 指针、offset（uint64_t，读写偏移量，初始化为 0）、flags（OpenFlags），以及一个 offset_lock_ 自旋锁保护并发访问。FDTable 类有构造函数（初始化所有槽位为 nullptr）、alloc（从 fd=3 开始线性扫描，受 lock_ 保护）、close（边界检查 + delete + 置空，受 lock_ 保护）、get（边界检查 + 返回指针，受 lock_ 保护）、set（强制设置指定 fd 的 File 指针，用于 pipe 和 dup2）。</p><p><strong>实现约束</strong>: FD_TABLE_SIZE 设为 256（与 Linux 默认的 RLIMIT_NOFILE 一致），FD_NONE = -1 作为失败返回值。alloc 的第一个可分配 fd 是 3（跳过 0/1/2）。close 必须做边界检查（fd &lt; 0 或 fd &gt;= 256 都返回 -1）和双重关闭检查（slot 为 nullptr 时返回 -1）。所有公共操作都通过 lock_ 自旋锁保护。</p><p><strong>踩坑预警</strong>: alloc 里用 <code>new File{...}</code> 分配对象——这需要内核堆已经初始化。在我们的启动顺序里没问题，但如果你写独立的 host 测试，确保链接了正确的内存分配器。另外，FDTable 目前没有加锁——在单核内核里没问题，但以后多进程/多线程时需要在 alloc 和 close 里加自旋锁保护。</p><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_common</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -5</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>编译应该无错误。如果写了 host 测试：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_fd_table</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./test/unit/test_fd_table</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="step-5-更新-cmakelists-txt" tabindex="-1">Step 5: 更新 CMakeLists.txt <a class="header-anchor" href="#step-5-更新-cmakelists-txt" aria-label="Permalink to &quot;Step 5: 更新 CMakeLists.txt&quot;">​</a></h3><p><strong>目标</strong>: 把新增的源文件加入构建系统。</p><p><strong>设计思路</strong>: 在 <code>kernel/CMakeLists.txt</code> 的 <code>big_kernel_common</code> 源文件列表中，在 <code>fs/ramdisk.cpp</code> 后面加入 <code>fs/file.cpp</code> 和 <code>fs/vfs_mount.cpp</code>（下章实现，现在可以先加），在 <code>lib/kprintf.cpp</code> 后面加入 <code>lib/string.cpp</code>。</p><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_common</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -E</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;(error|warning)&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>应该没有 error。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>此阶段只涉及数据结构定义和 FDTable 实现，还没有系统调用集成。验证方式是编译通过加上 FDTable 的 host 测试。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_fd_table</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -R</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fd_table</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>预期输出：24 个测试全部通过，0 failures。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>编译报 &quot;undefined reference to operator new&quot;</strong>: 确认内核堆已经初始化。FDTable::alloc 使用 new 分配 File，需要堆就绪。在 host 测试环境下，标准库的 new 是可用的，不会有这个问题。</p><p><strong>InodeOps 函数指针调用崩溃</strong>: 确认 ops 指针和具体函数指针都不为 nullptr 再调用。直接调用空指针会导致页错误或跳转到地址 0。在 syscall handler 里要做三层检查：file != nullptr、file-&gt;inode != nullptr、file-&gt;inode-&gt;ops != nullptr、file-&gt;inode-&gt;ops-&gt;read != nullptr。</p><p><strong>FDTable::alloc 返回 -1</strong>: 检查表是否确实满了——fd 3 到 255 都被分配了？如果刚初始化就返回 -1，说明构造函数可能没正确清空所有槽位。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>Inode</td><td>文件系统对象的统一表示，ino/size/type/ops/fs_private 五个核心字段 + stat 预留字段</td></tr><tr><td>InodeOps</td><td>函数指针结构体 (read/write/readdir/create/mkdir/unlink/stat)，不支持的操作设为 nullptr</td></tr><tr><td>FileSystem</td><td>抽象基类，mount() 返回 bool + lookup() 返回 Inode*</td></tr><tr><td>OpenFlags</td><td>RDONLY(0)/WRONLY(1)/RDWR(2)，与 Linux O_* 值对应</td></tr><tr><td>File</td><td>打开文件描述：绑定 Inode + offset + flags + offset_lock_</td></tr><tr><td>FDTable</td><td>256 槽位的文件描述符表，fd 0-2 保留，alloc 从 3 开始，受自旋锁保护</td></tr></tbody></table>`,58)])])}const u=s(n,[["render",l]]);export{c as __pageData,u as default};
