import{c as t,Q as a,j as s,m as o}from"./chunks/framework.D9IbVcwA.js";const u=JSON.parse('{"title":"017-mm-heap-4 · 堆管理","description":"","frontmatter":{"title":"017-mm-heap-4 · 堆管理"},"headers":[],"relativePath":"hands-on/017-mm-heap-4.md","filePath":"hands-on/017-mm-heap-4.md","lastUpdated":1779686691000}'),d={name:"hands-on/017-mm-heap-4.md"};function i(l,e,p,n,c,r){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="_017-4-c-new-delete-接管与测试策略" tabindex="-1">017-4 C++ new/delete 接管与测试策略 <a class="header-anchor" href="#_017-4-c-new-delete-接管与测试策略" aria-label="Permalink to &quot;017-4 C++ new/delete 接管与测试策略&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>前三章我们实现了堆分配器的核心——init、alloc、free、coalesce、expand。它已经是一个功能完整的 kmalloc/kfree 了。但在 C++ 内核中，我们不会直接调用 <code>g_heap.alloc()</code> 和 <code>g_heap.free()</code>——C++ 有自己的动态内存语法：<code>new</code> 和 <code>delete</code>。到 tag 016 为止，Cinux 的 <code>operator new</code> 和 <code>operator delete</code> 都是&quot;halt on use&quot;的空壳——调用它们内核直接死锁。因为那时候还没有堆分配器。</p><p>本章要做两件事。第一，把 <code>operator new</code>/<code>delete</code> 的所有重载版本全部重定向到 <code>Heap::alloc</code>/<code>free</code>，这样内核中所有的 C++ 动态内存操作（包括全局对象的构造、容器的内存分配等）都会走我们的堆分配器。第二，搭建一套双层测试体系——host 端单元测试（用 mock 的 PMM/VMM 在 Linux 用户空间运行）和 QEMU 端集成测试（在真实内核环境中运行）——来验证堆分配器的正确性和鲁棒性。</p><p>知识前置：你需要了解 C++ 的 operator new/delete 重载机制（全局重载 vs 类重载、sized delete、aligned new），以及 tag 016 的 VMM 测试策略（host 端 mock + QEMU 端真实环境）。</p><p>本章涉及的文件比较分散：<code>kernel/arch/x86_64/crt_stub.cpp</code>（new/delete 重定向）、<code>kernel/main.cpp</code>（启动序列集成）、<code>test/unit/test_heap.cpp</code>（host 端测试，866 行）和 <code>kernel/test/test_heap.cpp</code>（QEMU 端测试，301 行）。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="c-的-operator-new-delete-家族" tabindex="-1">C++ 的 operator new/delete 家族 <a class="header-anchor" href="#c-的-operator-new-delete-家族" aria-label="Permalink to &quot;C++ 的 operator new/delete 家族&quot;">​</a></h3><p>C++17 标准定义了一整套 operator new 和 operator delete 的重载版本。最基本的四个是：<code>operator new(size_t)</code> 分配单个对象、<code>operator new[](size_t)</code> 分配数组、<code>operator delete(void*)</code> 释放单个对象、<code>operator delete[](void*)</code> 释放数组。除此之外还有 sized delete 版本（<code>operator delete(void*, size_t)</code>），编译器在编译期已知对象大小时会调用这个版本，理论上可以用于优化释放操作——但在我们的实现中 size 参数被忽略，因为 BlockHeader 已经记录了大小。</p><p>C++17 还引入了 aligned new：<code>operator new(size_t, std::align_val_t)</code> 和对应的 delete 版本。<code>std::align_val_t</code> 是一个枚举类型的包装，用于传递对齐要求。比如 <code>new(std::align_val_t{64}) MyStruct</code> 会调用 <code>operator new(sizeof(MyStruct), std::align_val_t{64})</code>，请求 64 字节对齐的内存。我们的 <code>Heap::alloc</code> 天然支持对齐参数，所以实现 aligned new 只需要把 <code>std::align_val_t</code> 转换为 <code>size_t</code> 然后传递给 alloc。</p><p>所有这些重载版本都必须定义在全局命名空间中（不能在任何命名空间内），而且必须使用 C++ 的名称修饰（name mangling）——也就是说它们不能放在 <code>extern &quot;C&quot;</code> 块里。之前的空壳版本就是定义在 <code>extern &quot;C&quot;</code> 块外面的，新的重定向版本也是。</p><h3 id="为什么要接管全局-new-delete" tabindex="-1">为什么要接管全局 new/delete？ <a class="header-anchor" href="#为什么要接管全局-new-delete" aria-label="Permalink to &quot;为什么要接管全局 new/delete？&quot;">​</a></h3><p>你可能会问：为什么不直接用 <code>g_heap.alloc()</code> 和 <code>g_heap.free()</code>？有几个重要原因。第一，C++ 的 RAII 模式依赖 new/delete——当你写 <code>auto p = new MyClass()</code> 时，编译器自动调用 operator new 分配内存然后调用构造函数；写 <code>delete p</code> 时先调用析构函数再调用 operator delete。如果不接管，这些语言特性就无法使用。第二，标准库容器内部的内存分配都通过 operator new——接管它意味着所有容器自动使用我们的堆分配器。第三，全局对象的动态初始化也可能触发 operator new——不接管的话这些构造函数会触发 halt。</p><p>所有这些重载版本都必须定义在全局命名空间中（不能在任何命名空间内），而且必须使用 C++ 的名称修饰（name mangling）——也就是说它们不能放在 <code>extern &quot;C&quot;</code> 块里。之前的空壳版本就是定义在 <code>extern &quot;C&quot;</code> 块外面的，新的重定向版本也是。</p><h3 id="为什么要接管全局-new-delete-1" tabindex="-1">为什么要接管全局 new/delete？ <a class="header-anchor" href="#为什么要接管全局-new-delete-1" aria-label="Permalink to &quot;为什么要接管全局 new/delete？&quot;">​</a></h3><p>你可能会问：为什么不直接用 <code>g_heap.alloc()</code> 和 <code>g_heap.free()</code>？有几个重要原因。第一，C++ 的 RAII 模式依赖 new/delete——当你写 <code>auto p = new MyClass()</code> 时，编译器自动调用 operator new 分配内存然后调用构造函数；写 <code>delete p</code> 时先调用析构函数再调用 operator delete。如果我们不接管 operator new/delete，这些语言特性就无法使用。第二，标准库容器（如果将来引入）内部的内存分配都通过 operator new——接管它意味着所有容器自动使用我们的堆分配器。第三，全局对象的动态初始化（构造函数在 main 之前运行）也可能触发 operator new——不接管的话这些构造函数会触发 halt。</p><h3 id="双层测试策略" tabindex="-1">双层测试策略 <a class="header-anchor" href="#双层测试策略" aria-label="Permalink to &quot;双层测试策略&quot;">​</a></h3><p>Cinux 的堆分配器测试分为两层。第一层是 host 端单元测试（<code>test/unit/test_heap.cpp</code>），在 Linux 用户空间编译运行。由于堆分配器的算法本身不依赖硬件（它只操作链表和地址计算），我们可以在 host 端完全重新实现一遍算法逻辑，用 malloc 分配的缓冲区代替 VMM 映射的虚拟地址空间。这一层测试的优势是运行速度快（毫秒级）、可以用 GDB 调试、可以构造极端测试用例（比如 1000 次随机 alloc/free 循环）。缺点是测试的是&quot;重新实现的算法&quot;而不是&quot;真正的内核代码&quot;——如果重新实现时引入了和原始代码不同的 bug，测试结果就不可信了。</p><p>第二层是 QEMU 端集成测试（<code>kernel/test/test_heap.cpp</code>），在真实内核环境中运行。它使用真正的 <code>g_heap</code> 全局实例，背后是真实的 PMM 和 VMM。这一层测试的优势是验证端到端的正确性——从 PMM 分配物理页、到 VMM 映射虚拟地址、到 Heap 分配 payload、到数据写入和读回——整个链条都在真实环境中运行。缺点是运行在 QEMU 中，速度较慢，调试不如 host 端方便。</p><p>两层测试互补——host 端覆盖大量边界情况和压力测试（25+ 个用例），QEMU 端验证真实环境的集成正确性（10 个核心用例）。这种策略和 tag 015/016 中 PMM/VMM 的测试方式一脉相承。</p><p>两层测试互补——host 端覆盖大量边界情况和压力测试，QEMU 端验证真实环境的集成正确性。这种策略和 tag 015/016 中 PMM/VMM 的测试方式一脉相承。</p><h3 id="host-端-mock-策略" tabindex="-1">Host 端 mock 策略 <a class="header-anchor" href="#host-端-mock-策略" aria-label="Permalink to &quot;Host 端 mock 策略&quot;">​</a></h3><p>Host 端测试不链接任何内核代码，而是把 Heap 的算法完全重新实现在测试文件中。具体做法是：定义一个 TestHeap 类，它的 init 方法用 <code>calloc</code> 分配一块内存（代替 VMM 映射），alloc 和 free_mem 方法重新实现 first-fit、split、coalesce 的逻辑（和 heap.cpp 中的算法完全一致）。测试用例通过 TestHeap 的公开接口（alloc、free_mem、used、free_total、free_block_count 等）验证分配器的各种行为。</p><p>TestHeap 还提供了一些诊断方法，比如 <code>validate_free_list()</code> 检查链表中是否有环（通过遍历计数上限）、所有块是否标记为 free、所有 magic 是否正确；<code>has_valid_magic(ptr)</code> 和 <code>is_block_free(ptr)</code> 用于从外部检查某个 payload 指针对应的块状态。这些方法在内核版本的 Heap 中没有（为了减少开销），但在测试中非常有价值。</p><p><code>validate_free_list()</code> 的&quot;环检测&quot;机制值得一提——它记录遍历步数，超过 10000 就判定链表有环。这是一种简单但有效的策略：正常情况下空闲链表的块数不会超过几十个，如果遍历了 10000 步还没到链表末尾，几乎可以确定 next 指针形成了一个环。链表有环通常是由于 coalesce 中摘除节点时忘记清除 next 指针导致的。</p><p>本章涉及的文件分散在四个位置，但每个文件的修改量都不大。crt_stub.cpp 从&quot;halt on use&quot;变为&quot;delegate to g_heap&quot;是最大的逻辑变化，但每个重载函数体只有一行代码。main.cpp 只增加了 3 行（堆初始化调用）。测试文件虽然很长（866 行 + 301 行），但大部分是重复的测试用例模板，核心逻辑并不复杂。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-重定向-operator-new-delete-到-heap" tabindex="-1">Step 1: 重定向 operator new/delete 到 Heap <a class="header-anchor" href="#step-1-重定向-operator-new-delete-到-heap" aria-label="Permalink to &quot;Step 1: 重定向 operator new/delete 到 Heap&quot;">​</a></h3><p><strong>目标</strong>: 替换 crt_stub.cpp 中所有 operator new/delete 的 halt-on-use 空壳，改为调用 Heap::alloc 和 Heap::free。</p><p><strong>设计思路</strong>: 需要实现十个重载版本（4 个 new + 6 个 delete）。四个 new 版本：<code>operator new(size_t)</code> 调用 <code>g_heap.alloc(size)</code>；<code>operator new[](size_t)</code> 调用 <code>g_heap.alloc(size)</code>；<code>operator new(size_t, std::align_val_t)</code> 调用 <code>g_heap.alloc(size, static_cast&lt;size_t&gt;(align))</code>；<code>operator new[](size_t, std::align_val_t)</code> 调用 <code>g_heap.alloc(size, static_cast&lt;size_t&gt;(align))</code>。六个 delete 版本：<code>operator delete(void*)</code>、<code>operator delete(void*, size_t)</code>（sized）、<code>operator delete[](void*)</code>、<code>operator delete[](void*, size_t)</code>（sized array）、<code>operator delete(void*, std::align_val_t)</code>（aligned）、<code>operator delete(void*, size_t, std::align_val_t)</code>（sized aligned）——全部调用 <code>g_heap.free(ptr)</code>，size 和 alignment 参数被忽略。</p><p>需要新增 <code>#include &lt;new&gt;</code> 来获取 <code>std::align_val_t</code> 的定义，以及 <code>#include &lt;stddef.h&gt;</code> 来获取 <code>size_t</code>。</p><p><strong>实现约束</strong>: 所有函数定义在 <code>kernel/arch/x86_64/crt_stub.cpp</code> 中，位于 <code>extern &quot;C&quot;</code> 块之外（需要 C++ 名称修饰）。函数签名中的 <code>unsigned long</code> 对应 size_t（在 x86-64 上 size_t 就是 unsigned long）。使用 <code>static_cast&lt;size_t&gt;</code> 进行类型转换。noexcept 说明符不能少——operator delete 必须是 noexcept 的。</p><p><strong>踩坑预警</strong>: 如果你忘了加 <code>#include &lt;new&gt;</code>，<code>std::align_val_t</code> 会编译报错。如果你把 aligned 版本的函数放在了 <code>extern &quot;C&quot;</code> 块里，链接器会找不到它们（因为 C 链接不进行名称修饰，而编译器生成的新表达式使用 C++ 链接）。另一个坑是 <code>operator delete(void*, unsigned long, std::align_val_t)</code> 这个三参数版本——它的参数顺序是 (指针, 大小, 对齐)，不是 (指针, 对齐, 大小)。顺序写反了编译不报错（类型恰好兼容），但运行时 alignment 参数拿到了 size 的值，会导致 alloc 使用错误的对齐。</p><p>为什么要实现全部 6 个 delete？GCC 从某个版本开始会在 <code>-O2</code> 下生成 sized delete 的调用（如果存在对应的重载）。如果不提供 sized delete 的实现，链接器在启用 sized delete 时会报 <code>undefined reference</code>。aligned delete 同理——如果定义了 aligned new 但不定义 aligned delete，编译器在某些情况下会生成未定义的 delete 调用。六个重载全部实现保证了在任何编译选项下都不会出现链接错误。</p><p><strong>验证</strong>: 在内核中尝试用 <code>new</code> 分配一个对象，然后用 <code>delete</code> 释放。比如在某处写 <code>auto* p = new int(42); *p; delete p;</code>——如果不触发 halt，说明重定向成功。更正式的验证在后续的测试中进行。</p><p>你也可以用 <code>nm</code> 或 <code>objdump</code> 检查编译后的目标文件，确认 operator new 指向了调用 <code>g_heap.alloc</code> 的实现而不是死循环。命令示例：<code>x86_64-elf-nm build/kernel/big_kernel_common.a | grep &quot;operator new&quot;</code>。如果看到的地址指向一个 <code>cli; hlt</code> 循环，说明 crt_stub.cpp 没有被正确编译或者旧的目标文件被缓存了——尝试 clean build。</p><h3 id="step-2-编写-host-端单元测试" tabindex="-1">Step 2: 编写 host 端单元测试 <a class="header-anchor" href="#step-2-编写-host-端单元测试" aria-label="Permalink to &quot;Step 2: 编写 host 端单元测试&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>test/unit/test_heap.cpp</code> 中实现 TestHeap 类和全面的测试用例。</p><p><strong>设计思路</strong>: TestHeap 类的 init 方法用 <code>calloc</code> 分配内存并初始化第一个空闲块。alloc 和 free_mem 实现和 kernel 版本相同的 first-fit + split + coalesce 算法。不需要实现 expand（host 端的缓冲区是固定大小的，用于测试有限空间下的行为）。</p><p>为什么不直接链接 <code>heap.cpp</code>？因为直接链接会引入所有内核头文件的依赖（<code>kprintf</code>、<code>pmm.hpp</code>、<code>vmm.hpp</code>），这些头文件又依赖 <code>paging_config.hpp</code>、<code>serial.hpp</code> 等，编译依赖链非常复杂。镜像实现虽然需要维护两份代码，但完全自包含——不需要任何内核头文件，编译速度快，可以用 GDB 单步调试。</p><p>测试用例需要覆盖以下场景。第一组：基本功能——alloc 返回非空指针、默认 16 字节对齐、alloc(0) 返回 nullptr。第二组：对齐——64 字节对齐、4096 字节对齐。第三组：分裂——大块分配后空闲链表仍有剩余、分裂后的块 magic 正确。第四组：释放与合并——free(nullptr) 是安全的 no-op、free 减少 used 计数、相邻块正向合并、相邻块反向合并。第五组：double-free 检测——两次 free 同一个指针不会损坏 used 计数。第六组：多块分配——50 个连续分配全部成功、全部释放后 used 为 0。第七组：大对齐下的 front padding——4096 对齐的分配能正确工作，free 也能正确释放。第八组：三块相邻合并——无论释放顺序（正序、反序、乱序），三个相邻块最终都能合并。第九组：数据完整性——分配的内存被清零、写入的数据能正确读回、free 后重新 alloc 得到的内存也被清零。第十组：压力测试——200 次交错 alloc/free 循环加上 100 块分配后全部释放，验证无泄漏。第十一组：边界情况——alloc(1) 最小分配、堆耗尽时返回 nullptr、大块分配（接近页面大小）。第十二组：空闲链表完整性——碎片化分配释放后 validate_free_list 通过。</p><p><strong>实现约束</strong>: 测试文件定义 <code>TEST_FRAMEWORK_IMPL</code> 宏并 include <code>test_framework.h</code>。BlockHeader 结构体在测试文件内部重新定义（镜像 kernel 版本），和 kernel 代码没有链接关系。测试用例使用 <code>TEST(&quot;name&quot;) { ... }</code> 宏。</p><p><strong>踩坑预警</strong>: TestHeap 中 coalesce 的实现必须和 kernel 版本完全一致，否则测试的覆盖范围就打了折扣。最容易出偏差的地方是&quot;链表头摘除&quot;的处理——在 TestHeap 中也要检查 free_list_ == block 的情况。另一个要注意的是，TestHeap 的 free_mem 函数不调用 expand——如果缓冲区耗尽，alloc 返回 nullptr。这意味着&quot;堆耗尽&quot;测试在 host 端是可测的（给一个很小的缓冲区比如 1 页，然后不断分配直到返回 nullptr），但在 kernel 端不可测（因为 expand 会自动扩展）。</p><p><strong>验证</strong>: 构建并运行 host 端测试：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -L</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> heap</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>所有测试用例应通过。如果有失败，ctest 会打印具体的断言失败信息和行号。</p><h3 id="step-3-编写-qemu-端集成测试" tabindex="-1">Step 3: 编写 QEMU 端集成测试 <a class="header-anchor" href="#step-3-编写-qemu-端集成测试" aria-label="Permalink to &quot;Step 3: 编写 QEMU 端集成测试&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>kernel/test/test_heap.cpp</code> 中实现使用真实 g_heap 的测试用例。</p><p><strong>设计思路</strong>: QEMU 端测试在 <code>big_kernel_test</code> 中运行，使用真正的 <code>cinux::mm::g_heap</code> 全局实例。测试前需要确保堆已经初始化（在 <code>main_test.cpp</code> 中调用 <code>g_heap.init</code>）。</p><p>测试用例覆盖以下场景。第一个：基本分配——分配 64 字节，写入递增序列，读回验证。第二个：默认对齐——验证 alloc(64) 返回的地址是 16 字节对齐的。第三个：页对齐——alloc(64, 4096) 返回 4096 对齐的地址。第四个：alloc(0) 返回 nullptr。第五个：free(nullptr) 不 crash。第六个：多块分配不重叠——分配三个 64 字节块，写入不同模式（0xAA、0xBB、0xCC），读回验证各块数据独立。第七个：合并——分配三个块，全部释放，再分配一个 256 字节的大块，验证合并后空间足够。第八个：奇数大小——分配 37、23、41 字节，验证全部 16 字节对齐且数据正确。第九个：压力测试——50 次循环，每次分配不同大小的块，中间穿插释放和重新分配。第十个：dump_stats 不 crash。</p><p><strong>实现约束</strong>: 测试文件定义在 <code>kernel/test/test_heap.cpp</code> 中，使用 <code>big_kernel_test.h</code> 的 TEST_ASSERT 宏。由于内核中没有 memset，需要自己实现一个简单的 kmemset 辅助函数。入口函数是 <code>run_heap_tests()</code>，声明在 <code>big_kernel_test.h</code> 中，在 <code>main_test.cpp</code> 中调用。</p><p><strong>踩坑预警</strong>: QEMU 端测试使用的堆地址是 <code>0xFFFFFFFF80100000ULL</code>（和正式内核不同，因为测试内核的地址空间布局不同）。如果你用了正式内核的地址 <code>0xFFFF800000000000</code>，可能会和测试内核的代码段重叠。另外，QEMU 端测试的初始堆大小也是 64 KB，但 50 次压力测试可能触发 expand——这是正常的，测试不应该假设堆空间固定不变。</p><p>QEMU 端测试的一个重要价值是验证端到端的正确性——从 alloc 到页表映射到物理内存访问的整条链路。如果 VMM 的映射有误（比如映射到了错误的物理页），或者页表标志位不对（比如忘了设 writable），QEMU 端测试会立刻触发 page fault，而 host 端测试（使用 calloc，没有页表映射）完全无法发现这种问题。这就是为什么需要两层测试的原因。</p><p><strong>验证</strong>: 构建 big_kernel_test 并在 QEMU 中运行：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./build/run_qemu_test.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>串口输出应该显示：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=== Heap Tests (017) ===</span></span>
<span class="line"><span>  PASS: test_basic_alloc</span></span>
<span class="line"><span>  PASS: test_default_alignment</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>  PASS: test_many_cycles</span></span>
<span class="line"><span>  PASS: test_dump_no_crash</span></span>
<span class="line"><span>=== Heap Tests: XX passed, 0 failed ===</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h3 id="step-4-将测试集成到构建系统" tabindex="-1">Step 4: 将测试集成到构建系统 <a class="header-anchor" href="#step-4-将测试集成到构建系统" aria-label="Permalink to &quot;Step 4: 将测试集成到构建系统&quot;">​</a></h3><p><strong>目标</strong>: 在 CMakeLists.txt 中注册新的测试目标。</p><p><strong>设计思路</strong>: 有两处需要修改。第一处，<code>kernel/CMakeLists.txt</code>——在 <code>big_kernel_common</code> 库中添加 <code>mm/heap.cpp</code>（之前章节已经做了），在 <code>big_kernel_test</code> 的源文件列表中添加 <code>test/test_heap.cpp</code>。第二处，<code>test/CMakeLists.txt</code>——添加 <code>test_heap</code> 测试可执行文件，链接 <code>unit/test_heap.cpp</code>，加入 <code>test_host</code> 和 <code>test</code> 的依赖列表。</p><p>注意 <code>test_heap</code> 目标的 include 路径需要能找到 <code>test_framework.h</code>。如果 include 路径有误，编译时会报找不到头文件的错误。其他 include 路径（如内核头文件目录）对 host 测试不需要，因为 TestHeap 是自包含的。</p><p><strong>踩坑预警</strong>: 确保 <code>test_heap</code> 目标的 include 路径正确——它需要能找到 <code>test_framework.h</code>、<code>kernel/mm/heap.hpp</code>（用于类型定义的镜像参考，虽然不直接 include）、以及各种内核头文件。如果 include 路径有误，编译时会报找不到头文件的错误。</p><p><strong>验证</strong>: 完整构建并运行所有测试：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>所有测试（包括之前的 PMM、VMM、kprintf 测试）加上新增的 heap 测试应该全部通过。如果某个测试失败，ctest 会打印失败用例的名称和断言信息，帮助定位问题。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>本章完成了三件事：C++ new/delete 的重定向（10 个重载版本全部委托给 Heap）、堆在启动序列中的位置（VMM 之后、Framebuffer 之前）、以及双层测试体系（host 端 866 行 + QEMU 端 301 行）。tag 017 至此全部完成——Cinux 的内存管理栈终于完整了：PMM 管物理页，VMM 管虚拟映射，Heap 管任意大小的动态分配。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>本章的修改集中在 <code>kernel/arch/x86_64/crt_stub.cpp</code>（operator new/delete 重定向）、<code>test/unit/test_heap.cpp</code>（host 端测试）和 <code>kernel/test/test_heap.cpp</code>（QEMU 端测试）。</p><p>运行 host 端测试：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -L</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> heap</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>运行 QEMU 端测试：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./build/run_qemu_test.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>运行全部测试（包括之前的所有 tag）：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>operator new 调用后内核死锁</strong>: 如果在堆初始化之前有代码调用了 new，它会触发旧的 halt-on-use 代码（如果你忘了替换的话）或者触发 g_heap 的未初始化访问。检查 <code>crt_stub.cpp</code> 是否已经完整替换了所有 halt-on-use 的空壳。用 <code>nm</code> 或 <code>objdump</code> 检查编译后的目标文件，确认 operator new 指向了调用 g_heap.alloc 的实现而不是死循环。</p><p><strong>host 测试中 TestHeap 行为和 kernel Heap 不一致</strong>: 这是最棘手的问题——如果 TestHeap 的重新实现和 kernel Heap 有微妙差异，host 测试可能全部通过但 kernel 中实际有 bug。解决办法是在 QEMU 端测试中也运行足够的测试用例，特别是 alloc、free、coalesce 的核心路径。如果 QEMU 端测试通过但 host 端失败（或者反过来），说明两边的算法实现不一致，需要逐行对比 alloc 和 free 的逻辑。</p><p><strong>压力测试后 used_ 不为 0</strong>: 这是最经典的内存泄漏信号。在 host 测试中，如果 200 次 alloc/free 循环后 used_ 不是 0，说明某些分配没有被正确释放，或者 free 时 used_ 的更新有 bug。在循环的每一步打印 used_ 的值，找到它开始偏离预期的那个循环次数，然后检查那个循环中的 alloc/free 调用。</p><p><em><em>构建失败 &quot;undefined reference to operator delete(void</em>, unsigned long, std::align_val_t)&quot;</em>*: 这说明编译器生成了 aligned sized delete 的调用但链接器找不到实现。检查 crt_stub.cpp 是否包含了所有 6 个 delete 重载版本。如果你只实现了基本的 delete 而没有实现 aligned 和 sized 版本，就会触发这个链接错误。</p><p><strong>ctest 显示测试崩溃（segfault）</strong>: 如果 host 测试崩溃，通常是因为空闲链表被损坏（野指针或 double-free 未被检测到）。在 TestHeap 的 validate_free_list 中加入更详细的检查——遍历链表时打印每个块的地址、magic 和 size，在崩溃前定位到问题块。</p><p><strong>QEMU 测试中 double-free 没有被捕获</strong>: QEMU 端测试目前没有 double-free 测试用例，因为 double-free 只会打印 kprintf 警告不会 crash。要验证 double-free 检测，可以临时在 QEMU 测试中手动调用两次 free 同一个指针，然后检查串口输出是否包含 &quot;Double-free detected&quot;。</p><p><strong>host 测试和 QEMU 测试结果不一致</strong>: 如果 host 测试通过但 QEMU 测试失败（或者反过来），说明两边的算法实现有差异。逐行对比 TestHeap 和 Heap 的 alloc/free/coalesce 逻辑。最容易出偏差的地方是 coalesce 中&quot;链表头摘除&quot;的处理和 alloc 中&quot;usable &lt; size&quot;的边界检查。</p><h2 id="本章小结-1" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结-1" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>operator new/delete</td><td>十个重载版本全部重定向到 g_heap.alloc/free</td></tr><tr><td>aligned new</td><td>std::align_val_t 转为 size_t 传给 alloc 的 align 参数</td></tr><tr><td>sized delete</td><td>size 参数忽略，BlockHeader 已记录大小</td></tr><tr><td>Host 端测试</td><td>TestHeap 用 calloc mock VMM，重新实现算法，覆盖边界情况和压力测试</td></tr><tr><td>QEMU 端测试</td><td>使用真实 g_heap + PMM + VMM，验证端到端正确性</td></tr><tr><td>双层测试互补</td><td>host 端覆盖大量用例，QEMU 端验证真实环境集成</td></tr><tr><td>压力测试</td><td>200+ 次 alloc/free 循环，验证 used_ 归零（无泄漏）</td></tr><tr><td>validate_free_list</td><td>检查环、magic、free 标志、地址范围，诊断链表损坏</td></tr><tr><td>QEMU 测试地址</td><td>0xFFFFFFFF80100000（不同于正式内核的地址，避免和测试内核代码段重叠）</td></tr><tr><td>总代码量</td><td>heap.hpp(126行) + heap.cpp(353行) + crt_stub.cpp(修改) + 测试(1167行)</td></tr></tbody></table>`,85)])])}const k=t(d,[["render",i]]);export{u as __pageData,k as default};
