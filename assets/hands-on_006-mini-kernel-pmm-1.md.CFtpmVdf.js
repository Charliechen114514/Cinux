import{c as i,Q as a,j as s,m as o}from"./chunks/framework.D9IbVcwA.js";const m=JSON.parse('{"title":"006-mini-kernel-pmm-1 · 物理内存管理","description":"","frontmatter":{"title":"006-mini-kernel-pmm-1 · 物理内存管理"},"headers":[],"relativePath":"hands-on/006-mini-kernel-pmm-1.md","filePath":"hands-on/006-mini-kernel-pmm-1.md","lastUpdated":1779686691000}'),d={name:"hands-on/006-mini-kernel-pmm-1.md"};function n(c,e,t,l,r,p){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="_006-1-内存字面量与基础定义" tabindex="-1">006-1 内存字面量与基础定义 <a class="header-anchor" href="#_006-1-内存字面量与基础定义" aria-label="Permalink to &quot;006-1 内存字面量与基础定义&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一章我们让内核成功跑了起来——它能通过串口告诉我们 BootInfo 里有什么、E820 内存映射长什么样。但说实话，现在的内核就像一个只会看不会动手的旁观者，它能看到物理内存的布局，却连一个字节都分配不了。这一章我们要开始改变这个局面，第一步是搭建内存管理的基础设施：让代码里不用再写满屏幕的 <code>4096</code> 和 <code>0x100000</code>，而是用人类可读的方式表达内存大小。</p><p>完成本文后，你将拥有一套零运行时开销的内存字面量工具和对齐辅助函数，它们会在后续所有内存管理代码中被反复使用。</p><p>前置知识：上一章的 BootInfo 结构和 E820 内存映射，以及 C++ 基础的 <code>constexpr</code> 概念。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="为什么需要内存字面量" tabindex="-1">为什么需要内存字面量 <a class="header-anchor" href="#为什么需要内存字面量" aria-label="Permalink to &quot;为什么需要内存字面量&quot;">​</a></h3><p>写过底层代码的人一定对这种东西不陌生：<code>4096</code>、<code>0x100000</code>、<code>0x40000000</code>。单看数字你完全不知道它代表多大——4KB？1MB？还是 1GB？你只能靠数零来猜，而且很容易数错。C++11 引入的用户定义字面量（User-Defined Literals）可以很好地解决这个问题：我们定义 <code>4_KB</code>、<code>1_MB</code>、<code>4_GB</code> 这样的后缀，让编译器在编译期完成换算，运行时零开销。</p><p>这个机制的底层原理非常简单。我们定义一些名字形如 <code>operator&quot;&quot;_KB</code> 的 <code>constexpr</code> 函数，编译器看到 <code>4_KB</code> 这个 token 时，会自动调用 <code>operator&quot;&quot;_KB(4)</code> 并把返回值内联到使用处。因为整个计算都是编译期常量表达式，生成的机器码和你手写 <code>4096</code> 完全一样。</p><h3 id="对齐操作的位运算原理" tabindex="-1">对齐操作的位运算原理 <a class="header-anchor" href="#对齐操作的位运算原理" aria-label="Permalink to &quot;对齐操作的位运算原理&quot;">​</a></h3><p>内存管理里到处需要对齐操作——页对齐（4KB）、大页对齐（2MB）等等。对齐的本质就是把地址的低位清零。对于 2 的幂次对齐值（4KB = 2^12），有一个经典的位运算技巧：对齐值减一之后，低位全部是 1（比如 <code>4096 - 1 = 0xFFF</code>，12 个 1），取反就是低位全 0 的掩码。拿这个掩码和地址做 AND 操作，就完成了向下对齐；先加上 <code>align - 1</code> 再做 AND，就是向上对齐。</p><p>举个例子：<code>align_up(0x1234, 0x1000)</code> 的过程是 <code>0x1234 + 0xFFF = 0x2233</code>，然后 <code>0x2233 &amp; ~0xFFF = 0x2000</code>。结果 0x1234 被向上对齐到了 0x2000，即第二个 4KB 页的起始地址。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-创建内存字面量头文件" tabindex="-1">Step 1: 创建内存字面量头文件 <a class="header-anchor" href="#step-1-创建内存字面量头文件" aria-label="Permalink to &quot;Step 1: 创建内存字面量头文件&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel/mini/mm/</code> 目录下创建 <code>memory_literals.h</code>，提供 <code>_KB</code>、<code>_MB</code>、<code>_GB</code>、<code>_TB</code> 四个字面量后缀。</p><p><strong>设计思路</strong>：这些字面量本质上就是乘法运算的语法糖。<code>_KB</code> 乘 1024，<code>_MB</code> 乘 1024 的平方，以此类推。我们把它们放在 <code>cinux::mini::mm::literals</code> 命名空间里，然后在上一级命名空间 <code>cinux::mini::mm</code> 中用 <code>using namespace literals</code> 导入，这样在该命名空间内可以直接写 <code>4_KB</code> 而不用加限定符。</p><p><strong>实现约束</strong>：</p><ul><li>每个字面量运算符接收 <code>unsigned long long</code> 参数，返回 <code>uint64_t</code></li><li>必须全部标记为 <code>constexpr</code>，确保编译期计算</li><li>后缀名必须以下划线开头（<code>_KB</code>），因为非下划线后缀是标准保留的</li><li>不依赖任何标准库头文件，只包含 <code>&lt;stdint.h&gt;</code></li></ul><p><strong>踩坑预警</strong>：后缀名如果不用下划线开头（比如直接用 <code>KB</code>），标准规定只有标准库才能用不带下划线的字面量后缀，你的代码虽然可能编译通过，但属于未定义行为。另外别忘了乘法要用 <code>1024ULL</code> 而不是裸 <code>1024</code>，否则在 32 位平台上中间结果会溢出。</p><p><strong>验证</strong>：这一步只是头文件，编译通过即可。在后续文件中 include 它，如果能正常使用 <code>4_KB</code>、<code>1_MB</code> 等表达式且值正确，就说明没问题。</p><hr><h3 id="step-1-5-理解命名空间分层的设计意图" tabindex="-1">Step 1.5: 理解命名空间分层的设计意图 <a class="header-anchor" href="#step-1-5-理解命名空间分层的设计意图" aria-label="Permalink to &quot;Step 1.5: 理解命名空间分层的设计意图&quot;">​</a></h3><p><strong>目标</strong>：理解为什么字面量要放在子命名空间中再导入。</p><p><strong>设计思路</strong>：C++ 的用户定义字面量有一个容易忽略的问题——它们是&quot;无条件可见&quot;的。一旦你用 <code>using namespace</code> 导入了一个包含字面量运算符的命名空间，该命名空间内所有的字面量后缀都会参与重载决议。如果把 <code>_KB</code> 直接定义在全局命名空间或 <code>cinux</code> 命名空间中，那么在项目中任何用了 <code>using namespace cinux</code> 的地方，<code>_KB</code> 都会变得可见，可能和第三方库的字面量冲突。</p><p>所以我们采用&quot;定义在深层子命名空间、按需导入到直接父命名空间&quot;的模式：</p><ul><li>定义层：<code>cinux::mini::mm::literals</code>——字面量只在这个命名空间中存在</li><li>使用层：<code>cinux::mini::mm</code>——通过 <code>using namespace literals</code> 导入，使用 <code>mm</code> 命名空间的代码可以直接用 <code>4_KB</code></li><li>隔离层：其他命名空间（如 <code>cinux::mini::driver</code>）不受影响，除非它也显式 <code>using namespace cinux::mini::mm</code></li></ul><p>这种模式和 C++ 标准库的做法一致——<code>std::literals::string_literals</code> 定义了 <code>&quot;hello&quot;s</code>，但你必须显式 <code>using namespace std::string_literals</code> 才能用。我们的设计让字面量在 <code>mm</code> 子系统内自动可用，但不会泄漏到子系统外面。</p><p><strong>验证</strong>：编译通过即可。如果你在 <code>mm</code> 命名空间外的代码中写 <code>4_KB</code> 且没有 <code>using namespace cinux::mini::mm</code>，应该得到编译错误。</p><hr><h3 id="step-2-创建内存管理通用定义" tabindex="-1">Step 2: 创建内存管理通用定义 <a class="header-anchor" href="#step-2-创建内存管理通用定义" aria-label="Permalink to &quot;Step 2: 创建内存管理通用定义&quot;">​</a></h3><p><strong>目标</strong>：在同一个目录下创建 <code>mm_defines.h</code>，提供页大小常量和对齐辅助函数。</p><p><strong>设计思路</strong>：这个头文件是整个内存管理子系统的&quot;公共入口&quot;——它 include 了上面的 <code>memory_literals.h</code>，然后定义了三级页大小常量（4KB、2MB、1GB）和三个对齐工具函数。其他内存管理代码只需要 include 这一个头文件，就能获得所有基础定义。</p><p><strong>实现约束</strong>：</p><ul><li>三个页大小常量：4KB 对应标准页，2MB 对应大页，1GB 对应巨型页（都是 x86-64 硬件支持的页大小）</li><li><code>align_up(addr, align)</code> 将地址向上对齐到最近的 align 边界</li><li><code>align_down(addr, align)</code> 将地址向下对齐到最近的 align 边界</li><li><code>is_aligned(addr, align)</code> 检查地址是否已经是对齐的</li><li>对齐参数必须是 2 的幂，函数不做运行时检查（教学内核，保持简单）</li><li>所有函数都是 <code>constexpr</code>，可以在编译期使用</li></ul><p><strong>踩坑预警</strong>：<code>align_up</code> 里有一个经典的整数溢出陷阱——如果 <code>addr + align - 1</code> 溢出了 <code>uint64_t</code> 的范围，结果就会出错。不过在 64 位地址空间下，除非你在处理接近 <code>UINT64_MAX</code> 的地址，否则一般不会遇到。另一个容易忽略的点是：<code>align_down</code> 不需要加法，所以不存在溢出问题，但如果传入的地址本身比 align 小，结果会是 0，这在某些场景下可能是意外的。</p><p><strong>验证</strong>：同样只验证编译。你可以在测试文件中加几行静态断言来确认常量值是否正确，比如确认 <code>4_KB</code> 等于 4096，<code>align_up(0x1234, 0x1000)</code> 等于 0x2000 等。因为都是 <code>constexpr</code>，这些断言在编译期就能生效。</p><hr><h3 id="step-2-5-三级页大小的硬件背景" tabindex="-1">Step 2.5: 三级页大小的硬件背景 <a class="header-anchor" href="#step-2-5-三级页大小的硬件背景" aria-label="Permalink to &quot;Step 2.5: 三级页大小的硬件背景&quot;">​</a></h3><p><strong>目标</strong>：理解为什么我们定义了 4KB、2MB、1GB 三种页大小常量。</p><p><strong>设计思路</strong>：x86-64 架构的 MMU（Memory Management Unit）硬件支持三种不同的页大小，它们对应不同的页表层级跳过策略。4KB 是最基础的标准页——x86 从 386 时代的分页机制就开始用它，Intel SDM Vol.3A Section 4.5 明确规定 4KB 页要求 PTE 的 bit 11:0 必须为零（即物理地址必须 4KB 对齐）。2MB 大页（huge page）通过跳过页表中间层来减少 TLB miss——4KB 页需要四级页表（PML4 -&gt; PDPT -&gt; PD -&gt; PT -&gt; page），而 2MB 大页只需三级（PML4 -&gt; PDPT -&gt; PD -&gt; page），少了一次内存访问。1GB 巨型页跳得更多，只需要两级页表。</p><p>当前我们的 Mini Kernel 还没有启用分页，所以这些常量暂时只用于 PMM 的位图计算和地址对齐。但当我们在后续 tag 中实现虚拟内存管理器（VMM）时，2MB 和 1GB 页大小常量就会被直接使用——比如在创建内核页表时，可以用 2MB 大页来映射内核代码段，减少页表占用的物理内存。</p><p><strong>验证</strong>：这是一个概念理解步骤，不需要编写额外代码。了解这些常量的用途有助于后续 tag 的实现。</p><hr><h3 id="step-3-验证编译集成" tabindex="-1">Step 3: 验证编译集成 <a class="header-anchor" href="#step-3-验证编译集成" aria-label="Permalink to &quot;Step 3: 验证编译集成&quot;">​</a></h3><p><strong>目标</strong>：确认这两个头文件能被项目中的其他文件正确 include。</p><p><strong>设计思路</strong>：<code>mm_defines.h</code> 已经 include 了 <code>memory_literals.h</code>，所以我们只需要确认 <code>mm_defines.h</code> 能正确编译即可。这两个文件都是纯头文件，不需要修改 <code>CMakeLists.txt</code>——CMake 的 <code>target_include_directories</code> 已经把 <code>kernel/mini/</code> 目录加入了搜索路径，其他文件用 <code>#include &quot;mm/mm_defines.h&quot;</code> 就能找到它们。</p><p><strong>实现约束</strong>：</p><ul><li><code>mm_defines.h</code> include <code>memory_literals.h</code> 时使用相对路径（同一个 <code>mm/</code> 目录下）</li><li>命名空间约定：字面量定义在 <code>cinux::mini::mm::literals</code>，然后通过 <code>using namespace</code> 提升到 <code>cinux::mini::mm</code></li><li>两个文件都使用 <code>#pragma once</code> 作为 include guard</li></ul><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 在项目根目录</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -20</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 如果编译报错找不到头文件，检查 CMakeLists.txt 中</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># target_include_directories 是否包含了 kernel/mini/ 目录</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>编译成功就说明头文件的路径和命名空间配置都是正确的。</p><p><strong>额外验证——静态断言</strong>：</p><p>虽然本文只是头文件，但你可以通过 <code>static_assert</code> 在编译期验证这些工具的正确性。在你的测试文件或临时文件中尝试以下断言，如果编译通过就说明实现无误：</p><ul><li>验证 <code>4_KB</code> 等于 4096</li><li>验证 <code>1_MB</code> 等于 <code>1024 * 1024</code></li><li>验证 <code>align_up(0x1234, 0x1000)</code> 等于 <code>0x2000</code></li><li>验证 <code>align_down(0x1234, 0x1000)</code> 等于 <code>0x1000</code></li><li>验证 <code>is_aligned(0x1000, 0x1000)</code> 为 true，<code>is_aligned(0x1001, 0x1000)</code> 为 false</li></ul><p>这些断言在编译期求值，不产生任何运行时代码。如果某个断言失败，编译器会直接报错并指出哪个条件不满足。</p><hr><h2 id="进阶思考-constexpr-与模板的边界" tabindex="-1">进阶思考：constexpr 与模板的边界 <a class="header-anchor" href="#进阶思考-constexpr-与模板的边界" aria-label="Permalink to &quot;进阶思考：constexpr 与模板的边界&quot;">​</a></h2><h3 id="为什么不用模板元编程" tabindex="-1">为什么不用模板元编程 <a class="header-anchor" href="#为什么不用模板元编程" aria-label="Permalink to &quot;为什么不用模板元编程&quot;">​</a></h3><p>在 C++ 中实现编译期计算有多种方式：宏、<code>constexpr</code> 函数、模板元编程（Template Metaprogramming）。我们选择了 <code>constexpr</code> 函数，这里值得讨论一下为什么不用另外两种。</p><p>宏的问题前面已经提到——没有类型安全，是文本替换。模板元编程在理论上可以做同样的事（用模板特化和 <code>enum</code> 值来定义编译期常量），但它有几个缺点：语法更晦涩、编译错误信息难以阅读、在 freestanding 环境下某些高级模板特性可能不可用。<code>constexpr</code> 函数是 C++11 引入的&quot;平民版编译期计算&quot;——语法和普通函数几乎一样，加上 <code>constexpr</code> 修饰符就能在编译期求值。它生成的编译错误信息也比模板元编程清晰得多。</p><h3 id="constexpr-的隐式约束" tabindex="-1">constexpr 的隐式约束 <a class="header-anchor" href="#constexpr-的隐式约束" aria-label="Permalink to &quot;constexpr 的隐式约束&quot;">​</a></h3><p>值得注意的是，<code>constexpr</code> 函数有一些隐式约束。在 C++11 中，<code>constexpr</code> 函数的函数体只能包含一条 <code>return</code> 语句。C++14 放宽了这个限制，允许使用局部变量、<code>if</code>/<code>else</code>、循环等。我们的 <code>align_up</code> 函数只有一条 <code>return</code>，所以即使在严格的 C++11 模式下也能编译。但如果你在 <code>constexpr</code> 函数中使用了 C++14 才允许的语法（比如局部变量），确保你的编译器支持 C++14 或更高版本。</p><p>对于 <code>is_aligned</code> 函数，返回 <code>bool</code> 类型的 <code>constexpr</code> 函数可以用于 <code>static_assert</code>——这在编译期验证常量约束时非常有用。例如：<code>static_assert(is_aligned(4096, 4096), &quot;PAGE_SIZE must be aligned&quot;)</code>。</p><hr><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>本文只是基础设施，还没有修改内核的运行行为。编译通过即可：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/cinux</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>如果你想提前测试字面量是否工作，可以临时在 <code>main.cpp</code> 里加一行打印：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 注意这只是临时验证，验证完要删掉</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kprintf(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;4_KB = %u\\n&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 4_KB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期输出: 4_KB = 4096</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><hr><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><h3 id="常见问题排查" tabindex="-1">常见问题排查 <a class="header-anchor" href="#常见问题排查" aria-label="Permalink to &quot;常见问题排查&quot;">​</a></h3><p><strong>问题 1：编译报错 &quot;no matching literal operator&quot;</strong></p><p>这说明编译器没找到字面量运算符的定义。最常见的原因是忘记在 <code>using namespace cinux::mini::mm;</code> 的作用域内使用字面量，或者根本没有 include <code>mm_defines.h</code>。检查 include 路径和命名空间是否正确。</p><p><strong>问题 2：<code>constexpr</code> 函数在运行时计算</strong></p><p>这不会导致错误，但意味着你丢掉了编译期优化的好处。确保传给 <code>align_up</code> 等函数的参数都是编译期常量——如果你用运行时变量调用它们，<code>constexpr</code> 就只是个普通函数修饰符，不会报错但也不会在编译期求值。</p><hr><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>本文创建了两个纯头文件，为后续的物理内存管理打下了基础。<code>memory_literals.h</code> 用 C++ 用户定义字面量提供了 <code>4_KB</code>、<code>1_MB</code> 等人类可读的内存大小表达方式，<code>mm_defines.h</code> 则在此基础上定义了页大小常量和对齐辅助函数。两个文件都是零运行时开销的编译期工具。</p><table tabindex="0"><thead><tr><th>文件</th><th>提供的能力</th><th>存放位置</th></tr></thead><tbody><tr><td><code>memory_literals.h</code></td><td><code>_KB</code>/<code>_MB</code>/<code>_GB</code>/<code>_TB</code> 字面量</td><td><code>kernel/mini/mm/</code></td></tr><tr><td><code>mm_defines.h</code></td><td>页大小常量 + <code>align_up</code>/<code>align_down</code>/<code>is_aligned</code></td><td><code>kernel/mini/mm/</code></td></tr></tbody></table><p>关键设计决策回顾：</p><ul><li>字面量后缀以下划线开头，避免与标准库保留名冲突</li><li>使用 <code>1024ULL</code> 确保乘法不会在 32 位平台上溢出</li><li>对齐函数不检查参数是否为 2 的幂，保持教学内核的简单性</li><li><code>constexpr</code> 让所有计算在编译期完成，运行时零开销</li></ul><p>下一篇我们将在这个基础上实现 PMM 的核心位图逻辑和 E820 解析。</p>`,85)])])}const u=i(d,[["render",n]]);export{m as __pageData,u as default};
