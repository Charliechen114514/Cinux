import{c as s,Q as a,j as e,m as t}from"./chunks/framework.D9IbVcwA.js";const u=JSON.parse('{"title":"030-gui-wm-basic-3 · 窗口管理器","description":"","frontmatter":{"title":"030-gui-wm-basic-3 · 窗口管理器"},"headers":[],"relativePath":"tutorial/030-gui-wm-basic-3.md","filePath":"tutorial/030-gui-wm-basic-3.md","lastUpdated":1779686691000}'),n={name:"tutorial/030-gui-wm-basic-3.md"};function l(r,i,p,h,o,k){return a(),e("div",null,[...i[0]||(i[0]=[t(`<h1 id="鼠标交互与-gui-启动集成" tabindex="-1">鼠标交互与 GUI 启动集成 <a class="header-anchor" href="#鼠标交互与-gui-启动集成" aria-label="Permalink to &quot;鼠标交互与 GUI 启动集成&quot;">​</a></h1><blockquote><p>标签：鼠标拖拽, hit testing, PIT 事件循环, GUI 初始化, ISR 栈对齐 前置：<a href="./030-gui-wm-basic-2">030-2 窗口管理器</a></p></blockquote><h2 id="前言" tabindex="-1">前言 <a class="header-anchor" href="#前言" aria-label="Permalink to &quot;前言&quot;">​</a></h2><p>前两章我们分别搭好了输入管道（鼠标驱动+事件队列）和输出框架（Window+WindowManager+compositing），但它们还是各干各的——鼠标事件躺在队列里没人吃，窗口管理器空转不处理输入。这一章要把这两半串起来，让鼠标事件真正驱动窗口行为：点击 raise、标题栏拖拽、关闭按钮销毁。同时我们还要把整个 GUI 的初始化集成到内核启动序列里，处理好&quot;哪个阶段能做什么&quot;的依赖关系。</p><h2 id="环境说明" tabindex="-1">环境说明 <a class="header-anchor" href="#环境说明" aria-label="Permalink to &quot;环境说明&quot;">​</a></h2><ul><li>平台：x86_64 (QEMU, PS/2 鼠标 + VBE framebuffer)</li><li>屏幕分辨率：1024x768x32bpp</li><li>PIT 频率：100Hz（每 10ms 一次 tick）</li><li>GUI 刷新率：与 PIT 相同，最高 100fps</li></ul><h2 id="第一步——鼠标事件的状态机" tabindex="-1">第一步——鼠标事件的状态机 <a class="header-anchor" href="#第一步——鼠标事件的状态机" aria-label="Permalink to &quot;第一步——鼠标事件的状态机&quot;">​</a></h2><p>窗口管理器处理鼠标事件本质上是一个简单的状态机。空闲状态下收到 MouseDown 时，我们先做 hit test——从最顶层窗口向下遍历，找到第一个包含点击坐标的可见窗口。为什么要倒序遍历？因为上面的窗口遮挡下面的，从底层开始遍历会让被遮挡的窗口&quot;偷&quot;到点击事件。</p><p>找到了被点击的窗口之后，还要看点击落在窗口的哪个部位。如果命中右上角的关闭按钮（14x14 红色方块），就销毁窗口。如果命中标题栏（y 坐标在窗口顶部 20px 范围内），就进入拖拽模式。不管点在哪里，都先 raise 把窗口提到最顶层。</p><p>拖拽的关键是记录偏移量。当用户点击标题栏时，鼠标通常不在窗口左上角——可能在标题栏中间的某个位置。我们记录下&quot;点击时鼠标相对于窗口左上角的偏移&quot;，然后在后续的 MouseMove 事件中，用当前鼠标坐标减去偏移量得到窗口的新位置。没有这个偏移量，窗口会在点击的瞬间跳到鼠标位置，用户体验非常差。</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// MouseDown on title bar</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dragging_      </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">drag_offset_x_ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev.mouse.x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hit-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">drag_offset_y_ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev.mouse.y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hit-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// MouseMove while dragging</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> new_x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev.mouse.x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> drag_offset_x_;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> new_y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev.mouse.y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> drag_offset_y_;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">focused_-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_position</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(new_x, new_y);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>MouseUp 事件清除拖拽标志，回到空闲状态。整个状态机就这三个状态转换——简单但完整。</p><p>对比 SerenityOS 的窗口拖拽实现：它支持标题栏拖拽、边框 resize、窗口最大化/最小化动画，还有&quot;吸附到屏幕边缘&quot;的磁吸效果。Cinux 只实现了最基础的标题栏拖拽。对比 X11 的做法：拖拽是由窗口管理器进程处理的（通过 XGrabPointer + ConfigureNotify 事件），而不是内核直接操作——这是用户空间窗口管理器的好处之一，可以独立于内核升级和替换。</p><h2 id="第二步——pit-tick-驱动的-gui-事件循环" tabindex="-1">第二步——PIT tick 驱动的 GUI 事件循环 <a class="header-anchor" href="#第二步——pit-tick-驱动的-gui-事件循环" aria-label="Permalink to &quot;第二步——PIT tick 驱动的 GUI 事件循环&quot;">​</a></h2><p>我们的 GUI 没有独立的事件循环线程。取而代之的是借用 PIT 定时器的 tick 回调——每 10ms 一次的定时器中断里，我们做三件事：</p><p>第一，清空事件队列。循环 dequeue 直到队列空，每个事件根据类型分发给 handle_mouse 或 handle_key。这意味着从上一次 tick 到这一次之间积累的所有事件会被一次性处理。</p><p>第二，handle_key 处理键盘事件。在当前 tag 里 handle_key 是一个空操作——只是 <code>(void)ev;</code> 保留接口给后续 tag 使用。不过框架已经搭好：只要 EventQueue 里有 KeyDown/KeyUp 事件，它们就会被分发到 handle_key。</p><p>第三，调用 composite 合成一帧。清屏 → blit 所有窗口 → 画光标 → flip。</p><p>这种设计有一个明显的限制：GUI 帧率被 PIT 频率绑定了。100Hz 的 PIT 给我们最多 100fps。如果 compositing 耗时超过 10ms（在 1024x768 分辨率下不太可能，但更高分辨率就可能），tick 回调会堆积，导致中断延迟。</p><p>另一种设计是独立 GUI 线程——一个专用线程在主循环里做 dequeue + composite，不占用中断处理时间。SerenityOS 的 WindowServer 就是这么做的——它是一个用户空间进程，有自己的事件循环和渲染线程。不过对于我们的内核来说，tick 回调方案实现更简单，因为不需要额外的线程调度和同步机制。</p><h2 id="第三步——gui-启动的两阶段初始化" tabindex="-1">第三步——GUI 启动的两阶段初始化 <a class="header-anchor" href="#第三步——gui-启动的两阶段初始化" aria-label="Permalink to &quot;第三步——GUI 启动的两阶段初始化&quot;">​</a></h2><p>GUI 的初始化分成了两个阶段，因为各组件的依赖顺序不同。</p><p>第一阶段在 kernel_main 里完成。这个阶段在中断开启之前运行。我们创建 static Canvas 对象（避免栈溢出——Canvas 持有大块堆内存），调用 gui_init 初始化窗口管理器，渲染 demo 画面（深色背景 + 10 个随机色矩形 + &quot;Cinux GUI&quot; 标题文字），然后 unmask IRQ12（鼠标中断），为后续鼠标初始化做准备。demo 画面在窗口管理器接管之前闪一下，给用户一个&quot;从 Canvas 到 WM&quot;的过渡效果。</p><p>第二阶段在 kernel_init_thread 里完成。这个阶段在调度器启动之后运行。我们调用 gui_start，它做三件事：初始化鼠标驱动（需要中断已开启，要等 ACK）、设置屏幕边界、创建三个测试窗口（&quot;Window 1&quot; 320x200、&quot;Window 2&quot; 280x180、&quot;Window 3&quot; 250x160），最后注册 PIT tick 回调（gui_tick_callback）驱动事件循环。三个窗口按照创建顺序依次偏移 (30px, 30px)，避免完全重叠。</p><p>这种两阶段设计不是因为代码组织好看，而是因为真实的硬件依赖——Canvas 需要 Framebuffer 就绪，鼠标驱动需要中断开启，tick 回调需要调度器运行。每一层都有自己的前置条件，没法在同一个地方全部搞定。</p><h2 id="第四步——光标渲染与-qemu-双光标问题" tabindex="-1">第四步——光标渲染与 QEMU 双光标问题 <a class="header-anchor" href="#第四步——光标渲染与-qemu-双光标问题" aria-label="Permalink to &quot;第四步——光标渲染与 QEMU 双光标问题&quot;">​</a></h2><p>光标渲染在 composite 的最后阶段完成。我们用一个 16x16 的 uint16_t 数组编码经典箭头形状——每行的 bit 15 到 bit 0 对应最左到最右的像素。值为 1 的像素画白色（0xFFFFFF），四周画一圈黑色（0x000000）描边增加可见度。光标位置从 Mouse::x() 和 Mouse::y() 获取。</p><p>这里有一个有趣的问题：在 QEMU VNC 窗口里，你会看到两个光标。一个是我们画的白色箭头，一个是 QEMU 自带的圆点覆盖层。两者之间有一个固定偏移，方向取决于我们的初始光标位置。</p><p>原因在于 PS/2 协议只报告相对位移。QEMU 的 VNC 光标使用宿主机的绝对坐标，而我们的内核从初始位置（0, 0）开始累积 delta。两者的&quot;原点&quot;不同，所以永远有一个固定偏移。这不是代码 bug，而是 PS/2 鼠标在虚拟化环境下的固有缺陷。</p><p>临时解决方案是在 QEMU 启动参数里加 <code>-usb -device usb-tablet</code>。USB tablet 设备通过 HID Absolute Pointer 报告绝对坐标，QEMU 用它来同步宿主和客户机光标。不过我们的内核还没有 USB 驱动，这个参数主要是让 QEMU VNC 层面处理光标同步。</p><p>长期方案有两个方向：一是实现 USB HID 驱动读取 USB tablet 的绝对坐标，二是实现 PS/2 鼠标的 Intellimouse 扩展获取滚轮支持（但对绝对坐标没有帮助）。USB HID 是更彻底的解决方案。</p><h2 id="收尾" tabindex="-1">收尾 <a class="header-anchor" href="#收尾" aria-label="Permalink to &quot;收尾&quot;">​</a></h2><p>到这里，我们的 GUI 子系统已经完整了——PS/2 鼠标驱动收集输入，事件队列统一分发，窗口管理器处理交互，桌面合成呈现画面，PIT tick 驱动整个事件循环。<code>make run</code> 后你应该能看到深色桌面、彩色矩形装饰、白色箭头光标跟随鼠标移动。</p><p>如果一切正常，下一阶段我们会在这个基础上添加桌面图标、终端窗口和 shell 集成，让 GUI 变成一个真正能用的桌面环境。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>OSDev Wiki: <a href="https://wiki.osdev.org/PS/2_Mouse" target="_blank" rel="noreferrer">PS/2 Mouse</a> — streaming mode, ACK protocol</li><li>OSDev Wiki: <a href="https://wiki.osdev.org/I8042_PS/2_Controller" target="_blank" rel="noreferrer">I8042 PS/2 Controller</a> — auxiliary port commands</li><li>QEMU Mouse Cursor Offset: <a href="https://torgeir.dev/2024/02/qemu-mouse-cursor-offset/" target="_blank" rel="noreferrer">torgeir.dev</a> — PS/2 vs VNC cursor offset analysis</li><li>SerenityOS: <a href="https://github.com/SerenityOS/serenity/tree/master/Userland/Services/WindowServer" target="_blank" rel="noreferrer">WindowServer Compositor</a> — event loop + damage tracking</li></ul>`,36)])])}const c=s(n,[["render",l]]);export{u as __pageData,c as default};
