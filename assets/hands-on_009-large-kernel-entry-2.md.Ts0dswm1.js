import{c as i,Q as a,j as e,m as n}from"./chunks/framework.D9IbVcwA.js";const k=JSON.parse('{"title":"009-large-kernel-entry-2 · 大内核入口","description":"","frontmatter":{"title":"009-large-kernel-entry-2 · 大内核入口"},"headers":[],"relativePath":"hands-on/009-large-kernel-entry-2.md","filePath":"hands-on/009-large-kernel-entry-2.md","lastUpdated":1779686691000}'),t={name:"hands-on/009-large-kernel-entry-2.md"};function p(d,s,o,r,l,c){return a(),e("div",null,[...s[0]||(s[0]=[n(`<h1 id="_009-2-大内核基础设施-—-i-o-端口原语-串口驱动-kprintf" tabindex="-1">009-2 大内核基础设施 — I/O 端口原语 + 串口驱动 + kprintf <a class="header-anchor" href="#_009-2-大内核基础设施-—-i-o-端口原语-串口驱动-kprintf" aria-label="Permalink to &quot;009-2 大内核基础设施 — I/O 端口原语 + 串口驱动 + kprintf&quot;">​</a></h1><h2 id="章节导语" tabindex="-1">章节导语 <a class="header-anchor" href="#章节导语" aria-label="Permalink to &quot;章节导语&quot;">​</a></h2><p>上一章我们把大内核的骨架搭好了——链接脚本把代码安排到了 higher-half 虚拟地址，boot.S 负责从 mini kernel 手里接管 CPU，crt_stub.cpp 补齐了 C++ 运行时。现在大内核能启动了，但它还是个哑巴——没有输出能力，你根本不知道它跑到哪一步了。</p><p>这一章我们要给大内核装上&quot;嘴巴&quot;。具体来说，我们需要实现三个层次的抽象：最底层是 x86 I/O 端口原语（io.hpp），封装 <code>in/out</code> 指令；中间层是 UART 16550 串口驱动（serial.hpp/cpp），提供按字符和按字符串的输出能力；最上层是 kprintf 格式化输出引擎（kprintf.hpp/cpp），让内核代码能用类似 printf 的方式打印调试信息。完成本章后，大内核就能通过串口输出格式化的诊断信息了——这是后续所有调试工作的基础。</p><p>本章的前置知识是上一章（009-1）的大内核启动流程，因为 kprintf 会在 kernel_main 里被调用。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="x86-i-o-端口编程" tabindex="-1">x86 I/O 端口编程 <a class="header-anchor" href="#x86-i-o-端口编程" aria-label="Permalink to &quot;x86 I/O 端口编程&quot;">​</a></h3><p>x86 架构有两条独立的地址空间：一条是内存地址空间（通过 <code>mov</code> 等指令访问），另一条是 I/O 地址空间（通过 <code>in</code> 和 <code>out</code> 指令访问）。I/O 端口是一个 16 位地址，对应硬件设备的一个寄存器。串口控制器（UART 16550）的寄存器就是通过 I/O 端口访问的——COM1 的基地址是 <code>0x3F8</code>，各个寄存器从基地址开始依次偏移。</p><p><code>in</code> 指令从指定端口读取数据，<code>out</code> 指令向指定端口写入数据。这两个指令支持 byte（8位）、word（16位）、dword（32位）三种宽度，分别对应 <code>inb/outb</code>、<code>inw/outw</code>、<code>inl/outl</code>。在 GCC 内联汇编里，端口编号放在 <code>%dx</code> 寄存器（约束 <code>&quot;Nd&quot;</code>），数据通过 <code>%al/ax/eax</code> 传递（约束 <code>&quot;a&quot;</code>）。</p><p>还有一个细节：I/O 指令是同步操作，但编译器不知道这一点。如果不加 <code>&quot;memory&quot;</code> clobber，编译器可能会把 I/O 操作前后的内存访问重排。所以所有 I/O 函数都加了 <code>&quot;memory&quot;</code> clobber，充当编译器屏障。</p><h3 id="uart-16550-串口控制器" tabindex="-1">UART 16550 串口控制器 <a class="header-anchor" href="#uart-16550-串口控制器" aria-label="Permalink to &quot;UART 16550 串口控制器&quot;">​</a></h3><p>PC 兼容机上最常见的串口控制器是 16550 UART。它有 8 个寄存器（通过 I/O 端口偏移 0-7 访问），其中我们最关心的几个是：THR（Transmit Holding Register，偏移 0，写入数据发送字节）、LCR（Line Control Register，偏移 3，配置数据位/校验/停止位）、LSR（Line Status Register，偏移 5，查询发送/接收状态）。</p><p>发送一个字节的过程很简单：自旋等待 LSR 的 bit 5（THRE，Transmit Holding Register Empty）变为 1，表示 THR 已空闲可以写入，然后往 THR 写入要发送的字节。这就是所谓的&quot;轮询模式&quot;——不需要中断，不需要 DMA，适合早期内核的简单输出需求。</p><p>初始化序列大致是：禁用中断（IER = 0）、设置 8N1 格式（LCR = 0x03）、启用 FIFO（FCR = 0xC7）、设置 MCR（MCR = 0x03，DTR + RTS）。QEMU 的虚拟 UART 默认就是 115200 波特率，所以我们不需要编程除数锁存器（Divisor Latch）。</p><h3 id="kprintf-的设计哲学" tabindex="-1">kprintf 的设计哲学 <a class="header-anchor" href="#kprintf-的设计哲学" aria-label="Permalink to &quot;kprintf 的设计哲学&quot;">​</a></h3><p>内核的 printf 和用户态的 printf 有本质区别：没有堆、没有浮点、不需要线程安全（内核启动时是单核单线程）。所以我们只需要一个简洁的格式化引擎，支持最常用的格式化占位符就够了。</p><p>一个有意思的设计选择是把格式化引擎模板化：核心函数接受一个泛型的字符输出函数（<code>OutputFn</code>），这样同一个引擎可以输出到串口、QEMU debug console（0xE9 端口）、甚至测试中的 mock buffer，而不需要写多套代码。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-实现-i-o-端口原语-io-hpp" tabindex="-1">Step 1: 实现 I/O 端口原语 (io.hpp) <a class="header-anchor" href="#step-1-实现-i-o-端口原语-io-hpp" aria-label="Permalink to &quot;Step 1: 实现 I/O 端口原语 (io.hpp)&quot;">​</a></h3><p><strong>目标</strong>：封装 x86 的 <code>in/out</code> 指令，提供类型安全的 I/O 端口访问接口。</p><p><strong>设计思路</strong>：</p><p>创建一个头文件 <code>kernel/arch/x86_64/io.hpp</code>，在命名空间 <code>cinux::io</code> 中定义一组 inline 函数。每种宽度一个读函数和一个写函数：<code>io_inb/io_outb</code>（8位）、<code>io_inw/io_outw</code>（16位）、<code>io_inl/io_outl</code>（32位）。再加一个 <code>io_wait()</code> 函数，往端口 0x80 写入任意值产生约 1 微秒延迟——这在某些 ISA 设备（如 PIT、PIC）的操作之间是必要的。</p><p><strong>实现约束</strong>：</p><p>所有函数使用 GCC 内联汇编，端口参数类型为 <code>uint16_t</code>（I/O 端口地址空间是 16 位），数据类型对应宽度。输出操作 (<code>in*</code>) 使用 <code>&quot;=a&quot;</code> 约束把结果从 <code>%al/ax/eax</code> 取出，输入操作 (<code>out*</code>) 使用 <code>&quot;a&quot;</code> 约束把数据放入 <code>%al/ax/eax</code>、<code>&quot;Nd&quot;</code> 约束把端口放入 <code>%dx</code>。所有函数都加 <code>&quot;memory&quot;</code> clobber 作为编译器屏障。</p><p><strong>踩坑预警</strong>：AT&amp;T 语法中 <code>inb</code> 的操作数顺序是 <code>inb %dx, %al</code>（源在前，目的在后），<code>outb</code> 是 <code>outb %al, %dx</code>。别搞反了。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 构建，确认编译通过</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：编译成功，无汇编错误</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="step-2-实现-uart-16550-串口驱动-serial-hpp-cpp" tabindex="-1">Step 2: 实现 UART 16550 串口驱动 (serial.hpp/cpp) <a class="header-anchor" href="#step-2-实现-uart-16550-串口驱动-serial-hpp-cpp" aria-label="Permalink to &quot;Step 2: 实现 UART 16550 串口驱动 (serial.hpp/cpp)&quot;">​</a></h3><p><strong>目标</strong>：封装 UART 硬件操作，提供按字符和按字符串的串口输出接口。</p><p><strong>设计思路</strong>：</p><p>创建 <code>kernel/drivers/serial.hpp</code> 和 <code>kernel/drivers/serial.cpp</code>，在命名空间 <code>cinux::drivers</code> 中实现一个 <code>Serial</code> 类。</p><p><strong>数据结构描述</strong>：</p><p><code>Serial</code> 类持有一个 <code>uint16_t base_port_</code> 成员，表示 UART 的基地址。同时定义一组编译期常量：COM1-COM4 的端口地址（0x3F8、0x2F8、0x03E8、0x02E8），UART 寄存器偏移（RBR/THR=0、IER=1、FCR=2、LCR=3、MCR=4、LSR=5），以及 LSR 的关键位掩码（RX_READY=0x01、TX_READY=0x20）。</p><p><strong>接口描述</strong>：</p><p>构造函数接受一个端口号并存储到 <code>base_port_</code>，但不配置硬件。<code>init()</code> 函数做实际的 UART 配置：先禁中断（IER=0），设 8N1 格式（LCR=0x03），启 FIFO（FCR=0xC7），设 MCR（MCR=0x03），然后空读一次 LSR 清除可能的状态。<code>putc(char c)</code> 是核心输出函数：自旋等待 <code>LSR &amp; TX_READY</code> 非 0，然后往 THR 写入字符。<code>puts(const char* s)</code> 遍历字符串逐个 putc，遇到 <code>\\n</code> 时先输出 <code>\\r</code>。<code>is_ready()</code> 返回 THR 是否为空。</p><p><strong>踩坑预警</strong>：<code>putc</code> 的等待循环里建议加 <code>__asm__ volatile(&quot;pause&quot;)</code> 提示 CPU 这是一个 spin-wait，可以降低功耗和总线压力。<code>\\n</code> → <code>\\r\\n</code> 转换是必须的——串口终端通常需要 <code>\\r\\n</code> 才能正确换行，只发 <code>\\n</code> 的话光标会跳到下一行但不会回到行首。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 配合 kprintf 构建，运行后检查串口输出</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：看到 [BIG] Big kernel running @ 0x1000000</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="step-3-实现-kprintf-格式化引擎-kprintf-hpp-cpp" tabindex="-1">Step 3: 实现 kprintf 格式化引擎 (kprintf.hpp/cpp) <a class="header-anchor" href="#step-3-实现-kprintf-格式化引擎-kprintf-hpp-cpp" aria-label="Permalink to &quot;Step 3: 实现 kprintf 格式化引擎 (kprintf.hpp/cpp)&quot;">​</a></h3><p><strong>目标</strong>：为内核提供类似 printf 的格式化输出能力，支持常用格式化占位符。</p><p><strong>设计思路</strong>：</p><p>创建 <code>kernel/lib/kprintf.hpp</code> 和 <code>kernel/lib/kprintf.cpp</code>，在命名空间 <code>cinux::lib</code> 中实现。内部结构分三层：文件局部的数字格式化辅助函数（<code>format_decimal</code>、<code>format_hex</code>）、模板化的格式化引擎（<code>vkprintf_impl</code>）、以及公开的包装函数（<code>kprintf</code>、<code>kvprintf</code>、<code>kpanic</code>）。</p><p><strong>辅助函数描述</strong>：</p><p><code>format_decimal(int64_t value, char* buffer, int buffer_size)</code> 把有符号 64 位整数格式化为十进制字符串，处理负号和 INT64_MIN 特殊情况。<code>format_hex(uint64_t value, char* buffer, int buffer_size, bool lowercase)</code> 把无符号 64 位整数格式化为十六进制字符串，通过 <code>lowercase</code> 参数控制 a-f 还是 A-F。两个函数都返回写入的字符数（不含 NUL）。</p><p><strong>格式化引擎描述</strong>：</p><p><code>vkprintf_impl&lt;OutputFn&gt;(OutputFn&amp;&amp; putc_fn, const char* fmt, va_list args)</code> 是核心。它逐字符扫描格式字符串，遇到非 <code>%</code> 字符直接输出。遇到 <code>%</code> 后解析可选的 <code>0</code> 前缀（零填充标志）和宽度数字，然后根据类型字符处理。支持类型：<code>%</code>（字面百分号）、<code>c</code>（字符）、<code>s</code>（字符串，NULL 输出 &quot;(null)&quot;）、<code>d</code>（有符号十进制）、<code>u</code>（无符号十进制）、<code>x</code>（小写十六进制）、<code>X</code>（大写十六进制）、<code>p</code>（指针，输出 &quot;0x&quot; + 16 位十六进制）。宽度和零填充只对数字格式有效。</p><p><strong>全局串口实例</strong>：</p><p>文件局部持有一个 <code>static Serial g_serial(SERIAL_COM1)</code> 单例。<code>kprintf_init()</code> 调用 <code>g_serial.init()</code> 初始化。<code>kprintf</code> 用 lambda 捕获 <code>g_serial.putc</code> 作为 OutputFn 传给 <code>vkprintf_impl</code>。</p><p><strong>kpanic 描述</strong>：</p><p><code>kpanic</code> 的签名带 <code>[[noreturn]]</code> 属性，先格式化输出到串口，然后进入 <code>cli; hlt</code> 死循环。这是内核的&quot;不可恢复错误&quot;处理——调用 kpanic 就意味着内核承认自己挂了。</p><p><strong>踩坑预警</strong>：<code>%p</code> 的行为要特别注意——它输出 &quot;0x&quot; 前缀后，先把十六进制字符串格式化到 buffer，然后补前导零到 16 位，最后输出 buffer 内容。顺序不能错：先输出前导零，再输出实际数字。另外，<code>%d</code> 和 <code>%u</code> 的 <code>va_arg</code> 类型分别是 <code>int</code> 和 <code>unsigned int</code>，而 <code>%x</code> 和 <code>%X</code> 的类型是 <code>uint64_t</code>——这里的大小差异是有意的，<code>%x</code> 需要能格式化完整的 64 位地址。</p><p><strong>实现约束</strong>：格式化引擎是模板函数，定义在 .cpp 文件中——因为我们只在自己文件内部使用它，不需要在头文件中暴露。模板参数 <code>OutputFn</code> 是一个可调用对象（lambda、函数指针都行），接受一个 <code>char</code> 参数。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行内核，确认格式化输出正确</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：看到 [BIG] Big kernel running @ 0x1000000</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 格式化输出的 0x1000000 就是 %p 格式化的结果（如果用 %p 的话）</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行 kprintf 的 host 单元测试</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --test-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -R</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_kprintf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -V</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：所有 kprintf 格式化测试通过（mock Serial::putc，验证输出字符串）</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><h3 id="step-4-编写-kprintf-的-host-端单元测试" tabindex="-1">Step 4: 编写 kprintf 的 host 端单元测试 <a class="header-anchor" href="#step-4-编写-kprintf-的-host-端单元测试" aria-label="Permalink to &quot;Step 4: 编写 kprintf 的 host 端单元测试&quot;">​</a></h3><p><strong>目标</strong>：在 host 端验证 kprintf 的格式化逻辑，不需要真实的串口硬件。</p><p><strong>设计思路</strong>：</p><p>创建 <code>tests/unit/test_kprintf.cpp</code>，mock <code>Serial::putc</code> 函数，把输出捕获到一个 buffer 中，然后验证各种格式化占位符的输出是否正确。</p><p><strong>测试用例描述</strong>：</p><p>需要覆盖的测试场景至少包括：纯字符串输出、<code>%d</code> 正数和负数、<code>%d</code> 零和 INT_MIN、<code>%u</code> 无符号整数、<code>%x</code> 小写十六进制、<code>%X</code> 大写十六进制、<code>%p</code> 指针格式（16 位 + &quot;0x&quot; 前缀）、<code>%s</code> 字符串和 NULL 字符串、<code>%c</code> 字符、<code>%%</code> 百分号转义、<code>%0Nd</code> 零填充宽度。每个测试用例 mock 掉 Serial::putc，收集输出字符到 buffer，用字符串比较验证结果。</p><p><strong>验证</strong>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -R</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_kprintf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -V</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：所有测试 PASS</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><hr><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 完整构建</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行内核</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期串口输出包含：</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [MINI] ... (mini kernel 加载日志)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [BIG] Big kernel running @ 0x1000000</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行 host 单元测试</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -R</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_kprintf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -V</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><hr><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><h3 id="常见-bug-1-kprintf-输出为空" tabindex="-1">常见 Bug 1: kprintf 输出为空 <a class="header-anchor" href="#常见-bug-1-kprintf-输出为空" aria-label="Permalink to &quot;常见 Bug 1: kprintf 输出为空&quot;">​</a></h3><p>检查是否调用了 <code>kprintf_init()</code>。大内核的 kprintf 持有自己的 Serial 单例，必须显式初始化。如果忘调 init()，串口硬件处于未配置状态，putc 里的 LSR 轮询可能永远不返回。</p><h3 id="常见-bug-2-p-输出只有部分十六进制位" tabindex="-1">常见 Bug 2: %p 输出只有部分十六进制位 <a class="header-anchor" href="#常见-bug-2-p-输出只有部分十六进制位" aria-label="Permalink to &quot;常见 Bug 2: %p 输出只有部分十六进制位&quot;">​</a></h3><p>检查 <code>%p</code> 的实现是否正确地先格式化到 buffer，然后补零到 16 位，最后输出。如果先输出了 buffer 内容再补零，数字的顺序就反了。</p><h3 id="常见-bug-3-d-对负数输出不正确" tabindex="-1">常见 Bug 3: %d 对负数输出不正确 <a class="header-anchor" href="#常见-bug-3-d-对负数输出不正确" aria-label="Permalink to &quot;常见 Bug 3: %d 对负数输出不正确&quot;">​</a></h3><p>INT64_MIN 的特殊处理是必要的——直接取负会溢出（<code>-INT64_MIN</code> 是未定义行为）。需要硬编码字符串 <code>&quot;-9223372036854775808&quot;</code>。</p><h3 id="串口调试技巧" tabindex="-1">串口调试技巧 <a class="header-anchor" href="#串口调试技巧" aria-label="Permalink to &quot;串口调试技巧&quot;">​</a></h3><p>如果 QEMU 里看不到串口输出，确认启动参数里有 <code>-serial stdio</code>。也可以在 QEMU monitor 里用 <code>info serial</code> 查看串口状态。如果需要同时看 debug console 输出（0xE9 端口），检查启动参数里的 <code>-debugcon file:debug.log</code>。</p><hr><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>要点</th></tr></thead><tbody><tr><td>io.hpp</td><td>内联汇编封装 in/out，byte/word/dword 三种宽度，&quot;memory&quot; clobber</td></tr><tr><td>io_wait()</td><td>写端口 0x80，~1us 延迟</td></tr><tr><td>Serial 类</td><td>轮询模式，构造后调 init()，putc 自旋等 THR 空</td></tr><tr><td>UART 初始化</td><td>IER=0, LCR=0x03(8N1), FCR=0xC7, MCR=0x03</td></tr><tr><td>kprintf</td><td>模板化引擎 + OutputFn，支持 %d %u %x %X %s %p %c %%</td></tr><tr><td>%p</td><td>输出 &quot;0x&quot; + 16 位十六进制，前导零补齐</td></tr><tr><td>kpanic</td><td>[[noreturn]]，输出后 cli;hlt 死循环</td></tr><tr><td>单元测试</td><td>mock Serial::putc，buffer 收集输出字符串验证</td></tr></tbody></table>`,80)])])}const u=i(t,[["render",p]]);export{k as __pageData,u as default};
