import{c as a,Q as s,j as t,m as n}from"./chunks/framework.iD28w_5a.js";const h=JSON.parse('{"title":"028d-sync-safety-3 · 同步安全","description":"","frontmatter":{"title":"028d-sync-safety-3 · 同步安全"},"headers":[],"relativePath":"hands-on/028d-sync-safety-3.md","filePath":"hands-on/028d-sync-safety-3.md","lastUpdated":1779686691000}'),r={name:"hands-on/028d-sync-safety-3.md"};function o(d,e,l,i,p,c){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="_028d-3-并发测试——三层验证策略" tabindex="-1">028d-3: 并发测试——三层验证策略 <a class="header-anchor" href="#_028d-3-并发测试——三层验证策略" aria-label="Permalink to &quot;028d-3: 并发测试——三层验证策略&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>锁加完了，但怎么确认它们真的管用？并发 bug 的可怕之处在于它们是概率性的——在测试环境里可能跑一万次都不出问题，一上线就在用户的机器上炸了。所以我们需要系统化的测试策略，从多个角度验证同步机制的正确性。</p><p>这一章我们会搭建三层测试体系：Host 端用 <code>std::thread</code> 做真正的多线程压力测试，QEMU 内核态测试验证 RAII 守卫的正确性，以及一个生产内核的压力测试——在真实的抢占式多任务和中断环境下锤炼 PMM、Heap 和原子计数器。完成这一章后，我们可以有信心地说：内核的同步保护在真实并发环境下是正确的。</p><p>前置要求：前两章的所有实现必须完成并编译通过。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="为什么需要三层测试" tabindex="-1">为什么需要三层测试 <a class="header-anchor" href="#为什么需要三层测试" aria-label="Permalink to &quot;为什么需要三层测试&quot;">​</a></h3><p>你可能会问：既然有 Host 端的多线程测试，为什么还需要 QEMU 内核态测试？原因是 Host 端和内核态的环境差异非常大：</p><p>Host 端运行在 Linux（或 macOS/Windows）上，有完整的标准库和线程支持。我们可以用 <code>std::thread</code> 创建真正的并发线程，让多个线程同时操作同一个 Spinlock 保护的数据结构。这是验证锁正确性最直接的方式——如果锁有 bug，真正的并发会立刻暴露问题。但 Host 端无法模拟 x86 中断机制——没有 <code>pushfq</code>/<code>cli</code>/<code>popfq</code>，没有时钟中断打断执行流。所以 IrqGuard 和 InterruptGuard 在 Host 端只能用 stub 实现。</p><p>QEMU 内核态测试弥补了这个缺陷。它在真实的 x86 环境中运行，有真正的 IDT、时钟中断、cli/sti。我们可以直接测试 InterruptGuard 是否正确保存和恢复了 RFLAGS 的 IF 位，IrqGuard 是否在嵌套场景下正确工作。但它用的是 Cinux 自己的协作式调度器——虽然也有上下文切换，但不是真正的抢占式多线程。</p><p>生产内核压力测试则是最接近真实使用场景的——它启动 Cinux 的完整调度器，创建多个内核线程，在时钟中断驱动的抢占式多任务环境下并发操作 PMM 和 Heap。如果这个测试通过了，我们有理由相信内核在真实负载下是安全的。</p><h3 id="压力测试的设计原则" tabindex="-1">压力测试的设计原则 <a class="header-anchor" href="#压力测试的设计原则" aria-label="Permalink to &quot;压力测试的设计原则&quot;">​</a></h3><p>一个好的并发压力测试需要满足几个条件：</p><p>第一，操作次数要足够多。单次操作暴露 bug 的概率很小，跑几千次才能累积到可观测的概率。我们的 PMM 压力测试每个线程做 200 次 alloc/free 循环，4 个线程共 800 次。</p><p>第二，共享状态要可验证。纯跑不崩溃不等于正确——我们需要检查操作计数是否精确匹配预期值。比如 4 个线程各做 200 次 PMM alloc+free，那总操作数必须精确等于 800，少一次就说明有一次操作被吞了（竞争导致丢失更新）。</p><p>第三，测试结束后要等待所有线程完成。由于是并发执行，主线程不能假设子线程立刻完成。我们用原子计数器 <code>threads_done</code> 让每个线程在退出前递增，主线程在 <code>while</code> 循环中 yield 等待，直到所有线程都完成。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-host-端-spinlock-压力测试" tabindex="-1">Step 1: Host 端 Spinlock 压力测试 <a class="header-anchor" href="#step-1-host-端-spinlock-压力测试" aria-label="Permalink to &quot;Step 1: Host 端 Spinlock 压力测试&quot;">​</a></h3><p><strong>目标</strong>: 用 <code>std::thread</code> 验证 Spinlock 在真正的多核并发下的正确性。</p><p><strong>设计思路</strong>: Host 端需要自己的 Spinlock 实现（用 <code>std::atomic&lt;bool&gt;</code> 的 <code>exchange</code> 和 <code>store</code> 替代 x86 内联汇编）。测试方案很简单：创建 8 个线程，每个线程对一个共享计数器做 10000 次 increment——通过 Spinlock Guard 保护。如果最终计数器值精确等于 80000，说明锁提供了正确的互斥。</p><p><strong>实现约束</strong>:</p><ul><li>Host 端 Spinlock：<code>exchange(true, acquire)</code> 做 test-and-set，<code>store(false, release)</code> 做 clear</li><li>Host 端 IrqGuard：简化版，只调用 acquire/release，不涉及 x86 指令</li><li>Host 端 InterruptGuard：空操作（Host 没有中断概念）</li><li>辅助函数：创建 N 个线程各执行指定次数的 lambda，然后 join</li></ul><p><strong>踩坑预警</strong>: Host 端 Spinlock 的 <code>locked_</code> 必须是 <code>std::atomic&lt;bool&gt;</code> 而不是 <code>volatile bool</code>。<code>volatile</code> 不保证原子性，在高并发下计数器会出错。</p><p><strong>验证</strong>:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cmake --build build --target test_sync_concurrent_host</span></span>
<span class="line"><span>./build/test/unit/test_sync_concurrent</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>预期输出所有测试 PASS，计数器值精确匹配。</p><h3 id="step-2-qemu-内核态-raii-守卫测试" tabindex="-1">Step 2: QEMU 内核态 RAII 守卫测试 <a class="header-anchor" href="#step-2-qemu-内核态-raii-守卫测试" aria-label="Permalink to &quot;Step 2: QEMU 内核态 RAII 守卫测试&quot;">​</a></h3><p><strong>目标</strong>: 在真实 x86 环境中验证 InterruptGuard 和 IrqGuard 的正确性。</p><p><strong>设计思路</strong>: 测试分为四组，从简单到复杂。</p><p>第一组：InterruptGuard 基本功能。读取 RFLAGS，创建 Guard，读取 RFLAGS 确认 IF=0，销毁 Guard，读取 RFLAGS 确认 IF 恢复。</p><p>第二组：InterruptGuard 嵌套。创建外层 Guard，确认 IF=0；创建内层 Guard，确认 IF=0；销毁内层，确认 IF 仍然是 0（不是 1！）；销毁外层，确认 IF 恢复。这一组测试至关重要——如果内层销毁后 IF 变成了 1，说明嵌套不安全，在生产中会导致中断在临界区内被意外打开。</p><p>第三组：IrqGuard 基本操作。创建 IrqGuard，确认 Spinlock 被持有且 IF=0；销毁后确认 Spinlock 可被再次获取。</p><p>第四组：多任务 Spinlock 互斥。模拟 3 个 Task（通过设置 <code>g_per_cpu.current</code>），每个 Task 对一个共享计数器做若干次 increment（通过 Spinlock guard 保护），最终验证计数器值。</p><p><strong>实现约束</strong>:</p><ul><li>使用 TEST_ASSERT_EQ / TEST_ASSERT_FALSE 等测试宏</li><li>RFLAGS 的 IF 位在第 9 位（mask 0x200）</li><li>读取 RFLAGS 用 <code>pushfq; popq</code> 内联汇编</li><li>多任务测试中手动切换 <code>g_per_cpu.current</code> 来模拟不同 Task 上下文</li></ul><p><strong>踩坑预警</strong>: 第四组的&quot;多任务&quot;测试不是真正的并发——它只是模拟不同 Task 的上下文来测试 Spinlock 的互斥功能。真正的并发测试在生产内核压力测试中完成。</p><p><strong>验证</strong>:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cmake --build build --target big_kernel_test</span></span>
<span class="line"><span>qemu-system-x86_64 -cdrom build/big_kernel_test.iso -serial stdio -display none</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>在串口输出中找到 &quot;Sync Concurrent Tests (028d)&quot; 部分，确认所有测试 PASS。</p><h3 id="step-3-生产内核压力测试" tabindex="-1">Step 3: 生产内核压力测试 <a class="header-anchor" href="#step-3-生产内核压力测试" aria-label="Permalink to &quot;Step 3: 生产内核压力测试&quot;">​</a></h3><p><strong>目标</strong>: 在真实的抢占式多任务环境下验证 PMM、Heap 和原子计数器的线程安全性。</p><p><strong>设计思路</strong>: 这个测试替代了原来 kernel_main 最后调用的 <code>launch_first_user</code>——它先运行压力测试，测试通过后再启动 shell。</p><p>测试创建一个 &quot;boot continuation&quot; Task 和 N 个 &quot;stress&quot; Task。每个 stress Task 做三件事：对 PMM 做 alloc_page + free_page 循环（200 次），对 Heap 做 alloc + free 循环（200 次），对一个共享原子计数器做 1000 次 fetch_add。完成后递增 <code>threads_done</code> 原子计数器并调用 <code>Scheduler::exit_current()</code> 退出。</p><p>boot continuation Task 在循环中 yield，等待所有 stress Task 完成（<code>threads_done.load(acquire) &gt;= NUM_THREADS</code>）。完成后比较实际操作数和预期值，打印 PASS/FAIL 结果。</p><p><strong>实现约束</strong>:</p><ul><li>NUM_THREADS = 4，PMM_OPS = 200，HEAP_OPS = 200，原子计数器 OPS = 1000</li><li>共享计数器全部使用 <code>std::atomic</code></li><li><code>threads_done</code> 使用 <code>MemoryOrder::Release</code>（store）和 <code>MemoryOrder::Acquire</code>（load）建立同步关系</li><li>其他计数器使用 <code>MemoryOrder::Relaxed</code>（只关心原子性）</li><li>测试入口是 <code>run_concurrent_stress()</code>，在 kernel_main 中替代 <code>launch_first_user</code></li><li>测试完成后调用 <code>launch_first_user</code> 启动 shell</li></ul><p><strong>踩坑预警</strong>: 有两个容易忽略的细节。第一个：<code>threads_done</code> 必须用 release/acquire 语义而不是 relaxed——我们要保证 stress Task 中所有的写操作（包括 PMM/Heap 的状态修改）在 boot continuation Task 观察到 <code>threads_done</code> 增长之前对它可见。第二个：<code>run_concurrent_stress</code> 必须重新初始化 Scheduler（<code>Scheduler::init()</code>），因为在这个测试之前可能已有过调度器操作。</p><p><strong>验证</strong>:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cmake --build build --target big_kernel</span></span>
<span class="line"><span>qemu-system-x86_64 -cdrom build/big_kernel.iso -serial stdio -display none</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>在串口输出中找到 &quot;[STRESS]&quot; 开头的行，确认：</p><ul><li>PMM alloc/free ops: expected=800 actual=800 PASS</li><li>Heap alloc/free ops: expected=800 actual=800 PASS</li><li>Atomic counter: expected=4000 actual=4000 PASS</li><li>ALL PASSED -- launching shell</li></ul><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>完整的构建和测试流程：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Host 端并发测试</span></span>
<span class="line"><span>cmake --build build --target test_sync_concurrent_host</span></span>
<span class="line"><span>./build/test/unit/test_sync_concurrent</span></span>
<span class="line"><span></span></span>
<span class="line"><span># QEMU 内核态测试（包含 028d 新增测试）</span></span>
<span class="line"><span>cmake --build build --target big_kernel_test</span></span>
<span class="line"><span>qemu-system-x86_64 -cdrom build/big_kernel_test.iso -serial stdio -display none</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 生产内核压力测试</span></span>
<span class="line"><span>cmake --build build --target big_kernel</span></span>
<span class="line"><span>qemu-system-x86_64 -cdrom build/big_kernel.iso -serial stdio -display none</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>问题 1: Stress test 计数器不匹配</strong> 如果 PMM 或 Heap 的操作计数不对，说明存在竞争——某个操作被两个线程同时执行导致状态损坏。排查方法：减少线程数到 1，确认单线程下计数正确；然后逐步增加线程数，找到出问题的临界点。同时检查对应的锁是否真的在每个操作路径上被获取。</p><p><strong>问题 2: Stress test 永远等不到 threads_done</strong> 这说明某个 stress Task 没有正常退出。最可能的原因是 PMM 或 Heap 操作在持锁状态下崩溃（比如空指针解引用），导致 Task 被 kill 而没有执行到 <code>threads_done.fetch_add</code>。在 stress_thread_entry 的每个操作中加入 kprintf 来定位崩溃位置。</p><p><strong>问题 3: Host 端测试偶尔失败</strong> 如果在 CI 中 Host 端测试偶发失败，可能是 <code>ITERS</code> 设得太小——并发 bug 的触发概率与操作次数相关。把 ITERS 增大一个数量级（比如从 10000 增到 100000），如果失败频率显著增加，说明确实存在竞争 bug。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>测试层</th><th>环境</th><th>验证内容</th><th>关键技术</th></tr></thead><tbody><tr><td>Host 端</td><td>Linux + std::thread</td><td>真正多核并发下的 Spinlock 互斥</td><td>std::atomic, std::thread</td></tr><tr><td>QEMU 内核态</td><td>x86 + IDT</td><td>InterruptGuard/IrqGuard 的 RFLAGS 操作</td><td>pushfq/popfq, IF 位检查</td></tr><tr><td>生产内核</td><td>抢占式多任务 + 中断</td><td>PMM/Heap/atomic 在真实负载下的正确性</td><td>Scheduler::yield, atomic release/acquire</td></tr></tbody></table><p>到这里，028d 的同步安全改造全部完成。我们有了自旋锁、RAII 守卫、三层分级加锁策略、以及从 Host 到 QEMU 到生产内核的完整测试体系。内核现在可以安全地在抢占式多任务环境下运行了。</p>`,61)])])}const b=a(r,[["render",o]]);export{h as __pageData,b as default};
