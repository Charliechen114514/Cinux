import{c as s,Q as a,j as i,m as n}from"./chunks/framework.iD28w_5a.js";const g=JSON.parse('{"title":"010-big-kernel-gdt-idt-1 · GDT/IDT 重构","description":"","frontmatter":{"title":"010-big-kernel-gdt-idt-1 · GDT/IDT 重构"},"headers":[],"relativePath":"hands-on/010-big-kernel-gdt-idt-1.md","filePath":"hands-on/010-big-kernel-gdt-idt-1.md","lastUpdated":1779686691000}'),t={name:"hands-on/010-big-kernel-gdt-idt-1.md"};function l(d,e,o,r,c,p){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="_010-1-hands-on-大内核-gdt-初始化——段描述符工厂与-lgdt" tabindex="-1">010-1 Hands-on: 大内核 GDT 初始化——段描述符工厂与 lgdt <a class="header-anchor" href="#_010-1-hands-on-大内核-gdt-初始化——段描述符工厂与-lgdt" aria-label="Permalink to &quot;010-1 Hands-on: 大内核 GDT 初始化——段描述符工厂与 lgdt&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一章（009）我们让大内核跑起来了——串口能输出、kprintf 能格式化打印、压力测试也通过了。但说实话，现在的大内核是个&quot;聋哑人&quot;：CPU 遇到除零、缺页、非法指令这些异常时，只会 triple fault 然后 QEMU 重启，连一行遗言都留不下来。开发内核最痛苦的事情莫过于此：你写了半天代码，跑起来直接黑屏重启，连出问题的地方都不知道。</p><p>这一章的前半部分，我们要给大内核建一个全新的 GDT（全局描述符表）。你可能会想——002 章在 Bootloader 里不是配过一次吗？007 章的 mini kernel 不是也配过吗？为什么大内核还要再来一次？原因在于大内核运行在 higher-half 虚拟地址空间，段寄存器和 IDT 中引用的段选择子必须指向大内核自己管理的内存里的 GDT 条目。而且在 x86_64 的 long mode 下，虽然硬件基本忽略了分段机制的基地址和限长，但 CS/DS/SS 这些段寄存器仍然需要指向有效的 GDT 条目，CPU 才能正常工作——尤其是中断处理环节，IDT 里的每个条目都有一个&quot;代码段选择子&quot;字段指向 GDT 中的 code segment 描述符，如果 GDT 没配好，中断一触发就 triple fault。</p><p>完成本节后，大内核会拥有一个包含 null/kernel code64/kernel data64/user code64/user data64/TSS 占位的完整 GDT，所有段寄存器都会指向正确的描述符。这个 GDT 是下一节 IDT 和中断处理的前置条件——没有正确的 GDT，IDT 里的代码段选择子就无处可指。</p><p>前置知识：上一章（009）的大内核启动流程和 kprintf 串口输出。</p><h3 id="关于-配了三次-gdt-的疑问" tabindex="-1">关于&quot;配了三次 GDT&quot;的疑问 <a class="header-anchor" href="#关于-配了三次-gdt-的疑问" aria-label="Permalink to &quot;关于&quot;配了三次 GDT&quot;的疑问&quot;">​</a></h3><p>如果你是从 002 章一路走过来的，你可能已经配过两次 GDT 了——002 章在 Bootloader 的 boot.S 里配了一次（实模式切保护模式），007 章在 mini kernel 的 entry.S 里又配了一次（从 mini kernel 自己管理的内存建立 GDT）。为什么大内核还要配第三次？</p><p>原因有三点。第一，Bootloader 的 GDT 在实模式切换保护模式时起作用，但它存放在 Bootloader 的代码段里（低地址区域），大内核运行起来后无法可靠地访问它。第二，mini kernel 的 GDT 也是放在低地址区域，而大内核运行在 higher-half 虚拟地址空间 <code>0xFFFFFFFF80000000</code>，GDT 的 GDTR.base 必须指向大内核能直接访问的地址。第三，大内核需要更完整的 GDT——包括用户态段和 TSS，这些是 mini kernel 不需要的。</p><p>所以每次&quot;换壳&quot;（从 Bootloader 到 mini kernel 到大内核）都需要重新配一次 GDT。好消息是，这是最后一次了——后续章节不会再次重建 GDT，只会在现有的 GDT 上修改（比如填充 TSS 的 RSP0 和 IST 字段）。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="为什么-long-mode-下还需要-gdt" tabindex="-1">为什么 long mode 下还需要 GDT？ <a class="header-anchor" href="#为什么-long-mode-下还需要-gdt" aria-label="Permalink to &quot;为什么 long mode 下还需要 GDT？&quot;">​</a></h3><p>在 x86_64 的 long mode 下，分段机制被大幅弱化了——Base 被硬件当作 0，Limit 被当作 <code>0xFFFFFFFFF</code>，基本上就是&quot;平坦模式&quot;的硬件实现。那为什么我们还需要配 GDT？原因有两个。</p><p>第一，CS/DS/SS 这些段寄存器内部缓存着&quot;隐藏部分&quot;（段描述符缓存），CPU 在做特权级检查、权限验证、长模式代码执行判断时，仍然会读取这些缓存。比如 CPU 看到一个 64 位代码段描述符（L=1, D=0），它才知道当前是 64 位模式；如果 CS 指向的描述符 L=0, D=1，CPU 就按 32 位兼容模式执行代码——然后你的 <code>iretq</code> 指令直接变成非法操作。第二，IDT 里的每个条目都有一个 Segment Selector 字段，CPU 在中断处理时会用这个选择子加载 CS。如果 GDT 里没有正确的 code64 描述符，中断触发后 CPU 找不到合法的代码段，直接 #GP → Double Fault → Triple Fault → QEMU 重启。</p><h3 id="这次-gdt-比-mini-kernel-的-豪华-在哪里" tabindex="-1">这次 GDT 比 mini kernel 的&quot;豪华&quot;在哪里？ <a class="header-anchor" href="#这次-gdt-比-mini-kernel-的-豪华-在哪里" aria-label="Permalink to &quot;这次 GDT 比 mini kernel 的&quot;豪华&quot;在哪里？&quot;">​</a></h3><p>mini kernel 的 GDT 只有三项：null + code32 + data32，纯 32 位保护模式方案。大内核的 GDT 有七项——null、kernel code64、kernel data64、user code64、user data64、TSS low、TSS high。多了三样东西：用户态的 code 和 data 段（为将来跑用户进程做准备，DPL=3），以及一个 TSS（Task State Segment）占位符。</p><p>TSS 在 long mode 下的主要用途不是硬件任务切换——那是 32 位时代的遗产，64 位模式下已经不支持了。TSS 在 64 位模式下提供两个功能：一是 IST（Interrupt Stack Table），可以为 NMI、Double Fault 等关键中断提供已知良好的栈；二是 RSP0/RSP1/RSP2，当 CPU 从 Ring 3 切换到 Ring 0 处理中断时，它会自动从 TSS 里读取新的栈指针。我们的 TSS 目前是一个全零的占位符——所有字段都是 0，在设置好 IST 或用户态切换之前它不会起实际作用，但 GDT 里必须有这个条目，否则 <code>ltr</code> 指令会触发 #GP。</p><h3 id="gdt-布局与选择子计算" tabindex="-1">GDT 布局与选择子计算 <a class="header-anchor" href="#gdt-布局与选择子计算" aria-label="Permalink to &quot;GDT 布局与选择子计算&quot;">​</a></h3><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>大内核的 GDT 布局：</span></span>
<span class="line"><span></span></span>
<span class="line"><span>索引  选择子   内容               用途</span></span>
<span class="line"><span>────  ──────   ────               ────</span></span>
<span class="line"><span>0     0x00     Null Descriptor    Intel 规定第一项必须为空</span></span>
<span class="line"><span>1     0x08     Kernel Code64      内核代码段 (Ring 0, 64-bit, L=1)</span></span>
<span class="line"><span>2     0x10     Kernel Data64      内核数据段 (Ring 0, D/B=1)</span></span>
<span class="line"><span>3     0x1B     User Code64        用户代码段 (Ring 3, 64-bit, RPL=3)</span></span>
<span class="line"><span>4     0x23     User Data64        用户数据段 (Ring 3, RPL=3)</span></span>
<span class="line"><span>5-6   0x28     TSS (16 bytes)     任务状态段占位 (占两个 GDT slot)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>选择子的值是 <code>索引 * 8 + RPL</code>。对于内核态段，RPL=0，所以 Kernel Code64 = 1<em>8 = 0x08。对于用户态段，RPL=3，所以 User Code64 = 3</em>8 + 3 = 0x1B，User Data64 = 4<em>8 + 3 = 0x23。TSS 虽然占了两个 slot（索引 5 和 6），但选择子指向的是索引 5，即 5</em>8 = 0x28。</p><h3 id="scoped-enum-与类型安全的描述符构建" tabindex="-1">Scoped Enum 与类型安全的描述符构建 <a class="header-anchor" href="#scoped-enum-与类型安全的描述符构建" aria-label="Permalink to &quot;Scoped Enum 与类型安全的描述符构建&quot;">​</a></h3><p>段描述符的 access byte 是一个 8 位的位域，不同 bit 代表不同含义。如果直接用 <code>uint8_t</code> 传入 access 值，调用者完全可以传入一个语义上毫无意义的值（比如 <code>0xFF</code>），编译器不会报错，运行时 triple fault。我们的方案是用 scoped enum（<code>enum class SegmentAccess : uint8_t</code>）配合 <code>operator|</code> 重载，调用者只能用预定义的标志来组合——<code>SegmentAccess::Present | SegmentAccess::Ring0 | SegmentAccess::CodeData | SegmentAccess::Executable</code>，这样写即使漏掉了一个标志，编译器的类型系统也能帮你检查出来。</p><p><code>SegmentFlags</code> 对应描述符第 6 字节的高 4 位。这里有个容易混淆的地方——内核数据段的 flags 用的是 <code>Size32</code>（D/B=1）而不是 <code>LongMode</code>（L=1）。原因是 64 位模式下只有代码段才需要设置 L 位，数据段的 L 位被保留（必须为 0），数据段只需要设置 D/B 位为 1 即可。如果你把数据段的 L 位也置 1，CPU 行为是未定义的——实测中有的 CPU 会 #GP，有的会静默忽略。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-创建-gdt-头文件——类型、枚举与类声明" tabindex="-1">Step 1: 创建 GDT 头文件——类型、枚举与类声明 <a class="header-anchor" href="#step-1-创建-gdt-头文件——类型、枚举与类声明" aria-label="Permalink to &quot;Step 1: 创建 GDT 头文件——类型、枚举与类声明&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>kernel/arch/x86_64/gdt.hpp</code>，定义段选择子常量、<code>SegmentAccess</code> 和 <code>SegmentFlags</code> scoped enum、<code>GDT</code> class（含 Entry/Pointer/TaskStateSegment 内部结构体、constexpr 工厂函数、init/load 方法）。</p><p><strong>设计思路</strong>: 整个 GDT 的状态——描述符数组、GDTR 指针、TSS 结构体——全部封装在一个 <code>GDT</code> class 里，对外只暴露 <code>init()</code> 方法。这比用全局函数 + 全局数组的做法（比如 mini kernel 的方式）封装性好，将来如果要做 per-CPU GDT（像 Linux 那样），只需要把 <code>GDT g_gdt</code> 变成 per-CPU 变量即可。</p><p><strong>实现约束</strong>:</p><ul><li><code>Entry</code> 结构体必须 8 字节，用 <code>[[gnu::packed]]</code> 消除 padding，配合 <code>static_assert(sizeof(Entry) == 8)</code> 编译期检查</li><li><code>TaskStateSegment</code> 结构体必须 104 字节（Intel SDM Vol.3A Table 8-2 规定），同样用 <code>static_assert(sizeof(TaskStateSegment) == 104)</code> 保护</li><li><code>Pointer</code> 结构体 10 字节（2 字节 limit + 8 字节 base），用于 <code>lgdt</code> 指令</li><li>段选择子常量定义为 <code>constexpr uint16_t</code>：<code>GDT_KERNEL_CODE = 0x08</code>、<code>GDT_KERNEL_DATA = 0x10</code>、<code>GDT_USER_CODE = 0x1B</code>、<code>GDT_USER_DATA = 0x23</code>、<code>GDT_TSS = 0x28</code></li><li><code>SegmentAccess</code> 枚举需要包含：<code>Present</code>(bit7)、<code>Ring0</code>(0)、<code>Ring3</code>(3&lt;&lt;5)、<code>CodeData</code>(bit4)、<code>Executable</code>(bit3)、<code>ReadWrite</code>(bit1)、<code>TSS64Avail</code>(0x09)</li><li><code>SegmentFlags</code> 枚举需要包含：<code>Granularity4K</code>(bit3)、<code>LongMode</code>(bit1)、<code>Size32</code>(bit2)</li><li>两个枚举都需要 <code>operator|</code> 重载以支持组合</li><li>工厂函数：<code>null_entry()</code> 返回全零描述符、<code>segment_entry(access, flags)</code> 生成代码/数据段描述符、<code>tss_low_entry(base, limit)</code> 和 <code>tss_high_entry(base)</code> 生成 TSS 的两个 8 字节</li></ul><p><strong>踩坑预警</strong>:</p><ul><li>如果忘了 <code>[[gnu::packed]]</code>，编译器可能在 Entry 的 <code>access</code>(uint8_t) 和 <code>flags_limit_high</code>(uint8_t) 之间插入 padding，导致结构体超过 8 字节。<code>static_assert</code> 会帮你抓住这个错误。</li><li><code>SegmentFlags::Size32</code> 的值是 <code>1u &lt;&lt; 2</code>（即 bit 2，对应 D/B 位），不是 bit 1。别和 LongMode 的 bit 1 搞混。</li><li>TSS 描述符的 access byte 使用的是完整的值 <code>0x89</code>（Present=1, 64-bit TSS Available），不是按 bit 组合的——所以 <code>TSS64Avail = 0x09</code> 是一个预设值。</li></ul><p><strong>验证</strong>: 此步完成后编译应该通过。可以用 <code>static_assert</code> 验证各工厂函数生成的描述符值是否符合预期（比如 kernel code64 的 access 应该等于 0x9A）。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 编译验证</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -5</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h3 id="step-2-实现-gdt-初始化——填充描述符、lgdt、刷新段寄存器、ltr" tabindex="-1">Step 2: 实现 GDT 初始化——填充描述符、lgdt、刷新段寄存器、ltr <a class="header-anchor" href="#step-2-实现-gdt-初始化——填充描述符、lgdt、刷新段寄存器、ltr" aria-label="Permalink to &quot;Step 2: 实现 GDT 初始化——填充描述符、lgdt、刷新段寄存器、ltr&quot;">​</a></h3><p><strong>目标</strong>: 实现 <code>kernel/arch/x86_64/gdt.cpp</code>，包含全局 <code>g_gdt</code> 实例、<code>GDT::init()</code> 和 <code>GDT::load()</code> 方法。</p><p><strong>设计思路</strong>: <code>init()</code> 填充 7 个 GDT 条目后调用 <code>load()</code>。<code>load()</code> 是最关键的部分——它不仅执行 <code>lgdt</code> 加载 GDTR，还必须刷新所有段寄存器。CS 的刷新最为特殊：不能简单地 <code>mov</code> 到 CS，必须通过&quot;伪造一个 far return&quot;来实现——先把新的 CS 值压栈，再把返回地址压栈，然后 <code>lretq</code>。这样 CPU 在执行 lretq 时会从栈上弹出新 CS 和 RIP，完成 CS 的重新加载。DS/ES/FS/GS/SS 则可以直接 <code>mov</code> 加载。最后执行 <code>ltr</code> 加载任务寄存器。</p><p><strong>实现约束</strong>:</p><ul><li><code>GDT::init()</code> 填充顺序：entries_[0] = null, entries_[1] = kernel code64, entries_[2] = kernel data64, entries_[3] = user code64, entries_[4] = user data64, entries_[5] = TSS low, entries_[6] = TSS high</li><li>Kernel code64: <code>Present | Ring0 | CodeData | Executable | ReadWrite</code>, flags = <code>Granularity4K | LongMode</code></li><li>Kernel data64: <code>Present | Ring0 | CodeData | ReadWrite</code>, flags = <code>Granularity4K | Size32</code>（注意不是 LongMode）</li><li>User code64: 和 kernel code64 类似，但把 <code>Ring0</code> 换成 <code>Ring3</code></li><li>User data64: 和 kernel data64 类似，但把 <code>Ring0</code> 换成 <code>Ring3</code></li><li>TSS: <code>tss_low_entry</code> 的 access 自动设为 <code>Present | TSS64Avail</code>，limit 为 <code>sizeof(TaskStateSegment) - 1</code></li><li>GDTR 的 limit = <code>sizeof(entries_) - 1</code>（7 个 8 字节 entry = 56 字节，limit = 55），base 指向 entries_ 数组的地址</li><li>内联汇编中的 <code>lgdt</code> 操作数用 <code>&quot;m&quot;</code> 约束（内存操作数），CS 和 DS 用 <code>&quot;i&quot;</code> 约束（立即数）</li><li><code>ltr</code> 接受的是一个 16 位选择子值（0x28），用 <code>&quot;r&quot;</code> 约束传入寄存器</li></ul><p><strong>踩坑预警</strong>:</p><ul><li><code>lgdt</code> 后如果不做 far return 刷新 CS，CS 的隐藏缓存仍然是旧的描述符属性。CPU 内部已经切换了 GDTR 指向新 GDT，但 CS 缓存还没更新——此时任何涉及 CS 的操作（包括中断处理）都可能出问题。</li><li><code>lretq</code> 前的压栈顺序必须是：先压 CS（<code>pushq %[cs]</code>），再压 RIP（<code>leaq 1f(%%rip), %%rax; pushq %%rax</code>），这样 lretq 弹出时先弹 RIP 再弹 CS，顺序正确。</li><li><code>ltr</code> 会把 TSS 描述符标记为 &quot;busy&quot;，如果 GDT 里的 TSS 条目不存在或格式不对，<code>ltr</code> 直接触发 #GP。</li><li>在内联汇编中，<code>&quot;memory&quot;</code> clobber 必须加——因为 lgdt/lretq/mov-to-segment-reg 都会影响内存访问方式，编译器不能假设内存布局不变。</li></ul><p><strong>验证</strong>: 完成此步后，可以在 <code>kernel_main</code> 中调用 <code>g_gdt.init()</code>，然后读回段寄存器验证值是否正确。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 编译并运行</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big-kernel-test-image</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run-big-kernel-test</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：串口输出显示 CS=0x08, DS=0x10, SS=0x10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><hr><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>完整的构建和测试命令：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 编译大内核测试二进制</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big-kernel-test-image</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行测试（QEMU 自动退出）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run-big-kernel-test</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 或者运行生产大内核（手动 Ctrl+C 退出）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>QEMU 启动参数说明：测试目标使用 <code>isa-debug-exit</code> 设备（port 0xf4），测试通过写 0 退出（QEMU exit code 1），失败写 1 退出（QEMU exit code 3）。</p><hr><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>问题: lgdt 后 triple fault</strong> 最常见的原因是 GDTR 的 base 地址指向了错误的位置。用 QEMU monitor 的 <code>info registers</code> 命令查看 GDTR 的值，确认 base 指向你的 GDT 数组。另一个常见原因是 GDTR 的 limit 算错了——应该是 <code>sizeof(entries) - 1</code>，不是 <code>sizeof(entries)</code>。</p><p><strong>问题: ltr 触发 #GP</strong> 检查 GDT 中 TSS 描述符的格式。64 位 TSS 描述符占 16 字节（两个 slot），第二个 slot 的高 32 位是 TSS 基地址的高 32 位。如果 TSS 地址本身不对（比如指向了未映射的内存），也会出问题。</p><p><strong>问题: 段寄存器值不是预期的</strong> 检查 <code>lretq</code> 前的压栈顺序是否正确（CS 先压、RIP 后压）。用 QEMU monitor 的 <code>info registers</code> 可以看到所有段寄存器的值。</p><p><strong>问题: GDT 加载后系统正常但后续 IDT 加载时 triple fault</strong> 检查 GDT 中 kernel code64 描述符的 access byte 是否正确。最常见的错误是忘了设 LongMode 标志（L=1），导致 CPU 以为这是一个 32 位代码段，后续 <code>iretq</code> 指令在 32 位模式下不合法。另一个可能的原因是 data 段的 L 位被错误地置为 1——在 long mode 下数据段的 L 位必须为 0，否则 CPU 行为未定义。</p><p><strong>问题: 编译时 static_assert 失败（sizeof(Entry) != 8 或 sizeof(TaskStateSegment) != 104）</strong> 检查结构体定义是否缺少 <code>[[gnu::packed]]</code> 属性。没有 packed 的话编译器会在 <code>access</code>（uint8_t）和 <code>flags_limit_high</code>（uint8_t）之间插入 padding 字节，导致结构体超过 8 字节。TaskStateSegment 同理——如果 <code>reserved0</code>（uint32_t）后面没有 packed，编译器可能插入 4 字节 padding 使 <code>rsp[3]</code> 对齐到 8 字节边界。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 串口日志查看（如果使用 -serial file:serial.log）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/serial.log</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># GDB 调试</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run-gdb</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 另一个终端：</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/kernel/big/big_kernel</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (gdb) target remote :1234</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (gdb) break GDT::load</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (gdb) info registers  # 查看 GDTR</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><hr><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>GDT in Long Mode</td><td>Base/Limit 被忽略，但仍需有效描述符供 CS/DS/SS 使用</td></tr><tr><td>段选择子</td><td><code>索引 * 8 + RPL</code>，内核 RPL=0，用户 RPL=3</td></tr><tr><td>Kernel Code64</td><td>L=1, D=0, G=1, Access=Present|Ring0|Code|Exec|Read</td></tr><tr><td>Kernel Data64</td><td>L=0, D/B=1, G=1, Access=Present|Ring0|Data|Write</td></tr><tr><td>User Code64/Data64</td><td>与 kernel 版相同但 DPL=3 (Ring3)</td></tr><tr><td>TSS (64-bit)</td><td>104 字节，占 2 个 GDT slot，提供 RSP0/IST</td></tr><tr><td>lgdt</td><td>加载 GDTR，但不刷新 CS</td></tr><tr><td>lretq</td><td>远返回，用于刷新 CS 的段描述符缓存</td></tr><tr><td>ltr</td><td>加载任务寄存器，标记 TSS 为 busy</td></tr><tr><td>Scoped Enum</td><td>类型安全的描述符标志组合</td></tr><tr><td>static_assert</td><td>编译期验证结构体大小（Entry=8, TSS=104）</td></tr></tbody></table>`,60)])])}const k=s(t,[["render",l]]);export{g as __pageData,k as default};
