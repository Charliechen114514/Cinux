import{c as a,Q as n,j as e,m as i}from"./chunks/framework.D9IbVcwA.js";const h=JSON.parse('{"title":"004-boot-load-mini-kernel-c-2 · 内核加载 (C)","description":"","frontmatter":{"title":"004-boot-load-mini-kernel-c-2 · 内核加载 (C)"},"headers":[],"relativePath":"read-through/004-boot-load-mini-kernel-c-2.md","filePath":"read-through/004-boot-load-mini-kernel-c-2.md","lastUpdated":1779686691000}'),p={name:"read-through/004-boot-load-mini-kernel-c-2.md"};function l(o,s,t,c,r,d){return n(),e("div",null,[...s[0]||(s[0]=[i(`<h1 id="_004-通读版-·-内核启动、bss-陷阱与符号冲突" tabindex="-1">004 通读版 · 内核启动、BSS 陷阱与符号冲突 <a class="header-anchor" href="#_004-通读版-·-内核启动、bss-陷阱与符号冲突" aria-label="Permalink to &quot;004 通读版 · 内核启动、BSS 陷阱与符号冲突&quot;">​</a></h1><h2 id="概览" tabindex="-1">概览 <a class="header-anchor" href="#概览" aria-label="Permalink to &quot;概览&quot;">​</a></h2><p>在上一篇文章里，我们看着 bootloader 在 Long Mode 中把 BootInfo 填好，往 debugcon 敲了一个 <code>&#39;J&#39;</code>，然后 <code>jmp *%rax</code> 头也不回地跳进了 0xFFFFFFFF80020000。从那一刻起，CPU 执行的就是我们自己写的内核代码了。但内核要真正&quot;活过来&quot;，还得经历一系列初始化步骤：关中断、开 SSE、设栈、清 BSS、调用全局构造函数，最后才能进入 C++ 的 <code>main</code> 函数。</p><p>本文覆盖内核侧的核心代码——<code>kernel/mini/linker.ld</code>（链接器脚本和内存布局）、<code>kernel/mini/arch/x86_64/boot.S</code>（内核入口汇编序列，含 SSE 启用、BSS 清除和符号冲突修复）。下一篇覆盖 C++ 运行时支撑（<code>crt_stub.cpp</code>）、<code>main.cpp</code>（C++ 特性验证）、构建配置和设计决策。</p><p>在这个过程中，我们会遇到两个非常经典的内核启动 bug：<code>rep stosb</code> 指令意外破坏了 <code>%rdi</code> 中保存的 BootInfo 指针，以及链接器把汇编定义的 <code>.bss</code> 符号和 C++ 全局变量分配到了同一个地址。这两个 bug 的调试过程分别记录在 <code>document/notes/004-C/</code> 目录下，我会在讲到相关代码时把踩坑经历融入讲解。</p><p>关键设计决策一览：BootInfo 指针保存在 <code>.data</code> 段而非 <code>.bss</code> 段，以避免 BSS 清除时被意外清零；<code>.init_array</code> 的全局构造函数由内核自身遍历调用；链接脚本使用 <code>AT()</code> 指定物理加载地址（LMA），VMA 则指向高半核地址空间。</p><h2 id="架构图" tabindex="-1">架构图 <a class="header-anchor" href="#架构图" aria-label="Permalink to &quot;架构图&quot;">​</a></h2><p>内核入口到 C++ <code>main</code> 的完整执行序列，以及每一步的寄存器/内存状态变化：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  Bootloader 跳入内核</span></span>
<span class="line"><span>  %rdi = 0x7000 (BootInfo*)</span></span>
<span class="line"><span>  %rip = 0xFFFFFFFF80020000 (_start)</span></span>
<span class="line"><span>     |</span></span>
<span class="line"><span>     v</span></span>
<span class="line"><span>  boot.S: _start</span></span>
<span class="line"><span>     ├── cli                    关中断</span></span>
<span class="line"><span>     ├── outb &#39;1&#39;               debugcon: 到达 _start</span></span>
<span class="line"><span>     ├── movq stack_top, %rsp   设栈 (8KB, 在 .bss 中)</span></span>
<span class="line"><span>     ├── outb &#39;2&#39;               debugcon: 栈就绪</span></span>
<span class="line"><span>     ├── movq %rdi -&gt; .data     保存 BootInfo 指针到 __boot_info_ptr</span></span>
<span class="line"><span>     ├── rep stosb              清除 .bss 段 (注意: 会破坏 %rdi!)</span></span>
<span class="line"><span>     ├── outb &#39;3&#39;               debugcon: BSS 清除完成</span></span>
<span class="line"><span>     ├── call _init_global_ctors 调用 .init_array 中的全局构造函数</span></span>
<span class="line"><span>     ├── outb &#39;4&#39;               debugcon: 全局构造完成</span></span>
<span class="line"><span>     ├── movq .data -&gt; %rdi     从 __boot_info_ptr 恢复 BootInfo*</span></span>
<span class="line"><span>     └── call mini_kernel_main  进入 C++ 主函数</span></span>
<span class="line"><span>           |</span></span>
<span class="line"><span>           v</span></span>
<span class="line"><span>     main.cpp: mini_kernel_main</span></span>
<span class="line"><span>        ├── 输出 &quot;===CPP&quot;</span></span>
<span class="line"><span>        ├── Test 1: SimpleClass (构造/析构) -&gt; &quot;C1&quot;</span></span>
<span class="line"><span>        ├── Test 2: 虚函数/vtable -&gt; &quot;V&quot;, &quot;2&quot;</span></span>
<span class="line"><span>        ├── Test 3: 全局对象验证 -&gt; &quot;3&quot;</span></span>
<span class="line"><span>        ├── BootInfo 验证 -&gt; &quot;B&quot;</span></span>
<span class="line"><span>        └── 输出 &quot;===END&quot;, hlt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br></div></div><p>内核的内存布局（链接器脚本定义）：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  虚拟地址 (VMA)                    物理地址 (LMA)    段名      内容</span></span>
<span class="line"><span>  ──────────────────────────────────────────────────────────────────</span></span>
<span class="line"><span>  0xFFFFFFFF80020000               0x20000          .text     _start 入口</span></span>
<span class="line"><span>  0xFFFFFFFF80020xxx               ...              .text     其他代码</span></span>
<span class="line"><span>  0xFFFFFFFF80022xxx               ...              .rodata   只读数据</span></span>
<span class="line"><span>  ──────────────────────────────────────────────────────────────────</span></span>
<span class="line"><span>  0xFFFFFFFF800226e0               ...              .data     __boot_info_ptr</span></span>
<span class="line"><span>  0xFFFFFFFF800226e8              ...              .data     .init_array (全局构造函数指针)</span></span>
<span class="line"><span>  ──────────────────────────────────────────────────────────────────</span></span>
<span class="line"><span>  0xFFFFFFFF800227000              --               .bss      global_counter 等</span></span>
<span class="line"><span>  ...                               ...              .bss      __mini_stack (8KB)</span></span>
<span class="line"><span>  0xFFFFFFFF800229000              --               .bss      __mini_stack_top</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>预期 debugcon 输出：<code>OPLJ123G4===CPPGC1V123B===END</code></p><ul><li><code>OPLJ</code>：bootloader 阶段（O = Stage2 OK, P = Protected Mode, L = Long Mode, J = Jump）</li><li><code>123G4</code>：内核入口序列（1 = _start 到达, 2 = 栈就绪, 3 = BSS 清除, G = 全局构造函数输出, 4 = 全局构造完成）</li><li><code>===CPPGC1V123B===END</code>：C++ 特性验证（<code>===CPP</code> = 开始标记, <code>C1</code> = 类构造, <code>G</code> = 全局对象, <code>1</code> = 简单类验证, <code>V</code> = 虚函数构造, <code>2</code> = 虚函数验证, <code>3</code> = 全局对象验证, <code>B</code> = BootInfo 验证, <code>===END</code> = 结束标记）</li></ul><h2 id="代码精讲" tabindex="-1">代码精讲 <a class="header-anchor" href="#代码精讲" aria-label="Permalink to &quot;代码精讲&quot;">​</a></h2><h3 id="链接器脚本——内核内存布局的蓝图" tabindex="-1">链接器脚本——内核内存布局的蓝图 <a class="header-anchor" href="#链接器脚本——内核内存布局的蓝图" aria-label="Permalink to &quot;链接器脚本——内核内存布局的蓝图&quot;">​</a></h3><p>我们先从 <code>kernel/mini/linker.ld</code> 开始看，因为它定义了所有其他代码运行的地址空间基础。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OUTPUT_FORMAT(&quot;elf64-x86-64&quot;)</span></span>
<span class="line"><span>ENTRY(_start)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>KERNEL_PHYS_BASE = 0x20000;</span></span>
<span class="line"><span>KERNEL_Virt_BASE = 0xFFFFFFFF80000000;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SECTIONS</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    . = KERNEL_Virt_BASE + KERNEL_PHYS_BASE;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>OUTPUT_FORMAT</code> 指定输出为 ELF64 x86-64 格式。虽然最终会用 <code>objcopy -O binary</code> 转成扁平二进制，但编译链接阶段仍然需要完整的 ELF 格式——链接器需要 section header 来定位符号、解析重定位。</p><p><code>ENTRY(_start)</code> 告诉链接器入口点是 <code>_start</code>，这个信息在扁平二进制中会被丢弃（扁平二进制从文件头开始执行），但在 ELF 格式中有意义——如果你用 QEMU 的 <code>-kernel</code> 参数直接加载 ELF 文件，QEMU 会自动跳到 <code>_start</code>。</p><p>两个关键常量：<code>KERNEL_PHYS_BASE = 0x20000</code> 是 bootloader 加载内核的物理地址，<code>KERNEL_Virt_BASE = 0xFFFFFFFF80000000</code> 是高半核的虚拟基址。起始 VMA 设为两者之和 <code>0xFFFFFFFF80020000</code>，这就是 <code>_start</code> 最终被链接到的虚拟地址。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    .text : AT(ADDR(.text) - KERNEL_Virt_BASE) {</span></span>
<span class="line"><span>        *(.text.start)        /* _start must be first! */</span></span>
<span class="line"><span>        *(.text .text.*)</span></span>
<span class="line"><span>        *(.rodata .rodata.*)</span></span>
<span class="line"><span>    }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>.text</code> 段放代码和只读数据。<code>AT(ADDR(.text) - KERNEL_Virt_BASE)</code> 计算 LMA（Load Memory Address）——因为 VMA 是 <code>KERNEL_Virt_BASE + KERNEL_PHYS_BASE = 0xFFFFFFFF80020000</code>，减去 <code>KERNEL_Virt_BASE</code> 后得到 <code>KERNEL_PHYS_BASE = 0x20000</code>。这就是 bootloader 把内核 flat binary 放到内存中的实际物理位置。<code>*(.text.start)</code> 放在 <code>*(.text .text.*)</code> 之前，确保 <code>_start</code> 函数始终在输出文件的最前面——boot.S 把 <code>_start</code> 放在 <code>.text.start</code> 段中。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    .data : AT(ADDR(.data) - KERNEL_Virt_BASE) {</span></span>
<span class="line"><span>        *(.data .data.*)</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    .init_array : AT(ADDR(.init_array) - KERNEL_Virt_BASE) {</span></span>
<span class="line"><span>        __init_array_start = .;</span></span>
<span class="line"><span>        KEEP(*(.init_array .init_array.*))</span></span>
<span class="line"><span>        __init_array_end = .;</span></span>
<span class="line"><span>    }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>.data</code> 段紧接着 <code>.text</code> 段，存放有初始值的全局变量。<code>.init_array</code> 是独立的段，不在 <code>.data</code> 内部——这样链接器可以分别管理两个段的布局。<code>KEEP()</code> 包裹 <code>.init_array</code> 防止被链接器的 section garbage collection 丢弃。LMA 都用 <code>ADDR() - KERNEL_Virt_BASE</code> 计算，确保物理地址连续排列。</p><p><code>__init_array_start</code> 和 <code>__init_array_end</code> 是两个链接器符号，标记了 <code>.init_array</code> 段的起止位置。GCC 为每个有构造函数的全局对象在 <code>.init_array</code> 中生成一个函数指针（名为 <code>_GLOBAL__sub_I_XXX</code>），内核的启动代码需要遍历这个数组，依次调用每个函数指针来初始化全局对象。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    .bss : {</span></span>
<span class="line"><span>        __bss_start = .;</span></span>
<span class="line"><span>        *(.bss .bss.*)</span></span>
<span class="line"><span>        *(COMMON)</span></span>
<span class="line"><span>        __bss_end = .;</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    __mini_kernel_end = .;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>.bss</code> 段不需要 <code>AT()</code>——BSS 段在 ELF 文件中不占空间（它只是记录了大小和起始地址），运行时由启动代码负责清零。<code>__bss_start</code> 和 <code>__bss_end</code> 标记了 BSS 的边界，<code>boot.S</code> 中的 <code>rep stosb</code> 指令就用这两个符号来确定要清除的内存范围。</p><p><code>/DISCARD/</code> 段丢弃了 <code>.comment</code>、<code>.note</code> 和 <code>.eh_frame</code>——这些是编译器生成的调试和注解信息，内核不需要。</p><h3 id="内核入口序列——boot-s-的每一步" tabindex="-1">内核入口序列——boot.S 的每一步 <a class="header-anchor" href="#内核入口序列——boot-s-的每一步" aria-label="Permalink to &quot;内核入口序列——boot.S 的每一步&quot;">​</a></h3><p><code>kernel/mini/arch/x86_64/boot.S</code> 是内核的入口点。bootloader 通过 <code>jmp *%rax</code> 跳入 <code>_start</code>，此时 <code>%rdi</code> 中保存着 BootInfo 指针（0x7000），CPU 处于 Long Mode，中断已关闭（bootloader 跳转前没有显式 <code>cli</code>，但我们不能依赖这一点）。</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.start, &quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.code64</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _start</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.type _start, @function</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_start:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* Disable interrupts */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    cli</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* Enable SSE: set CR4.OSFXSR (bit 9) and CR4.OSXMMEXCPT (bit 10) */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cr4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    orq $(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;&lt; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* OSFXSR: enable FXSAVE/FXRSTOR */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    orq $(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;&lt; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         /* OSXMMEXCPT: enable SIMD #XF */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cr4</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    clts</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                          /* Clear CR0.TS (Task Switched) */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* Output &#39;1&#39; to debugcon - _start reached */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x31, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              /* &#39;1&#39; */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br></div></div><p>第一步是 <code>cli</code> 关中断。此时内核还没有设置 IDT（中断描述符表），如果此时发生了任何硬件中断（定时器、键盘中断等），CPU 会尝试通过 IDT 查找处理程序——但 IDT 还不存在，结果就是一个 Triple Fault，QEMU 直接重启。<code>cli</code> 确保在 IDT 设置完成之前不会有任何中断打扰。</p><p>紧接着是启用 SSE。Long Mode 下 SSE 不是默认开启的——我们需要在 CR4 寄存器中设置两个位：OSFXSR（bit 9）告诉 CPU 操作系统支持 FXSAVE/FXRSTOR 指令，OSXMMEXCPT（bit 10）告诉 CPU 操作系统能处理 SIMD 浮点异常。<code>clts</code> 指令清除 CR0 的 TS（Task Switched）位，避免使用 SSE 指令时触发 Device Not Available 异常（#NM）。虽然当前的测试代码碰巧不依赖浮点运算，但编译器可能会在某些场景下生成 SSE 指令（比如结构体拷贝优化），不启用 SSE 的话这些指令会触发 #NM 导致 Triple Fault。</p><p>然后往 debugcon 输出 <code>&#39;1&#39;</code>（0x31 = ASCII &#39;1&#39;），表示 <code>_start</code> 已经到达。这是第一个内核侧的 debugcon 输出，和 bootloader 的 <code>&#39;J&#39;</code> 之间不应该有任何其他字符。</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $__mini_stack_top, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rsp</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    xorq %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rbp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rbp</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x32, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>设置栈指针。<code>__mini_stack_top</code> 定义在 boot.S 的 <code>.bss</code> 段末尾，是一个 8KB 栈区的顶端。x86 的栈是向下增长的（push 递减 %rsp），所以栈顶是高地址。<code>xorq %rbp, %rbp</code> 把帧指针清零——这是 C++ 运行时的约定：<code>main</code> 函数的 <code>%rbp</code> 为 0 表示没有调用者（栈回溯的终止条件）。</p><p>输出 <code>&#39;2&#39;</code> 表示栈已就绪。到目前为止，一切都还算平淡——接下来就是真正刺激的部分了。</p><h3 id="bss-清除与-bootinfo-指针的惊险时刻" tabindex="-1">BSS 清除与 BootInfo 指针的惊险时刻 <a class="header-anchor" href="#bss-清除与-bootinfo-指针的惊险时刻" aria-label="Permalink to &quot;BSS 清除与 BootInfo 指针的惊险时刻&quot;">​</a></h3><p>现在我们来到整个内核启动中最容易出 bug 的地方。先看代码：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, __boot_info_ptr</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $__bss_start, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $__bss_end, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rcx</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    subq %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rcx</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    xorq %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    rep</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> stosb</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x33, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>这里做的事情很简单：先把 <code>%rdi</code>（BootInfo 指针 = 0x7000）保存到 <code>__boot_info_ptr</code>，然后把 <code>%rdi</code> 设为 BSS 起始地址，用 <code>rep stosb</code> 逐字节地把 BSS 段全部清零。</p><p>但这里有两个非常隐蔽的陷阱，每一个都足够让你调试到凌晨三点。</p><p>第一个陷阱是 <code>rep stosb</code> 指令本身对 <code>%rdi</code> 的破坏。<code>rep stosb</code> 的语义是：以 <code>%rdi</code> 为目标地址，以 <code>%rax</code> 为填充值（这里是 0），以 <code>%rcx</code> 为计数器，逐字节写入并自动递增 <code>%rdi</code>。执行完毕后，<code>%rdi</code> 不再是 BSS 起始地址了——它已经递增到了 BSS 末尾之后。这意味着如果我们在 <code>rep stosb</code> 之后试图从 <code>%rdi</code> 读取 BootInfo 指针，拿到的是一个垃圾地址。</p><p>所以第一行 <code>movq %rdi, __boot_info_ptr</code> 至关重要——它必须在 BSS 清除之前执行。但是这里还有第二个陷阱，更隐蔽，也更让人血压拉满。</p><p>第二个陷阱是关于 <code>__boot_info_ptr</code> 应该放在哪个段。最初我们把 <code>__boot_info_ptr</code> 定义在 <code>.bss</code> 段：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .bss</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.skip </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__boot_info_ptr:</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>.bss</code> 段的内容会被 <code>rep stosb</code> 整体清零——也就是说，你在 BSS 清除之前把 BootInfo 指针写到了 <code>__boot_info_ptr</code>，然后 <code>rep stosb</code> 把它又清回了 0。等你在 <code>mini_kernel_main</code> 里去读 <code>__boot_info_ptr</code> 的时候，拿到的是 0 而不是 0x7000。</p><p>这个 bug 的调试过程非常曲折。最初我们怀疑是 bootloader 传递的 <code>%rdi</code> 值就是错的，于是在 <code>_start</code> 入口处加了 debugcon 输出，确认 <code>%rdi = 0x7000</code> 没问题。然后又在 <code>movq %rdi, __boot_info_ptr</code> 之后立即读取 <code>__boot_info_ptr</code> 的值做输出，发现也是 0x7000——到这里保存是成功的。但是 BSS 清除之后再读，值变成了一个诡异的 <code>0x2a00000000</code>。</p><p><code>0x2a00000000</code> 这个值不是 0，而是 <code>42 &lt;&lt; 32</code>。42 是什么？如果你看过 <code>main.cpp</code> 里的全局构造函数测试，<code>global_construction_count</code> 被赋值为 42。这个&quot;巧合&quot;指向了第三个层次的问题——符号地址冲突。</p><h3 id="符号冲突-当链接器把两个变量放在同一个地址" tabindex="-1">符号冲突：当链接器把两个变量放在同一个地址 <a class="header-anchor" href="#符号冲突-当链接器把两个变量放在同一个地址" aria-label="Permalink to &quot;符号冲突：当链接器把两个变量放在同一个地址&quot;">​</a></h3><p>前面说到 <code>__boot_info_ptr</code> 在 BSS 清除后变成了 <code>0x2a00000000</code> 而不是 0。按理说 BSS 清除应该把它清成 0 才对。用 <code>objdump -t</code> 检查符号表，真相才浮出水面：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ffffffff800226e8 g       .bss  0000000000000000 __boot_info_ptr</span></span>
<span class="line"><span>ffffffff800226e8 g     O .bss  0000000000000001 global_counter</span></span>
<span class="line"><span>ffffffff800226ec l     O .bss  0000000000000004 _ZL25global_construction_count</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>__boot_info_ptr</code> 和 <code>global_counter</code> 被链接器分配到了同一个地址 <code>0xffffffff800226e8</code>。</p><p>为什么会这样？问题出在汇编定义 <code>.bss</code> 符号的方式上：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .bss</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.skip </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__boot_info_ptr:</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>.skip 8</code> 在标签之前。GNU AS 的行为是 <code>.skip</code> 在当前位置分配空间，然后标签指向 <code>.skip</code> 之后的地址。但链接器在合并来自不同目标文件的 <code>.bss</code> 段时，看到的是一个没有大小的符号（因为它只是一个标签，不像 C++ 的全局变量有明确的大小信息）。链接器在分配 <code>.bss</code> 空间时，<code>__boot_info_ptr</code> 的地址被分配到了和 <code>global_counter</code> 相同的位置。</p><p>执行流程是这样的：BSS 清除之后，<code>__boot_info_ptr</code> 的内存被清零。然后 <code>_init_global_ctors</code> 被调用，<code>GlobalCounter</code> 的构造函数执行，<code>global_construction_count = 42</code>（写入 0x800226ec，4 字节）。因为 <code>__boot_info_ptr</code> 和 <code>global_counter</code> 共享地址 0x800226e8，<code>global_counter</code> 是 1 字节对象，而 <code>__boot_info_ptr</code> 是 8 字节。当构造函数往 <code>global_construction_count</code>（0x800226ec，紧邻）写入 42 时，由于地址重叠，<code>__boot_info_ptr</code> 的 8 字节视图里看到了低 4 字节为 0（来自 <code>global_counter</code> 的默认值）、高 4 字节为 42（来自 <code>global_construction_count</code>），合在一起就是 <code>0x0000002a00000000</code>。</p><p>这个 bug 的修复方案是把 <code>__boot_info_ptr</code> 从 <code>.bss</code> 移到 <code>.data</code> 段：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .data</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.align </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__boot_info_ptr:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .quad </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>.data</code> 段不会被 BSS 清除操作触及，而且 <code>.data</code> 段和 <code>.bss</code> 段在链接器脚本中是完全独立的 section，不会有地址重叠的问题。<code>.quad 0</code> 给了一个显式的初始值，让链接器知道这个符号占用 8 字节的空间，大小信息明确。修复之后 <code>objdump -t</code> 确认 <code>__boot_info_ptr</code> 在 <code>.data</code> 段有了独立的地址：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ffffffff800226e8 g     O .data 0000000000000008 __boot_info_ptr</span></span>
<span class="line"><span>ffffffff800226f4 g     O .bss  0000000000000001 global_counter</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>两个符号现在各有各的地址，再也不会互相踩踏了。</p><p>下一篇我们将覆盖全局构造函数调用机制、C++ 运行时支撑函数（crt_stub.cpp）、内核主函数和 C++ 特性验证、以及构建配置。</p>`,63)])])}const k=a(p,[["render",l]]);export{h as __pageData,k as default};
