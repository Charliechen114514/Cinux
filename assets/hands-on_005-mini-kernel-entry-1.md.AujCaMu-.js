import{c as o,Q as d,j as c,m as t}from"./chunks/framework.iD28w_5a.js";const b=JSON.parse('{"title":"005-mini-kernel-entry-1 · 内核入口","description":"","frontmatter":{"title":"005-mini-kernel-entry-1 · 内核入口"},"headers":[],"relativePath":"hands-on/005-mini-kernel-entry-1.md","filePath":"hands-on/005-mini-kernel-entry-1.md","lastUpdated":1779686691000}'),a={name:"hands-on/005-mini-kernel-entry-1.md"};function n(r,e,i,s,l,p){return d(),c("div",null,[...e[0]||(e[0]=[t(`<h1 id="_005-1-动手篇-串口驱动与-i-o-端口编程" tabindex="-1">005-1 动手篇：串口驱动与 I/O 端口编程 <a class="header-anchor" href="#_005-1-动手篇-串口驱动与-i-o-端口编程" aria-label="Permalink to &quot;005-1 动手篇：串口驱动与 I/O 端口编程&quot;">​</a></h1><blockquote><p>本章完成后的可见效果：QEMU 串口终端（stdout）输出由 kprintf 产生的格式化字符串，证明内核可以通过 UART 驱动向外部世界&quot;说话&quot;了</p><p>前置要求：已完成 <code>004_boot_load_mini_kernel_C</code>，理解 Bootloader 到 Mini Kernel 的跳转过程</p></blockquote><hr><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一章我们在 Bootloader 的末尾打出那个 <code>H</code> 字符然后一记 <code>jmp *%rax</code> 跳了过去，内核终于开始跑自己的代码了。但说实话，此刻的内核依然是一个彻头彻尾的黑盒子——它在跑，我们看不到任何输出，不知道它跑到了哪里，不知道变量是什么值，不知道有没有出错。对于内核开发来说，这种&quot;失明&quot;状态是致命的，一旦出 bug 就只能对着 QEMU 的重启干瞪眼。</p><p>所以这一章我们要给内核装上第一件正经的武器：串口驱动。串口是内核开发中最基础也最可靠的调试输出手段，不需要显卡驱动、不需要帧缓冲、不需要字体渲染，只要几条 I/O 端口指令就能把字符送出内核。完成本篇后，我们的内核就拥有了向外部世界输出文字的能力——虽然只有文字，但对于内核开发者来说，这已经是最强大的 debugging 工具了。</p><p>本篇要完成的事情分三层。最底层是 x86 I/O 端口机制的封装——<code>inb</code>/<code>outb</code> 这两个内联函数，它们是所有端口 I/O 的基础。中间层是 UART 8250/16550 串口驱动，负责把一个字节变成串口线上的一帧数据。最上层是 Serial 类，提供 <code>putc</code>/<code>puts</code> 这样人类友好的接口，顺带处理一下 <code>\\n</code> 到 <code>\\r\\n</code> 的转换——这一步不做的话你的终端输出会全部挤在一行上，非常刺激。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="x86-i-o-端口机制-cpu-和外设的-专用电话线" tabindex="-1">x86 I/O 端口机制：CPU 和外设的&quot;专用电话线&quot; <a class="header-anchor" href="#x86-i-o-端口机制-cpu-和外设的-专用电话线" aria-label="Permalink to &quot;x86 I/O 端口机制：CPU 和外设的&quot;专用电话线&quot;&quot;">​</a></h3><p>x86 架构给 CPU 和外设之间准备了两套通信机制：内存映射 I/O（MMIO）和端口映射 I/O（PMIO）。串口用的是后者——它通过两条专用指令 <code>in</code> 和 <code>out</code> 来完成数据交换，而不是像 MMIO 那样读写某个内存地址。你可以把 PMIO 想象成 CPU 和外设之间的一根专用电话线：每根线有一个编号（端口号），CPU 拿起某根线说&quot;读&quot;或者&quot;写&quot;，对应的外设就响应。</p><p>这个机制的实现很直接。<code>out</code> 指令把寄存器里的值写到指定的端口，<code>in</code> 指令从指定端口读一个值到寄存器。端口号可以是 8 位立即数（0x00 到 0xFF），也可以放在 DX 寄存器里（支持 16 位端口号，0x0000 到 0xFFFF）。数据宽度可以是字节（8 位）、字（16 位）或双字（32 位），对应的指令后缀分别是 <code>b</code>、<code>w</code>、<code>l</code>。</p><p>Intel SDM Vol. 2A 第 3 章对 IN/OUT 指令有详细说明——IN 在 ~page 3-370，OUT 在 ~page 3-510。关键点在于，这两条指令在保护模式下受到 IOPL（I/O Privilege Level）和 TSS I/O 位图的控制，但在我们的内核里运行在 ring 0，所以不存在权限问题。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CPU 寄存器  ←——inb——→  I/O 端口 (0x0000-0xFFFF)</span></span>
<span class="line"><span>    AL           1 byte</span></span>
<span class="line"><span>    AX           2 bytes</span></span>
<span class="line"><span>    EAX          4 bytes</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>在 AT&amp;T 汇编语法下，<code>inb</code> 的操作数顺序是&quot;从端口读到寄存器&quot;，写成 <code>inb %port, %reg</code>；<code>outb</code> 是&quot;从寄存器写到端口&quot;，写成 <code>outb %reg, %port</code>。注意 AT&amp;T 语法里源操作数在前面、目的操作数在后面，和 Intel 语法是反的——这个细节搞反了就是读到一个垃圾值然后写到错误的端口，非常难查。</p><h3 id="uart-8250-16550-串口背后的那块芯片" tabindex="-1">UART 8250/16550：串口背后的那块芯片 <a class="header-anchor" href="#uart-8250-16550-串口背后的那块芯片" aria-label="Permalink to &quot;UART 8250/16550：串口背后的那块芯片&quot;">​</a></h3><p>UART（Universal Asynchronous Receiver/Transmitter）是串口背后的硬件控制器。PC 上用的 UART 芯片从早期的 8250 演进到后来的 16550A（增加了 FIFO 缓冲），但编程接口完全兼容。在 QEMU 里模拟的也是 16550A。</p><p>PC 上最多有四个串口，各有固定的 I/O 端口基地址。COM1 是 0x3F8，COM2 是 0x2F8，COM3 是 0x3E8，COM4 是 0x2E8。我们用的是 COM1，因为它是 QEMU 默认启用并且通过 <code>-serial stdio</code> 参数重定向到终端的那个。</p><p>UART 的寄存器通过基地址加偏移来访问。我们关心的寄存器并不多：</p><p>偏移 +0 处，读的时候是 RBR（接收缓冲寄存器），写的时候是 THR（发送保持寄存器）。这是同一个端口地址，但读写行为不同——读就是拿数据，写就是发数据。</p><p>偏移 +1 是 IER（中断使能寄存器）。我们用轮询模式，不需要中断，所以把它设成 0 就行了。</p><p>偏移 +2 写的时候是 FCR（FIFO 控制寄存器）。值设成 0xC7 的含义是：启用 FIFO、清空发送和接收缓冲区、设置 14 字节的中断触发阈值。</p><p>偏移 +3 是 LCR（线路控制寄存器）。值 0x03 代表&quot;8N1&quot;——8 位数据位、无奇偶校验、1 位停止位。这是串口通信最标准的配置。</p><p>偏移 +5 是 LSR（线路状态寄存器），只读。它的第 5 位叫 THRE（Transmit Holding Register Empty），当这个位为 1 时表示发送缓冲区空了，可以写入下一个字节。我们在发送字符之前必须轮询这个位——不等待的话，如果上一个字节还没发完你就往 THR 里塞新数据，上一个字节就丢了。</p><p>初始化序列的步骤是这样的：先禁用中断（IER 写 0），然后设置 8N1 数据格式（LCR 写 0x03），接着启用 FIFO 并清空缓冲区（FCR 写 0xC7），最后设置 MCR（Modem 控制寄存器）为 0x03 表示 RTS + DTR 信号有效。至于波特率——QEMU 默认就是 115200，而且模拟环境里波特率对数据正确性没有影响（虚拟串口不存在信号失真），所以我们跳过了 DLAB 分频器的设置。在真实硬件上你需要根据目标波特率计算分频系数，但那是以后的事。</p><p>参考来源：OSDev Wiki Serial Ports（<a href="https://wiki.osdev.org/Serial_Ports%EF%BC%89%E5%AF%B9" target="_blank" rel="noreferrer">https://wiki.osdev.org/Serial_Ports）对</a> UART 寄存器映射和初始化流程有完整的说明。</p><p>Intel SDM Vol. 3A Chapter 18 对 I/O 保护机制有详细说明——IOPL 字段在 EFLAGS 中控制 IN/OUT 指令的特权级别，TSS 中的 I/O 位图可以精确到每个端口的访问控制。不过由于我们的内核运行在 Ring 0，所有 I/O 端口操作都是允许的，所以暂时不需要关心这些保护机制。</p><h3 id="qemu-的两条输出通道-串口-vs-debugcon" tabindex="-1">QEMU 的两条输出通道：串口 vs debugcon <a class="header-anchor" href="#qemu-的两条输出通道-串口-vs-debugcon" aria-label="Permalink to &quot;QEMU 的两条输出通道：串口 vs debugcon&quot;">​</a></h3><p>在继续之前，我们需要厘清 Cinux 项目里两个不同的调试输出通道，因为后面会同时用到它们。</p><p>第一个是<strong>串口</strong>（COM1，端口 0x3F8）。这是标准的硬件接口，真实 PC 上也有，通过 QEMU 的 <code>-serial stdio</code> 参数重定向到宿主机的标准输出。数据经过完整的 UART 流程：等待 THRE -&gt; 写 THR -&gt; QEMU 捕获 -&gt; 显示到终端。优点是兼容真实硬件，缺点是每次输出都有轮询开销。</p><p>第二个是 <strong>debugcon</strong>（端口 0xE9）。这是 QEMU 的私有调试设备，不存在于真实硬件上。往端口 0xE9 写一个字节，QEMU 就直接把它输出到 debugcon 日志文件（通过 <code>-debugcon file:debug.log</code> 配置）。不需要初始化、不需要等待、不需要任何握手——单条 <code>outb</code> 指令就完事。缺点是只有 QEMU 支持，而且输出到文件而不是终端。</p><p>在之前几章里我们一直用 debugcon 输出 <code>O</code>、<code>P</code>、<code>L</code>、<code>H</code> 这些标记字符来追踪 Bootloader 的执行进度。从本篇开始，我们要加一条新的通道——串口。串口是内核正式的输出通道，<code>kprintf</code> 的输出走串口；<code>kdebugf</code> 的输出走 debugcon。两条通道互为备份，串口看不到的时候去看 debugcon 日志，往往能找到线索。</p><p>在实际的内核开发中，这种&quot;双通道&quot;策略非常实用——串口输出面向开发者日常查看（实时、直观），debugcon 输出面向自动化测试和事后分析（持久化、不影响串口交互）。两个通道的格式化引擎是同一套代码（下一篇会讲的 <code>vkprintf_impl</code> 模板函数），只是输出后端不同。</p><hr><p>好了，概念铺垫得差不多了，下面我们正式开始动手。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-创建-i-o-端口封装层" tabindex="-1">Step 1: 创建 I/O 端口封装层 <a class="header-anchor" href="#step-1-创建-i-o-端口封装层" aria-label="Permalink to &quot;Step 1: 创建 I/O 端口封装层&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel/mini/driver/io.h</code> 中实现 <code>inb</code> 和 <code>outb</code> 两个内联函数，封装 x86 的 IN/OUT 指令。</p><p><strong>设计思路</strong>：我们选择用 C++ <code>inline</code> 函数而不是 C 宏来封装 I/O 端口操作。宏虽然也能干这活，但它不做类型检查、参数可能被多次求值、而且不好调试。内联函数有完整的类型信息，编译器会在调用点展开，性能和宏完全一样，但安全得多。</p><p>这两个函数的核心是 GCC 内联汇编。<code>inb</code> 的内联汇编模板把 <code>inb</code> 指令的输出约束到 AL 寄存器（通过 <code>&quot;=a&quot;</code> 约束），输入端口号约束为 <code>&quot;Nd&quot;</code>——意思是可以是 8 位立即数也可以是 DX 寄存器（x86 的 I/O 指令只接受这两种形式）。<code>outb</code> 反过来，输入值通过 <code>&quot;a&quot;</code> 约束放到 AL 寄存器，端口号同样是 <code>&quot;Nd&quot;</code> 约束。</p><p>两个函数都给汇编语句加了 <code>volatile</code> 修饰符。这一点非常重要：I/O 操作是副作用操作，编译器不知道 <code>outb</code> 会改变外部硬件状态，如果它觉得这个函数的返回值没被使用就可能把整个调用优化掉。<code>volatile</code> 明确告诉编译器&quot;不要删、不要重排、老老实实生成这条指令&quot;。</p><p><strong>实现约束</strong>：把这两个函数放在一个命名空间下（比如 <code>io</code>），避免和系统头文件里的同名函数冲突。头文件只需要包含 <code>&lt;stdint.h&gt;</code>，不依赖任何其他东西。这两个函数既不依赖 Serial 类也不被 Serial 类以外的代码使用（暂时），所以放在 <code>driver/</code> 目录下作为驱动层的最底层基石。</p><p><strong>踩坑预警</strong>：千万别把 <code>inb</code> 汇编模板里的操作数顺序搞反了。AT&amp;T 语法下 <code>inb %1, %0</code> 的意思是&quot;从 <code>%1</code>（端口）读到 <code>%0</code>（输出变量）&quot;，如果你写成 <code>inb %0, %1</code>，编译不会报错（约束都对得上），但运行时读到的值是垃圾，因为端口号和输出变量的角色互换了。另外，<code>&quot;Nd&quot;</code> 约束里的大写 N 表示&quot;8 位立即数&quot;，小写 n 没有这个含义——写成小写 n 的话编译器可能把端口号放到任意寄存器里，而 x86 的 in/out 指令不接受除 DX 以外的寄存器。</p><p><strong>验证</strong>：这一步只有头文件，没有独立的可执行代码。编译通过即可——确保 <code>kernel/mini/driver/io.h</code> 存在且被其他驱动文件 include 后编译无报错。</p><h3 id="step-2-实现-serial-类——uart-初始化" tabindex="-1">Step 2: 实现 Serial 类——UART 初始化 <a class="header-anchor" href="#step-2-实现-serial-类——uart-初始化" aria-label="Permalink to &quot;Step 2: 实现 Serial 类——UART 初始化&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel/mini/driver/serial.h</code> 和 <code>serial.cpp</code> 中实现串口驱动类，首先是 <code>init()</code> 方法完成 UART 初始化。</p><p><strong>设计思路</strong>：Serial 类需要持有串口的基地址（COM1 = 0x3F8），通过构造函数传入。<code>init()</code> 方法按照标准的 UART 初始化序列设置各个寄存器：禁用中断 -&gt; 设置 8N1 -&gt; 启用 FIFO -&gt; 设置 MCR。这个序列是 OSDev Wiki 和所有 UART 文档都推荐的标准流程。</p><p>类的设计上，我们把寄存器偏移量定义为枚举常量（比如 THR 偏移 0、IER 偏移 1、FCR 偏移 2、LCR 偏移 3、LSR 偏移 5），LSR 的 THRE 位也定义为常量（值 0x20，即第 5 位）。这样代码里写的是 <code>base_port + LSR</code> 和 <code>value &amp; TX_READY</code>，而不是一堆裸的魔数 <code>base_port + 5</code> 和 <code>0x20</code>。</p><p><strong>实现约束</strong>：<code>init()</code> 内部每一步都是调用 <code>io::outb()</code> 写入对应的寄存器偏移。基地址加上偏移就是目标端口地址——<code>io::outb(0x3F8 + 0, value)</code> 写 THR，<code>io::outb(0x3F8 + 1, value)</code> 写 IER，以此类推。不要直接在代码里硬编码 0x3F8，而是用成员变量 <code>base_port</code> 加上偏移枚举来计算。</p><p><strong>踩坑预警</strong>：关于波特率的问题——很多人照着教程设置了 DLAB（Divisor Latch Access Bit，LCR 的第 7 位）和分频系数，然后在 QEMU 里发现设不设都一样。这是因为 QEMU 的虚拟串口不受物理时钟限制，115200 的默认波特率在模拟环境下始终成立。但如果你的代码将来要跑在真实硬件上，就必须正确设置 DLAB 和分频系数。所以一种稳妥的做法是：在 QEMU 开发阶段先跳过波特率设置（反正不影响功能），等上真机测试前再补上。</p><p><strong>验证</strong>：这一步暂时还没有可见的输出效果，需要等 Step 3 的 <code>putc()</code> 实现后一起验证。但你可以先检查 <code>serial.h</code> 是否被正确 include 到项目中——在 <code>main.cpp</code> 中 <code>#include &quot;driver/serial.h&quot;</code>，编译通过说明头文件路径配置正确。</p><h3 id="step-3-实现-serial-类——字符和字符串输出" tabindex="-1">Step 3: 实现 Serial 类——字符和字符串输出 <a class="header-anchor" href="#step-3-实现-serial-类——字符和字符串输出" aria-label="Permalink to &quot;Step 3: 实现 Serial 类——字符和字符串输出&quot;">​</a></h3><p><strong>目标</strong>：实现 <code>Serial::putc()</code> 和 <code>Serial::puts()</code> 方法，前者发送单个字符，后者发送整个字符串。</p><p><strong>设计思路</strong>：<code>putc()</code> 的逻辑很简单：轮询 LSR 的 THRE 位，等它变成 1（表示发送缓冲区空了），然后往 THR 写入目标字符。轮询循环里加了一条 <code>__asm__ volatile(&quot;pause&quot;)</code> 指令——这是 x86 的 hint 指令，告诉 CPU&quot;我在等一个可能很慢的事件&quot;，CPU 会降低功耗并改善超线程环境下的性能。在 QEMU 里其实区别不大，但这是一个好习惯。</p><p><code>puts()</code> 遍历字符串的每个字符，逐个调用 <code>putc()</code>。但有一个关键细节：遇到 <code>\\n</code>（换行符）时，必须先发送一个 <code>\\r</code>（回车符），再发送 <code>\\n</code>。原因是串口终端（以及 QEMU 重定向到的标准输出）对换行的处理方式和 VGA 文本模式不同——串口终端收到 <code>\\n</code> 只是光标下移一行，但不回到行首；只有收到 <code>\\r\\n</code> 组合才会回到行首再下移。如果你不处理这个转换，终端里所有输出都会呈阶梯状偏移，就像打字机不回车直接换行一样——看起来非常混乱。另外，<code>puts()</code> 需要处理传入空指针的情况，直接 return 即可。</p><p><strong>实现约束</strong>：<code>putc()</code> 中轮询条件的写法需要正确——先读 LSR，然后检查 THRE 位是否为 1。如果写成 <code>while (io::inb(base_port + LSR) &amp; THRE)</code> 就错了——THRE 为 1 才表示&quot;可以发送&quot;，所以应该是&quot;等待 THRE 不为 1 的时候继续等&quot;，即 <code>while (!(io::inb(base_port + LSR) &amp; THRE))</code>。</p><p><strong>踩坑预警</strong>：<code>\\r\\n</code> 的问题真的会坑到很多人。在调试时如果看到输出像楼梯一样一行比一行缩进多，多半就是忘了在 <code>\\n</code> 前面加 <code>\\r</code>。而且这个问题在 debugcon 通道里不会出现（debugcon 直接输出到文件，不做终端控制），所以如果你看 debugcon 正常但串口输出阶梯状，第一个嫌疑就是回车符。</p><p><strong>验证</strong>：现在可以验证了。在 <code>main.cpp</code> 的 <code>mini_kernel_main()</code> 里调用 Serial 的初始化和 puts，然后构建运行。具体来说：你需要获取一个 Serial 对象（通过全局单例或者直接构造），调用其 <code>init()</code> 方法，然后调用 <code>puts(&quot;Hello from serial!\\n&quot;)</code>。构建并运行 QEMU 后，串口终端（标准输出）应该出现这行文字。如果看到了阶梯状的偏移输出，说明 <code>\\r\\n</code> 转换没做好。</p><h3 id="step-4-全局-serial-单例与-debugcon-输出函数" tabindex="-1">Step 4: 全局 Serial 单例与 debugcon 输出函数 <a class="header-anchor" href="#step-4-全局-serial-单例与-debugcon-输出函数" aria-label="Permalink to &quot;Step 4: 全局 Serial 单例与 debugcon 输出函数&quot;">​</a></h3><p><strong>目标</strong>：提供一个全局可用的 Serial 对象，以及一个直接写 debugcon 端口的字符输出函数。</p><p><strong>设计思路</strong>：C++ 的全局构造函数机制（<code>.init_array</code> 段）允许我们在 <code>main()</code> 之前执行初始化代码。我们利用这个机制创建一个全局 Serial 对象，在全局构造函数里自动调用 <code>init()</code>——这样内核的任何代码在任何时刻都可以直接使用这个已经初始化好的串口，不需要显式调用初始化。</p><p>具体做法是定义一个返回 Serial 引用的函数（比如 <code>get_initial_serial()</code>），函数内部持有一个静态的 Serial 实例。由于内核的 <code>.init_array</code> 机制已经在之前的 tag 里实现了（<code>boot.S</code> 里的 <code>_init_global_ctors</code> 调用），全局构造函数会在 <code>mini_kernel_main()</code> 被调用之前执行完毕，所以 Serial 的初始化一定在第一次使用之前完成。</p><p>同时我们还要实现一个 <code>debugcon_putc()</code> 函数，它只需要一条 <code>outb</code> 指令把字符写到端口 0xE9。这个函数将来要被 <code>kdebugf</code> 使用。</p><p><strong>实现约束</strong>：<code>debugcon_putc()</code> 不需要等待任何状态位——debugcon 不是真正的 UART，QEMU 直接把写入的字节捕获到日志文件，没有任何缓冲或流控。所以这个函数就是一条裸的 <code>outb</code>。</p><p><strong>踩坑预警</strong>：全局构造函数的执行依赖于链接脚本里 <code>.init_array</code> 段的正确设置，以及 <code>boot.S</code> 里 <code>_init_global_ctors</code> 的正确调用。如果这两步有任何一个出了问题（比如下一节要讲的链接脚本 LMA 计算错误），全局构造函数数组里存放的就是垃圾指针（0xFFFFFFFF 之类），一调用就 Triple Fault。所以如果你的内核在进入 <code>mini_kernel_main()</code> 之前就崩溃了，除了检查 boot.S 的逻辑，还要检查 <code>.init_array</code> 的内容——用 <code>readelf -l kernel.elf</code> 查看 LMA 是否正确。</p><p>另外需要注意的是，如果你的 <code>boot.S</code> 中没有正确处理 <code>__boot_info_ptr</code>（把它保存在 BSS 清零之前），全局构造函数中如果有人访问了依赖 BootInfo 的数据，也会出问题。不过我们的 Serial 不依赖 BootInfo，所以这一点在这里不影响。</p><p><strong>验证</strong>：在 <code>main.cpp</code> 中通过全局获取函数拿到 Serial 对象，调用 <code>puts</code> 输出一段文字。同时用 <code>debugcon_putc</code> 输出几个字符到 debugcon。构建运行后，串口终端应该看到 <code>puts</code> 的输出，<code>debug.log</code> 文件里应该看到 debugcon 的输出。注意，如果你在 <code>mini_kernel_main</code> 之前就尝试使用串口（比如在全局构造函数里），这也是安全的——因为全局 Serial 对象的构造函数会先于其他全局构造函数执行（只要它在链接顺序上排在前面）。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>完成以上步骤后，在项目根目录执行完整构建。确保 CMake 配置中 <code>kernel/mini/CMakeLists.txt</code> 已经把 <code>driver/serial.cpp</code> 加入了 <code>mini_kernel</code> 的源文件列表中（<code>target_sources</code> 或 <code>add_executable</code> 参数中应包含 <code>driver/serial.cpp</code>），然后构建并运行：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cmake -B build -DCMAKE_BUILD_TYPE=Debug</span></span>
<span class="line"><span>cmake --build build</span></span>
<span class="line"><span>cd build &amp;&amp; make run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>如果编译时出现 <code>undefined reference to &#39;cinux::mini::io::outb(unsigned short, unsigned char)&#39;</code> 之类的错误，说明 <code>io.h</code> 的 include 路径配置有问题。检查 <code>serial.cpp</code> 中的 <code>#include &quot;driver/io.h&quot;</code> 能否被正确解析——<code>target_include_directories</code> 应该包含 <code>kernel/mini/</code> 目录。</p><p>QEMU 启动后，你应该在终端看到两个输出通道的结果。串口终端（stdout）应该出现类似这样的输出：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Hello from serial!</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>debugcon 日志文件（<code>build/debug.log</code>）里应该有 Bootloader 阶段的标记字符（如 <code>OPL</code> 加上你通过 debugcon 输出的字符）。</p><p>如果串口终端没有任何输出，但 debugcon 有内容，说明 Serial 驱动有问题——检查 <code>init()</code> 是否被调用、LSR 轮询是否退出、THR 写入是否执行。如果两个通道都没有输出，说明内核根本没有执行到 C++ 代码——检查 boot.S 和 linker script。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><h3 id="区分串口和-debugcon-的问题" tabindex="-1">区分串口和 debugcon 的问题 <a class="header-anchor" href="#区分串口和-debugcon-的问题" aria-label="Permalink to &quot;区分串口和 debugcon 的问题&quot;">​</a></h3><p>当你发现&quot;没有输出&quot;的时候，第一步是判断是串口的问题还是内核根本没跑起来。查看 <code>build/debug.log</code>——如果里面有 Bootloader 的标记字符（OPL 加上高半核跳转标记），说明内核入口代码至少执行到了 boot.S；如果 debugcon 日志里出现了你在 C++ 代码中通过 <code>debugcon_putc</code> 输出的字符，说明 C++ 运行时（包括全局构造函数）已经正常工作了。此时如果串口依然没有输出，问题就锁定在 Serial 驱动本身——多半是初始化序列有遗漏，或者 LSR 轮询卡住了。</p><h3 id="单步跟踪-serial-初始化" tabindex="-1">单步跟踪 Serial 初始化 <a class="header-anchor" href="#单步跟踪-serial-初始化" aria-label="Permalink to &quot;单步跟踪 Serial 初始化&quot;">​</a></h3><p>如果串口初始化看起来没有效果，可以在 GDB 里单步跟踪。在 <code>Serial::init()</code> 设置断点，观察每一步 <code>outb</code> 之后对应端口的状态。虽然 GDB 不能直接读 UART 的内部寄存器，但你可以检查 <code>base_port</code> 的值是否是 0x3F8、<code>io::outb</code> 被调用时的参数是否正确。</p><h3 id="qemu-的串口模拟细节" tabindex="-1">QEMU 的串口模拟细节 <a class="header-anchor" href="#qemu-的串口模拟细节" aria-label="Permalink to &quot;QEMU 的串口模拟细节&quot;">​</a></h3><p>QEMU 对串口的模拟有一个值得注意的特点：它的虚拟 UART 在任何时刻都是&quot;就绪&quot;的——THRE 位永远为 1。这意味着在 QEMU 环境下，<code>putc()</code> 里的轮询循环实际上永远不会真的等，第一次检查就通过了。但在真实硬件上，THRE 位的轮询是必须的，因为物理 UART 发送一个字节需要时间（115200 波特率下大约 87 微秒），连续写入而不等待会导致数据丢失。所以在 QEMU 里&quot;能跑&quot;不代表在真机上也没问题——这一点在代码审查时要注意。</p><h3 id="关于-io-h-中额外的-i-o-函数" tabindex="-1">关于 io.h 中额外的 I/O 函数 <a class="header-anchor" href="#关于-io-h-中额外的-i-o-函数" aria-label="Permalink to &quot;关于 io.h 中额外的 I/O 函数&quot;">​</a></h3><p>实际的 <code>io.h</code> 中除了 <code>inb</code>/<code>outb</code> 之外，还定义了 <code>inw</code>/<code>outw</code>（16 位）和 <code>inl</code>/<code>outl</code>（32 位）函数。它们的实现模式完全相同——唯一的区别是约束的寄存器大小和汇编指令后缀（<code>w</code> 对应 AX，<code>l</code> 对应 EAX）。当前我们只用到了 <code>inb</code>/<code>outb</code>（8 位），但后续章节在配置 <code>isa-debug-exit</code> 设备时会用到 <code>outl</code>，所以理解它们的封装方式是有价值的。如果感兴趣可以提前看看 <code>io.h</code> 中 16 位和 32 位版本的定义，它们和 8 位版本是对称的。</p><h3 id="用-readelf-验证构建产物" tabindex="-1">用 readelf 验证构建产物 <a class="header-anchor" href="#用-readelf-验证构建产物" aria-label="Permalink to &quot;用 readelf 验证构建产物&quot;">​</a></h3><p>构建完成后可以用 <code>readelf -l build/kernel/mini/mini_kernel</code> 查看 ELF 的 Program Headers。你应该能看到至少两个 LOAD 段：一个只读的（.text + .rodata）和一个可写的（.data + .init_array + .bss）。VirtAddr 列应该显示 0xFFFFFFFF8002xxxx 的高半核地址，PhysAddr 列应该显示 0x000000000002xxxx 的物理地址。如果 PhysAddr 不是从 0x20000 开始的，说明链接脚本的 LMA 计算有问题——这个问题我们在第三篇会详细讲到。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>x86 I/O 端口 (PMIO)</td><td>通过 IN/OUT 指令访问，端口号 0x0000-0xFFFF，受 IOPL 和 TSS 位图控制</td></tr><tr><td>inb/outb 内联函数</td><td>GCC 内联汇编封装，<code>volatile</code> 防止优化，<code>&quot;Nd&quot;</code> 约束匹配 x86 端口操作数</td></tr><tr><td>UART 8250/16550 寄存器</td><td>THR(+0W) 发送、IER(+1) 中断、FCR(+2W) FIFO、LCR(+3) 线路、LSR(+5R) 状态</td></tr><tr><td>串口初始化序列</td><td>IER=0 -&gt; LCR=0x03(8N1) -&gt; FCR=0xC7 -&gt; MCR=0x03</td></tr><tr><td>THRE 轮询</td><td>发送前等待 LSR bit5=1，QEMU 下始终就绪但真机必须等待</td></tr><tr><td>\\r\\n 转换</td><td><code>puts()</code> 中遇到 <code>\\n</code> 先输出 <code>\\r</code>，否则终端呈阶梯状偏移</td></tr><tr><td>全局 Serial 单例</td><td>利用 <code>.init_array</code> 全局构造函数自动初始化</td></tr><tr><td>debugcon vs 串口</td><td>debugcon(0xE9) 是 QEMU 私有快通道，串口(0x3F8) 是标准硬件接口</td></tr></tbody></table><hr><p><strong>下一篇预告</strong>：有了串口驱动，内核能发送固定字符串了。但调试时我们需要输出变量的值、地址、十六进制数——这些需要格式化输出的支持。下一篇我们实现 <code>kprintf</code>，内核的格式化输出引擎。</p>`,90)])])}const h=o(a,[["render",n]]);export{b as __pageData,h as default};
