import{c as a,Q as n,j as i,m as e}from"./chunks/framework.iD28w_5a.js";const k=JSON.parse('{"title":"002-boot-gdt-protected-2 · GDT 与保护模式","description":"","frontmatter":{"title":"002-boot-gdt-protected-2 · GDT 与保护模式"},"headers":[],"relativePath":"read-through/002-boot-gdt-protected-2.md","filePath":"read-through/002-boot-gdt-protected-2.md","lastUpdated":1779686691000}'),p={name:"read-through/002-boot-gdt-protected-2.md"};function l(t,s,d,c,o,h){return n(),i("div",null,[...s[0]||(s[0]=[e(`<h1 id="_002-2-·-从实模式到保护模式" tabindex="-1">002-2 · 从实模式到保护模式 <a class="header-anchor" href="#_002-2-·-从实模式到保护模式" aria-label="Permalink to &quot;002-2 · 从实模式到保护模式&quot;">​</a></h1><h2 id="概览" tabindex="-1">概览 <a class="header-anchor" href="#概览" aria-label="Permalink to &quot;概览&quot;">​</a></h2><p>上一篇我们把 GDT 定义好了、linker script 修正了、debugcon 配置到位了——整个舞台已经搭好，只差最后也是最关键的一幕：让 CPU 真正从实模式切换到保护模式。这一步涉及一系列必须严格按序执行的操作：关中断、清 DS、加载 GDTR、设置 CR0 的 PE 位、执行远跳转，然后在 32 位环境下初始化段寄存器和栈。整个过程容不得半点差错——任何一步的顺序或操作码出问题，等待你的都是经典的三重故障（Triple Fault），QEMU 窗口直接关闭，连报错的机会都不给你。</p><p>本文我们就逐行拆解 <code>stage2.S</code> 中从 <code>cli</code> 到 <code>hlt</code> 的完整保护模式切换代码，搞清楚每一步为什么必须存在、为什么在这个位置、如果省略或调换会发生什么。</p><h2 id="流程图" tabindex="-1">流程图 <a class="header-anchor" href="#流程图" aria-label="Permalink to &quot;流程图&quot;">​</a></h2><p>保护模式切换是一个严格有序的过程，Intel SDM Vol.3A §10.9.1 给出了官方推荐的步骤。下面是 Cinux stage2 的完整切换流程：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  Real Mode (16-bit)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  cli   禁用中断                  │  ← IDT 还没设置，中断会触发 #GP</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  DS = 0   清零数据段             │  ← lgdt 地址计算需要 DS*16+offset=offset</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  lgdt gdt_ptr   加载 GDTR        │  ← CPU 记住 GDT 位置，不验证内容</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  CR0.PE = 1   设置保护使能位     │  ← CPU &quot;标记&quot;自己准备进入保护模式</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  ljmp $0x08, $pm_entry           │  ← 远跳转刷新 CS 隐藏缓存</span></span>
<span class="line"><span>  │  (far jump 不可省略!)            │     CPU 开始用 32 位解码</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   保护模式分界线</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  .code32 pm_entry                │</span></span>
<span class="line"><span>  │  DS/ES/FS/GS/SS = 0x10          │  ← 加载数据段选择子</span></span>
<span class="line"><span>  │  ESP = 0x90000                   │  ← 设置保护模式栈</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  outb &#39;P&#39;, 0xE9                  │  ← debugcon 验证输出</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  ┌─────────────────────────────────┐</span></span>
<span class="line"><span>  │  hlt + jmp .pm_halt              │  ← 死循环停机</span></span>
<span class="line"><span>  └─────────────────────────────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  Protected Mode (32-bit)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br><span class="line-number">29</span><br><span class="line-number">30</span><br><span class="line-number">31</span><br><span class="line-number">32</span><br><span class="line-number">33</span><br><span class="line-number">34</span><br><span class="line-number">35</span><br><span class="line-number">36</span><br><span class="line-number">37</span><br><span class="line-number">38</span><br><span class="line-number">39</span><br><span class="line-number">40</span><br><span class="line-number">41</span><br><span class="line-number">42</span><br><span class="line-number">43</span><br><span class="line-number">44</span><br><span class="line-number">45</span><br><span class="line-number">46</span><br><span class="line-number">47</span><br><span class="line-number">48</span><br><span class="line-number">49</span><br><span class="line-number">50</span><br></div></div><p>这个流程图里的每一步都有不可省略的理由，我们接下来按照代码的实际顺序逐一拆解。</p><h2 id="代码精讲" tabindex="-1">代码精讲 <a class="header-anchor" href="#代码精讲" aria-label="Permalink to &quot;代码精讲&quot;">​</a></h2><h3 id="step-1-禁用中断与-ds-清零" tabindex="-1">Step 1: 禁用中断与 DS 清零 <a class="header-anchor" href="#step-1-禁用中断与-ds-清零" aria-label="Permalink to &quot;Step 1: 禁用中断与 DS 清零&quot;">​</a></h3><p>在 VESA 帧缓冲信息保存完毕之后（上一篇的内容），stage2 的实模式使命已经完成，接下来要进入保护模式了。第一步非常简单但绝对不能跳过：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    cli</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                           // disable interrupts again</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // ============================================================</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // Switch to Protected Mode</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // ============================================================</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IMPORTANT:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> lgdt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .code32 uses absolute address, but </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> real mode</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // CPU calculates physical address as </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> + offset. We must set </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // so that the address calculation is </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">correct:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> + </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0x8160</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0x8160</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                  // Clear </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AX</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                 // Set </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (required for </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">lgdt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> real mode)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>cli</code> 指令清除 EFLAGS 寄存器中的 IF（Interrupt Flag）位，让 CPU 不再响应可屏蔽硬件中断。为什么要在这里关中断？原因很直接：保护模式下的中断处理依赖 IDT（Interrupt Descriptor Table），而我们此时还没有设置 IDT。如果中断开着，任何一个硬件中断（定时器、键盘中断等）到达时，CPU 会尝试去 IDT 查找处理函数——但 IDT 还是一片空白，结果就是触发 General Protection Fault（#GP），而 #GP 本身也需要 IDT 来处理，于是又触发 Double Fault，Double Fault 还是找不到处理函数，最终变成 Triple Fault，CPU 直接复位，QEMU 窗口闪一下就没了。</p><p><code>cli</code> 在 stage2 一开始就执行过一次（在 <code>_start</code> 入口处），后来 <code>sti</code> 重新开了中断给 VESA 的 BIOS 调用用。现在 VESA 操作全部结束，是时候再次关掉了。</p><p>紧接着 <code>cli</code> 之后，我们把 <code>%ds</code> 清零。这个操作和 <code>lgdt</code> 指令的地址计算方式直接相关。在实模式下，CPU 计算内存地址的公式是 <code>物理地址 = 段寄存器 × 16 + 偏移</code>。当我们写 <code>lgdt gdt_ptr</code> 时，<code>gdt_ptr</code> 是一个链接器算出的绝对地址（比如 <code>0x8160</code>），CPU 会用 <code>DS × 16 + 0x8160</code> 来计算实际要读的内存位置。如果此时 <code>DS</code> 还是之前 VESA 操作时设的某个非零值（比如 <code>0x600</code>，因为 VESA 缓冲区在 <code>0x6000</code> 附近），那实际读到的地址就变成了 <code>0x600 × 16 + 0x8160 = 0xE160</code>——完全不是 <code>gdt_ptr</code> 的真正位置，读出来的 GDTR 内容是垃圾数据。</p><p>把 <code>DS</code> 清零后，地址计算变成 <code>0 × 16 + 0x8160 = 0x8160</code>，正好是 <code>gdt_ptr</code> 的真实物理地址。说实话，这个坑在调试时真的让人血压拉满——<code>lgdt</code> 本身不会报错，CPU 很乖地把 GDTR 加载了一个错误的 base 地址，然后 <code>ljmp</code> 时去 GDT 查描述符查到垃圾数据，直接三重故障，你甚至不知道是 <code>lgdt</code> 那一步出了问题还是后面的步骤。</p><h3 id="step-2-加载-gdt-lgdt" tabindex="-1">Step 2: 加载 GDT (lgdt) <a class="header-anchor" href="#step-2-加载-gdt-lgdt" aria-label="Permalink to &quot;Step 2: 加载 GDT (lgdt)&quot;">​</a></h3><p>DS 清零之后，现在可以安全地执行 <code>lgdt</code> 了：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Load </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GDT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pointer (with </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, physical address = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0x8160</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    lgdt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gdt_ptr                  // Load </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GDTR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> from absolute address</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>就一行，但背后的 CPU 行为值得展开说说。<code>lgdt</code> 指令做的事情是把 <code>gdt_ptr</code> 指向的 6 字节数据加载到 CPU 内部的 GDTR（Global Descriptor Table Register）。GDTR 是一个 48 位寄存器，由两部分组成：16 位的 Limit（GDT 的大小减 1）和 32 位的 Base（GDT 的线性基地址）。</p><p>在上一篇里我们定义了 <code>gdt_ptr</code>：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdt_ptr:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">word</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (gdt_end - </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gdt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> - </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)      // Limit = sizeof(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GDT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) - </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">23</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> descriptors * </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> - </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .long </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gdt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                      // Linear address of </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GDT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> base (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">-bit)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>执行 <code>lgdt gdt_ptr</code> 后，CPU 内部的 GDTR 变成了 <code>Limit = 0x17</code>（23）、<code>Base = gdt 的链接地址</code>（大约 <code>0x8140</code> 左右，取决于 <code>.text</code> section 的大小）。但请注意一个非常重要的细节：<code>lgdt</code> <strong>只是把数据搬进了 GDTR，它不会验证 GDT 内容的正确性</strong>。如果 GDT 里的描述符是乱写的——比如 Code 段的 Access Byte 写成 <code>0x00</code>——<code>lgdt</code> 不会报错。错误会在后续使用段选择子访问 GDT 条目时才暴露：<code>ljmp</code> 尝试加载 CS 时 CPU 去查描述符，发现 Present 位是 0，直接触发 #GP。</p><p>这意味着如果你的 GDT 定义有误（比如字段写反了、Limit 不够大、Access Byte 拼错了），<code>lgdt</code> 这一步是看不出问题的，你只会看到 CPU 在执行 <code>ljmp</code> 时突然三重故障，然后需要回头去检查 GDT 的每个字节是否正确——我们确实踩过这个坑，调试了半天才发现是 Access Byte 写错了一位。</p><h3 id="step-3-设置-cr0-pe-位" tabindex="-1">Step 3: 设置 CR0.PE 位 <a class="header-anchor" href="#step-3-设置-cr0-pe-位" aria-label="Permalink to &quot;Step 3: 设置 CR0.PE 位&quot;">​</a></h3><p>GDTR 就绪，接下来是真正&quot;拨动开关&quot;的一步：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Set PE bit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> CR0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movl %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cr0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               // Read </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CR0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    orb $0x1, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                 // Set PE bit (bit </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movl %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cr0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               // Write </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CR0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>CR0 是 x86 的控制寄存器之一，其中包含了多个控制系统级行为的标志位。我们关心的只有一个：bit 0，也叫 PE（Protection Enable）位。把它从 0 设成 1，CPU 就进入了保护模式——至少理论上是这样。</p><p>CR0 寄存器的关键位布局如下：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CR0 (32-bit Control Register)</span></span>
<span class="line"><span>Bit 31    Bit 30    Bit 29         Bit 0</span></span>
<span class="line"><span>  PG        CD        NW     ...     PE</span></span>
<span class="line"><span> (Paging) (Cache)  (NoWrite)      (Protection</span></span>
<span class="line"><span>                                 Enable)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>PE 位是最低位（bit 0），值 <code>0x1</code>。为什么我们用 <code>orb $0x1, %al</code> 而不是 <code>orl $0x1, %eax</code>？其实两种写法效果一样——都是把 bit 0 设成 1、其余位不变。用 <code>orb</code> 操作 <code>%al</code>（CR0 的低 8 位）只是编码更紧凑，省一个字节。这里的 <code>orb</code> 对应的机器码是 <code>0C 01</code>（两个字节），而 <code>orl $0x1, %eax</code> 的机器码是 <code>83 C8 01</code>（三个字节）。在 Bootloader 里省一个字节感觉有点鸡肋，但这是一个好习惯——养成只改你需要的位的习惯，避免无意中改动其他控制位。</p><p>你可能注意到了，<code>movl %cr0, %eax</code> 不能直接 <code>or</code> 再 <code>movl %eax, %cr0</code>——必须先读出来、修改、再写回去。这是因为 CR0 不能作为 ALU 指令的操作数，必须通过通用寄存器中转。这是 x86 的规定，没有什么高深的原因，就是 ISA 的限制。</p><p>有一个非常微妙的点需要特别说明：执行 <code>movl %eax, %cr0</code> 把 PE 位设成 1 之后，CPU 并没有立刻变成保护模式——至少从指令流水的角度来看是这样。此时 CPU 的预取队列里可能还缓存着按 16 位解码的指令（因为我们一直都在 <code>.code16</code> 下），CS 寄存器的隐藏部分（Cache）里存的还是实模式的段基址和属性。要让保护模式真正生效，必须做下一步：远跳转。</p><h3 id="step-4-远跳转-ljmp" tabindex="-1">Step 4: 远跳转 (ljmp) <a class="header-anchor" href="#step-4-远跳转-ljmp" aria-label="Permalink to &quot;Step 4: 远跳转 (ljmp)&quot;">​</a></h3><p>这是整个切换流程中最不可省略的一步，也是最容易被忽略的一步：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> Far</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> jump - manually construct for correct </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">-bit encoding</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // ljmp $0x08, $pm_entry </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">-bit </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">format:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ea &lt;offset16&gt; &lt;seg16&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ljmp $0x08, $pm_entry</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>ljmp</code>（Long Jump，也叫 Far Jump）和普通的 <code>jmp</code> 不同——它同时修改 CS 和 EIP，而不是只修改 IP。它的操作数由两部分组成：一个段选择子（<code>$0x08</code>）和一个目标偏移（<code>$pm_entry</code>）。</p><p>为什么这个远跳转绝对不可省略？原因在于 CS 寄存器的工作方式。x86 的段寄存器（CS、DS、ES 等）不是简单的 16 位寄存器——每个段寄存器内部都有一个&quot;隐藏部分&quot;（Hidden Part，也叫 Segment Cache），存储着当前段的 Base Address、Limit 和 Access Rights。在实模式下，CS 的隐藏部分存的是实模式的段属性（Base = CS × 16，Limit = 0xFFFF，16 位默认操作数大小）。当我们在 Step 3 中设置了 CR0.PE = 1 之后，CPU 的&quot;模式&quot;已经变成了保护模式，但 CS 的隐藏缓存还是实模式的旧值——CPU 仍然在用旧的 Base 和旧的属性来翻译地址和解码指令。</p><p>远跳转 <code>ljmp $0x08, $pm_entry</code> 做了两件事：第一，它把 CS 加载为新的段选择子 <code>0x08</code>，CPU 会用 <code>0x08</code> 去查 GDTR 指向的 GDT，找到索引 1（<code>0x08 &gt;&gt; 3 = 1</code>）的描述符，也就是我们定义的 <code>gdt_code</code>——一个 Base=0、Limit=4GB、32 位代码段的描述符。第二，它把目标地址 <code>pm_entry</code> 加载到 EIP 中，同时 CPU 用新描述符的属性刷新了 CS 的隐藏缓存。这意味着从此刻起，CPU 开始按 32 位模式解码和执行指令。</p><p>如果你省掉了这个远跳转，直接在 <code>movl %eax, %cr0</code> 后面继续写 32 位指令，CPU 的预取队列里还残留着按 16 位模式解码的指令，而新写的指令又是按 32 位编码的——两种编码混在一起，CPU 解析出完全错误的指令，大概率直接执行到非法操作码，然后三重故障。这个坑非常经典，Intel SDM Vol.3A §10.9.1 明确要求：设置 PE 位之后必须紧跟一个远跳转操作。</p><p>选择子 <code>0x08</code> 的含义是：<code>Index = 0x08 &gt;&gt; 3 = 1</code>（GDT 中的第二个条目，即 <code>gdt_code</code>），<code>TI = 0</code>（使用 GDT 而不是 LDT），<code>RPL = 0</code>（请求特权级为 Ring 0）。这个选择子正好指向我们在上一篇中定义的 32 位代码段描述符。</p><p>这里有一个关于指令编码的细节值得一提。代码注释里写了 <code>ljmp $0x08, $pm_entry in 16-bit format: ea &lt;offset16&gt; &lt;seg16&gt;</code>。因为我们此时还在 <code>.code16</code> 模式下，GAS 会把 <code>ljmp</code> 编码为 16 位远跳转——操作码 <code>EA</code> 后面跟 2 字节偏移 + 2 字节段选择子，总共 5 字节。但在保护模式下，<code>ljmp</code> 的编码是 <code>EA</code> 后面跟 4 字节偏移 + 2 字节段选择子，总共 7 字节。所以这里的 <code>pm_entry</code> 偏移会被截断为 16 位——这没问题，因为 stage2 的代码全部在 <code>0x8000</code>-<code>0x10000</code> 范围内，16 位偏移足够表示。</p><p>远跳转之后，代码执行流进入 <code>pm_entry</code>，同时 <code>.code32</code> 指令告诉汇编器从这开始生成 32 位指令编码——CPU 和汇编器终于同步了。</p><h3 id="step-5-保护模式段寄存器初始化" tabindex="-1">Step 5: 保护模式段寄存器初始化 <a class="header-anchor" href="#step-5-保护模式段寄存器初始化" aria-label="Permalink to &quot;Step 5: 保护模式段寄存器初始化&quot;">​</a></h3><p>远跳转成功后，CPU 已经在保护模式下运行了。但此时的段寄存器（DS、ES、FS、GS、SS）还残留着实模式的值，需要全部重载为保护模式的段选择子：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">// ============================================================</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">// Protected Mode Entry Point</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">// ============================================================</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.code32                          // Now </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">-bit protected mode</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pm_entry:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Set up data segment registers</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw $0x10, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               // Data selector value</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ds</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">es</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">fs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movw %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ss</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p><code>.code32</code> 指令切换汇编器的输出模式——从这行开始，所有指令按 32 位编码生成。注意 <code>.code32</code> 不是一个 CPU 指令，它不会出现在最终的二进制文件中；它只是告诉 GAS &quot;接下来请用 32 位操作码编码&quot;。真正让 CPU 切换到 32 位执行的是上一步的 <code>ljmp</code> 刷新了 CS 的隐藏缓存。两者必须配合：<code>ljmp</code> 让 CPU 用 32 位解码，<code>.code32</code> 让汇编器生成 32 位编码，缺一不可。</p><p>接下来我们把 DS、ES、FS、GS、SS 全部设成 <code>0x10</code>。这个选择子的含义和 <code>0x08</code> 类似：<code>Index = 0x10 &gt;&gt; 3 = 2</code>（GDT 中的第三个条目，即 <code>gdt_data</code>），<code>TI = 0</code>（使用 GDT），<code>RPL = 0</code>（Ring 0）。它指向我们定义的 32 位数据段描述符——Base=0、Limit=4GB、可读写。</p><p>为什么需要给每个段寄存器都加载一次？因为段寄存器的&quot;隐藏缓存&quot;不会因为你设置了 CR0.PE 就自动更新。在远跳转时我们只刷新了 CS，其余的段寄存器（DS、ES、FS、GS、SS）还保持着实模式的旧缓存。在保护模式下，任何通过这些段寄存器的内存访问都会使用隐藏缓存中的 Base 和 Limit 来翻译地址——如果缓存里还是实模式的旧值，内存访问就会出错。逐一重载每个段寄存器，CPU 就会用新的选择子去 GDT 查对应的描述符，然后把正确的 Base、Limit、Access Rights 写入隐藏缓存。</p><p>你可能会注意到我们没有给 CS 再做一次 <code>movw</code>——因为远跳转 <code>ljmp $0x08, $pm_entry</code> 已经完成了 CS 的重载。DS、ES、FS、GS 这些数据段寄存器不能通过远跳转来刷新，必须用 <code>movw</code> 直接写入。</p><h3 id="step-6-栈设置" tabindex="-1">Step 6: 栈设置 <a class="header-anchor" href="#step-6-栈设置" aria-label="Permalink to &quot;Step 6: 栈设置&quot;">​</a></h3><p>段寄存器全部就位后，接下来设置保护模式下的栈：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Set up new stack</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movl $0x90000, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">esp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">           // New stack </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> protected mode</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>ESP</code> 设为 <code>0x90000</code>，意味着栈从 <code>0x90000</code> 向低地址方向增长。为什么选这个地址？几个原因：第一，<code>0x90000</code> 在实模式可寻址范围内（1MB 以内），保护模式切换刚完成时 CPU 还没启用分页，访问的仍然是物理地址，用这个地址不会出问题。第二，它远离 stage2 的代码区域（<code>0x8000</code>-<code>0x10000</code>）和 VESA 缓冲区（<code>0x6000</code>-<code>0x64FF</code>），不会冲突。第三，它离 640KB 的&quot;常规内存&quot;上限还有一段距离，给栈留了足够的增长空间——从 <code>0x90000</code> 向下增长到 <code>0x80000</code> 有整整 64KB，对 Bootloader 阶段来说绰绰有余。</p><p>在实模式阶段，我们用的是 <code>SS:SP = 0x9000:0xFFFE</code>，对应物理地址 <code>0x9000 × 16 + 0xFFFE = 0x9FFFE</code>。进入保护模式后，段寄存器的语义变了——SS 不再存储段地址，而是存储段选择子，所以我们重新把 SS 设成 <code>0x10</code>（数据段选择子），然后把 ESP 直接设成 <code>0x90000</code>。在 Flat Memory Model 下，SS 对应的数据段 Base 是 <code>0x00000000</code>，所以 <code>0x90000</code> 就是实际的物理栈顶地址。</p><h3 id="step-7-debugcon-验证输出" tabindex="-1">Step 7: Debugcon 验证输出 <a class="header-anchor" href="#step-7-debugcon-验证输出" aria-label="Permalink to &quot;Step 7: Debugcon 验证输出&quot;">​</a></h3><p>所有初始化工作完成，现在用一个最简单的操作来验证保护模式确实已经正常工作了：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Debug </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">output:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &#39;P&#39; for Protected Mode</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x50, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               // &#39;P&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9               // Debugcon output</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>0x50</code> 是字符 <code>P</code> 的 ASCII 码。<code>outb</code> 指令把 <code>%al</code> 的值写到 I/O 端口 <code>0xE9</code>。这个端口是 QEMU 的 debug console——在 <code>cmake/qemu.cmake</code> 里我们已经配置了 <code>-debugcon file:debug.log -global isa-debugcon.iobase=0xe9</code>，所以写入 <code>0xE9</code> 的字节会被 QEMU 捕获并写入 <code>debug.log</code> 文件。</p><p>你可能会问：为什么不继续用之前的 <code>INT 0x10</code> BIOS 中断来输出字符？原因很简单——保护模式下 BIOS 中断不可用。BIOS 中断服务程序运行在实模式下，使用实模式的段地址翻译方式。进入保护模式后，CPU 使用 GDT 中的描述符进行地址翻译，中断处理方式也完全变了（需要 IDT 而不是 IVT）。如果我们强行 <code>int $0x10</code>，CPU 会尝试在保护模式下处理这个中断——没有 IDT，结果就是 #GP，然后三重故障。所以我们不能再用任何 BIOS 服务，必须使用纯硬件 I/O 操作来输出调试信息。</p><p>debugcon 是最简单的选择：一条 <code>outb</code> 就够了，不需要初始化 UART、不需要设置波特率、不需要查询发送缓冲区是否为空。当然，debugcon 是 QEMU 专有的调试接口，真实硬件上不存在——但这在 Bootloader 开发阶段完全够用，后续进入 long mode 之后我们会切换到 COM1 串口作为正式的输出通道。</p><p>如果一切顺利，执行完 <code>outb %al, $0xE9</code> 之后，<code>debug.log</code> 文件里会出现一个 <code>P</code>。这个孤零零的字符就是我们的胜利旗帜——它意味着从 <code>cli</code> 到 <code>outb</code> 的整个保护模式切换流程全部正确执行，CPU 现在运行在 32 位保护模式下，段寄存器、栈、I/O 操作全部正常。</p><h3 id="step-8-停机循环" tabindex="-1">Step 8: 停机循环 <a class="header-anchor" href="#step-8-停机循环" aria-label="Permalink to &quot;Step 8: 停机循环&quot;">​</a></h3><p>验证输出完成后，stage2 的保护模式阶段任务就结束了。在当前的 tag 002 里，我们还没有实现后续的 long mode 切换，所以直接进入停机：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    // </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Infinite halt (we&#39;re </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> protected mode now)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    cli</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pm_halt:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    hlt</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    jmp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .pm_halt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>cli</code> 再次确认中断关闭——虽然前面已经 <code>cli</code> 过了，但这是防御性编程，确保万无一失。<code>hlt</code> 指令让 CPU 进入 halt 状态，停止执行直到下一个外部中断到来。但因为中断已经关了，所以 <code>hlt</code> 会一直停住——直到 NMI（不可屏蔽中断）或者系统复位。不过 NMI 在正常情况下不会发生，所以 CPU 就停在这了。下面的 <code>jmp .pm_halt</code> 是额外的保险：万一 CPU 因为某种原因从 <code>hlt</code> 唤醒了（比如某些虚拟化场景），就跳回来再 <code>hlt</code> 一次，形成死循环。</p><h2 id="设计决策" tabindex="-1">设计决策 <a class="header-anchor" href="#设计决策" aria-label="Permalink to &quot;设计决策&quot;">​</a></h2><h3 id="决策-栈地址选择-0x90000" tabindex="-1">决策：栈地址选择 0x90000 <a class="header-anchor" href="#决策-栈地址选择-0x90000" aria-label="Permalink to &quot;决策：栈地址选择 0x90000&quot;">​</a></h3><p><strong>问题</strong>：保护模式下的栈放在哪个地址？</p><p><strong>本项目的做法</strong>：<code>ESP = 0x90000</code>，栈从 <code>0x90000</code> 向下增长到 <code>0x80000</code>，有 64KB 空间。</p><p><strong>备选方案</strong>：xv6 使用 <code>0x7C00</code> 作为栈顶——即 MBR 的加载地址，栈从 <code>0x7C00</code> 向下增长到 <code>0x0000</code>。这个做法更&quot;节约&quot;内存，但只有 31KB 的栈空间，且栈和 BIOS 数据区（<code>0x400</code>-<code>0x4FF</code>）以及中断向量表（<code>0x0000</code>-<code>0x03FF</code>）靠得很近，稍不留神就会覆盖。</p><p><strong>为什么选 0x90000</strong>：在 Bootloader 阶段的内存布局中，<code>0x8000</code>-<code>0x10000</code> 是 stage2 代码区，<code>0x6000</code>-<code>0x64FF</code> 是 VESA 缓冲区，<code>0x5000</code>-<code>0x5FFF</code> 是后续的 E820 内存映射。<code>0x90000</code> 在这些区域之上，有足够的间隔，64KB 的空间对 Bootloader 阶段来说非常宽裕。更重要的是，这个地址在实模式可寻址范围内（低于 1MB），保护模式切换刚完成时还没有分页，直接用物理地址即可。</p><h3 id="决策-使用-debugcon-而非串口" tabindex="-1">决策：使用 debugcon 而非串口 <a class="header-anchor" href="#决策-使用-debugcon-而非串口" aria-label="Permalink to &quot;决策：使用 debugcon 而非串口&quot;">​</a></h3><p><strong>问题</strong>：保护模式下如何输出验证信息？</p><p><strong>本项目的做法</strong>：QEMU debugcon 端口 <code>0xE9</code>，一条 <code>outb</code> 即可。</p><p><strong>备选方案</strong>：初始化 COM1 串口（I/O 端口 <code>0x3F8</code>），需要设置波特率除数、中断使能、FIFO 控制等多个寄存器，大约需要 5-8 条 I/O 操作。</p><p><strong>为什么选 debugcon</strong>：在 tag 002 阶段，我们只验证&quot;保护模式切换是否成功&quot;，一个字节就够了。Debugcon 零配置——不需要初始化 UART、不需要等待发送就绪、不需要处理中断，直接写端口就完事。真正的串口初始化会留到 tag 003 进入 long mode 之后，届时我们需要一个更完善的输出通道来打印内核启动日志。</p><p>当然，debugcon 的局限在于它是 QEMU 专有的，Bochs 仿真器也支持但真实硬件上不存在。不过对于 Bootloader 开发来说，我们几乎 100% 时间都在 QEMU 里调试，这个局限完全可接受。</p><h2 id="扩展方向" tabindex="-1">扩展方向 <a class="header-anchor" href="#扩展方向" aria-label="Permalink to &quot;扩展方向&quot;">​</a></h2><p><strong>1. 保护模式下的串口输出</strong>（中等难度）—— 当前我们只用 debugcon 输出了一个字节。尝试在保护模式下初始化 COM1 串口（8-N-1, 115200 baud），实现一个 <code>printf</code> 风格的输出函数。关键步骤是：设置波特率除数（先写 <code>0x80</code> 到 LCR 寄存器打开 DLAB，然后写 DLL 和 DLH），配置 Line Control Register（8 位数据、无校验、1 位停止位），然后通过查询 LSR 寄存器的 THRE 位来逐字符发送。这将让你在真实硬件上也能看到调试输出。</p><p><strong>2. 保护模式下的屏幕输出（Framebuffer 写字）</strong>（较高难度）—— 我们已经通过 VESA 设置了 <code>1024×768×32</code> 的图形模式，帧缓冲区的物理地址保存在 <code>0x6400</code>。进入保护模式后，可以尝试直接往帧缓冲区写像素数据来显示文字——你需要一个位图字体（可以从 Linux 内核的 <code>fonts/</code> 目录找一个 8×16 的 console font），然后在帧缓冲区上逐像素绘制字符。这个练习能让你深入理解&quot;图形模式没有文字输出&quot;意味着什么。</p><p><strong>3. 多段模型（Non-Flat GDT）</strong>（进阶）—— 当前的 Flat Model 把代码段和数据段都设成 Base=0、Limit=4GB。作为学习练习，尝试定义不同的 Base 和 Limit 来创建隔离的内存区域——比如把代码段限制在 <code>0x8000</code>-<code>0x10000</code>，数据段限制在 <code>0x10000</code>-<code>0x90000</code>，看看 CPU 如何通过段级保护阻止越界访问。虽然现代 OS 不使用这种方式（全部交给分页），但理解分段保护的工作原理对理解 x86 架构的历史和设计哲学非常有帮助。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>Intel SDM Vol.3A §10.9.1 — Switching to Protected Mode：完整的 6 步切换流程（CLI、LGDT、CR0.PE、Far Jump、段寄存器重载、第一条保护模式指令）</li><li>Intel SDM Vol.3A §10.1 — Processor Initialization：CR0.PE 位定义，处理器状态转换</li><li>Intel SDM Vol.3A §2.4.1 — Global Descriptor Table Register (GDTR)：<code>lgdt</code> 指令的行为和 GDTR 寄存器格式</li><li>OSDev Wiki: <a href="https://wiki.osdev.org/Protected_Mode" target="_blank" rel="noreferrer">Protected Mode</a> — 进入保护模式的步骤和常见陷阱</li><li>xv6 bootasm.S: <a href="https://github.com/mit-pdos/xv6-public/blob/master/bootasm.S" target="_blank" rel="noreferrer">https://github.com/mit-pdos/xv6-public/blob/master/bootasm.S</a> — xv6 的保护模式切换实现，流程与 Cinux 几乎一致</li></ul>`,84)])])}const E=a(p,[["render",l]]);export{k as __pageData,E as default};
