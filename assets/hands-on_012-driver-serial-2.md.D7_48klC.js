import{c as i,Q as e,j as a,m as n}from"./chunks/framework.iD28w_5a.js";const k=JSON.parse('{"title":"012-driver-serial-2 · 串口驱动","description":"","frontmatter":{"title":"012-driver-serial-2 · 串口驱动"},"headers":[],"relativePath":"hands-on/012-driver-serial-2.md","filePath":"hands-on/012-driver-serial-2.md","lastUpdated":1779686691000}'),d={name:"hands-on/012-driver-serial-2.md"};function t(p,s,l,o,c,r){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="_012-2-kprintf-引擎重构-提取、对齐修饰符与单元测试" tabindex="-1">012-2 kprintf 引擎重构：提取、对齐修饰符与单元测试 <a class="header-anchor" href="#_012-2-kprintf-引擎重构-提取、对齐修饰符与单元测试" aria-label="Permalink to &quot;012-2 kprintf 引擎重构：提取、对齐修饰符与单元测试&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一篇我们把 Serial 驱动独立成了正式的驱动模块，kprintf 也完成了格式化引擎与 Serial 的 lambda 集成。但如果你对比一下大内核（<code>kernel/lib/kprintf.cpp</code>）和小内核（<code>kernel/mini/lib/kprintf.cpp</code>）的代码，你会发现一个非常尴尬的事实——两个文件里的格式化引擎几乎是复制粘贴的。同样的十进制转换、同样的十六进制转换、同样的格式说明符解析，写了两遍，还各自维护。这在软件工程里叫做 DRY 违反（Don&#39;t Repeat Yourself），在内核开发里尤其危险——因为将来你要加一个新的格式说明符（比如 <code>%b</code> 二进制输出），必须同时改两个文件，漏一个就是 bug。</p><p>这一篇我们要做三件事：把格式化引擎提取成一个共享的头文件模板（<code>vkprintf_impl.hpp</code>），给这个引擎加上左对齐修饰符（<code>%-Nd</code>、<code>%-Ns</code>）的支持，然后把单元测试彻底重写——让测试直接使用真正的格式化引擎而不是手写的 mock 副本。完成这一篇后，你会拥有一个在 host 端就能跑测试的格式化引擎，以后任何格式说明符的修改都能通过 <code>ctest</code> 自动验证，不需要每次都启动 QEMU。</p><p>前置知识方面，你需要了解上一篇的 Serial 驱动和 kprintf 的基本架构。对 C++ 模板有基本了解就够了——我们用的模板非常简单，就是一个接受回调函数的泛型函数。</p><hr><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="dry-原则与内核代码共享" tabindex="-1">DRY 原则与内核代码共享 <a class="header-anchor" href="#dry-原则与内核代码共享" aria-label="Permalink to &quot;DRY 原则与内核代码共享&quot;">​</a></h3><p>DRY（Don&#39;t Repeat Yourself）是软件工程里最基本的信条之一——每一份知识在系统里应该只有一个明确的表示。在内核开发里，这个原则说起来容易做起来难。大内核和小内核是两个独立的编译目标，它们链接不同的源文件、运行在不同的地址空间（小内核在 0x200000 物理地址，大内核在 0x1000000 高半核虚拟地址），但它们需要共享一些纯粹的逻辑代码——比如格式化引擎。</p><p>Cinux 的解决方案是把格式化引擎提取成一个头文件模板（<code>vkprintf_impl.hpp</code>），放在 <code>kernel/lib/private/</code> 目录下。这个头文件不包含任何硬件相关的代码——不 include <code>io.hpp</code>，不 include <code>serial.hpp</code>，它只依赖 <code>&lt;stdarg.h&gt;</code> 和 <code>&lt;stdint.h&gt;</code> 这两个语言级别的头文件。大内核的 <code>kprintf.cpp</code> 和小内核的 <code>kprintf.cpp</code> 各自 include 这个共享头文件，然后传入自己的输出回调函数。</p><p>这个设计的关键洞察是：格式化引擎接受一个 <code>OutputFn</code> 回调——任何可调用的东西，只要能接受一个 <code>char</code> 参数就行。串口输出传 <code>g_serial.putc</code>，debugcon 输出传 <code>outb_debugcon</code>，单元测试传一个往 <code>std::string</code> 追加字符的 lambda。格式化引擎完全不知道字节最终去了哪里，它只负责解析格式字符串和生成字符。</p><h3 id="模板化-outputfn-的精妙之处" tabindex="-1">模板化 OutputFn 的精妙之处 <a class="header-anchor" href="#模板化-outputfn-的精妙之处" aria-label="Permalink to &quot;模板化 OutputFn 的精妙之处&quot;">​</a></h3><p>如果你对 C++ 模板不太熟悉，可能会觉得&quot;接受任意回调函数&quot;听起来很复杂。实际上它极其简单——<code>vkprintf_impl</code> 是一个函数模板，模板参数 <code>OutputFn</code> 可以是任何类型。调用的时候编译器会根据你传入的实参自动推导 <code>OutputFn</code> 的类型。如果传的是 lambda，<code>OutputFn</code> 就是那个 lambda 的编译器生成类型；如果传的是函数指针，<code>OutputFn</code> 就是函数指针类型。</p><p>这种设计比传统的&quot;传函数指针 + void* ctx&quot;方式有几个好处。第一，lambda 可以内联——编译器在优化的时候可以看到 lambda 的完整实现，有机会把回调调用完全内联掉，生成和手写循环一样高效的代码。第二，lambda 可以捕获状态——比如测试里的 MockOutput 可以是一个捕获了 <code>std::string</code> 引用的 lambda，不需要额外的 context 参数。第三，类型安全——编译器在编译期就能检查 OutputFn 的签名是否正确，而不是运行时才发现函数指针类型不对。</p><h3 id="左对齐修饰符-nd-和-ns" tabindex="-1">左对齐修饰符：<code>%-Nd</code> 和 <code>%-Ns</code> <a class="header-anchor" href="#左对齐修饰符-nd-和-ns" aria-label="Permalink to &quot;左对齐修饰符：\`%-Nd\` 和 \`%-Ns\`&quot;">​</a></h3><p>标准 printf 的格式说明符语法是 <code>%[flags][width][.precision][length]type</code>。Cinux 的 kprintf 不需要实现完整的 printf——那太复杂了，也没有必要。但基本的宽度控制和对齐方式是很有用的，尤其是在打印表格数据或者对齐日志的时候。</p><p>这个 tag 新增的左对齐修饰符（<code>-</code> flag）的工作方式是这样的：对于 <code>%-10d</code>（最小宽度 10，左对齐），如果格式化后的数字只有 4 个字符，引擎会在数字后面补 6 个空格，而不是像默认的右对齐那样在前面补空格。对于 <code>%-20s</code>（最小宽度 20，左对齐字符串），如果字符串只有 5 个字符，引擎会在字符串后面补 15 个空格。</p><p>实现上，解析器在遇到 <code>%</code> 之后先检查是否有 <code>-</code> 标志，如果有就设置 <code>left_align = true</code>。然后检查是否有 <code>0</code> 标志（零填充），然后解析宽度数字，然后解析长度修饰符（<code>l</code>、<code>ll</code>），最后根据类型字符走不同的分支。在输出阶段，左对齐和右对齐的区别就是&quot;先输出内容再补 padding&quot;还是&quot;先补 padding 再输出内容&quot;。</p><p>有一个边界情况值得特别说明——<code>%06d</code> 配合负数时的行为。比如 <code>%06d</code> 格式化 -42，你期望的输出应该是 <code>-00042</code>（符号在最前面，中间补零，最后是数字），而不是 <code>000-42</code>（零填充跑到符号前面去了）。为了正确处理这个情况，格式化引擎在遇到&quot;右对齐 + 零填充 + 有符号&quot;的组合时，会先把负号输出，然后补零，最后输出数字部分。这个顺序很关键——写错了会变成一个虽然能编译通过但输出明显错误的 bug。</p><hr><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-提取格式化引擎到共享头文件" tabindex="-1">Step 1: 提取格式化引擎到共享头文件 <a class="header-anchor" href="#step-1-提取格式化引擎到共享头文件" aria-label="Permalink to &quot;Step 1: 提取格式化引擎到共享头文件&quot;">​</a></h3><p><strong>目标</strong>：创建 <code>kernel/lib/private/vkprintf_impl.hpp</code>，把 <code>kprintf.cpp</code> 里的 <code>format_decimal</code>、<code>format_hex</code> 和 <code>vkprintf_impl</code> 模板全部提取出来，放到 <code>cinux::lib::detail</code> 命名空间下。</p><p><strong>设计思路</strong>：提取工作的核心是&quot;剥离硬件依赖&quot;。原来在 <code>kprintf.cpp</code> 里，格式化函数和 Serial 输出是紧耦合的——<code>vkprintf</code> 直接调用 <code>g_serial.putc</code>。提取之后，<code>vkprintf_impl</code> 变成一个函数模板，接受一个 <code>OutputFn</code> 参数作为输出回调。所有格式化函数（<code>format_decimal</code>、<code>format_hex</code>）是纯计算函数——接受一个数值和缓冲区，返回格式化后的字符串，不涉及任何 I/O 操作，所以它们可以直接搬过来，不需要修改。</p><p><strong>实现约束</strong>：共享头文件的结构应该是这样的。首先包含 <code>&lt;stdarg.h&gt;</code> 和 <code>&lt;stdint.h&gt;</code> 两个标准头文件。在 <code>cinux::lib::detail</code> 命名空间内定义两个格式化辅助函数：<code>format_decimal(int64_t value, char* buffer, int buffer_size)</code> 返回写入的字符数，需要处理 INT64_MIN 的特殊情况（因为 <code>-INT64_MIN</code> 会溢出）；<code>format_hex(uint64_t value, char* buffer, int buffer_size, bool lowercase)</code> 处理十六进制，<code>lowercase</code> 参数控制使用 a-f 还是 A-F。这两个函数都使用&quot;先逆序写入临时数组再翻转&quot;的经典手法来生成数字字符串。</p><p>然后定义核心的 <code>vkprintf_impl</code> 函数模板，模板参数 <code>OutputFn</code> 是一个可以调用 <code>void(char)</code> 的类型。函数签名接受 <code>OutputFn&amp;&amp; putc_fn</code>、<code>const char* fmt</code>、<code>va_list args</code> 三个参数。函数体就是格式字符串的解析循环——遇到普通字符直接输出，遇到 <code>%</code> 就解析格式说明符。</p><p>头文件名用 <code>private/</code> 子目录是有意为之的——这是一个内部实现细节，不应该被外部的公开头文件 include。只有 <code>kprintf.cpp</code> 和测试文件才应该 include 它。</p><p><strong>踩坑预警</strong>：提取的时候最容易犯的错误是忘记处理 va_list 的语义。<code>va_list</code> 在某些 ABI 上是一个数组类型（比如 x86_64 的 <code>va_list</code> 实际上是 <code>__va_list_tag[1]</code>），这意味着按值传递 <code>va_list</code> 的行为可能和你预期的不一样。在 Cinux 的实现里，<code>vkprintf_impl</code> 接受 <code>va_list</code> 按值传递，调用者（<code>kprintf</code>、<code>kvprintf</code>）用 <code>va_start</code>/<code>va_end</code> 管理生命周期。这个设计在 GCC 的 x86_64 目标上是安全的，但如果将来移植到其他架构可能需要注意 va_list 的传递语义。</p><p><strong>验证</strong>：提取后重新构建，所有串口输出应该和之前完全一致。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：所有输出和提取前完全一样，没有任何变化</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 特别注意 int $3 触发后的寄存器 dump 中的 %p 和 %d 格式化是否正确</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h3 id="step-2-实现左对齐修饰符" tabindex="-1">Step 2: 实现左对齐修饰符 <a class="header-anchor" href="#step-2-实现左对齐修饰符" aria-label="Permalink to &quot;Step 2: 实现左对齐修饰符&quot;">​</a></h3><p><strong>目标</strong>：在 <code>vkprintf_impl</code> 的格式解析器中添加 <code>-</code> 标志的解析，并在 <code>%d</code>、<code>%u</code>、<code>%x</code>、<code>%X</code>、<code>%s</code> 的输出分支中实现左对齐逻辑。</p><p><strong>设计思路</strong>：格式说明符的解析顺序是固定的——<code>%</code> 之后依次检查 <code>-</code>（左对齐标志）、<code>0</code>（零填充标志）、宽度数字（0-9 序列）、长度修饰符（<code>l</code>、<code>ll</code>），最后是类型字符。左对齐标志只需要一个 bool 变量来记录。在输出阶段，对于右对齐的情况先输出 padding 再输出内容，对于左对齐的情况先输出内容再输出 padding。对于 <code>%s</code> 的左对齐，需要先测量字符串长度，然后决定补多少空格。</p><p><strong>实现约束</strong>：解析阶段在遇到 <code>%</code> 之后（消费掉 <code>%</code> 之后），首先检查 <code>*fmt == &#39;-&#39;</code>，如果是就设置 <code>left_align = true</code> 并前进一步。然后检查 <code>*fmt == &#39;0&#39;</code> 设置 <code>zero_pad</code>。然后解析宽度数字。这些标志的解析顺序不能乱——<code>%-10d</code> 里的 <code>-</code> 必须在宽度 <code>10</code> 之前被消费。</p><p>对于 <code>%d</code>（有符号十进制）的左对齐，有一个特殊的边界情况需要处理：当 <code>left_align = false</code> 且 <code>zero_pad = true</code> 且值为负数时，需要先输出负号，然后补零，最后输出数字——也就是前面说的 <code>%06d</code> 配合 -42 应该得到 <code>-00042</code> 而不是 <code>000-42</code>。这意味着 <code>%d</code> 的输出分支需要三个子路径：右对齐+零填充+有符号（先符号再零填充再数字）、右对齐普通（先 padding 再整个内容）、左对齐（先内容再空格 padding）。</p><p>对于 <code>%s</code> 的左对齐，先测量字符串长度（逐字符遍历到 <code>\\0</code>），然后如果 <code>left_align</code> 为 true，先输出字符串所有字符，再补空格到指定宽度；如果为 false，先补空格再输出字符串。</p><p><strong>踩坑预警</strong>：左对齐的 padding 字符永远是空格——即使你同时指定了 <code>0</code> 标志。也就是说 <code>%-010d</code> 等价于 <code>%-10d</code>，零填充标志在左对齐模式下应该被忽略。这是因为左对齐的语义就是&quot;内容靠左、右边补空格&quot;，如果你用零来填充，数字的可读性会被破坏。如果你发现左对齐的数字后面跟了一堆零而不是空格，检查一下是不是在左对齐的分支里也用了 <code>zero_pad</code> 标志。另外，<code>%06d</code> 和负数的组合是最容易出错的边界情况，一定要写测试覆盖。</p><p><strong>验证</strong>：这一步最好在单元测试里验证（下一步会写），但也可以直接在 kernel_main 里加一行格式化输出来手动检查。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 在 kernel_main 里添加一行测试输出，检查对齐效果</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：左对齐的数字和字符串后面跟的是空格，不是零</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 右对齐+零填充+负数的输出格式应该是 -00042 而不是 000-42</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h3 id="step-3-重写单元测试——用真正的格式化引擎" tabindex="-1">Step 3: 重写单元测试——用真正的格式化引擎 <a class="header-anchor" href="#step-3-重写单元测试——用真正的格式化引擎" aria-label="Permalink to &quot;Step 3: 重写单元测试——用真正的格式化引擎&quot;">​</a></h3><p><strong>目标</strong>：重写 <code>tests/unit/test_kprintf.cpp</code>，让它直接 include <code>kernel/lib/private/vkprintf_impl.hpp</code>，使用 MockOutput 类捕获输出，测试所有格式说明符包括新增的左对齐。</p><p><strong>设计思路</strong>：旧版的单元测试有一个严重的问题——它复制了格式化辅助函数然后在测试文件里重新实现了一个 mock vprintf。这意味着测试测的不是真正的格式化引擎，而是一份&quot;可能已经和真实代码不同步&quot;的副本。新版的测试直接 include 共享头文件，使用真正的 <code>vkprintf_impl</code> 函数模板，只是传入一个 mock 的 OutputFn——一个往 <code>std::string</code> 里追加字符的 lambda。这样测试覆盖的就是实际运行的代码，不存在不同步的问题。</p><p><strong>实现约束</strong>：测试文件需要定义一个辅助函数 <code>do_printf</code>，它接受格式字符串和可变参数，内部创建一个空的 <code>std::string</code>，然后用 <code>vkprintf_impl</code> 格式化到一个往这个字符串追加字符的 lambda 里，最后返回这个字符串。这样测试断言就可以写成非常清晰的形式——比较 <code>do_printf(&quot;%d&quot;, 42)</code> 的返回值是否等于 <code>&quot;42&quot;</code>。</p><p>测试用例需要覆盖以下场景：基本的 <code>%d</code>、<code>%u</code>、<code>%x</code>、<code>%X</code>、<code>%s</code>、<code>%c</code>、<code>%p</code>、<code>%%</code>；宽度修饰符 <code>%10d</code>（右对齐空格填充）、<code>%010d</code>（右对齐零填充）；左对齐 <code>%-10d</code>、<code>%-20s</code>；负数零填充 <code>%06d</code> 配合 -42 得到 <code>-00042</code>；混合格式说明符的字符串；边界值（0、INT_MAX、INT_MIN、0x0 指针）；长度修饰符 <code>%ld</code>、<code>%lu</code>、<code>%llx</code>。</p><p><strong>踩坑预警</strong>：在 host 端编译内核代码时要注意编译器的差异。<code>vkprintf_impl.hpp</code> 使用了 <code>va_list</code> 和 <code>va_start</code>/<code>va_end</code>，这些在 host 端的 g++ 下是可用的，但如果你不小心 include 了任何内核特有的头文件（比如 <code>io.hpp</code>），编译会直接失败——因为 host 端没有 <code>io_outb</code> 这些函数。确保测试文件只 include <code>vkprintf_impl.hpp</code> 和标准库头文件。</p><p>另一个容易忽略的问题是 <code>int64_t</code> 在 host 端和内核端的定义可能不同——在 x86_64 LP64 ABI 上，<code>long</code> 和 <code>long long</code> 都是 64 位的，所以 <code>%ld</code> 和 <code>%lld</code> 的行为在 host 端和内核端应该一致。但如果将来移植到 32 位目标，<code>long</code> 变成 32 位，这些格式说明符的行为就会不一样。目前 Cinux 只支持 x86_64，所以这不是问题。</p><p><strong>验证</strong>：在 host 端直接运行单元测试，不需要 QEMU。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：所有 kprintf 相关的测试用例全部通过</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 包括左对齐、零填充+负数、基本格式说明符、边界值等</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h3 id="step-4-精简-kprintf-cpp——用共享引擎替换旧代码" tabindex="-1">Step 4: 精简 kprintf.cpp——用共享引擎替换旧代码 <a class="header-anchor" href="#step-4-精简-kprintf-cpp——用共享引擎替换旧代码" aria-label="Permalink to &quot;Step 4: 精简 kprintf.cpp——用共享引擎替换旧代码&quot;">​</a></h3><p><strong>目标</strong>：把 <code>kernel/lib/kprintf.cpp</code> 里的旧格式化代码（<code>format_decimal</code>、<code>format_hex</code>、<code>vkprintf</code> 实现）全部删除，替换为 include <code>vkprintf_impl.hpp</code>，只保留 Serial 全局对象管理和格式化调用的转发代码。</p><p><strong>设计思路</strong>：重构后的 <code>kprintf.cpp</code> 应该非常精简——它只做三件事：管理全局 Serial 单例、初始化 Serial 并通过 <code>kprintf_init</code> 暴露、把 kprintf/kvprintf/kpanic 的调用转发给 <code>vkprintf_impl</code>。所有格式化的逻辑都交给共享头文件，<code>kprintf.cpp</code> 不再包含任何格式化相关的代码。</p><p><strong>实现约束</strong>：<code>kprintf.cpp</code> 需要包含 <code>kernel/lib/private/vkprintf_impl.hpp</code> 和 <code>kernel/drivers/serial.hpp</code>。匿名命名空间中包含全局 <code>static Serial g_serial(SERIAL_COM1)</code> 和 <code>using cinux::lib::detail::vkprintf_impl</code> 声明。公开的 <code>kprintf_init</code> 调用 <code>g_serial.init()</code>。<code>kprintf</code> 和 <code>kvprintf</code> 各自调用 <code>vkprintf_impl</code>，传入一个捕获 <code>g_serial</code> 引用的 lambda。<code>kpanic</code> 在格式化输出后进入 <code>cli; hlt</code> 死循环。</p><p>小内核那边也需要做同样的精简——<code>kernel/mini/lib/kprintf.cpp</code> 删除旧代码，include <code>kernel/mini/lib/private/vkprintf_impl.h</code>（注意小内核用的是 <code>.h</code> 后缀，因为小内核的版本可能做了一些 C 兼容的调整，比如不用 C++ 命名空间）。</p><p><strong>踩坑预警</strong>：删代码永远比写代码容易出问题。精简的时候一定要确保 include 路径正确——<code>private/vkprintf_impl.hpp</code> 的路径必须被 CMake 的 include 目录列表覆盖。另外，删除旧代码之后如果编译报&quot;undefined reference to format_decimal&quot;之类的错误，说明某个地方还在引用旧函数而不是使用共享头文件里的新函数。用 grep 搜一下整个项目确保没有残留的旧格式化函数定义。</p><p><strong>验证</strong>：精简后重新构建并运行，所有串口输出应该和之前完全一致。同时运行单元测试确认格式化功能没有回退。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：单元测试全部通过，串口输出和精简前完全一致</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><h3 id="step-5-kernel-main-里加回归测试" tabindex="-1">Step 5: kernel_main 里加回归测试 <a class="header-anchor" href="#step-5-kernel-main-里加回归测试" aria-label="Permalink to &quot;Step 5: kernel_main 里加回归测试&quot;">​</a></h3><p><strong>目标</strong>：在 <code>kernel_main</code> 里添加几行格式化回归测试，用 kprintf 打印各种格式说明符的组合，确认引擎在实际硬件环境（QEMU）下的输出是正确的。</p><p><strong>设计思路</strong>：单元测试在 host 端跑，覆盖的是格式化逻辑本身。但在实际的 QEMU 环境里，va_list 的 ABI、Serial 输出的字符正确性等因素都可能影响最终结果。所以加几行回归测试是有意义的——不是为了替代单元测试，而是作为端到端的冒烟测试。</p><p><strong>实现约束</strong>：在 kernel_main 的初始化完成之后、进入 idle 循环之前，加几行 kprintf 调用，覆盖 <code>%d</code>、<code>%-10d</code>、<code>%06d</code> 配合负数、<code>%-20s</code>、<code>%p</code> 等格式。这些输出会直接显示在串口终端上，你可以目视确认格式是否正确。</p><p><strong>验证</strong>：运行内核，目视检查串口输出中的格式化结果。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 预期：在初始化日志之后，你能看到几行格式化测试输出</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 检查左对齐是否右边有空格、右对齐是否左边有空格/零</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 负数零填充是否是 -00042 而不是 000-42</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><hr><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 切换到 tag</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> checkout</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 012_driver_serial</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 配置 + 构建</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_BUILD_TYPE=Debug</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 先跑单元测试（不需要 QEMU）</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output-on-failure</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 再跑内核（需要 QEMU）</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>单元测试应该覆盖所有格式说明符的基本功能和边界情况。内核运行时串口输出应该和重构前完全一致——任何差异都意味着重构引入了 bug。</p><hr><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>格式化输出和预期不一致</strong></p><p>最直接的排查方法是在单元测试里加一个失败的断言——比如 <code>ASSERT_EQ(do_printf(&quot;%-10d&quot;, 42), &quot;42 &quot;)</code>（42 后面跟 8 个空格）。如果断言失败，测试框架会打印期望值和实际值，你可以直接对比。对于只在 QEMU 环境下才出现的问题（host 端测试通过但内核输出不对），检查 va_list 的传递是否正确——在 x86_64 的 ABI 下 <code>va_list</code> 是一个数组类型，传参时要注意。</p><p><strong>提取后编译报 undefined reference</strong></p><p>这说明某个编译单元还在引用旧版本的格式化函数，而不是使用共享头文件里的新版本。用 grep 搜索整个项目里所有 <code>format_decimal</code>、<code>format_hex</code> 的定义和声明，确保只存在于 <code>vkprintf_impl.hpp</code> 和小内核的 <code>vkprintf_impl.h</code> 中。<code>kprintf.cpp</code> 和 <code>mini/lib/kprintf.cpp</code> 不应该有这些函数的定义。</p><p><strong>左对齐的 padding 是零而不是空格</strong></p><p>检查格式化引擎里左对齐分支的 padding 字符——它应该硬编码为空格字符 <code>&#39; &#39;</code>，而不是根据 <code>zero_pad</code> 标志来选择。左对齐模式下零填充标志应该被忽略。</p><hr><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>文件路径</th><th>说明</th></tr></thead><tbody><tr><td>共享格式化引擎</td><td><code>kernel/lib/private/vkprintf_impl.hpp</code></td><td>模板化 OutputFn，硬件无关</td></tr><tr><td>小内核版本</td><td><code>kernel/mini/lib/private/vkprintf_impl.h</code></td><td>C 兼容适配，同样的格式化逻辑</td></tr><tr><td>左对齐标志</td><td><code>%-Nd</code>、<code>%-Ns</code></td><td><code>-</code> 标志先输出内容再补空格</td></tr><tr><td>负数零填充</td><td><code>%06d</code> 配合 -42 = <code>-00042</code></td><td>先输出符号再补零再输出数字</td></tr><tr><td>MockOutput 测试</td><td><code>tests/unit/test_kprintf.cpp</code></td><td>直接使用真正的 vkprintf_impl</td></tr><tr><td>DRY 重构</td><td>大/小内核共享同一份格式化代码</td><td>新增格式说明符只需改一个文件</td></tr><tr><td>lambda 集成</td><td><code>[&amp;](char c) { g_serial.putc(c); }</code></td><td>格式化引擎通过回调与 Serial 解耦</td></tr></tbody></table>`,77)])])}const u=i(d,[["render",t]]);export{k as __pageData,u as default};
