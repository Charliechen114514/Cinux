import{c as a,Q as i,j as n,m as e}from"./chunks/framework.D9IbVcwA.js";const k=JSON.parse('{"title":"004-boot-load-mini-kernel-c-2 · 内核加载 (C)","description":"","frontmatter":{"title":"004-boot-load-mini-kernel-c-2 · 内核加载 (C)"},"headers":[],"relativePath":"tutorial/004-boot-load-mini-kernel-c-2.md","filePath":"tutorial/004-boot-load-mini-kernel-c-2.md","lastUpdated":1779686691000}'),l={name:"tutorial/004-boot-load-mini-kernel-c-2.md"};function p(t,s,d,o,h,r){return i(),n("div",null,[...s[0]||(s[0]=[e(`<h1 id="内核的第一次呼吸-bss-清除陷阱、符号冲突侦探与-c-运行时" tabindex="-1">内核的第一次呼吸：BSS 清除陷阱、符号冲突侦探与 C++ 运行时 <a class="header-anchor" href="#内核的第一次呼吸-bss-清除陷阱、符号冲突侦探与-c-运行时" aria-label="Permalink to &quot;内核的第一次呼吸：BSS 清除陷阱、符号冲突侦探与 C++ 运行时&quot;">​</a></h1><blockquote><p>标签：kernel entry, boot.S, BSS clearing, C++ runtime, crt_stub, linker script, 全局构造函数 前置：<a href="./004-boot-load-mini-kernel-c-1">004C-1 从 Bootloader 到内核的交接</a></p></blockquote><h2 id="写在前面" tabindex="-1">写在前面 <a class="header-anchor" href="#写在前面" aria-label="Permalink to &quot;写在前面&quot;">​</a></h2><p>在上一篇文章里，我们看着 bootloader 在 Long Mode 中把 BootInfo 填好，往 debugcon 敲了一个 <code>&#39;J&#39;</code>，然后 <code>jmp *%rax</code> 头也不回地跳进了 <code>0xFFFFFFFF80020000</code>。从那一刻起，CPU 执行的就是我们自己写的内核代码了。</p><p>但内核要真正&quot;活过来&quot;，还得经历一系列初始化步骤：关中断、开 SSE、设栈、清 BSS、调用全局构造函数，最后才能进入 C++ 的 <code>main</code> 函数。这些步骤中的每一步都有它存在的理由，而其中两步——BSS 清除和全局构造函数调用——藏着非常经典的坑，每一个都足够让你调试到凌晨三点。说实话，这两个 bug 是我写 Cinux 以来印象最深的调试经历：第一个是 <code>rep stosb</code> 指令意外破坏了 <code>%rdi</code> 中保存的 BootInfo 指针，第二个是链接器把汇编定义的 <code>.bss</code> 符号和 C++ 全局变量分配到了同一个地址。后者产生了一个神秘的值 <code>0x2a00000000</code>（等于 42 左移 32 位），光看这个值你完全不知道它从哪来的——直到你发现代码里有个全局变量被赋值为 42。</p><p>本文覆盖内核侧的全部代码：<code>kernel/mini/arch/x86_64/boot.S</code>（内核入口汇编序列）、<code>kernel/mini/arch/x86_64/crt_stub.cpp</code>（C++ 运行时支撑函数）、<code>kernel/mini/linker.ld</code>（链接器脚本和内存布局）、<code>kernel/mini/main.cpp</code>（内核主函数及 C++ 特性验证），以及 <code>kernel/mini/CMakeLists.txt</code>（构建配置）。在讲解过程中，我会把那两个 bug 的调试故事融入对应的代码段落——不是为了吐槽，而是因为这些踩坑经历本身就是理解底层机制最好的教材。</p><h2 id="环境说明" tabindex="-1">环境说明 <a class="header-anchor" href="#环境说明" aria-label="Permalink to &quot;环境说明&quot;">​</a></h2><p>内核运行环境的关键参数：CPU 已经处于 64-bit Long Mode，中断控制器还没有初始化（IDT 不存在），没有物理内存管理器（PMM），没有堆分配器。编译选项是 <code>-ffreestanding -fno-exceptions -fno-rtti -mcmodel=large -mno-red-zone</code>——每一个都在告诉编译器&quot;这是内核代码，别给我用那些花哨的用户态特性&quot;。链接脚本把内核的 VMA 设在 <code>0xFFFFFFFF80020000</code>（高半核地址），LMA 设在 <code>0x20000</code>（物理加载地址）。</p><p>预期 debugcon 输出是一个精心设计的字符序列：<code>OPLJ123G4===CPPGC1V123B===END</code>。每个字符都有明确的含义——<code>OPLJ</code> 是 bootloader 阶段的输出（上一篇的内容），<code>1234</code> 是内核启动序列中四个检查点的输出，<code>G</code> 是全局构造函数执行时的输出，<code>===CPPGC1V123B===END</code> 是 C++ 特性验证的输出。如果这个序列完整出现，说明从 bootloader 到内核的全部代码路径都是正确的。</p><h2 id="链接器脚本-内核内存布局的蓝图" tabindex="-1">链接器脚本：内核内存布局的蓝图 <a class="header-anchor" href="#链接器脚本-内核内存布局的蓝图" aria-label="Permalink to &quot;链接器脚本：内核内存布局的蓝图&quot;">​</a></h2><p>我们先从 <code>kernel/mini/linker.ld</code> 开始看，因为它定义了所有其他代码运行的地址空间基础。不搞清楚链接器脚本，后面的 BSS 清除和符号冲突你根本没法理解。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OUTPUT_FORMAT(&quot;elf64-x86-64&quot;)</span></span>
<span class="line"><span>ENTRY(_start)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>KERNEL_PHYS_BASE = 0x20000;</span></span>
<span class="line"><span>KERNEL_Virt_BASE = 0xFFFFFFFF80000000;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SECTIONS</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    . = KERNEL_Virt_BASE + KERNEL_PHYS_BASE;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>OUTPUT_FORMAT</code> 指定输出为 ELF64 x86-64 格式。虽然最终会用 <code>objcopy -O binary</code> 转成扁平二进制，但编译链接阶段仍然需要完整的 ELF 格式——链接器需要 section header 来定位符号、解析重定位。<code>ENTRY(_start)</code> 告诉链接器入口点是 <code>_start</code>，这个信息在扁平二进制中会被丢弃（扁平二进制从文件头开始执行），但在 ELF 格式中有意义。</p><p>两个关键常量：<code>KERNEL_PHYS_BASE = 0x20000</code> 是 bootloader 加载内核的物理地址，<code>KERNEL_Virt_BASE = 0xFFFFFFFF80000000</code> 是高半核的虚拟基址。起始 VMA 设为两者之和 <code>0xFFFFFFFF80020000</code>，这就是 <code>_start</code> 最终被链接到的虚拟地址。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    .text : AT(ADDR(.text) - KERNEL_Virt_BASE) {</span></span>
<span class="line"><span>        *(.text.start)        /* _start must be first! */</span></span>
<span class="line"><span>        *(.text .text.*)</span></span>
<span class="line"><span>        *(.rodata .rodata.*)</span></span>
<span class="line"><span>    }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>.text</code> 段放代码和只读数据。<code>AT(ADDR(.text) - KERNEL_Virt_BASE)</code> 计算 LMA——因为 VMA 起始于 <code>KERNEL_Virt_BASE + KERNEL_PHYS_BASE</code>，减去 <code>KERNEL_Virt_BASE</code> 后得到 <code>KERNEL_PHYS_BASE = 0x20000</code>。<code>*(.text.start)</code> 放在最前面，确保 <code>_start</code> 函数始终在输出文件的开头。VMA 和 LMA 的分离是高半核设计的核心：代码在物理内存的 0x20000 处，但 CPU 通过页表映射从 <code>0xFFFFFFFF80020000</code> 访问它。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    .data : AT(ADDR(.data) - KERNEL_Virt_BASE) {</span></span>
<span class="line"><span>        *(.data .data.*)</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    .init_array : AT(ADDR(.init_array) - KERNEL_Virt_BASE) {</span></span>
<span class="line"><span>        __init_array_start = .;</span></span>
<span class="line"><span>        KEEP(*(.init_array .init_array.*))</span></span>
<span class="line"><span>        __init_array_end = .;</span></span>
<span class="line"><span>    }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>.data</code> 段紧接着 <code>.text</code> 段，存放有初始值的全局变量。<code>.init_array</code> 是独立的段，不在 <code>.data</code> 内部——这样链接器可以分别管理两个段的布局。<code>KEEP()</code> 包裹 <code>.init_array</code> 防止被链接器的 section garbage collection 丢弃。</p><p><code>__init_array_start</code> 和 <code>__init_array_end</code> 是两个链接器符号，标记了 <code>.init_array</code> 段的起止位置。GCC 为每个有构造函数的全局对象在 <code>.init_array</code> 中生成一个函数指针（名为 <code>_GLOBAL__sub_I_XXX</code>），内核的启动代码需要遍历这个数组，依次调用每个函数指针来初始化全局对象。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    .bss : {</span></span>
<span class="line"><span>        __bss_start = .;</span></span>
<span class="line"><span>        *(.bss .bss.*)</span></span>
<span class="line"><span>        *(COMMON)</span></span>
<span class="line"><span>        __bss_end = .;</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    __mini_kernel_end = .;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>.bss</code> 段不需要 <code>AT()</code>——BSS 段在 ELF 文件中不占空间（它只是记录了大小和起始地址），运行时由启动代码负责清零。<code>__bss_start</code> 和 <code>__bss_end</code> 标记了 BSS 的边界，<code>boot.S</code> 中的 <code>rep stosb</code> 指令就用这两个符号来确定要清除的内存范围。</p><p>注意 <code>.data</code> 在 <code>.bss</code> 之前——这个顺序在后面修复符号冲突 bug 时非常关键。因为 <code>.data</code> 和 <code>.bss</code> 是完全独立的 section，它们的地址空间不重叠，所以把 <code>__boot_info_ptr</code> 放在 <code>.data</code> 段就永远不会被 BSS 清除操作触及。</p><h2 id="boot-s-入口序列-每一步都事出有因" tabindex="-1">boot.S 入口序列：每一步都事出有因 <a class="header-anchor" href="#boot-s-入口序列-每一步都事出有因" aria-label="Permalink to &quot;boot.S 入口序列：每一步都事出有因&quot;">​</a></h2><p><code>kernel/mini/arch/x86_64/boot.S</code> 是内核的入口点。bootloader 通过 <code>jmp *%rax</code> 跳入 <code>_start</code>，此时 <code>%rdi</code> 中保存着 BootInfo 指针（0x7000），CPU 处于 Long Mode，但中断状态不确定。</p><h3 id="第一步-关中断" tabindex="-1">第一步：关中断 <a class="header-anchor" href="#第一步-关中断" aria-label="Permalink to &quot;第一步：关中断&quot;">​</a></h3><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .text</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.code64</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _start</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.type _start, @function</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_start:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    cli</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x31, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>第一步是 <code>cli</code> 关中断。此时内核还没有设置 IDT（中断描述符表），如果此时发生了任何硬件中断（定时器、键盘中断等），CPU 会尝试通过 IDT 查找处理程序——但 IDT 还不存在，结果就是一个 Triple Fault，QEMU 直接重启。<code>cli</code> 确保在 IDT 设置完成之前不会有任何中断打扰。</p><p>然后往 debugcon 输出 <code>&#39;1&#39;</code>（0x31 = ASCII &#39;1&#39;），表示 <code>_start</code> 已经到达。这是第一个内核侧的 debugcon 输出，和 bootloader 的 <code>&#39;J&#39;</code> 之间不应该有任何其他字符。</p><h3 id="第二步-设栈" tabindex="-1">第二步：设栈 <a class="header-anchor" href="#第二步-设栈" aria-label="Permalink to &quot;第二步：设栈&quot;">​</a></h3><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $__mini_stack_top, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rsp</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    xorq %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rbp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rbp</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x32, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>设置栈指针。<code>__mini_stack_top</code> 定义在 boot.S 的 <code>.bss</code> 段末尾，是一个 8KB 栈区的顶端。x86 的栈是向下增长的（push 递减 <code>%rsp</code>），所以栈顶是高地址。<code>xorq %rbp, %rbp</code> 把帧指针清零——这是 C++ 运行时的约定：<code>main</code> 函数的 <code>%rbp</code> 为 0 表示没有调用者（栈回溯的终止条件）。输出 <code>&#39;2&#39;</code> 表示栈已就绪。</p><h3 id="第三步-保存-bootinfo-指针-真正的坑从这里开始" tabindex="-1">第三步：保存 BootInfo 指针（真正的坑从这里开始） <a class="header-anchor" href="#第三步-保存-bootinfo-指针-真正的坑从这里开始" aria-label="Permalink to &quot;第三步：保存 BootInfo 指针（真正的坑从这里开始）&quot;">​</a></h3><p>接下来就是整个内核启动中最容易出 bug 的地方。先看代码：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, __boot_info_ptr</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $__bss_start, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $__bss_end, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rcx</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    subq %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rcx</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    xorq %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rax</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    rep</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> stosb</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x33, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>这里做的事情很简单：先把 <code>%rdi</code>（BootInfo 指针 = 0x7000）保存到 <code>__boot_info_ptr</code>，然后把 <code>%rdi</code> 设为 BSS 起始地址，用 <code>rep stosb</code> 逐字节地把 BSS 段全部清零。</p><p>BSS 清除是 C++ 运行时的硬性要求——C/C++ 标准规定所有未初始化的全局变量和静态变量的初始值必须为零。在用户态程序中，操作系统加载 ELF 时会把 BSS 段清零（Linux 的 <code>load_elf_binary</code> 会做这件事），但在我们的场景中，bootloader 只是把 flat binary 原封不动地搬到了 0x20000，没有任何人帮你清零 BSS。OSDev Wiki 的 Multiboot 规范页面也提到，GRUB 加载 ELF 内核时会保证 BSS 被清零，但 Cinux 使用的是自写 bootloader 加载 flat binary，所以必须自己来。</p><p><code>rep stosb</code> 的语义（Intel SDM Vol. 2A, &quot;STOS/STOSB/STOSD/STOSQ&quot;）是：以 <code>%rdi</code> 为目标地址，以 <code>%rax</code> 为填充值（这里是 0），以 <code>%rcx</code> 为计数器，逐字节写入并自动递增 <code>%rdi</code>。执行完毕后，<code>%rdi</code> 不再是 BSS 起始地址了——它已经递增到了 BSS 末尾之后。</p><p>这就是第一个陷阱：<code>rep stosb</code> 会破坏 <code>%rdi</code>。如果你在 <code>rep stosb</code> 之后试图从 <code>%rdi</code> 读取 BootInfo 指针，拿到的是一个垃圾地址。所以第一行 <code>movq %rdi, __boot_info_ptr</code> 至关重要——它必须在 BSS 清除之前执行。</p><p>但事情到这里还没完。真正的坑在后面。</p><h3 id="bss-清除的第二个陷阱-指针被自己清零" tabindex="-1">BSS 清除的第二个陷阱：指针被自己清零 <a class="header-anchor" href="#bss-清除的第二个陷阱-指针被自己清零" aria-label="Permalink to &quot;BSS 清除的第二个陷阱：指针被自己清零&quot;">​</a></h3><p>我们最初把 <code>__boot_info_ptr</code> 定义在 <code>.bss</code> 段：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .bss</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.skip </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__boot_info_ptr:</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>你看到问题了吗？<code>.bss</code> 段的内容会被 <code>rep stosb</code> 整体清零——也就是说，你在 BSS 清除之前把 BootInfo 指针写到了 <code>__boot_info_ptr</code>，然后 <code>rep stosb</code> 把它又清回了 0。等你在 <code>mini_kernel_main</code> 里去读 <code>__boot_info_ptr</code> 的时候，拿到的是 0 而不是 0x7000。</p><p>这个 bug 的调试过程相当曲折。最初我们怀疑是 bootloader 传递的 <code>%rdi</code> 值就是错的，于是在 <code>_start</code> 入口处加了 debugcon 输出，确认 <code>%rdi = 0x7000</code> 没问题。然后又在 <code>movq %rdi, __boot_info_ptr</code> 之后立即读取 <code>__boot_info_ptr</code> 的值做输出，发现也是 0x7000——到这里保存是成功的。但是 BSS 清除之后再读，值变成了一个诡异的 <code>0x2a00000000</code>。</p><p>等等，<code>0x2a00000000</code>？不是 0？如果你只是在 <code>.bss</code> 里存了 0x7000 然后 BSS 清零，结果应该是 0 才对。但实际值不是 0——它是 <code>42 &lt;&lt; 32</code>。42 是什么？这就引出了我们遇到过的最离奇的 bug。</p><h3 id="符号冲突侦探故事-0x2a00000000-的谜团" tabindex="-1">符号冲突侦探故事：0x2a00000000 的谜团 <a class="header-anchor" href="#符号冲突侦探故事-0x2a00000000-的谜团" aria-label="Permalink to &quot;符号冲突侦探故事：0x2a00000000 的谜团&quot;">​</a></h3><p>42 这个数字在我们的测试代码里出现过——<code>global_construction_count = 42</code>，这是 <code>GlobalCounter</code> 全局对象构造函数里的赋值。一个巧合？绝对不是。</p><p>用 <code>objdump -t</code> 检查符号表，真相才浮出水面：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ffffffff800226e8 g       .bss  0000000000000000 __boot_info_ptr</span></span>
<span class="line"><span>ffffffff800226e8 g     O .bss  0000000000000001 global_counter</span></span>
<span class="line"><span>ffffffff800226ec l     O .bss  0000000000000004 _ZL25global_construction_count</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>__boot_info_ptr</code> 和 <code>global_counter</code> 被链接器分配到了同一个地址 <code>0xffffffff800226e8</code>。</p><p>为什么会这样？问题出在汇编定义 <code>.bss</code> 符号的方式上。注意我们的定义：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .bss</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.skip </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__boot_info_ptr:</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>.skip 8</code> 在标签之前。GNU AS 的行为是 <code>.skip</code> 在当前位置分配空间，然后标签指向 <code>.skip</code> 之后的地址。但链接器在合并来自不同目标文件的 <code>.bss</code> 段时，看到的是一个没有大小的符号——因为它只是一个标签，不像 C++ 的全局变量有明确的大小信息（<code>global_counter</code> 有 <code>O</code> 标记和大小 1）。链接器在分配 <code>.bss</code> 空间时，<code>__boot_info_ptr</code> 的地址被分配到了和 <code>global_counter</code> 相同的位置。</p><p>执行流程是这样的：BSS 清除之后，<code>__boot_info_ptr</code> 的内存被清零。然后 <code>_init_global_ctors</code> 被调用，<code>GlobalCounter</code> 的构造函数执行，<code>global_construction_count = 42</code>（写入 <code>0x800226ec</code>，4 字节）。因为 <code>__boot_info_ptr</code> 和 <code>global_counter</code> 共享地址 <code>0x800226e8</code>，当构造函数往 <code>global_construction_count</code>（紧邻，偏移 4 字节）写入 42 时，<code>__boot_info_ptr</code> 的 8 字节视图里看到了低 4 字节为 0（来自 <code>global_counter</code> 的默认值）、高 4 字节为 42（来自 <code>global_construction_count</code>），合在一起就是 <code>0x0000002a00000000</code>。</p><p>这个调试技巧值得记住：当你看到一个&quot;神秘值&quot;时，把它转成二进制看看是否和代码中的常量有关。<code>0x2a00000000 = 42 &lt;&lt; 32</code>，而 42 = 0x2A 是我们代码中 <code>global_construction_count</code> 的值——这个线索直接指向了内存覆盖。类似地，如果你看到 <code>0xDEADBEEF</code>，那很可能是未初始化内存的填充值；如果看到 <code>0xFFFFFFFF</code>，可能是 -1 的无符号表示。</p><h3 id="修复-将-boot-info-ptr-移到-data-段" tabindex="-1">修复：将 <code>__boot_info_ptr</code> 移到 <code>.data</code> 段 <a class="header-anchor" href="#修复-将-boot-info-ptr-移到-data-段" aria-label="Permalink to &quot;修复：将 \`__boot_info_ptr\` 移到 \`.data\` 段&quot;">​</a></h3><p>修复方案是把 <code>__boot_info_ptr</code> 从 <code>.bss</code> 移到 <code>.data</code> 段：</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .data</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.align </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__boot_info_ptr:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .quad </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>.data</code> 段不会被 BSS 清除操作触及，而且 <code>.data</code> 段和 <code>.bss</code> 段在链接器脚本中是完全独立的 section，不会有地址重叠的问题。<code>.quad 0</code> 给了一个显式的初始值，让链接器知道这个符号占用 8 字节的空间，大小信息明确。修复之后 <code>objdump -t</code> 确认 <code>__boot_info_ptr</code> 在 <code>.data</code> 段有了独立的地址：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ffffffff800226e8 g     O .data 0000000000000008 __boot_info_ptr</span></span>
<span class="line"><span>ffffffff800226f4 g     O .bss  0000000000000001 global_counter</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>两个符号现在各有各的地址，再也不会互相踩踏了。</p><p>这个 bug 的教训是：当汇编和 C++ 混合编程时，避免将汇编定义的符号放在 <code>.bss</code> 段。原因是多方面的：<code>.bss</code> 段会被运行时清零，破坏你保存的值；链接器可能将不同来源的符号分配到同一地址；C++ 全局变量的初始化可能覆盖汇编符号。把关键数据放在 <code>.data</code> 段，用 <code>objdump -t</code> 检查符号表确认没有地址冲突——这两步可以避免绝大多数此类问题。</p><h2 id="全局构造函数调用-c-的隐藏基础设施" tabindex="-1">全局构造函数调用：C++ 的隐藏基础设施 <a class="header-anchor" href="#全局构造函数调用-c-的隐藏基础设施" aria-label="Permalink to &quot;全局构造函数调用：C++ 的隐藏基础设施&quot;">​</a></h2><p>BSS 清除完成后，内核的内存状态终于干净了——所有未初始化的全局变量都被清零。接下来要做的是调用全局对象的构造函数。</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _init_global_ctors</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    movb $0x34, %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    outb %</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">al</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $0xE9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>输出 <code>&#39;4&#39;</code> 表示全局构造完成。</p><p>在有标准 C++ 运行时的环境中（比如 Linux 用户态），<code>crt0.o</code>（C Runtime Startup）会在调用 <code>main</code> 之前自动遍历 <code>.init_array</code> 段，调用所有注册的初始化函数。但我们用的是 <code>-nostdlib -ffreestanding</code>，没有任何 C 运行时代码——<code>_start</code> 是程序真正的入口，没有人帮我们做这件事。如果不去调用全局构造函数，<code>GlobalCounter global_counter</code> 这个全局对象的构造函数永远不会执行，<code>global_construction_count</code> 永远是 0，所有依赖全局初始化的代码都会出错。</p><h2 id="crt-stub-cpp-freestanding-环境下的-c-运行时支撑" tabindex="-1">crt_stub.cpp：freestanding 环境下的 C++ 运行时支撑 <a class="header-anchor" href="#crt-stub-cpp-freestanding-环境下的-c-运行时支撑" aria-label="Permalink to &quot;crt_stub.cpp：freestanding 环境下的 C++ 运行时支撑&quot;">​</a></h2><p><code>kernel/mini/arch/x86_64/crt_stub.cpp</code> 提供了 C++ 在 freestanding 环境下必须存在的一组运行时函数。这些函数在有 libstdc++ 的环境中由标准库提供，但在内核开发中必须手动实现。</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;C&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">noreturn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">]]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> __cxa_pure_virtual</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        __asm__</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cli; hlt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">noreturn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">]]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> __stack_chk_fail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        __asm__</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cli; hlt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> __cxa_atexit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (*)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p><code>__cxa_pure_virtual</code> 在通过虚函数表调用纯虚函数时被触发——这永远是一个编程错误。在用户态程序中它通常会打印错误信息然后 <code>abort()</code>，在内核中我们只能停机。即使没有任何代码直接调用它，vtable 的生成也会隐式引用它，所以链接器要求它必须存在。</p><p><code>__stack_chk_fail</code> 是栈保护检测到溢出时的处理函数。<code>__cxa_atexit</code> 是 <code>atexit</code> 的底层实现——在内核中我们不支持&quot;程序退出&quot;，所以直接返回 0（假装注册成功）。有些编译器生成的析构代码会引用它，链接器也需要这个符号存在。</p><p>接下来是核心函数——全局构造函数初始化：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (*__init_array_start[])();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (*__init_array_end[])();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _init_global_ctors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">func)() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init_array_start; func </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init_array_end; func</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">func)();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>__init_array_start</code> 和 <code>__init_array_end</code> 是链接器脚本中定义的符号，标记了 <code>.init_array</code> 段的起止。<code>.init_array</code> 段的内容是一个函数指针数组——GCC 为每个有非平凡构造函数的全局对象生成一个名为 <code>_GLOBAL__sub_I_XXX</code> 的函数，这个函数负责调用该对象的构造函数，然后把函数指针放在 <code>.init_array</code> 段中。</p><p>遍历方式很直接：从 <code>__init_array_start</code> 开始，逐个调用函数指针，直到 <code>__init_array_end</code>。这是 OSDev Wiki 上推荐的 &quot;ARM BPABI method&quot;——简单、可靠、不依赖 <code>.init</code> 段的级联调用链。OSDev Wiki 的 <a href="https://wiki.osdev.org/Calling_Global_Constructors" target="_blank" rel="noreferrer">Calling Global Constructors</a> 页面详细描述了两种方式：一种是 System V ABI 的 <code>crt*.o</code> 级联方案（<code>crti.o</code> 提供 prologue，<code>crtbegin.o</code> 插入调用代码，<code>crtn.o</code> 提供 epilogue），另一种是直接遍历 <code>.init_array</code> 的函数指针数组。我们选择了后者，因为它只需要 5 行代码，不需要提供自定义的 <code>crti.o</code> 和 <code>crtn.o</code>。</p><p>然后是 <code>operator new/delete</code>：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // extern &quot;C&quot; ends here</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> operator</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> ptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">noexcept</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)ptr;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        __asm__</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cli; hlt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void*</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> operator</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)size;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        __asm__</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cli; hlt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><p>这些操作符的 halt 实现看起来很激进——调用 <code>new</code> 或 <code>delete</code> 直接停机。但在当前阶段这是合理的：内核还没有物理内存管理器（PMM），没有任何动态内存分配的基础设施。如果代码试图 <code>new</code> 一个对象，那一定是个 bug，停机是最好的选择——比起返回 <code>nullptr</code> 然后在某个遥远的地方触发空指针解引用，直接 halt 能让你立刻定位问题所在。</p><p>注意 <code>new/delete</code> 必须放在 <code>extern &quot;C&quot;</code> 块之外——它们需要 C++ 的 name mangling，因为 <code>operator new</code> 和 <code>operator delete</code> 是通过 C++ 的重载解析机制来匹配的。如果你把它们包在 <code>extern &quot;C&quot;</code> 里，链接器会找不到正确的 mangled 名称，报 missing symbol 错误。</p><h3 id="与-linux-内核的-initcall-机制对比" tabindex="-1">与 Linux 内核的 initcall 机制对比 <a class="header-anchor" href="#与-linux-内核的-initcall-机制对比" aria-label="Permalink to &quot;与 Linux 内核的 initcall 机制对比&quot;">​</a></h3><p>Linux 内核也使用类似的机制来调用初始化函数，但远比我们复杂。Linux 定义了多个 level 的 initcall：<code>pure_initcall</code>、<code>core_initcall</code>、<code>postcore_initcall</code>、<code>arch_initcall</code>、<code>subsys_initcall</code>、<code>fs_initcall</code>、<code>device_initcall</code>、<code>late_initcall</code>——每个 level 对应不同的初始化优先级，内核启动时按 level 顺序依次调用。这些 level 的底层实现和 Cinux 一样是通过链接器脚本中的 section 排列来保证顺序的（<code>__initcall0_start</code>、<code>__initcall1_start</code> 等），只不过 Linux 把一个 <code>.init_array</code> 拆成了多个子区间。</p><p>Linux 之所以需要这么复杂的分级，是因为它有数百个初始化函数，而且有些初始化之间存在严格的依赖关系——比如中断控制器必须在设备驱动之前初始化，内存管理必须在 slab 分配器之前就绪。Cinux 作为一个教学操作系统，目前只有一两个全局构造函数，一个扁平的 <code>.init_array</code> 就够了。但如果后续内核规模增长，Linux 的多级 initcall 方案是一个很好的参考。</p><h3 id="与-xv6-的对比" tabindex="-1">与 xv6 的对比 <a class="header-anchor" href="#与-xv6-的对比" aria-label="Permalink to &quot;与 xv6 的对比&quot;">​</a></h3><p>xv6 的入口代码（<code>entry.S</code>）比 Cinux 简单得多，原因是它省略了两个步骤。第一，xv6 不需要手动清 BSS——它依赖 GRUB（Multiboot 协议）加载 ELF 内核，Multiboot 规范要求 bootloader 在加载 ELF 时清零 BSS 段。第二，xv6 不需要调用全局构造函数——它是纯 C 写的，没有 <code>.init_array</code>，没有全局对象。</p><p>xv6 的入口序列只有四步：开启 4MB 大页（CR4.PSE）、设置页目录（CR3）、开启分页（CR0.PG）、设栈，然后直接 <code>jmp *%eax</code> 跳到 <code>main()</code>。Cinux 的入口序列多了 SSE 初始化、BSS 清除和全局构造函数调用——每一步都是因为 C++ 和自写 bootloader 这两个设计选择带来的额外复杂度。这些复杂度不是没有回报的：C++ 的类、虚函数、RAII 等特性让内核代码更安全和更有表达力，而自写 bootloader 让我们从头到尾理解了 x86 启动的每一个环节。</p><h2 id="内核主函数-c-特性验证" tabindex="-1">内核主函数：C++ 特性验证 <a class="header-anchor" href="#内核主函数-c-特性验证" aria-label="Permalink to &quot;内核主函数：C++ 特性验证&quot;">​</a></h2><p><code>kernel/mini/main.cpp</code> 是内核的 C++ 主函数。在当前 tag 中，它的主要职责不是做什么有用的工作，而是验证 C++ 的各种特性在 freestanding 环境下是否正常工作。</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;C&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __boot_info_ptr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> c</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    __asm__</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">outb %0, $0xE9</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : : </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(c));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>__boot_info_ptr</code> 声明为 <code>uint64_t</code>（不是指针类型），因为它是 <code>boot.S</code> 中 <code>.data</code> 段的一个 8 字节变量，里面存的是 BootInfo 的地址值。把它声明为 <code>extern uint64_t</code> 然后强制转换为 <code>BootInfo*</code> 是最直接的做法。</p><p>第一个测试是简单的类构造和析构，验证最基本的 C++ 特性——成员初始化列表、构造函数调用、成员函数调用都能正常工作。构造时输出 <code>&#39;C&#39;</code> 加上 value 的数字（比如 <code>C1</code>）。</p><p>第二个测试是虚函数和多态——<code>Base</code> 是一个抽象类（有两个纯虚函数），<code>Derived</code> 继承并实现了它们。通过基类指针调用虚函数需要 vtable 的间接寻址。如果链接器没有正确生成 vtable（比如缺少 <code>__cxa_pure_virtual</code> 的定义），或者 <code>.rodata</code> 段的地址映射有问题，这里就会崩溃。输出 <code>&#39;V&#39;</code> 表示 Derived 的构造函数被调用。</p><p>第三个测试是全局对象构造——<code>global_counter</code> 是一个全局对象，它的构造函数不是由 <code>mini_kernel_main</code> 中的代码显式调用的，而是由 <code>_init_global_ctors</code> 遍历 <code>.init_array</code> 时间接调用的。GCC 会生成一个 <code>_GLOBAL__sub_I_global_counter</code> 函数放在 <code>.init_array</code> 中，这个函数调用 <code>GlobalCounter::GlobalCounter()</code>。这就是我们在符号冲突故事中提到的那个全局对象——正是它的 <code>global_construction_count = 42</code> 导致了 <code>__boot_info_ptr</code> 被覆盖为 <code>0x2a00000000</code>。</p><p>最后看 <code>mini_kernel_main</code> 的主体：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;C&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> [[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">noreturn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">]]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mini_kernel_main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> boot_info_addr) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    BootInfo</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> boot_info </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (BootInfo</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)__boot_info_ptr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)boot_info_addr;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;=&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;=&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;=&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;C&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;P&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;P&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    SimpleClass</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> obj1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (obj1.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> obj1.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getMarker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;S&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    Derived</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> derived</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> base </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">derived;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (base-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;D&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> base-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;2&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (global_counter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getCount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;3&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (boot_info-&gt;entry_point </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FFFFFFFF80020000</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> boot_info-&gt;kernel_phys_base </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;B&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;=&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;=&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;=&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;E&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;N&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    debugcon_putc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;D&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        __asm__</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cli; hlt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br><span class="line-number">29</span><br><span class="line-number">30</span><br><span class="line-number">31</span><br><span class="line-number">32</span><br><span class="line-number">33</span><br><span class="line-number">34</span><br><span class="line-number">35</span><br><span class="line-number">36</span><br><span class="line-number">37</span><br><span class="line-number">38</span><br><span class="line-number">39</span><br><span class="line-number">40</span><br><span class="line-number">41</span><br><span class="line-number">42</span><br></div></div><p>这里有一个值得注意的设计选择：<code>mini_kernel_main</code> 接收 <code>boot_info_addr</code> 参数（来自 <code>%rdi</code>），但实际使用的是 <code>__boot_info_ptr</code>（来自 <code>.data</code> 段）。<code>boot_info_addr</code> 被 <code>(void)</code> 显式忽略。这样做的原因是使用 <code>.data</code> 段中保存的值更可靠——它不受 System V ABI 调用约定的 volatile 寄存器规则影响，也不会被 BSS 清除或符号冲突破坏（修复之后）。</p><p>BootInfo 验证检查了两个字段：<code>entry_point</code> 应该是 <code>0xFFFFFFFF80020000</code>，<code>kernel_phys_base</code> 应该是 <code>0x20000</code>。如果这两个值正确，说明 BootInfo 从 bootloader 到内核的传递链条是完整的——bootloader 在 <code>stage2.S</code> 中填充、通过 <code>%rdi</code> 传递、<code>boot.S</code> 保存到 <code>.data</code> 段、<code>main.cpp</code> 读回来验证。</p><h2 id="构建配置" tabindex="-1">构建配置 <a class="header-anchor" href="#构建配置" aria-label="Permalink to &quot;构建配置&quot;">​</a></h2><p><code>kernel/mini/CMakeLists.txt</code> 定义了小内核的构建规则，编译选项每一个都有存在的理由：</p><div class="language-makefile vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">makefile</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">target_compile_options(mini_kernel PRIVATE</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -ffreestanding</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -fno-exceptions</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -fno-rtti</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -fno-pie</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -mcmodel=large</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -mno-red-zone</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    -Wall</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>-ffreestanding</code> 告诉编译器这是 freestanding 环境（没有标准库），禁止隐式引用标准库函数。<code>-fno-exceptions</code> 和 <code>-fno-rtti</code> 禁用异常和运行时类型信息——这两者在内核中既不必要也有性能开销。<code>-fno-pie</code> 禁用位置无关可执行文件——内核加载地址是固定的，不需要 PIC 重定位。<code>-mcmodel=large</code> 允许代码访问任意虚拟地址——默认的 <code>small</code> 内存模型假设所有符号都在低 2GB 地址空间内，但我们的内核在高半核地址，超出这个范围。<code>-mno-red-zone</code> 禁用 x86-64 的 Red Zone 优化——内核代码随时可能被中断打断，Red Zone（栈指针以下 128 字节不可触碰区域）在中断上下文中不安全。</p><p>链接后用 <code>objcopy -O binary</code> 把 ELF 转成扁平二进制——这个命令剥离所有 ELF header 和 section header，只保留各段的原始内容，按 LMA 排列输出。bootloader 直接把 flat binary 加载到物理地址 0x20000 后，代码的相对偏移就是正确的。</p><h2 id="验证" tabindex="-1">验证 <a class="header-anchor" href="#验证" aria-label="Permalink to &quot;验证&quot;">​</a></h2><p>构建并运行 QEMU：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>然后检查 <code>debug.log</code> 文件。如果一切正常，你会看到完整的输出序列：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OPLJ123G4===CPPGC1V123B===END</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>逐字符解读：</p><ul><li><code>OPLJ</code>：bootloader 阶段完成</li><li><code>1</code>：内核 <code>_start</code> 到达</li><li><code>2</code>：栈设置完成</li><li><code>3</code>：BSS 清除完成</li><li><code>G</code>：全局构造函数执行（<code>GlobalCounter</code> 构造函数输出）</li><li><code>4</code>：全局构造完成</li><li><code>===CPP</code>：C++ 特性测试开始</li><li><code>C1</code>：简单类构造验证通过</li><li><code>V</code>：虚函数类构造</li><li><code>2</code>：虚函数调用验证通过</li><li><code>3</code>：全局对象验证通过</li><li><code>B</code>：BootInfo 验证通过</li><li><code>===END</code>：全部测试通过</li></ul><p>如果序列在某处中断，根据最后一个字符就能定位问题所在。比如停在 <code>OPLJ12</code> 说明 BSS 清除出了问题（没看到 <code>3</code>），停在 <code>123G</code> 说明全局构造之后的恢复出了问题（没看到 <code>4</code>），停在 <code>===CPPC1V</code> 说明虚函数之后的某个验证失败了。</p><h2 id="收尾" tabindex="-1">收尾 <a class="header-anchor" href="#收尾" aria-label="Permalink to &quot;收尾&quot;">​</a></h2><p>回过头看，这一章我们做了内核侧的完整启动流程：从 <code>_start</code> 入口到 C++ <code>main</code> 函数，经历关中断、设栈、保存 BootInfo 指针、清 BSS、调用全局构造函数这些步骤。过程中遇到了两个非常经典的 bug——<code>rep stosb</code> 破坏 <code>%rdi</code> 是理解 x86 字符串指令语义的好教材，而符号地址冲突（<code>0x2a00000000 = 42 &lt;&lt; 32</code>）则是理解链接器行为和段布局的绝佳案例。</p><p>设计上有几个值得记住的决策：<code>__boot_info_ptr</code> 放在 <code>.data</code> 段而不是 <code>.bss</code> 段，是为了避免 BSS 清除和符号冲突的双重打击；<code>new</code>/<code>delete</code> 的 halt 实现是在没有 PMM 阶段最安全的做法；直接遍历 <code>.init_array</code> 比使用 <code>crt*.o</code> 级联更简单可靠。</p><p>与 xv6 和 Linux 的对比揭示了&quot;自写 bootloader + C++ 内核&quot;这个组合带来的额外复杂度——xv6 依赖 GRUB 清 BSS 所以不需要这步，Linux 用 <code>%r15</code> 保存参数所以不需要内存存储。但正是这些额外的步骤让我们对 x86 启动的底层机制有了更深入的理解。</p><p>到这里，tag 004C 的全部工作就完成了。从 Bootloader 到内核的整条链路——E820 内存探测（004A）、ELF 加载（004B）、BootInfo 握手与高半核跳转（004C-1）、内核初始化与 C++ 运行时（004C-2）——已经全部打通。接下来的 tag 将会让内核开始做真正有用的事情：建立中断处理、实现物理内存管理、加载真正的内核。到那时，我们今天搭好的 C++ 运行时基础就会派上真正的用场了。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li><p>Intel 64 and IA-32 Architectures Software Developer&#39;s Manual, Vol. 2A, &quot;STOS/STOSB/STOSD/STOSQ -- Store String&quot;: <code>REP STOSB</code> 的完整语义——<code>%rdi</code> 为目标地址，<code>%rax</code> 为填充值，<code>%rcx</code> 为计数器，每写一字节自动递增 <code>%rdi</code>。方向标志 DF（由 <code>cld</code>/<code>std</code> 控制）决定递增还是递减。这是理解 BSS 清除破坏 <code>%rdi</code> 这一 bug 的关键。 <a href="https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html" target="_blank" rel="noreferrer">https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html</a></p></li><li><p>Intel 64 and IA-32 Architectures Software Developer&#39;s Manual, Vol. 3A, Section 3.4.3 -- System V AMD64 ABI Calling Convention: 第一个整数参数在 <code>%rdi</code>，callee-saved 寄存器为 <code>%rbx, %rbp, %r12~%r15</code>。<code>%rdi</code> 是 caller-saved（易失的），函数调用后可能被修改。Cinux 把 BootInfo 指针存在 <code>.data</code> 段而不是依赖寄存器保存，规避了这个易失性问题。</p></li><li><p>OSDev Wiki -- Calling Global Constructors: <code>.init_array</code> 段的遍历方法详解，包括 ARM BPABI 风格（直接遍历函数指针数组）和传统 GNU 风格（<code>crti.o</code>/<code>crtbegin.o</code> 级联）。Cinux 使用前者。 <a href="https://wiki.osdev.org/Calling_Global_Constructors" target="_blank" rel="noreferrer">https://wiki.osdev.org/Calling_Global_Constructors</a></p></li><li><p>OSDev Wiki -- C++ Bare Bones: freestanding 环境下必须提供的运行时函数清单（<code>__cxa_pure_virtual</code>、<code>__cxa_atexit</code>、<code>operator new/delete</code>），以及 <code>-ffreestanding</code>、<code>-fno-exceptions</code>、<code>-fno-rtti</code> 编译选项的说明。 <a href="https://wiki.osdev.org/C%2B%2B_Bare_Bones" target="_blank" rel="noreferrer">https://wiki.osdev.org/C%2B%2B_Bare_Bones</a></p></li><li><p>OSDev Wiki -- Multiboot Specification: GRUB 为 ELF 内核清除 BSS 的保证。Cinux 不使用 GRUB，所以必须手动清零——这也是触发 BSS 清除 bug 的根本原因。 <a href="https://wiki.osdev.org/Multiboot" target="_blank" rel="noreferrer">https://wiki.osdev.org/Multiboot</a></p></li><li><p>Linux Kernel <code>arch/x86/kernel/head_64.S</code>: Linux 的 <code>startup_64</code> 入口序列，对比 Cinux 的 <code>boot.S</code>。Linux 把 boot_params 保存到 <code>%r15</code>（callee-saved），依赖 decompressor 清零 BSS，使用 <code>__START_KERNEL_map</code> 作为高半核基址。 <a href="https://github.com/torvalds/linux/blob/master/arch/x86/kernel/head_64.S" target="_blank" rel="noreferrer">https://github.com/torvalds/linux/blob/master/arch/x86/kernel/head_64.S</a></p></li><li><p>xv6 <code>entry.S</code> (MIT): xv6 的入口代码——比 Cinux 简单得多，因为 xv6 依赖 GRUB（Multiboot）完成 BSS 清零，且是纯 C（没有 <code>.init_array</code> 和全局构造函数）。 <a href="https://github.com/mit-pdos/xv6-public/blob/master/entry.S" target="_blank" rel="noreferrer">https://github.com/mit-pdos/xv6-public/blob/master/entry.S</a></p></li></ul>`,117)])])}const E=a(l,[["render",p]]);export{k as __pageData,E as default};
