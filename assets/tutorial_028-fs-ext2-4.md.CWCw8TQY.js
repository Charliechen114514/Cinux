import{c as i,Q as a,j as e,m as n}from"./chunks/framework.iD28w_5a.js";const c=JSON.parse('{"title":"028-fs-ext2-4 · Ext2 文件系统","description":"","frontmatter":{"title":"028-fs-ext2-4 · Ext2 文件系统"},"headers":[],"relativePath":"tutorial/028-fs-ext2-4.md","filePath":"tutorial/028-fs-ext2-4.md","lastUpdated":1779686691000}'),t={name:"tutorial/028-fs-ext2-4.md"};function p(l,s,d,h,r,o){return a(),e("div",null,[...s[0]||(s[0]=[n(`<h1 id="ext2-目录操作与实战验证" tabindex="-1">ext2 目录操作与实战验证 <a class="header-anchor" href="#ext2-目录操作与实战验证" aria-label="Permalink to &quot;ext2 目录操作与实战验证&quot;">​</a></h1><blockquote><p>标签：ext2、目录操作、路径解析、VFS 集成、QEMU 验证 前置：<a href="./028-fs-ext2-3">028_fs_ext2 第 3 篇</a></p></blockquote><h2 id="前言" tabindex="-1">前言 <a class="header-anchor" href="#前言" aria-label="Permalink to &quot;前言&quot;">​</a></h2><p>前三篇我们搭好了 ext2 驱动的数据结构、DMA 缓冲区和文件读取路径。现在还差最后几块拼图——目录操作和路径解析。文件系统的&quot;目录&quot;不是什么神秘的东西，本质上就是一个&quot;名字到 inode 号&quot;的映射表，存在磁盘上的数据块里。但在 ext2 中，这个映射表的格式有一些有趣的设计选择——变长记录、链表式连接、4 字节对齐——这些细节在遍历和搜索目录项时都需要正确处理。</p><p>这一篇我们要讲的是目录遍历（readdir）、路径解析（lookup）以及 VFS 集成，最后用 QEMU 启动完整系统做端到端验证——这是整个 tag 028 的高潮。</p><h2 id="ext2direntry-变长的链表式目录项" tabindex="-1">Ext2DirEntry：变长的链表式目录项 <a class="header-anchor" href="#ext2direntry-变长的链表式目录项" aria-label="Permalink to &quot;Ext2DirEntry：变长的链表式目录项&quot;">​</a></h2><p>ext2 的目录数据块由一系列 <code>Ext2DirEntry</code> 结构体组成。每个结构体有一个 8 字节的固定头部（inode 号 4 字节 + rec_len 2 字节 + name_len 1 字节 + file_type 1 字节），后面紧跟变长文件名。整个条目的长度由 <code>rec_len</code> 决定，必须 4 字节对齐。所以即使文件名只有 3 字节，<code>rec_len</code> 也是 12（8 头部 + 3 名字 + 1 填充，对齐到 4）。</p><p>这种变长设计比固定长度的目录项（比如 FAT 的 32 字节条目）灵活得多——文件名最长可达 255 字节，而且通过 rec_len 合并可以高效地回收已删除条目的空间。</p><h2 id="目录遍历-readdir-的实现" tabindex="-1">目录遍历：readdir 的实现 <a class="header-anchor" href="#目录遍历-readdir-的实现" aria-label="Permalink to &quot;目录遍历：readdir 的实现&quot;">​</a></h2><p>Cinux 的 <code>ext2_dir_readdir</code> 是一个静态函数，通过 <code>g_ext2_instance</code> 访问 Ext2 对象。它接受一个 VFS inode 和一个索引号，返回对应索引处的文件名。索引 0 和 1 被硬编码返回 &quot;.&quot; 和 &quot;..&quot;——这是所有 Unix 目录都有的两个特殊条目，不需要从磁盘读取。索引 2+ 的条目需要扫描目录数据块。</p><p>扫描过程是：先计算目录占用的总块数（<code>dir_size / block_size</code>），然后对每个块调用 <code>read_block()</code> 读取数据，在块内按 <code>rec_len</code> 步进遍历每一个条目。当 <code>found</code> 计数器到达 <code>target</code>（即 <code>index - 2</code>）时就找到了目标条目，拷贝文件名到输出 buffer 并返回 1。</p><p>这种&quot;索引号&quot;驱动的 readdir 接口和 Linux 的 <code>getdents</code> 系统调用有微妙的区别。Linux 的 <code>getdents</code> 返回的是从指定偏移开始的一批目录项（批量返回，减少系统调用次数），而 Cinux 的接口是&quot;给我第 N 个条目&quot;——更简单，每次只返回一个。在 Shell 的 <code>ls</code> 命令实现中用起来很方便：从 index=0 开始递增调用 readdir，直到返回 0（没有更多条目）。</p><h2 id="路径解析-从-到目标-inode" tabindex="-1">路径解析：从 &quot;/&quot; 到目标 inode <a class="header-anchor" href="#路径解析-从-到目标-inode" aria-label="Permalink to &quot;路径解析：从 &quot;/&quot; 到目标 inode&quot;">​</a></h2><p>有了目录遍历的能力，路径解析就是顺理成章的事情了。<code>Ext2::lookup(const char* path)</code> 实现的是经典的 Unix 路径解析算法：从根 inode（inode 2）开始，逐级分解路径组件，每级在当前目录中查找匹配的目录项。</p><p>解析过程从跳过前导 <code>/</code> 开始，然后进入 while 循环。每次迭代提取一个路径组件（到下一个 <code>/</code> 或字符串末尾的连续非斜杠字符），调用 <code>lookup_in_dir()</code> 在当前目录中查找。<code>lookup_in_dir()</code> 本身就是一次完整的目录扫描——读取目录 inode 的所有数据块，在每个块中按 <code>rec_len</code> 步进，逐字节比对每个条目的 <code>name_len</code> 和文件名内容。找到匹配就返回 inode 号，找不到返回 0。</p><p>如果中间某一级不是目录（通过读取 inode 的 <code>i_mode</code> 检查 <code>EXT2_S_IFDIR</code> 位），解析直接失败——你不能在普通文件后面继续跟路径组件。所有组件解析完毕后，调用 <code>get_cached_inode(current_ino)</code> 返回最终的 VFS inode。</p><p>这个实现虽然简单，但有一个性能特性值得了解：每次路径解析的每一级都需要从磁盘读取目录块。对于路径 <code>/etc/motd</code>，需要读根目录块（找到 <code>etc</code>），再读 <code>etc</code> 目录块（找到 <code>motd</code>）——至少两次磁盘读取。Linux 通过 dentry cache 把&quot;目录项到 inode 号&quot;的映射缓存下来，第二次访问同一路径时几乎不需要磁盘 I/O。Cinux 没有这一层缓存，但教学内核的场景下路径解析的频率很低（主要发生在 Shell 执行 <code>ls</code>、<code>cat</code> 时），影响可以接受。</p><h2 id="vfs-集成-inodeops-的分发机制" tabindex="-1">VFS 集成：InodeOps 的分发机制 <a class="header-anchor" href="#vfs-集成-inodeops-的分发机制" aria-label="Permalink to &quot;VFS 集成：InodeOps 的分发机制&quot;">​</a></h2><p>ext2 驱动和 VFS 的对接点在 tag 027 定义好的 <code>InodeOps</code> 结构体上。<code>populate_vfs_inode()</code> 在把磁盘 inode 转换为 VFS inode 时，根据文件类型自动绑定对应的 ops 对象——目录绑 <code>&amp;ext2_dir_ops</code>，普通文件绑 <code>&amp;ext2_file_ops</code>：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint16_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mode_type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> disk.i_mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EXT2_S_IFMT;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (mode_type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EXT2_S_IFDIR) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cached.vfs_inode.type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> InodeType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Directory;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cached.vfs_inode.ops  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ext2_dir_ops;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (mode_type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EXT2_S_IFREG) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cached.vfs_inode.type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> InodeType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Regular;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cached.vfs_inode.ops  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ext2_file_ops;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>tag 028 的 <code>populate_vfs_inode</code> 只设置了五个字段：<code>ino</code>（inode 编号）、<code>size</code>（文件大小）、<code>type</code>（文件类型）、<code>ops</code>（InodeOps 函数指针表）和 <code>fs_private</code>（回指指针）。没有设置 mode、nlink、blocks、uid、gid 等字段——这些会在后续 tag 的 stat 功能中按需添加。</p><p>系统调用处理函数在拿到 Inode 之后，通过 <code>inode-&gt;ops-&gt;read(...)</code> 或 <code>inode-&gt;ops-&gt;readdir(...)</code> 调用对应的 ext2 静态函数——整个过程对系统调用层是透明的，它不知道也不需要知道底层是 ext2。</p><p>内核启动时的集成代码是这样的：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cinux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">drivers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ahci</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::AHCI ahci;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ... AHCI 初始化 ...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cinux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Ext2</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ext2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ahci</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // g_ext2_instance = this</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ext2.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    cinux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lib</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kprintf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[EXT2] mount failed!</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cinux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vfs_mount_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cinux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vfs_mount_add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ext2);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>注意 <code>AHCI</code> 对象从局部变量改为 <code>static</code>——因为 <code>Ext2</code> 构造函数保存了 <code>ahci</code> 的引用，如果 <code>ahci</code> 是局部变量，出了作用域后引用就悬空了。这个改动是 tag 028 引入的，很容易被忽略。</p><h2 id="qemu-端到端验证" tabindex="-1">QEMU 端到端验证 <a class="header-anchor" href="#qemu-端到端验证" aria-label="Permalink to &quot;QEMU 端到端验证&quot;">​</a></h2><p>理论讲完了，现在来跑一下。首先创建 ext2 测试镜像，然后用 QEMU 启动内核（ext2.img 挂在 AHCI 端口），如果一切正常，你会在串口输出中看到类似这样的挂载日志：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[EXT2] Mounting ext2 filesystem on AHCI port 1</span></span>
<span class="line"><span>[EXT2] Superblock valid: magic=0xef53</span></span>
<span class="line"><span>[EXT2]   block_size=1024  inode_size=128</span></span>
<span class="line"><span>[EXT2]   blocks=4096  inodes=128  groups=1</span></span>
<span class="line"><span>[EXT2] BGDT loaded: 1 groups</span></span>
<span class="line"><span>[EXT2] Root inode: size=1024 mode=0x41ed</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>进入 Shell 后，可以尝试以下命令验证读取功能：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ls /</span></span>
<span class="line"><span>cat /hello.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>cat /hello.txt</code> 应该输出 &quot;Hello from ext2!&quot; 这行文字。这短短几个字代表的调用链是：Shell 解析命令 -&gt; sys_open -&gt; VFS 路径解析 -&gt; ext2 lookup -&gt; 读取根目录块 -&gt; 找到 &quot;hello.txt&quot; 条目 -&gt; 读取 inode -&gt; 通过直接块指针定位数据块 -&gt; AHCI DMA 读取 -&gt; 拷贝到用户空间。tag 028 只验证了读取——写入操作（create、mkdir、unlink）将在 tag 028b 中实现和验证。</p><h2 id="设计反思与下一步" tabindex="-1">设计反思与下一步 <a class="header-anchor" href="#设计反思与下一步" aria-label="Permalink to &quot;设计反思与下一步&quot;">​</a></h2><p>回头看整个 ext2 驱动，最明显的简化在于两个方面。一是 I/O 模型——单 DMA buffer 没有缓存层，每次操作都是同步磁盘 I/O。Linux 的 page cache 和 SerenityOS 的 BlockView 都是为了解决这个问题的。但对于教学内核来说，这种&quot;每次都和磁盘打交道&quot;的方式有一个被低估的优势：行为完全可预测，调试时不用担心缓存一致性问题。</p><p>二是 InodeOps 的设计——静态函数指针表配合全局指针 <code>g_ext2_instance</code>，虽然限制了单实例，但实现极其简洁。tag 028b 会将 <code>InodeOps</code> 扩展为虚基类，通过引用注入支持多分区挂载。</p><p>我们在这个 tag 中实现了大约 1300 行的 ext2 驱动代码（ext2.cpp 约 757 行 + ext2.hpp 约 245 行 + ext2_types.hpp 约 310 行），涵盖了 Superblock 解析、BGDT 加载、inode 寻址、文件读取（含间接块）、目录遍历、路径解析和 VFS 集成。这是一个功能完整的只读磁盘文件系统驱动，已经能支撑 Shell 的 ls/cat 操作。</p><p>下一个 tag (028b) 将为 ext2 添加写入能力——文件创建（create）、目录创建（mkdir）、删除（unlink），以及底层的块/inode 位图分配器。届时 ext2 将从一个只读骨架进化为支持完整文件系统操作的驱动。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>OSDev Wiki: <a href="https://wiki.osdev.org/Ext2#Directory_Entry" target="_blank" rel="noreferrer">ext2 Directory Entry</a> -- 目录项格式和 rec_len 语义</li><li>Linux ext2 目录操作: <a href="https://github.com/torvalds/linux/blob/master/fs/ext2/dir.c" target="_blank" rel="noreferrer">fs/ext2/dir.c</a></li><li>Linux ext2 namei 操作: <a href="https://github.com/torvalds/linux/blob/master/fs/ext2/namei.c" target="_blank" rel="noreferrer">fs/ext2/namei.c</a></li><li>SerenityOS Ext2FS: <a href="https://github.com/SerenityOS/serenity/tree/master/Kernel/FileSystem/Ext2FS" target="_blank" rel="noreferrer">Kernel/FileSystem/Ext2FS/</a></li></ul>`,38)])])}const E=i(t,[["render",p]]);export{c as __pageData,E as default};
