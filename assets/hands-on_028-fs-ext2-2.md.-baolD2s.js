import{c as e,Q as a,j as i,m as t}from"./chunks/framework.D9IbVcwA.js";const c=JSON.parse('{"title":"028-fs-ext2-2 · Ext2 文件系统","description":"","frontmatter":{"title":"028-fs-ext2-2 · Ext2 文件系统"},"headers":[],"relativePath":"hands-on/028-fs-ext2-2.md","filePath":"hands-on/028-fs-ext2-2.md","lastUpdated":1779686691000}'),n={name:"hands-on/028-fs-ext2-2.md"};function r(o,s,p,l,d,k){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="_028-2-hands-on-ext2-挂载与块-i-o" tabindex="-1">028-2 Hands-on: ext2 挂载与块 I/O <a class="header-anchor" href="#_028-2-hands-on-ext2-挂载与块-i-o" aria-label="Permalink to &quot;028-2 Hands-on: ext2 挂载与块 I/O&quot;">​</a></h1><h2 id="导语" tabindex="-1">导语 <a class="header-anchor" href="#导语" aria-label="Permalink to &quot;导语&quot;">​</a></h2><p>上一章我们用 dumpe2fs 和 hexdump 把 ext2 的磁盘布局翻了个底朝天——Superblock、BGDT、Inode、Directory Entry 的字段含义和布局规则都搞清楚了。现在我们要把这些知识变成可以执行的代码：实现 ext2 的 mount() 函数，让内核从 SATA 磁盘上读出 Superblock，验证它的合法性，加载块组描述符表，最终读取根目录的 inode。完成本章后，你会在串口输出里看到 ext2 超级块的关键参数——这意味着内核已经成功理解了磁盘上的文件系统结构。</p><p>这一章还涉及一个底层的工程问题：块 I/O。AHCI 驱动（tag 025）使用 DMA 方式读写磁盘，需要一个物理连续的缓冲区。我们会分配一个物理页面作为 DMA buffer，把它映射到内核虚拟地址空间，所有 ext2 的块读取都通过这个单一缓冲区完成。</p><p>知识前置：028-1（ext2 磁盘布局）、025（AHCI 驱动）、015/016（PMM/VMM）、027（VFS FileSystem 抽象基类）。</p><h2 id="概念精讲" tabindex="-1">概念精讲 <a class="header-anchor" href="#概念精讲" aria-label="Permalink to &quot;概念精讲&quot;">​</a></h2><h3 id="superblock-的读取——从字节偏移到扇区编号" tabindex="-1">Superblock 的读取——从字节偏移到扇区编号 <a class="header-anchor" href="#superblock-的读取——从字节偏移到扇区编号" aria-label="Permalink to &quot;Superblock 的读取——从字节偏移到扇区编号&quot;">​</a></h3><p>上一章我们知道 Superblock 在字节偏移 1024 处，占 1024 字节。但 AHCI 驱动只认扇区（LBA），不认&quot;字节偏移&quot;。所以我们需要做一个简单的换算：字节偏移 1024 = LBA 2（每个扇区 512 字节，1024/512=2），读取长度 1024 字节 = 2 个扇区。也就是说，调用 AHCI 的 read 函数时，参数是 LBA=2、count=2——一次读出完整的 Superblock。</p><p>这里有一个细节值得注意：我们是用 AHCI 的扇区级接口读的，不是 ext2 的块级接口。原因很简单——在 Superblock 解析之前，我们根本不知道块大小是多少，所以没法用块级抽象。Superblock 的读取是整个挂载流程中唯一一次&quot;硬编码&quot;扇区参数的操作，之后的读取都可以走 read_block。</p><h3 id="read-block-——-从块号到物理读取" tabindex="-1">read_block —— 从块号到物理读取 <a class="header-anchor" href="#read-block-——-从块号到物理读取" aria-label="Permalink to &quot;read_block —— 从块号到物理读取&quot;">​</a></h3><p>有了 Superblock 之后，我们知道了块大小（假设是 1024），就可以实现 read_block：给定一个块号，换算成 LBA（块号 * 每块扇区数），然后用 AHCI read 把数据读到 DMA buffer 里。这个函数是 ext2 驱动的 I/O 核心——所有后续的 BGDT 读取、inode 读取、文件数据读取，最终都通过 read_block 完成。</p><p>一个值得思考的设计选择是：read_block 只使用一个全局 DMA buffer（一个物理页，4KB）。这意味着每次 read_block 都会覆盖上一次的数据——调用方必须在 read_block 之后、下一次 read_block 之前把数据拷贝走。对于单核内核来说这完全没问题，因为我们没有任何并发。但对于生产级文件系统来说，这就不够了——Linux 使用 buffer_head 子系统管理成百上千个缓存块，而我们的&quot;缓存&quot;就是一个 4KB 的窗口。</p><p>这个设计选择有一个直接的后果：在读取间接块时需要特别小心。当我们需要通过 i_block[12] 间接块读取文件数据时，过程是先 read_block(间接块号) 把间接块读进 DMA buffer，从中取出目标数据块号，然后再 read_block(数据块号) 把实际数据读进来。问题在于，第二次 read_block 会覆盖第一次读到的间接块数据——但这没关系，因为我们已经把需要的信息（目标数据块号）提取出来了。只要你不试图在第二次 read_block 之后再去 DMA buffer 里读间接块的数据，就不会出问题。</p><h3 id="dma-buffer-的分配与映射" tabindex="-1">DMA buffer 的分配与映射 <a class="header-anchor" href="#dma-buffer-的分配与映射" aria-label="Permalink to &quot;DMA buffer 的分配与映射&quot;">​</a></h3><p>DMA buffer 需要满足两个条件：物理地址连续（因为 AHCI 控制器通过 DMA 直接访问物理内存），以及在内核虚拟地址空间中可访问（因为 CPU 需要读取 DMA buffer 的内容）。分配过程分两步：先用 PMM 的 alloc_page 分配一个 4KB 物理页，然后用 VMM 的 map 函数把这个物理页映射到一个预留给 ext2 DMA 的虚拟地址上。这个虚拟地址在内存布局中是固定预留的（KMEM_EXT2_DMA_BASE），不会和内核的其他区域冲突。</p><p>映射完成后，我们得到了一对地址——dma_buf_phys（物理地址，传给 AHCI 的 read/write）和 dma_buf_virt（虚拟地址，CPU 访问数据用）。在整个 ext2 驱动的生命周期中，这对地址不会变。</p><p>这里有一个你可能关心的问题：为什么不直接用 kmalloc 分配 DMA buffer？答案是 DMA 的物理连续性要求。kmalloc 虽然能分配内核虚拟地址空间中的内存，但在我们的内核中，kmalloc 底层使用的堆分配器不保证物理连续——它可能把多个不连续的物理页拼接成一个虚拟连续区域。而 AHCI 控制器的 DMA 引擎只认物理地址，它不知道页表的存在。所以我们必须用 PMM 的 alloc_page 直接分配一个物理连续的页，然后手动映射到虚拟地址空间。这个模式和我们在 AHCI 驱动本身的初始化中做的事情完全一样。</p><h3 id="挂载流程的完整步骤" tabindex="-1">挂载流程的完整步骤 <a class="header-anchor" href="#挂载流程的完整步骤" aria-label="Permalink to &quot;挂载流程的完整步骤&quot;">​</a></h3><p>mount() 函数做的事情可以归纳为六步，每一步都依赖上一步的结果：第一步，分配并映射 DMA buffer；第二步，用 AHCI 从 LBA 2 读 2 个扇区，获取 Superblock 原始字节；第三步，验证 magic number 为 0xEF53——如果不匹配，直接返回 false，挂载失败；第四步，从 <code>s_log_block_size</code> 计算实际块大小，同时计算每块扇区数、每组 inode 数、组数等派生参数；第五步，从 BGDT 起始块开始逐块读取，把所有块组描述符拷贝到内存中的 bgdt 数组；第六步，读取 inode 2（根目录），把它放入 inode cache 的第 0 号槽位，建立 VFS Inode。</p><p>这六步走完之后，Ext2 对象的 mounted_ 标志被设为 true，后续的 lookup 和文件读取操作就可以正常工作了。整个 mount 过程涉及的磁盘 I/O 并不多——一次 Superblock（2 扇区）、一到几次 BGDT（取决于块组数量）、一次根 inode（2 扇区），加起来可能不超过十次扇区读取。在 QEMU 中这些操作几乎是瞬间完成的，但在真实硬件上你会感受到机械硬盘寻道的延迟——这也是 ext2 块组设计想要优化的问题。</p><h3 id="inode-编号的寻址公式" tabindex="-1">Inode 编号的寻址公式 <a class="header-anchor" href="#inode-编号的寻址公式" aria-label="Permalink to &quot;Inode 编号的寻址公式&quot;">​</a></h3><p>读取根 inode（inode 2）时用到的寻址公式是整个 ext2 驱动最核心的数学之一，后续每次 read_disk_inode 都会复用。给定 inode 编号 ino：首先计算它所属的块组 <code>group = (ino - 1) / inodes_per_group</code>，然后从 BGDT 中取出该组的 <code>bg_inode_table</code>（inode 表的起始块号），再计算组内索引 <code>index = (ino - 1) % inodes_per_group</code>，进而算出字节偏移 <code>byte_offset = index * inode_size</code>，最终得到目标块号 <code>target_block = bg_inode_table + byte_offset / block_size</code> 和块内偏移 <code>within_block_offset = byte_offset % block_size</code>。</p><p>整个计算链条中最容易出错的地方是&quot;先减 1&quot;——inode 编号从 1 开始，所有下标计算都要先把 ino 减 1 变成 0-based 索引。如果你忘了这一步，inode 2 会被当成 group 0 的第 2 个 inode（实际应该是第 1 个），所有后续的路径解析都会偏移一格。</p><p>我们用一个具体的例子来验证这个公式。假设 ino=2（根目录）、inodes_per_group=2048、inode_size=128、block_size=1024，且 BGDT[0] 中 bg_inode_table=5。那么 group = (2-1)/2048 = 0，index = (2-1)%2048 = 1，byte_offset = 1*128 = 128，target_block = 5 + 128/1024 = 5 + 0 = 5，within_block_offset = 128 % 1024 = 128。也就是说，inode 2 的数据在 block 5 的第 128 字节处——紧跟着 inode 1（在 block 5 的第 0 字节处）。你可以用 hexdump 在宿主机上验证这一点。</p><p>再来看一个稍大的例子——假设 ino=13，且 inodes_per_group=2048、inode_size=128、block_size=1024、bg_inode_table=5。那么 group = (13-1)/2048 = 0，index = (13-1)%2048 = 12，byte_offset = 12*128 = 1536，target_block = 5 + 1536/1024 = 5 + 1 = 6，within_block_offset = 1536 % 1024 = 512。所以 inode 13 在 block 6 的第 512 字节处——它已经不在第一个 inode table 块里了，跨到了第二个块。这个例子说明了为什么 inode table 需要占用多个块（对于 1024 字节块，每个块只能放 8 个 inode——1024/128=8），以及为什么我们的公式必须正确处理跨块的情况。</p><h2 id="动手实现" tabindex="-1">动手实现 <a class="header-anchor" href="#动手实现" aria-label="Permalink to &quot;动手实现&quot;">​</a></h2><h3 id="step-1-定义-ext2-磁盘数据结构头文件" tabindex="-1">Step 1: 定义 ext2 磁盘数据结构头文件 <a class="header-anchor" href="#step-1-定义-ext2-磁盘数据结构头文件" aria-label="Permalink to &quot;Step 1: 定义 ext2 磁盘数据结构头文件&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>kernel/fs/ext2_types.hpp</code>，定义 Ext2Superblock、Ext2BlockGroupDescriptor、Ext2Inode、Ext2DirEntry 四个 packed 结构体以及相关常量。</p><p><strong>设计思路</strong>: 所有的磁盘结构体都用 <code>[[gnu::packed]]</code> 属性标记，因为磁盘上的布局不允许编译器插入填充字节。用 <code>static_assert</code> 确保每个结构体的大小与规范一致——Ext2Superblock 必须是 1024 字节、Ext2BlockGroupDescriptor 32 字节、Ext2Inode 128 字节。常量定义包括 EXT2_SUPER_MAGIC（0xEF53）、EXT2_DIRECT_BLOCKS（12）、EXT2_INODE_CACHE_SIZE（64）、EXT2_SUPERBLOCK_OFFSET（1024）、EXT2_SECTOR_SIZE（512）等。这些常量在后续的 mount 和 read_block 中都会大量使用。</p><p><strong>实现约束</strong>: 这些结构体只定义磁盘上的布局，不包含任何行为。Ext2CachedInode 是一个辅助结构——把磁盘 inode 和 VFS inode 绑在一起，还有 ino 编号和 in_use 标志。</p><p><strong>踩坑预警</strong>: Ext2Superblock 的字段非常多，但 Cinux 不需要用到全部字段。不过为了保证结构体大小正确（1024 字节），后面的字段必须全部列出来（包括大量的 reserved/padding 字段）。一个常见错误是少写了末尾的 padding 数组，导致 sizeof 不是 1024。</p><p><strong>验证</strong>: 编译通过且 static_assert 全部成立：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_common</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -E</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;(error|static_assert)&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="step-2-定义-ext2-驱动类" tabindex="-1">Step 2: 定义 Ext2 驱动类 <a class="header-anchor" href="#step-2-定义-ext2-驱动类" aria-label="Permalink to &quot;Step 2: 定义 Ext2 驱动类&quot;">​</a></h3><p><strong>目标</strong>: 创建 <code>kernel/fs/ext2.hpp</code>，声明 Ext2 类（继承 FileSystem）。InodeOps 使用 ext2_file_ops 和 ext2_dir_ops 两个静态函数指针表。</p><p><strong>设计思路</strong>: Ext2 类持有 AHCI 引用和端口号、Superblock 缓存、BGDT 数组、inode cache 数组、DMA buffer 地址对、以及各种派生参数（block_size、sectors_per_block、group_count 等）。InodeOps 使用静态函数指针表（ext2_file_ops 和 ext2_dir_ops），回调函数通过全局指针 g_ext2_instance 访问 Ext2 对象的 read_block、read_disk_inode 等底层方法。</p><p><strong>实现约束</strong>: BGDT 数组大小上限为 128（EXT2_MAX_GROUPS），足以覆盖约 8GB 的磁盘（128 组 * 每组 8192 块 * 4096 字节/块）。inode cache 固定 64 条目，使用简单的驱逐策略（cache 满时用 ino 做哈希选择淘汰槽位，slot 0 永远留给根 inode）。</p><p><strong>验证</strong>: 编译通过。</p><h3 id="step-3-实现-dma-buffer-分配" tabindex="-1">Step 3: 实现 DMA buffer 分配 <a class="header-anchor" href="#step-3-实现-dma-buffer-分配" aria-label="Permalink to &quot;Step 3: 实现 DMA buffer 分配&quot;">​</a></h3><p><strong>目标</strong>: 在 <code>kernel/fs/ext2.cpp</code> 中实现 <code>ensure_dma_buffer()</code> 方法。</p><p><strong>设计思路</strong>: 第一次调用时分配物理页、映射到预留虚拟地址、清零。后续调用直接返回 true（dma_ready_ 标志防止重复分配）。如果 PMM 分配失败或 VMM 映射失败，打印错误信息并返回 false。</p><p><strong>实现约束</strong>: 虚拟地址使用内存布局中预留的 KMEM_EXT2_DMA_BASE。映射标志为 PRESENT | WRITABLE（内核数据，不需要用户态访问）。分配完成后要把整个 buffer 清零——虽然 AHCI read 会覆盖这些字节，但清零可以防止 mount 过程中的调试打印意外输出垃圾数据。</p><p><strong>踩坑预警</strong>: 确保在内核启动序列中，PMM 和 VMM 已经初始化完毕之后再调用 ext2 的 mount()。如果 PMM 还没初始化就调 alloc_page，你会收获一个非常漂亮的 kernel panic。另外，如果你之前的 tag 中已经在 KMEM_EXT2_DMA_BASE 这个虚拟地址上映射了别的东西（比如调试用的临时映射），ensure_dma_buffer 会尝试重新映射同一个虚拟地址——VMM 的 map 函数需要处理这种情况（要么报错，要么先 unmap 再重新 map）。</p><p><strong>验证</strong>: 编译通过。在 mount 的 kprintf 中加入 DMA buffer 物理地址和虚拟地址的打印，确认两者都是非零值。</p><h3 id="step-4-实现-read-block-和-write-block" tabindex="-1">Step 4: 实现 read_block 和 write_block <a class="header-anchor" href="#step-4-实现-read-block-和-write-block" aria-label="Permalink to &quot;Step 4: 实现 read_block 和 write_block&quot;">​</a></h3><p><strong>目标</strong>: 将块号换算为 LBA，调用 AHCI 的 read/write 方法。</p><p><strong>设计思路</strong>: LBA = block_num * sectors_per_block_，sector count = sectors_per_block_。read 把数据读到 DMA buffer（物理地址），write 从 DMA buffer（物理地址）写出。调用方通过 dma_buf_virt() 获取虚拟地址来访问数据。</p><p><strong>实现约束</strong>: 每次 read_block 会覆盖 DMA buffer 中上一次的数据——这是单缓冲区设计的固有行为。如果调用方需要保留之前读到的数据，必须在下一次 read_block 之前拷贝走。read_block 的第一步总是 ensure_dma_buffer()——这是一个幂等操作，buffer 已经分配时直接返回 true，不会有额外开销。</p><p><strong>验证</strong>: 编译通过。你可以在 read_block 中加入一个调试打印，显示每次读取的 block_num 和换算后的 LBA，方便追踪 I/O 操作。但在正式使用时记得去掉——否则串口输出会非常啰嗦。</p><h3 id="step-5-实现-mount-函数" tabindex="-1">Step 5: 实现 mount() 函数 <a class="header-anchor" href="#step-5-实现-mount-函数" aria-label="Permalink to &quot;Step 5: 实现 mount() 函数&quot;">​</a></h3><p><strong>目标</strong>: 按照上面描述的六步流程实现完整的挂载逻辑。</p><p><strong>设计思路</strong>: 先 ensure_dma_buffer，然后读 Superblock（硬编码 LBA 2、2 扇区），验证 magic，计算派生参数，读 BGDT，最后读根 inode。每一步都有对应的串口打印，方便调试。整个 mount 过程是同步的——不存在异步回调或中断驱动的 I/O 等待，read_block 调用返回时数据已经在 DMA buffer 中了（因为 AHCI 的 read 在我们的实现中是同步阻塞的）。</p><p><strong>实现约束</strong>: BGDT 的起始块号取决于块大小——1024 字节时从 block 2 开始（block 0 是 boot block，block 1 是 Superblock），更大的块从 block 1 开始（Superblock 在 block 0 的偏移 1024 处）。inode_size 在 revision 0 时固定 128 字节，revision &gt;= 1 时取 s_inode_size 字段值。group_count 通过向上取整计算：<code>(s_blocks_count + blocks_per_group - 1) / blocks_per_group</code>。</p><p><strong>踩坑预警</strong>: 有两个地方特别容易出错。第一，BGDT 可能跨越多个块——如果块组数很多，一个块装不下所有描述符（1024 字节块只能装 32 个描述符），需要循环读取多个块并拼接。虽然我们的 8MB 测试镜像只有 1 个块组，但代码必须处理多块的情况——否则换成更大的磁盘就会挂掉。第二，根 inode 必须放入 cache slot 0——这个槽位被 root_inode_ 引用，后续 lookup(&quot;/&quot;) 时直接返回这个引用，不再走 cache 查找。如果你把根 inode 放到了别的 slot，lookup(&quot;/&quot;) 就会返回一个未初始化的 Inode，接下来的操作必定崩溃。</p><p><strong>验证</strong>: 编译并在 QEMU 中运行，串口应输出类似以下内容：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_common</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qemu-system-x86_64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -serial</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel.bin</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -drive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> file=ext2.img,format=raw,if=none,id=ahci0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -device</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ahci,id=ahci_ctlr</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;\\[EXT2\\]&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>预期看到：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[EXT2] Mounting ext2 filesystem on AHCI port 1</span></span>
<span class="line"><span>[EXT2] Superblock valid: magic=0xef53</span></span>
<span class="line"><span>[EXT2]   block_size=1024  inode_size=128</span></span>
<span class="line"><span>[EXT2]   blocks=XXXX  inodes=XXXX  groups=X</span></span>
<span class="line"><span>[EXT2]   blocks_per_group=8192  inodes_per_group=XXXX</span></span>
<span class="line"><span>[EXT2] BGDT loaded: X groups</span></span>
<span class="line"><span>[EXT2] Root inode: size=1024 mode=0x41ed</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h3 id="step-6-更新-cmakelists-txt" tabindex="-1">Step 6: 更新 CMakeLists.txt <a class="header-anchor" href="#step-6-更新-cmakelists-txt" aria-label="Permalink to &quot;Step 6: 更新 CMakeLists.txt&quot;">​</a></h3><p><strong>目标</strong>: 把 ext2.cpp 加入构建系统。</p><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_common</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -E</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;(error|warning)&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>应该没有 error。</p><h3 id="step-7-在内核初始化中调用-ext2-mount" tabindex="-1">Step 7: 在内核初始化中调用 ext2 mount <a class="header-anchor" href="#step-7-在内核初始化中调用-ext2-mount" aria-label="Permalink to &quot;Step 7: 在内核初始化中调用 ext2 mount&quot;">​</a></h3><p><strong>目标</strong>: 在内核的启动序列中，在 AHCI 初始化完成之后，调用 ext2 的 mount() 函数。</p><p><strong>设计思路</strong>: 在 <code>kernel/main.cpp</code> 或你使用的初始化入口中，找到 AHCI 初始化之后的位置。创建一个全局的 Ext2 对象（或使用 new 在堆上分配），传入 AHCI 引用和端口号 1，然后调用 mount()。如果挂载成功，打印一条确认信息；如果失败，打印警告但不 panic——因为我们还可能有 Ramdisk 作为后备。</p><p><strong>实现约束</strong>: Ext2 对象的生命周期必须覆盖整个内核运行期间——不能用栈变量。最安全的做法是把它声明为全局静态对象，或者在堆上 new 之后不 delete。Ext2 的构造函数除了初始化成员变量之外不做任何 I/O 操作，真正的磁盘读取都在 mount() 中发生，所以不用担心构造函数在初始化早期被调用的问题——只要 mount() 在 AHCI 就绪之后调用就行。</p><p><strong>验证</strong>: 在 QEMU 中运行，确认串口输出包含 <code>[EXT2]</code> 相关的挂载信息。如果你看到 <code>[EXT2] Root inode: size=1024 mode=0x41ed</code>，恭喜你——根目录的 inode 已经成功读取了，mode=0x41ed 表示这是一个目录（0x4000）加上 rwxr-xr-x 权限（0x1ed）。</p><h2 id="构建与运行" tabindex="-1">构建与运行 <a class="header-anchor" href="#构建与运行" aria-label="Permalink to &quot;构建与运行&quot;">​</a></h2><p>完整验证流程：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 确保镜像存在</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -la</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ext2.img</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 如果还没有创建镜像，参考 028-1 Step 1 创建</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 构建内核</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel_common</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行——注意 QEMU 的磁盘参数配置</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qemu-system-x86_64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -serial</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> big_kernel.bin</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -drive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> file=../ext2.img,format=raw,if=none,id=ahci0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -device</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ahci,id=ahci_ctlr</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;\\[EXT2\\]&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>如果你看到完整的挂载信息（Superblock valid、BGDT loaded、Root inode），说明 mount() 成功了。如果只看到 &quot;Mounting&quot; 和 &quot;Failed to read superblock&quot;，说明 AHCI 读取失败了——检查 QEMU 参数和 AHCI 驱动初始化。</p><p>你也可以单独验证 Superblock 的内容是否和宿主机上 dumpe2fs 的输出一致。比如，如果串口打印 block_size=1024，那么 s_log_block_size 应该是 0；如果 blocks=8192 且 blocks_per_group=8192，那 group_count 应该是 1。把这些数字和 <code>dumpe2fs ext2.img | head -20</code> 的输出逐行对比，如果全部吻合，说明 Superblock 解析完全正确。</p><h2 id="调试技巧" tabindex="-1">调试技巧 <a class="header-anchor" href="#调试技巧" aria-label="Permalink to &quot;调试技巧&quot;">​</a></h2><p><strong>mount() 返回 false，没有 Superblock 输出</strong>: 检查 AHCI 驱动是否正确初始化。在 tag 025 中，AHCI 探测和端口初始化必须在 ext2 mount 之前完成。如果你在串口中看到 <code>[EXT2] Failed to read superblock</code>，说明 AHCI read 调用失败了——可能是端口号不对，也可能是 QEMU 的 AHCI 设备没有正确配置。</p><p><strong>magic 不匹配（看到 0x0000 或其他值）</strong>: 最大的嫌疑是镜像文件路径或 QEMU 的 -drive 参数配置有误。确认 <code>ext2.img</code> 确实是 ext2 格式（用 <code>file ext2.img</code> 查看），并且 QEMU 的 AHCI 设备和端口对应关系正确。</p><p><strong>group_count 特别大或特别小</strong>: 检查 blocks_per_group 和 blocks_count 的值是否合理。8MB 镜像、1024 字节块大小下，block 数约为 8192，blocks_per_group 为 8192，group_count 应该是 1。如果 group_count 是几百，说明某处读取了错误的字段值。可以在 mount 的 kprintf 输出中对照查看 blocks_count 和 blocks_per_group 的实际数值。</p><p><strong>BGDT 读取失败</strong>: 确认 BGDT 起始块号计算正确——对于 1024 字节块，它应该在 block 2。用 <code>hexdump -C ext2.img -s 2048 -n 64</code> 验证 block 2 的内容非全零。BGDT 读取失败的另一个常见原因是 sectors_per_block_ 计算有误——如果 block_size 在这一步还没被正确设置（比如你把 s_log_block_size 的值直接当 block_size 用了），sectors_per_block_ 可能是 0，导致 LBA 计算全部错误。</p><p><strong>根 inode 读取失败但 BGDT 加载正常</strong>: 确认 read_disk_inode 的寻址公式中每一步的值是否正确。在 read_disk_inode 中加入调试打印：输出 group、inode_table_block、index_in_group、target_block、within_block_offset 的值，然后和你在宿主机上用 dumpe2fs + hexdump 手动计算的结果对比。</p><p><strong>根 inode 的 mode 不是 0x41ED</strong>: 0x41ED 表示&quot;目录 + rwxr-xr-x 权限&quot;。如果你看到 0x8000 或其他值，说明你读到了错误的 inode——很可能是寻址公式中忘了减 1，导致读到了 inode 1（bad blocks inode）而不是 inode 2（根目录）。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th>要点</th></tr></thead><tbody><tr><td>Superblock 读取</td><td>LBA 2，2 扇区，硬编码扇区参数</td></tr><tr><td>magic 验证</td><td>必须是 0xEF53，否则挂载失败</td></tr><tr><td>block_size</td><td>1024 &lt;&lt; s_log_block_size，不是直接值</td></tr><tr><td>DMA buffer</td><td>一个物理页（4KB），PMM 分配 + VMM 映射</td></tr><tr><td>read_block</td><td>块号 * 每块扇区数 = LBA，数据读到 DMA buffer</td></tr><tr><td>BGDT 加载</td><td>从 Superblock 之后的块开始，可能跨多个块</td></tr><tr><td>根 inode</td><td>inode 2，放入 cache slot 0</td></tr><tr><td>ino 寻址</td><td>group=(ino-1)/ipg, index=(ino-1)%ipg, 偏移=index*inode_size</td></tr><tr><td>初始化顺序</td><td>PMM → VMM → 堆 → AHCI → ext2 mount → vfs_mount_add</td></tr><tr><td>全局 Ext2 对象</td><td>必须是全局静态或堆分配，不能是栈变量</td></tr></tbody></table><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><ul><li>OSDev Wiki: <a href="https://wiki.osdev.org/Ext2#Superblock" target="_blank" rel="noreferrer">ext2 Superblock</a> -- Superblock 各字段偏移和含义</li><li>OSDev Wiki: <a href="https://wiki.osdev.org/Ext2#Block_Group_Descriptor" target="_blank" rel="noreferrer">ext2 Block Group Descriptor</a> -- BGDT 结构</li><li>ext2 Superblock 规范: <a href="https://wiki.osdev.org/Ext2#Superblock" target="_blank" rel="noreferrer">https://wiki.osdev.org/Ext2#Superblock</a></li><li>Linux ext2 mount 实现: <a href="https://github.com/torvalds/linux/blob/master/fs/ext2/super.c" target="_blank" rel="noreferrer">https://github.com/torvalds/linux/blob/master/fs/ext2/super.c</a></li></ul>`,84)])])}const b=e(n,[["render",r]]);export{c as __pageData,b as default};
